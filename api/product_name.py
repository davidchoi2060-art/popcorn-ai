# -*- coding: utf-8 -*-
"""견적 표시용 상품명 — **파는 이름**과 **설명하는 이름**을 가른다 (A-38 ⑤ · 2026-08-10).

■ 왜 축이 둘인가
  `products.product_name` 은 **마켓에 내보내는 판매용 제목**이다. 그래서 뒤에 판매조건이
  꼬리로 붙는다("… 회원가입 계좌이체 맞춤할인 2.5%"). 마켓에서는 그게 맞다 — 검색에
  걸리고 조건을 알린다.
  그런데 견적서는 **"왜 이 부품인가"를 말하는 자리**다(A-01). 근거 옆에 할인 안내가
  붙으면 근거가 광고에 묻힌다. 두 맥락은 목적이 다르므로 이름도 다르다.

■ 그래도 컬럼을 새로 두지 않는다
  기존 쇼핑몰은 `pd_name`(단품 판매) / `pd_name_pc`(조립PC 표시) 두 벌을 사람이 채운다.
  우리는 조립PC를 **팔지** 않는다 — 엔진이 매번 생성하는 **견적**이다. 두 벌을 두면
  적재가 이름을 갱신할 때 한쪽만 바뀌어 갈라진다(이 리포가 가장 자주 앓은 병).
  그래서 **원천 하나를 두고 표시할 때 파생**한다.

■ 규칙은 실측에서 나왔다 (2026-08-10 · 전체 22,840건)
  '회원가입'으로 시작하는 꼬리 **17종 · 868건(3.8%)**, 전부 판매조건이고 부품 정보가 아니다.
  867건이 깨끗하게 잘린다. 그 앞은 한 글자도 건드리지 않는다 —
  '(정품)' '(멀티팩)' '(벌크)' 는 유통 형태라 **부품 정보다**(같이 지우면 안 된다).

  나머지 광고 계열('맞춤가이벤트' 앞머리 · '특가판매' 꼬리 · '(무료배송)')은 패턴이
  제각각이고 추천 후보에 **1건**뿐이라 규칙을 만들지 않았다. 지어낸 규칙은
  진짜 부품 정보를 지운다.

■ 잘라서 이름이 없어지면 자르지 않는다
  원천이 통째로 광고인 상품이 1건 있다(123034 — AMD AM5/DDR5/65W CPU · 재고 1 · 판매중).
  이건 파생으로 풀 문제가 아니라 **검수 대상**이다. 빈 이름을 내미느니 광고를 내미는 게
  낫다 — 이름 없는 부품이 견적에 들어가면 고객은 무엇을 사는지 알 수 없다.

■ 쓰는 자리 — **카탈로그를 읽는 자리에서만**
  ① **고객이 보는 곳만.** 운영자 화면은 원천을 그대로 본다 — 마켓 노출을 관리하는 자리라
     실제 나가는 제목을 봐야 한다(`/api/admin/*` 는 이 함수를 부르지 않는다).
  ② **스냅샷·원장은 기록된 대로 보여준다.** `order_items.name_snap` 과
     `quote_snapshots.items.parts[].name` 은 **고객이 그때 본 것의 기록**이다. 표시할 때
     다시 파생하면 지나간 화면을 고쳐 쓰는 셈이다(원장 규약과 같은 이유).
     대신 **기록되는 시점에 이미 파생돼 있다** — 견적이 카탈로그에서 이름을 만들 때 한 번
     걷고, 그 값이 스냅샷·주문으로 그대로 흘러간다. 그래서 새 주문서는 깨끗하고,
     이 변경 이전의 옛 세션만 원천 그대로 남는다(그게 사실이므로 맞다).

■ 2026-08-16 개정 — 꼬리가 대괄호로 감싸이기 시작했다
  2026-08-15 몰(윈윈) 카탈로그 24,721건 적재 이후 꼬리 표기가 바뀌었다:
  이전 `회원가입 계좌이체 맞춤할인 2.5%`(대괄호 없음·양수) → 오늘 이후
  `[회원가입 계좌이체 맞춤할인 -2.5%]`(대괄호로 감쌈·음수). 옛 정규식은 "회원가입"부터
  지웠는데, 여는 대괄호 `[` 는 "회원가입" **앞**에 있어 매치에 안 걸리고 그대로 남았다
  (실측: 원천 865건 중 대괄호형 842건, 그중 841건이 파생 후 `[` 로 끝남 · 그중 399건이
  추천 후보). 닫는 대괄호 `]` 는 원래도 `.*$` 가 끝까지 삼켜 문제가 없었다 — 새는 것은
  여는 대괄호뿐이었다.

  **고친 것**: "회원가입" 바로 앞에 붙은 대괄호(그 앞의 공백까지)도 함께 걷도록
  `\\[?` 를 추가했다. **바로 앞**만 본다 — 대괄호와 "회원가입" 사이에 다른 글자가 있으면
  (예: `[한정수량 회원가입 …]`) 손대지 않는다. 그래야 `ASUS ROG STRIX [WHITE EDITION]`
  같이 대괄호가 상품명 자체의 일부인 경우, 그리고 `128GB [32G x 4][회원가입 …]` 처럼
  판매조건 대괄호 바로 앞에 붙은 **다른** 정당한 대괄호(용량 표기)를 지우지 않는다 —
  둘 다 실측으로 확인했다(전자는 매치 자체가 없고, 후자는 이미 닫힌 `]` 뒤에 새 매치가
  시작해 앞쪽 대괄호는 매치 밖이다).
"""

import re

# 실측된 꼬리 17종이 전부 이 한 낱말로 시작한다('회원가입시', '회원가입 구입시' 포함).
# `\[?` — 2026-08-16: 대괄호로 감싼 새 형태에서 여는 대괄호가 "회원가입" 바로 앞에 붙어
# 있으면 그 대괄호(와 앞의 공백)도 함께 걷는다. 바로 앞이 아니면(다른 글자가 끼어 있으면)
# 손대지 않는다 — 정당한 상품명 대괄호까지 지우지 않기 위해서다(위 개정 노트 참조).
_TAIL = re.compile(r"\s*\[?\s*회원가입.*$")

# 자른 뒤 이보다 짧아지면 이름 구실을 못 한다고 본다(실측 최단 유효 이름은 9자).
_MIN_LEN = 4


def display_name(raw):
    """판매조건 꼬리를 걷어낸 견적 표시용 이름. 못 걷으면 원천을 그대로 돌려준다."""
    if not raw:
        return raw
    cut = _TAIL.sub("", raw).strip()
    return cut if len(cut) >= _MIN_LEN else raw
