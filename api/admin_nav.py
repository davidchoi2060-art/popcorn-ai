"""운영자 좌측 메뉴(LNB) — **새 IA 의 단일 원천** (UX-34 · 2026-08-09).

전체 화면 표와 판정 근거는 `docs/design/admin-ia-draft-2026-08-09.md` 가 원천이고,
**여기는 그 표를 코드로 옮긴 것**이다. 둘이 어긋나면 문서가 맞다.

■ 왜 JS 가 아니라 Python 인가
  기존 시스템은 `mockups/shared/admin-menu-data.js` 를 브라우저가 읽어 런타임에 그린다.
  그 방식은 "그리는 시점"이 어긋나 아이콘이 안 붙는 사고를 냈고, 무엇보다 **화면이
  서버 렌더가 되면 메뉴만 클라이언트에 남을 이유가 없다.** 레이아웃이 서버에서 한 벌로
  그리면 그리는 시점 문제가 구조적으로 사라진다.
  → 새 레이아웃은 `admin-menu.js` 를 로드하지 않는다. 로드하면 이 메뉴를 덮어쓴다.

■ 범위 축소 — 54항목 → 35항목 (사용자 결정 2026-08-11 · `docs/decisions/decision-log.md`)
  팝콘AI 는 **신규 고객 유입 채널**이다. 용도·조건을 받아 호환성을 판정하고 견적까지
  만든 뒤 **팝콘PC 쇼핑몰로 인계**한다. 결제·배송·환불·회원관리는 그쪽 일이다.

      뺐다 (22)  주문 5 · 클레임 4 · 배송·판매 설정 4 · 매출·세무 4 · 고객 4 · 재고 입고 1
      새로 (3)   쇼핑몰 동기화 · 인계 기록 · 유입 성과
      성격 변경  「매입·재고」→「매입·소싱」 · 「판매가」는 정하는 곳이 아니라 예외를 잡는 곳

  **우리가 팔지 않으므로 매출·정산·클레임이 발생하지 않는다.** 대신 재야 할 것이
  생겼다 — 몇 건을 넘겨 몇 건이 팔렸는가(유입 성과). 그 축이 통째로 없었다.

■ 구화면 링크 정보 삭제 — 개정 기록 (2026-08-12 사장님 지시)
  *"모든 admin/ 연결된 링크는 삭제하고 신규로 만드니까.. 그렇게 변경해줘"*
  전 화면을 admin2 로 신규 재구축하기로 확정되면서 항목이 들고 있던
  old(`/admin/*.html`) 값을 전부 지웠다. 「기존 화면 있음/없음」 구분은
  재구축 **순서**를 정할 때의 근거였는데, 전면 재구축이 확정되어 그 근거가
  소멸했다 — 어차피 전부 새로 짓는다면 구화면 유무는 순서를 가르지 않는다.
  기존 화면 파일(`mockups/admin/*.html`)은 지우지 않는다(병행 신축 ·
  직접 열기는 계속 가능) — 메뉴 정본이 그 경로를 더 들고 있지 않을 뿐이다.
      new  재구축된 화면(`/admin2/...`). **링크는 이것만**
      (없으면) 신설 예정 — 링크 없이 「신설」 표시

■ 이름은 문서를 따른다
  「상품 매핑」→ 상품 분류 매핑, 「사양 항목 정의」→ 상품 사양 정의,
  「상품 검수」→ 상품 사양 검수, 「용도 하한」→ 용도별 최소 사양,
  「부품 등급판」→ 부품 등급 관리. 테이블·API 식별자는 안 바꾼다 — 화면 라벨만이다.

■ 숫자 배지를 여기 담지 않는다
  기존 메뉴가 '상품 검수 9' 같은 수를 마크업에 박아 두었다가 실제(5,162)와 어긋났고,
  `clearDummyBadges()` 가 로드마다 지우고 있었다. 정본에 담으면 **거짓을 원천에 새기는
  것**이다. 실수치가 필요하면 서버 응답으로 채운다.

■ href 연결 — 기록자, T-02 조건 충족 (2026-08-15)
  하네스의 「검증 통과」 통보(T-02 조건)를 받고 셋을 연다 — 조립 호환 규칙
  (`/admin2/compat-rules`) · 추천 기준 보기(`/admin2/policy-weights`) ·
  견적 상담 기록(`/admin2/consult-sessions`). 커밋 `d792434`·`3ffe740` 재검증 «결함 0».
  **다른 항목은 그대로 둔다** — href=None 인데 `api/admin_ui_*.py` 라우트 파일은 이미
  있는 화면이 여럿이다(추천 가능 재고 현황 포함). 그건 「검증 통과 통보」가 아직
  없었거나(대부분) 라우트 파일이 지금 다른 작업으로 미커밋 수정 중이라(추천 가능
  재고 현황) 열지 않았다 — 목록은 하네스 보고에 남긴다. 라벨은 이미 정본이라 바꾸지
  않았다(「추천 기준 보기」는 커밋 `a84fbc0`에서 이미 개명 완료).

■ href 연결 추가 — 부품 등급 관리 (2026-08-15, 같은 날 2차)
  하네스가 통보를 빠뜨렸던 것을 스스로 정정 — 부품 등급 관리(`/admin2/part-grade`)는
  커밋 `112c5a7`에서 이미 검증 완료(서열 미리보기 페이지 분리·저장 후 네 곳 갱신·삭제
  확인/취소·슬롯 칩 9개 DB 대조·쓰기 원복 1행→1행 + 768px 결함 수정)였는데 위 1차
  연결에서 빠졌다. 나머지 10개(위 1차 항목 참조)는 그대로 둔다 — 이번 통보는 이
  하나만이다.

■ 「마진 정책」 신설 + href 연결 둘 (2026-08-15, 같은 날 3차 — 판매가 그룹)
  「판매가」 그룹은 `docs/design/req/req-reprice.md` §②가 이미 번호를 매겨 뒀다
  (010 가격 검토 대기 · 020 마진 정책 · 030 가격 이력 · 040 단가표 일일 반영 ·
  050 판매가 재산정 · 060 판매가 관리) — **ID 를 새로 짓지 않고 이미 배정된 것을
  적는다.** 화면 ID `ADM-PRC-020`, 경로 `/admin2/margin-policy`(`api/admin_ui_
  margin_policy.py` 실측), 위치는 010 다음·030 앞.
  **href 는 가격 검토 대기·마진 정책 둘만 연다** — 마진 정책은 확인자 2차 검증
  결함 0(커밋 `a4d5d44`). 가격 검토 대기는 확인자가 실측 검증(빈 상태 3줄·숫자
  하드코딩 0·셸 상속 정상)까지 끝냈는데 통보가 빠져 있었다(하네스가 이번에 정정).
  **가격 이력은 열지 않는다** — 제작자 자기 검증까지만 끝났고 브라우저 실측이
  아직이다(확인자 배정됨). 040·050은 라우트 자체가 없거나(040) 제작 중이라(050)
  이 파일에 항목을 만들지 않았다 — 상태는 `docs/design/req/INDEX.md`에서 관리한다.

■ 「가격 이력」href 연결 (2026-08-15, 같은 날 4차)
  확인자가 브라우저 실측까지 끝냈다(7행 3종 판정 스크린샷 대조·ref_id null 실값
  확인·401/400/404 경계 실제 HTTP 재현·768px scrollWidth==clientWidth·가로 스크롤
  300px 밀어도 안내줄 안 끌려감 실측·서버 기본 상품 빈 이력 분기 렌더 확인).
  이 화면 하나만 연다 — 판매가 그룹 나머지는 그대로다: 040 단가표 일일 반영은
  라우트 파일 자체가 없고, 050 판매가 재산정은 지금 확인자가 검증 중이고,
  060 판매가 관리는 재검증 대기다.

■ href 연결 추가 — 매입 · 소싱 셋 + 유입 성과, «오늘 검증된 넷» (2026-08-15, 같은 날 5차)
  사장님 확정: 「오늘 검증된 넷만 먼저」— 나머지는 언제 검증됐는지 기록이 없어
  뒤로 미룬다. 하네스의 「확인자 재검증 통과」 통보(2026-08-15)를 받고 연다.
  **넷 다 nav 항목은 이미 있었다 — 새로 만들지 않고 href 만 채웠다.**

      공급처          /admin2/suppliers            커밋 e5572eb
        공급처 등록·매입가 이력·「되살리기」. 제작 완료 시점 자기 기록은
        "검증 중"이었고, 그 뒤 확인자 재검증 통과.
      매입 견적(용산)   /admin2/sourcing             커밋 d53732c
        재고 소진·안전재고 미달 자동 등록 -> 견적 요청 -> 공급처 회신
        비교 -> 확정, 확정 전·후 가격 표시. 위와 같은 경위.
      단가표 반영      /admin2/price-import          커밋 4b23e78
        공급처별 파일 diff -> 반영 -> 되돌리기, danawa_code 확정 매칭
        신규 모델 반영 허용(A-46). 커밋 메시지 자신이 "재검증 진행
        중"이라 적어 뒀던 화면 — 그 뒤 통과 통보를 받고 지금 연다.
      유입 성과        /admin2/funnel-performance    커밋 adb6d4d
        인계 -> 구매 전환을 기간 탭(㉮/㉯/㉰)으로 재집계. 화면 ID
        (ADM-HND-030)는 여전히 후보일 뿐이지만 경로 연결까지 막지는 않는다.

  ⚠ **3차·4차 노트의 「040(단가표 반영) 라우트 파일 자체가 없다」는 그 시각
  (11:42·12:11) 기준으로는 맞았다.** 그 뒤(17:45) 라우트·템플릿이 커밋
  `4b23e78`로 지어졌다 — 지우지 않고 여기서 정정한다(CANON §5).

  **나머지는 href=None 그대로 둔다** — 상태는 `docs/design/req/INDEX.md`가
  화면별로 관리한다(추천 가능 재고 현황·부품 교체·클릭 기록·상품 사양 정의·
  용도별 최소 사양·상품 일괄 등록·삭제 상품 조회·판매가 관리·쇼핑몰 동기화·
  인계 기록). **판매가 재산정(`/admin2/reprice`)은 nav 항목 자체를 새로
  만들지 않는다** — 검증 기록이 없다(스캐폴딩 커밋 `82a55a1`이 스스로
  "구축 중 · 검증 전"이라 적었다). 지금 만들면 이 파일의 「검증 통과가
  조건」 규약을 스스로 어기는 것이다.

■ 「판매가 재산정」 항목 신설 + href 연결 (2026-08-15, 같은 날 6차 — 판매가 그룹)
  ⚠ **5차 노트의 위 문단(「nav 항목 자체를 새로 만들지 않는다」)은 그 시각
  기준으로는 맞았다** — 검증 기록이 없었다. 그 뒤 확인자 검증이 통과해
  **이번에 항목을 새로 만든다** — 지우지 않고 여기서 정정한다(CANON §5).
  앞선 다섯 차수는 전부 라벨이 이미 있고 href만 비어 있었지만, 이 화면은
  **라벨도 없던 유일한 화면**이다(조사자 실측 — `/admin2/reprice`는 라우트가
  살아 있는데 nav 항목 자체가 없는 유일한 경우였다).

  화면 ID `ADM-PRC-050`, 경로 `/admin2/reprice`(`api/admin_ui_reprice.py`
  실측 — `APIRouter(prefix="/admin2")` + `@router.get("/reprice")`로 확인).

  **위치 — ID 순서로 판단하되 실측을 앞세운다.** 계약(`docs/design/spec-
  reprice.md` §선행 작업)은 판매가 그룹을 010 검토대기 · 020 마진정책 · 030
  이력 · 040 단가표 · 060 관리 순으로 적었다. **그런데 이 nav 의 실제 순서를
  재보면 그 ID 순서가 아니다** — 판매가 관리(제안 060, `req-sale-price.md`
  자신도 "미배정 · 다음 빈 번호일 뿐"이라 적었다)가 그룹 맨 앞에 있다(그룹을
  만든 커밋 701f7e4부터 그랬다 — 나중에 순서가 어긋난 게 아니라 처음부터
  ID 순서가 아니었다). 020 마진 정책을 끼워 넣을 때도(3차 노트) 이 060
  위치는 그대로 두고 "010 다음 · 030 앞"으로 **국소적으로만** ID 순서를
  맞췄다(커밋 879ee61). 같은 원칙을 이어 쓴다 — 그룹의 물리적 끝(030 가격
  이력 뒤)에 둔다. 060을 그룹 끝으로 옮겨 ID 순서 전체를 바로잡는 것은
  기존 배치를 바꾸는 판단이라 이번 지시 범위 밖으로 본다.

  **연결 근거 — 확인자 실측(2026-08-15):**
      「지난 회차」 표가 맨 앞 · `note` 열 실재 · 간격이 "1분 1.5초" 형식
        (사람이 읽는 단위)으로 정확히 변환
      금지 문구(「99.99%」·「세 번째도」·「사유가 없습니다」·목업 리터럴)
        grep 전수 0건 — 계약이 "쓰지 않는다"로 명시한 경고 톤이 실제로 없다
      ⚠⚠ apply(7,174건, `live` 범위 changed 기준) -> 새 네비게이션 -> undo
        왕복을 실제로 완주 — log_id 7720. 구 화면의 핵심 결함("새로고침하면
        되돌릴 방법이 사라진다", 계약 §구 화면)이 실측으로 해소 확인됐다
      되돌린 뒤 수치가 원값과 완전 일치(target 7,279 · up 6,899 · down 275 ·
        unchanged 105 · locked 0)
      미로그인 401을 preview · apply · undo · 페이지 네 곳 모두 확인 — 계약
        §⑧이 "무세션 실측은 못 했다"고 남긴 항목이 이번에 해소됐다
      콘솔 에러 0 · 404 자원 0 · 외부 CDN 0

  ⚠ 남은 결함 하나(마크다운 별표가 화면에 그대로 노출됨)는 다른 제작자가
  지금 고치는 중이고 **ⓐ류(문구 렌더링 — 설계를 다시 그릴 필요는 없음)**다.
  이 결함 때문에 연결을 미루지 않았다 — 위 검증 항목(왕복·되돌림 정확성·
  미로그인 차단)이 전부 통과했고 남은 것은 표시 서식 하나뿐이기 때문이다.

  ⚠ **이번은 `total` 도 함께 는다** — 앞선 다섯 차수는 라벨이 이미 있어
  href만 채웠지만(total 불변), 이번은 항목 자체를 신설해 `total` 38 -> 39,
  `new` 29 -> 30이다.

  확인법: `product_price_history` 를 `log_id=7720` 으로 대조하면 이번
  검증의 apply·undo 왕복이 되짚인다.

  확인법: `.venv/Scripts/python -c "from api.admin_nav import counts; print(counts())"`
  — `new`(href 있음)가 이번에 연 수만큼 늘고 `todo`가 그만큼 줄었는지 본다.
  (판매가 재산정 신설은 예외 — 위 참조. `total` 도 함께 늘어야 정상이다.)
"""

# 그룹: (제목, 아이콘, [항목])
# 항목: (라벨, new, 비고)  — new 가 None 이면 아직 없다(신설 예정)
NAV = [
    ("대시보드", "pie-chart", [
        ("대시보드", "/admin2/", None),
    ]),

    ("상품관리", "box", [
        ("상품 분류 관리", "/admin2/categories", None),
        ("상품 관리", "/admin2/products", None),
        ("상품 분류 매핑", "/admin2/category-mapping", None),
        ("상품 일괄 등록", None, "이력·오류건 통합 예정"),
        ("삭제 상품 조회", None, "신설"),
    ]),

    # 도매꾹에 대응물이 통째로 없는 축. 파는 일이 아니라 **추천되게 하는 일**이다.
    ("상품사양관리", "sliders", [
        ("조립 호환 지도", "/admin2/build-map", "신설 · 관계 22쌍 한 장"),
        ("조립 사양 표준", "/admin2/spec-standard", "신설 · 사람이 항목을 늘린다"),
        ("상품 사양 정의", None, None),
        ("상품 사양 검수", "/admin2/reviews", "신설 · 사장님 확정 화면(2026-08-13)"),
        ("조립 호환 규칙", "/admin2/compat-rules", None),
        ("용도별 최소 사양", None, None),
        ("부품 등급 관리", "/admin2/part-grade", None),
        ("추천 기준 보기", "/admin2/policy-weights", None),
        ("추천 가능 재고 현황", None, None),
        ("견적 상담 기록", "/admin2/consult-sessions", None),
        ("부품 교체 · 클릭 기록", None, None),
    ]),

    # 소싱은 **우리 일이다.** 가격 원천은 몰이지만, 직접 공급사에서 소싱하는 경우
    # 공급가·판매가를 우리가 덮어쓴다(결정 2026-08-11). 그래서 이 축은 남는다.
    # 「재고 입고」만 뺐다 — 재고 원장은 쇼핑몰이 갖는다. 두 곳이 세면 반드시 어긋난다.
    ("매입 · 소싱", "truck", [
        ("공급처", "/admin2/suppliers", None),
        ("매입 견적(용산)", "/admin2/sourcing", None),
        ("단가표 반영", "/admin2/price-import", None),
    ]),

    # **성격이 바뀐 축이다.** 가격을 «정하는» 화면이 아니라 «예외를 잡는» 화면이다.
    #   기본 = 몰 가격 그대로 · 예외 = 소싱해서 우리가 덮어쓴 값(잠금)
    #   경보 = 잠기지 않았는데 몰과 어긋난 건 → 동기화 누락
    # 지금은 값이 하나뿐이라 셋을 구분할 수 없다(2026-08-11 실측: 표본 10건 중 2건 불일치).
    ("판매가", "dollar-sign", [
        ("판매가 관리", None, "몰값·우리값·낡은값 구분 필요"),
        ("가격 검토 대기", "/admin2/price-review", None),
        ("마진 정책", "/admin2/margin-policy", None),
        ("가격 이력", "/admin2/price-history", None),
        ("판매가 재산정", "/admin2/reprice", None),
    ]),

    # ── 신설 축 — 유입 채널의 성적표 ──────────────────────────────────
    # 자체 판매 전제로 짜인 IA 라 「매출 집계」가 있고 「인계 성과」가 없었다.
    # 우리는 팔지 않는다. 넘길 뿐이다. 그러면 재야 할 것은 매출이 아니라 **전환**이다.
    ("인계 · 성과", "share-2", [
        ("쇼핑몰 동기화", None, "신설 — 받아온 시각 · 어긋난 건수"),
        ("인계 기록", None, "신설 — 무엇을 언제 어떤 견적으로 넘겼나"),
        ("유입 성과", "/admin2/funnel-performance", "신설 — 인계 → 구매 전환"),
    ]),

    ("AI 관리", "cpu", [
        ("작업 현황판", "/admin2/dash", "신설 · 진행 보기 + 말 걸기"),
        ("웹 사양 채움", "/admin2/spec-fill", "신설(ADM-AI-020) · B안 확정 2026-08-13"),
        ("AI 작업 설정", "/admin2/ai-task-settings", "신설 — 작업↔모델 · 폴백"),
        ("AI 연동 설정", "/admin2/ai-integration", "신설 — 키는 서버 env"),
        ("AI 사용량 · 비용", "/admin2/ai-usage-cost", None),
        ("AI 응답 기록", "/admin2/ai-response-log", "신설"),
        ("운영 도우미 설정", "/admin2/ops-assistant", "신설 — 처음엔 조회 전용"),
    ]),

    ("시스템", "server", [
        ("오픈 단계 설정", "/admin2/ops-settings", None),
        ("운영자 · 권한", "/admin2/operators", None),
        ("작업 기록", "/admin2/activity-logs", None),
        ("엑셀 다운로드 관리", "/admin2/excel-exports", "신설 — 정적 뼈대(큐·API 없음)"),
    ]),
]


def nav_for(current_path: str = "") -> list:
    """템플릿이 쓰기 좋은 형태로 편다.

    `state` 두 가지 — 화면이 어디까지 왔는지를 메뉴가 정직하게 말한다:
      new   재구축 완료. `/admin2/` 로 간다. **링크는 이것만 붙는다**
      todo  신설 예정. 링크 없음

    ■ 구화면(`/admin/*.html`)으로는 잇지 않는다
      2026-08-11 사용자 지시: *"확정된 화면만 링크를 붙이되, admin2 로 모두 붙여
      나갑시다."* 새 메뉴가 옛 시스템으로 데려가면 **어디까지 지었는지가 흐려진다** —
      운영자는 자기가 신·구 어느 쪽에 있는지 모른 채 일하게 된다. 지금 이 프로젝트가
      고치고 있는 병이 바로 그것(화면이 사실을 흐리는 것)이다.

      그때는 `old`(구화면 있음)를 state 로 남겨 재구축 순서의 근거로 썼다.
      2026-08-12 전면 재구축 확정으로 그 근거가 소멸해 `old` 자체를 폐지했다
      (모듈 docstring 개정 기록). 구화면은 계속 `/admin/*.html` 에서 직접 열 수 있다.
    """
    out = []
    for title, icon, items in NAV:
        rows = []
        for label, new, note in items:
            href = new           # 구화면으로는 잇지 않는다 — 위 결정
            state = "new" if new else "todo"
            rows.append({
                "label": label, "href": href, "state": state, "note": note,
                "active": bool(href) and current_path.rstrip("/") == href.rstrip("/"),
            })
        out.append({
            "title": title, "icon": icon, "items": rows,
            "done": sum(1 for r in rows if r["state"] == "new"),
            "total": len(rows),
            "active": any(r["active"] for r in rows),
        })
    return out


def counts() -> dict:
    """진척 — 화면이 지어내지 않게 여기서 센다. `old` 키는 폐지(2026-08-12)."""
    total = sum(len(i) for _t, _ic, i in NAV)
    new = sum(1 for _t, _ic, items in NAV for _l, n, _nt in items if n)
    return {"groups": len(NAV), "total": total, "new": new, "todo": total - new}
