# Decision Log — 팝콘PC AI

> 이 파일은 프로젝트의 **확정/미정/폐기 결정의 단일 원천**이다.
> `CLAUDE.md`와 `HANDOFF.md`가 작업 시작 전 반드시 읽으라고 지정한 문서다.
> 결정을 바꾸거나 미정을 확정할 때는 **이 파일을 먼저 갱신**하고, 그 다음 목업·프롬프트에 반영한다.
>
> 최초 작성: 2026-07-02 · 단계: **모델링(클릭 가능한 UI 목업 확정)**
> 출처: `CLAUDE.md`, `HANDOFF.md`, `docs/prompts/S0~S4`, `docs/infra/GCP_SETUP.md`
>
> **상품 DB 재설계 (2026-07-07):** 상품 스키마의 단일 원천은 [`docs/06_db-erd.md`](../06_db-erd.md)(ERD **Ver 3.0**), 결정 근거는 [`docs/decisions/2026-07-07_product-data.md`](2026-07-07_product-data.md)(ADR 9건). 관리자 화면(ADM-\*)·목 데이터의 컬럼명·상태값·reason 코드는 이 두 문서를 표준으로 따른다. 인계 원본: GDRIVE `.../5단계/HANDOFF_DB_2026-07-07.md`.
>
> **LLM 실연동 파일럿 보류 (2026-07-21):** 팝콘톡에 실제 Claude API를 붙여 서버에서 구동하는 파일럿을 검토했으나 **보류** — 모델링 단계 규칙(백엔드·LLM 연동·서버 배포 금지)은 그대로 유지한다. 추후 착수 시의 방향은 확정: ① 범위 = **팝콘톡 파일럿 한정**(FastAPI `/api/talk` 단일 엔드포인트, A-03 유지 — LLM은 지식 답변+조건 파싱만, 견적 생성 없음), ② 노출 = **공개**(Firebase 목업에서 직접 호출 — 비용 캡·요청 제한·접근 게이트 필수), ③ 모델 = **Claude Opus 4.8**(`claude-opus-4-8`). 그 전까지 배포 대상은 정적 목업(Firebase Hosting)뿐이다.
>
> **🚧 백엔드 수직 슬라이스 착수 승인 (2026-07-23):** DB 구축 완료 직후 사용자 승인 — 금지 목록에서 **백엔드 API 서버 해제**(운영 배포·LLM 연동은 여전히 별도 승인). 스택 = **FastAPI(Python)**(db/와 동일 언어 — SQLAlchemy·Alembic 공유, 추후 엔진·LLM도 Python). 방식 = **화면 단위 수직 슬라이스**: 목업 한 장을 DB→API→화면 바인딩까지 관통해 이음새부터 검증, 통과 후 다음 화면. 첫 슬라이스 = **ADM-PRD-010 관리자 상품 목록**(읽기 1본, `GET /api/admin/products`) — 목업은 API 성공 시 실데이터, 실패 시 기존 더미 폴백(배지로 구분, Firebase 검토 배포 무영향). 서버 필터·페이지네이션·인증은 실데이터 26,480행 시점으로 명시 연기. **슬라이스 1 검증 완료(2026-07-23)**: API 단독(items 13·kpis 13/8/4/1/0·P-7102 사양 3/4 검수 서사) + 화면 통합(실데이터 배지·탭 카운트·검색·재고 0 강조·콘솔 에러 0) + 폴백 회귀(정적 서버에서 더미 20행 유지) 전부 통과 — DB→API→화면 이음새 관통 실증. **슬라이스 2 검수 큐 완료(2026-07-23, 첫 쓰기 이음새)**: ERD 3차 개정(product_reviews에 origin/suggested/confidence — 마이그레이션 0002, §8 T2 해제·승격 조건 명문화) + 시드 패치(대기 5건, 검수 대상 필드는 확정 전 specs NULL 유지 원칙 — §7.2) + API 4본(목록/처리/되돌리기/일괄 확정 — 승인 전이: specs 값→verified→locked_fields→게이트 해제 전이 시에만 ai_candidate 승격, FOR UPDATE 409 방어, 활동 로그 before 스냅샷 기반 undo). 검증: 전이·원복·409·companion 뷰 진입/이탈 왕복·화면 실연(처리→토스트→되돌리기, 2단계 일괄)·폴백(더미 10건)·상품 목록 교차(검수 5·추천 7·P-6012 전환) 전부 통과. 의도된 차이: 실데이터 P-4821 위험도 '치명'(ERD §7.3 기준, 더미는 '주의'). 미결 이관: REQUIRED_SPEC_FIELDS(api) vs ERD §5 필수 매트릭스 불일치 — 시드는 양쪽 충족으로 회피, 맵 교정 vs §5 개정은 추후 결정. **슬라이스 3 단가표 일일 반영 완료(2026-07-23, 첫 파일 기반 대량 쓰기)**: 엑셀 파싱은 제외(실파일 미보유 — 확보 시 별도 슬라이스), 이음새 = 스냅샷 diff 서버 계산(직전 파일 대비, 매칭 map→danawa→유사도, pending=psp 비교로 멱등) → 선택 반영(psp upsert → 공급처 간 재판정: purchase='가능' 최저 → sale=매입가+수수료 2.2% 천원 half-up 반올림, 잠금 가드 — "선택 반영 클릭=승인" 해석은 ERD §6.4와의 의도된 차이로 API 헤더 명시) → 가격 이력(reason=price_import, 삭제 없는 원장 — undo는 역방향 이력) → undo(3중 가드 + 재판정 재실행, purchase 완전 복귀 시 기록된 원본 sale 복원). 검증: diff 기대값·부분 반영→2차 완료·재판정 무변경 케이스·미매칭 거부·undo 원복(판매가 원본 포함)·이중 undo 409·화면 실연(반영→토스트→되돌리기)·폴백·DB 교차 전부 통과. **슬라이스 4 — S1 후보 풀 카운터 실데이터화(2026-07-23, 결정론 엔진 v0)**: `POST /api/candidates/count` — LLM 없이 뷰 1쿼리+순차 필터, 같은 입력+같은 재고=같은 출력(A-02). base = v_recommendation_candidates ∧ stock_qty>0(UX-08 실측 — 시드 기준 total 9, 목업 26,480은 폴백 연출로만 존치). **v0 하드필터 2종만 정직 적용**: ① 예산 = 부품별 상한 배분율(CPU25·GPU40·MB15·RAM10·SSD12·HDD8·POWER10·CASE8·COOLER8%, '이상'·무숫자 미적용 — 휴리스틱, S2 엔진 배분으로 대체 예정) ② 태그('저소음'→tag_silent, '화이트'→tag_white)를 스코프 부품(저소음: GPU·POWER·CASE·COOLER / 화이트: CASE)에만 적용(미태깅 전멸 방지). **그 외 제약(용도·대상·연결 등)은 applied=false 정직 표기**("구성 단계 반영 — 후보 수 영향 없음") — 임의 배수 연출은 A-01과 충돌해 LIVE에서 폐기. effects=제약 순서 누적 델타(순서 교환 시 배분만 변동 — 정의된 결정론). 목업은 addConstraint 래퍼 단일 지점 디바운스 재조회+effects 전체 재렌더, 0개 서사는 서버 count=0 조건으로 일반화(고정 점프 2140/1240/3400/0 제거는 LIVE 한정 — 과제약 칩 실측 4). seed_0004 부작용: 상품 목록 13→19행·P-2001 사양 4/4. 검증: 기대값 매트릭스 12케이스·재현성 3회·순서 결정론·guided 완주(6)·talk 파싱(8)·과제약(4)·폴백(26,480 원형)·교차(19행) 전부 통과. **U-08 확정(2026-07-23, §5에서 이동)**: [견적 확정] 최소 활성 조건 = **(용도 또는 상황) + 예산**. 클라이언트는 라벨 존재로 활성화(전 모드 공통 — talk 게이트를 일반화), `/api/recommend`가 서버에서 재검증(미달 400 + 부족 항목 명시). 파워유저는 자유입력 한 줄("게임용 100만")로 즉시 충족, expert는 부품 직접 지정이라 별도 경로. **슬라이스 5 — S2 견적 제안서 실데이터화(2026-07-23, 견적 생성 엔진 v1)**: `POST /api/recommend` — U-08 검증 → 태그·예산 하드필터(candidates 재사용) → **고정 슬롯 순서 DFS(백트래킹)**로 호환 5종(소켓·메모리·쿨러 TDP·장착·전력)을 제약으로, 티어를 슬롯 내 정렬로 통합: 가성비=오름차순 / 고성능=내림차순·예산 캡 미적용(초과는 budget.verdict='over' 정직 표기) / 추천=캡 풀+내림차순+**예산 총액 가지치기**("캡 내 최고가 합산은 캡 합 136%라 예산 초과(100만→113만)" 결함을 DFS 예산 제약으로 해소), 무숫자 예산은 중간 순위, tie=product_code. **가성비(최소 구성) 불가면 전 티어 불가**(최소 구성이 예산 밖이면 견적 불성립 — 70만 케이스). 성능 proxy=가격(벤치 부재 정직 명기, S2 스코어 엔진에서 대체). consult_sessions+quote_snapshots(≤3) 기록, companion은 제시본(offered) 저장(ERD §12 주석 각주). S1 확정 guided/chat/talk 연결(expert 제외), localStorage popcorn-quote로 S2 전달. S2 LIVE: 실총액·부품 전량·깔때기 실수치(S1 카운터와 동일 규칙 — 18→13 일치)·전원 여유 실%(118/107 등), **정직 숨김·표기**(사용자 확정): FPS·성능점수·시장최저가·트레이드오프 숨김, 검수 수치는 "출고 시 실측 리포트" 표기, companion 실데이터({c,n,p} S4 계약 유지). seed_0005(신규 9종 — 계획의 "P-6012 specs 보강"은 검수 서사 충돌로 신규 SSD 대체) 부작용: 상품 28행(ok 22)·카운터 total 18. 검증: 400 계열·손계산 기대값(945,000/993,000/1,633,000·150만 1,347,000·200만+ 1,149,000·70만 전부 null)·저소음 시 고성능 GPU 후퇴·재현성·DB 스냅샷·S1→S2 여정 실연·U-08 잠금·폴백 원형·교차 회귀 전부 통과. **슬라이스 6 — S4 주문 확인·자체 결제, 커머스 원장 첫 쓰기(2026-07-24)**: `POST /api/orders` — own 경로만 실구현(mall 주문 생성은 연동 없는 가짜 원장이라 제외, 인계 연출은 폴백 존치). **ERD 4차 개정+마이그레이션 0003**: orders.shipping_snap(배송지 스냅샷 저장처 부재 해소)·session_id(주문↔견적 스냅샷 링크). **seed_0006 = A-10 전환 2단계 승격**(pay·settle→own + history v2) — ops_snapshot은 DB 5키 그대로(감사 정합), 서버는 pay≠own 409, S4는 GET /api/ops 동기화(데모 전환 비활성). 단일 트랜잭션(ERD §8 T7): 견적 스냅샷 원장 재조회(클라 가격 불신 — 본체·periph 모두 스냅샷 가격, periph는 offered 멤버십 검증) → 재고 검증(판매중∧재고, 정렬 FOR UPDATE, 부족 409 soldout 전체 롤백) → 회원 email upsert(mock 인증 수용) → 발번 MAX+1+advisory lock → 접수→라인(core 8+조립 30,000+periph)→hold→목업 PG 승인(pg_ref TX-)→converted→own_sale 차감→결제완료(이벤트 actor 고객/PG(목업)). 실 PG 도입 시 hold/confirm 2단계 분리 지점 주석. 전달 계약: popcorn-quote-tier(S2 담기 시 티어 저장)+periph {c,n,p,code}(하위 호환)+popcorn-order(S5 실값). 검증: ORD-84217(1,729,000·held 10·라인 11·재고 전 품목 -1·member 재사용)·409 soldout(임시 재고 조정·원복)·pay 가드·멤버십 가드·재주문 84218(중복 방지 v1 제외)·전체 여정 실연(S1 시나리오→S2 담기→S4 1,375,000 결제→S5 ORD-84219 실값·딥링크·키 클리어)·폴백 원형 전부 통과. 주문·이력은 dev DB 누적 수용 — 다음 관리자 주문 슬라이스의 실데이터. **슬라이스 7 — ADM-ORD-020 관리자 주문 관리 실데이터화(2026-07-24, 주문 상태 전이 첫 성문화)**: `GET /api/admin/orders`(서버가 목업 O 형식 파생 조립 — ops 문장은 표시 4키(pay·settle·ship·refund) 기준 "전 항목 쇼핑몰 인계" 판정 / pay·ship 3분기·hold 실측 / refund 라벨 "RF-{1023+refund_id}" 표시 전용 — refund_no 컬럼 성문화는 ADM-CLM-010 슬라이스로 이관 / LABELS=PART_TYPE_LABELS+COOLER). **전이 규칙 v1 성문화**: 결제완료→[조립 시작]조립중→[검사 완료·출고 처리]배송중(shipments 생성 — **ship_mode=주문 ops_snapshot.ship**(own 고정 아님, 감사 정합), 송장 결정론 발번. ERD '출고' 상태는 목업이 건너뛰어 v1 미사용 — SHIP 슬라이스 세분화 여지)→[배송 완료 처리]완료. 단일 공용 API `POST /advance`(기대 상태 409·**활성 환불 시 전 액션 409** — 목업은 버튼만 숨겼음, SHIP 화면도 추후 재사용: 원장 단일·화면은 뷰). undo=직전 1단계 — **order_events는 불변 원장: 삭제 없이 역방향 이벤트**(actor '운영자(되돌림)'), shipments만 생성분 삭제(송장은 활동 로그 detail 보존). 목업 확장(공백 메우기): 결제완료 pill·[조립 시작] 액션 신설(실주문 3건 전부 결제완료 — 기존 목업 미지원). 의도된 차이: 84216 hold 실측 2건(목업 "7건"은 core product_code NULL의 폴백 연출), 83907 "(1단계 프리셋)" 접미 생략. seed_0007=주문 1 spec_snap part_type 백필(라벨 재현, 주변기기 포함 9행). 검증: 목록 7건·파생 전수·전진 3단계(ship_mode=mall·송장 6483-0005)·undo 역순 3회(이벤트 +6 무삭제·shipment 삭제·결제완료 원복)·409 3종(이중 undo·상태 불일치·환불 가드)·화면 실연(배지·결제완료 필터·조립 시작→되돌리기)·폴백 원형 전부 통과. **운영 루프 폐합: 고객 주문(S4) → 관리자 확인·처리(ADM-ORD)까지 실데이터.** **슬라이스 8 — S3 부품 변경·연쇄 스왑 실데이터화(2026-07-24, 스왑 = 새 스냅샷 원장)**: `POST /api/swap/candidates`(8슬롯 일괄 — 현 구성은 products⨝specs 직조인 재구성(품절 부품도 렌더·unavailable 정직 표기), 대안은 판매 풀 한정, 후보별 전 슬롯 재검증 → 즉시/연쇄/미노출(UX-11 ①), 연쇄=위반 슬롯 가격 오름차순 순회 1패스+전체 재검증(위반이 스왑 슬롯 자신이면 미노출 — 파워 하향 케이스), chain_reason 고정 템플릿(A-03), 정렬 가격 오름차순+code(A-02)) + `POST /api/swap/apply`(풀 멤버십 실패=swap_soldout 409·전 슬롯 재검증 실패=incompatible 409+violations → **같은 session·tier 새 quote_snapshot INSERT**(companion 복사 — orders periph 검증 유지, reasons에 변경 1행) — orders가 최신 1건을 집으므로 S4 주문 자동 반영). **가격 원칙: 비변경 슬롯=스냅샷 가격 보존, 변경 슬롯만 라이브**("보여준 그대로" 원칙 6). compat 조립을 `build_compat()`로 추출(recommend.py 내부 — /api/recommend 회귀 0 확인). 전달 계약: S2 '부품 변경'·S3 '담기'에도 popcorn-quote-tier 저장, apply 성공 시 popcorn-quote.sets[tier] 동기 갱신(화면·원장 불일치 차단). S3 LIVE: 8행·대안 실배지·즉시/연쇄 모달(연쇄=사유+동시 교체 명시+승인 시 2건 apply)·MB/COOLER 대안 0 정직·품절 표기·자연어 교체 "준비 중" 정직 비활성(LLM 보류)·파워 표기 headroom 원값 통일. 의도된 차이: chain_options 라디오→최저가 단일, 예산 재판정 v1 제외, swap_event_logs(레거시 users FK) 미기록 — ERD 개정 시 이관. 검증: 회귀(945,000/1,347,000/1,633,000)·배지 8슬롯 기대값·4060Ti 즉시 +73,000·4070S 연쇄(POWER GX-750 +44,000, 700W 사유)→1,244,000·107%·승인 후 POWER 배지 1→0·S2 복귀 반영(총액·부품·여유)·409 2종·POWER 단독 989,000·136%·폴백 Intel 원형 전부 통과. **고객 축 완결: S0~S5 전 구간 실데이터.** **슬라이스 9 — ADM-SHIP-010 배송 관리 실데이터화(2026-07-24, 신규 API 0 — "원장 단일·화면은 뷰" 실증)**: 신규 엔드포인트 없이 `GET /api/admin/orders` 재사용 + `recipient`(shipping_snap→name) 1필드 추가(시드 4건 스냅샷 부재 → null 방어, 주문 화면 무영향). **상태 어휘 정직화(의도된 차이)**: 목업 '출고 대기'는 검사 통과를 전제한 가공 상태(대응 원장 없음 — 가공행 ORD-84210에서만 성립) → DB 결제완료→**"조립 대기"**(버튼 없음·주문 관리 안내), 조립중→**"조립·검사 중"**(버튼 [검사 완료·송장 등록·출고]=advance ship — 주문 화면과 동일 API·의미). ERD '출고' 세분화 계속 보류, ship_mode 필드 노출은 다음 이터레이션. KPI '평균 출고' = "—(집계 준비 중)" 정직 표기. 활성 환불(84213)은 환불·클레임 링크(409 가드 정합). 검증: 매핑 7건(조립 대기 3·조립·검사 중 1·배송중 1·완료 2)·84216 출고(송장 6483-0001-7919 결정론)→undo 원복·콘솔 0·폴백 5행 원형 통과. **슬라이스 10 — MY-020 고객 주문 내역·환불 접수 실데이터화(2026-07-24, 환불 원장 첫 기록 — 고객 접수가 운영 액션을 잠근다)**: `GET /api/my/orders?email=`(mock 인증 — email 파라미터는 로컬 데모 한정, 실 인증 별도 슬라이스. 미가입 빈 목록 정직. _item_label·STEP 재사용, RF 라벨 `refund_label()` 추출로 admin과 단일 원천, refund_mode=주문 ops_snapshot) + `POST /api/my/refunds`(FOR UPDATE·404/403 소유·활성 환불 409·접수/취소 상태 거부 — **결제완료 접수 허용='주문 취소'**(목업 kind 삼항 보정)·사유 칩 화이트리스트 → INSERT refunds(mode=스냅샷, amount=전액, '접수')). r-note는 저장처 없어 미전송(목업도 미저장 — 회귀 0), reason_detail·부분 환불·**접수 철회 부재**는 ADM-CLM-010 이관(접수된 84219는 활성 잠금 누적 — CLM 슬라이스 실데이터). **회원 경계 실증(의도된 차이)**: 김민준 5건, 83907(이서연) 제외. 검증: GET 3계정·가드 3종(403/409/400)·84219 접수→**RF-1025**(전액 1,375,000·mall)→**관리자 주문·배송 advance 즉시 409(원장 단일 폐합 실증)**·양 화면 라벨 일치·화면(딥링크 상세·RF 추적 칩·5건·진행 중 필터 3·스냅샷 10행·접수 UI mall 안내)·폴백 더미 3건 원형·콘솔 0 통과. **슬라이스 11 — ADM-CLM-010 환불·클레임 실데이터화(2026-07-24, T8 환불 전이 성문화 + complete 비가역 경계 첫 도입)**: `GET /api/admin/refunds`(R 형식 서버 파생 — kind는 주문 상태 파생에 '취소'→'주문 취소' 안정화 분기, 활성 우선 정렬, note '—' 정직) + `POST /advance`(**T8 성문화 — ERD §8 행 추가**: 접수→[검토]→[승인 — own 수거·PG 예약/mall 인계 기록 문구 분기]수거·처리→[완료], 반려는 접수·검토에서만 — 목업 reject 상태 미전이는 버그로 정정, 스키마 무개정). **complete = 비가역**(undo_id null — 부수효과 3원장: ① payments **환불 행 추가·음수 표기·원 결제 레일 승계**(pay_mode·method 복사, pg_ref own={원참조}-R — refund_mode는 절차 모드지 자금 레일 아님) ② 실물 라인 전부 stock_movements('return', ref 'refund')+재고 복귀(84219=10부품 +1 / 84213=통합 1행 대상 0 — 시드 한계 정직) ③ 주문 결제완료·조립중·**배송중**→'취소'+이벤트(배송중 유지안은 done advance 루프홀로 기각), 완료(반품·교환)만 유지). review·approve·reject는 undo(before 복원·이중 409·**reject-undo는 동일 주문 활성 환불 검사**). own 승인 분기는 전 건 refund='mall'이라 실증 불가(구현만). KPI '완료' 누적·평균 처리 "—(집계 준비 중)"(completed_at 없음). 표시 정합 패치: 주문 stBadge 취소 danger·MY kindOf 취소 분기·MY 반려 칩 별도 표시/뱃지 status 반영/반려 후 재접수 버튼. 검증: 잠금 왕복(RF-1024 반려→84213 done 성공→주문 undo→반려 undo→재차 409)·RF-1025 풀 처리(review undo 왕복→완료: TX-84219-R -1,375,000·return 10행·재고 10부품 +1·84219 취소+이벤트·complete undo 409)·취소 주문 재접수 409·MY 완료 칩/취소 뱃지 반영·폴백 3건 원형 전부 통과. 이관 유지: refund_no·reason_detail·completed_at 컬럼화·부분 환불·접수 철회·settlements 반영(ADM-PAY). **커머스 클레임 루프 폐합: 고객 접수 → 관리자 처리 → 원장 3종 반영 → 고객 화면 자동 동기화.** **슬라이스 12 — MY-030 결제 내역 실데이터화(2026-07-24, 읽기 전용 원장 뷰 — 스키마·ERD 무개정)**: `GET /api/my/payments?email=`(신규 api/my_payments.py — mock 인증 한정·미가입 빈 목록. **전 행 원장**: 승인·취소·환불이 각 한 줄, my_orders의 DISTINCT ON(주문당 최신 1행)은 84219 승인 행 소실이라 금지 성문. 정렬 paid_at DESC NULLS LAST+payment_id DESC 결정론. **pg_ref·payment_id는 응답 제외**(고객 미노출 계약 — 미전송)). **KPI 클라 계산**(응답 rows 단일 원천): 이번 달=당월∧(승인·환불) 순액 / 누적=(승인·환불) 순액·건수는 원장 전 행 — '취소'는 화이트리스트로 합산 제외(돈이 안 움직임, 현 0건 선제 가드), '대기' 색 성문화 보류(현 쓰기 경로상 발생 불가 — 실 PG 슬라이스 이관, 미지정 상태는 중립 회색 폴백). 누적 라벨 "(N건)" 동적화(id 부여 — 정적 "(3건)" 잔존 차단), 0건 빈 상태 신설. psum(주변기기 가산)은 더미 리터럴 전용 — LIVE 실액에 이미 포함이라 재적용 금지(이중 계상). 의도된 차이: 84216 실액 2,087,000(더미 1,735,000+psum), 런타임 행 method '카드'(더미 '신용카드'). 검증: 원장 8행 전수 재확인·API 4계정(김민준 6건 — 환불 -1,375,000 최상단·84219 승인+환불 2행 원장 실증·pg_ref 키 부재 / 이서연·박도윤 1건 / 미가입 0건)·화면 LIVE(6행·환불 빨강 칩·KPI 손계산 이번 달=누적 6,357,000 (6건)·84216 psum 제거 실증·딥링크)·이서연 월 경계(이번 달 0원/누적 1,284,000)·미가입 빈 상태·로그아웃 더미+psum 원형·정적 서버 폴백 원형·콘솔 0·my-orders 교차 회귀 전부 통과. 이관: 실 인증·'대기' 색·paid_at 타임존 정규화·settlements/pg_ref 노출(ADM-PAY-010). **고객 3화면(주문·환불·결제) 원장 동기화 완결 — 다음: ADM-PAY-010(사용자 지시 "1번부터 순서대로").** **슬라이스 13 — ADM-PAY-010 결제·정산 실데이터화(2026-07-24, T9 일 정산 마감 성문화 — settlements·settlement_batches 첫 쓰기)**: `GET /api/admin/payments` **합본 응답**({fee_rate, today, payments, settles} — KPI 4종이 두 데이터셋 소비, 단일 응답=단일 원천) + `POST /settlements/{date}/close` + `POST /settlements/undo/{log_id}`(신규 api/admin_payments.py). **T9 성문화(ERD §8 행 추가)**: 대상 = own∧(승인·환불) 화이트리스트(취소·대기 = 돈 미이동), 수수료 = pricing_settings.card_fee_rate **Decimal 직접 로드**(price_import._settings는 float라 ±1원 왜곡 — 재사용 기각)·원 단위 half-up·부호 보존(환불 음수 fee = 수수료 환급), **개별 계산→합산**(settlements.fee_amount → batch.fee=Σ, 불변식 보장·안분 금지), settle_mode = 결제 pay_mode 승계("정산은 결제를 따라간다" — ship_mode 전례). **'대기' = 저장 없는 파생 상태**(batch 부재 일자 — batch '대기' 선생성 금지, 어휘 v1 미사용·시드 불필요: 빈 테이블=전 일자 대기가 정직한 초기값), settle_date UNIQUE + ON CONFLICT 무반환 409 = 이중 마감 경합 원자화. **마감 후 동일 일자 유입 = late 정직 표기**("마감 후 유입 N건 — 미정산", 재정산·보정 배치 v2 이관·undo 차단 사유 아님). **undo 허용**(refund complete류 비가역 아님 — 부수효과가 내부 정산 원장뿐, 생성분 삭제 = shipments 전례, 수치는 로그 detail 보존, 3중 가드: 404/이중 409/batch 수치 불일치 409). kind='settlement' 신설. 이관 실현 2건: **환불 settlements 반영**(슬라이스 11 — 07-24 환불 단독 일자가 음수 batch -1,375,000/-30,250/-1,344,750로 마감됨 실증) + **pg_ref 관리자 노출**(슬라이스 12 — PG 대사 탭 TX-84219-R). KPI 클라 계산: 오늘 = 전 채널 순액(**-1,375,000 음수 = 기대값**·text-danger)/이번 달 자체 6,357,000(**MY-030 김민준 누적과 동치 — 교차 일치 실증**)/인계 2건/대기 N건. LIVE 렌더는 서버 계산값 사용(마감 행 = 마감 당시 수수료 보존 — FEE 상수 재계산 금지), 액션 후 reload() 재조회(대기 행이 서버 파생이라 로컬 패치보다 정합). 타임존 v1 = UTC 일자 축 일관(KST 정산일 경계 이관 유지). 검증: GET 손계산 3일자(07-23 6,920,000/152,240/6,767,760·4건 / 07-14 812,000/17,864/794,136 / 07-24 음수·refund_n 1)·close Σ개별=batch 불변식(45,914+38,038×2+30,250=152,240)·이중 마감 409·undo 왕복(대기 재등장 동일 수치·이중 undo 409·kind 로그 3행)·화면 실연(배지 플립·8행·"오늘 (07-24)" 환불 반영·마감 toast 실값→되돌리기 복원·KPI 대기 1↔0)·폴백 원형(KPI 2,087,000·SETTLES 3)·콘솔 0·교차 회귀(my-payments 6·admin-orders 7) 전부 통과. 개입 변경 가드는 재현 수단 없어 코드 리뷰 확인. 잔류: **07-14·07-23 마감 + 07-24 대기**(목업 서사 마감 2·대기 1 재현). 이관: 재정산·보정 배치(v2)·KST 경계·PG 실대사·kind 어휘 정리(ADM-LOG). **커머스 자금 축 완결: 결제(T7) → 환불(T8) → 정산(T9) 원장 삼각형 폐합.** **슬라이스 14 — 단가표 엑셀 실파일 파싱·업로드(2026-07-24, T6 전 구간 실현 — 슬라이스 3의 "실파일 확보 시 별도 슬라이스" 이행)**: 실파일 2본(GBT PCD 0721클릭 = 클릭나라·MSI_단가표0716MTF = 웨이코스 — **시드 baseline과 동명**) 기반. **api/price_parser.py 신규**(순수 함수 — rules(JSONB) 소비: 헤더 **앵커 자동 탐지**(model_col 라벨 상단 12행 탐색 — GBT POWER 2+3행·MSI AMD 2+3행 오프셋 편차 흡수, 시트별 하드코딩 회피)·병합 칩셋 forward-fill·\xa0/공백 정규화·가격 0/빈 셀 무효·병합 부헤더 키 충돌 dedupe) + `POST /api/admin/price-import/upload`(multipart, **dry_run 지원**, 공급처 인식 = rules.file_pattern 파일명 부분 일치 — 실패 400, 바이너리 미보관) + **seed_0008**(rules 확장: file_pattern·model_col(+fallback)·state_col·memo_col, MSI exclude에 저장장치·파워-쿨러 추가(이질 레이아웃 v2 이관 — 응답 skipped_sheets 정직 표기), MSI price_cols를 헤더 정규화 키와 정합 교정) + ERD §11.1 rules 성문·T6 실현 명기. **동명 파일 재수신 허용**(새 file 행·received_at 구분 — GET은 최신만 보므로 재수신=최신본, 동일 내용 재업로드는 diff 변경 0 자연 수렴. 실파일이 baseline과 동명이라 409 차단 시 E2E 불가 — 허용이 실무·데모 정합. 해시 중복 검출 이관). 상태 판정 = state_map 정확 일치 → 키워드(품절·문의) → 기본 가능, **memo '문의' 최종 override**('단종예정'→가능·원문 memo 보존). openpyxl+python-multipart 의존성 추가. 검증: 파서 GBT **193행**(4시트: VGA 71·INTEL 46·AMD 65·POWER 11, 다나와코드 추출·상태 {가능120·품절60·문의13})·MSI **95행**(3시트+제외 4시트 표기)·실업로드 file_id 5/6·인식 실패 400·손상 파일 400(화면 토스트)·**실데이터 첫 실작동 확인**: 결측 대체가("판매가 결측→대형몰딜러가 적용")·유사도 후보(MSI PRO B650M-A→P-2001 37%)·모델명 crossover chg 3건(전부 sku=None 미매칭 — **pending 0 = 상품 가격 무오염**, 기검증 수치 보존)·화면 수신함 2건 실파일·폴백 원형·콘솔 0 통과. 한계 기록: prev-only 행(직전 파일에만 있는 모델)은 diff에 미표시(사라진 모델 카테고리 없음 — 이관), 매칭 확정·신규 상품 편입은 검수 소관(전 행 신규가 정직 결과). **T1~T9 트랜잭션 카탈로그 전 구간 실현 완료.**
>
> **🚧 구현 단계 진입 — DB 범위 승인 (2026-07-23):** ERD 4.0 1차 검토(이슈 6건 반영) 후 **사용자 승인** — P-01 모델링 절대 규칙 중 **DB 스키마·마이그레이션 금지가 해제**된다(백엔드 API 서버·실제 LLM 연동은 여전히 별도 승인 대상). 새 규칙: **목업 = 스펙** — 손으로 검증된 화면 범위만 구현하고, ERD 4.0이 스키마의 단일 원천. 대상 = **Cloud SQL PostgreSQL(구글 클라우드, 기구성)**, DB명 popcorn_pc, 도구 = Alembic + SQLAlchemy, 접속 정보는 GCP_SETUP.md(로컬 전용·커밋 금지). 순서: ① 마이그레이션 뼈대+전체 DDL(로컬 작성·커밋) → ② 전용 DB·계정 생성 → ③ 적용+목업 더미 시드.
>
> **A-10 커머스 확장 전체 완결 — P2 관리자 4종 (2026-07-23):** **ADM-PAY-010 결제·정산**(PG 대사 + 일 정산 — 정산=결제 동행, 수수료 2.2% 차감 순액, 정산 마감, 인계분 분리 집계) · **ADM-SHIP-010 배송 관리**(조립 완료→출고 대기 유입, 송장 자동 발번, 배송 완료) · **ADM-CUS-020 후기 관리**(구매 인증 전제, 신고 숨김 처리, **S2 근거 인용 토글**) · **ADM-CUS-010 회원 관리 승격**(쇼핑몰 계정 매핑 상태 + 동의 기반 매핑 요청 — MY-010 연동, 주문/상담/후기/관심 집계). 사이드바 24페이지 갱신(메뉴 3종 신설 + "회원 관리" 개명). **이로써 A-10 카탈로그(프론트+관리자)의 계획 화면이 전부 목업 완료** — 관리자 22 → 28화면.
>
> **커머스 관리자 짝 완결 — 주문 관리 승격 + 환불·클레임 큐 (2026-07-23):** A-09 시절 "주문 (쇼핑몰 전달)" 읽기 전용 화면을 **ADM-ORD-020 주문 관리**로 전면 승격 — 주문 원장(항상 본 시스템) 관점: KPI·상태/채널(자체·쇼핑몰 인계) 필터·상세(주문 시점 스냅샷 라인 + **주문 시점 운영 모드 스냅샷** 표시 + 결제/배송/재고 hold 블록)·상태 진행 액션(출고·배송 완료, 되돌리기·작업 기록). **ADM-CLM-010 환불·클레임** 신설 — 고객 접수(RF-1024)와 연동된 마스터-디테일 큐, 4단계 처리, **모드 분기**(자체=수거·PG 환불 / 인계=쇼핑몰 절차 결과만 기록, 원장은 본 시스템), 완료 시 결제 내역 환불 행·재고 복귀 명시. 사이드바 23페이지 갱신(라벨 개명 + 메뉴 신설). **A-10 커머스 P1 화면 세트(고객+관리자) 완결.**
>
> **커머스 고객 여정 완결 — S5·주문 내역·결제 내역·환불 접수 (2026-07-23):** 쇼핑몰 기본 기능(장바구니·주문목록·결제내역·취소/환불/반품) 검토 → 확정: **장바구니 = 견적 체크아웃 유지**(보관형 아님) · **결제 내역 = 별도 탭**. 신규 3화면: **S5-010 주문 완료**(S4 자체 결제 성공 시 자동 이동, hold→확정 배정·4단계 일정·스냅샷 보존 서사) · **MY-020 주문 내역**(상태 필터 목록 + 주문 시점 스냅샷 상세 + 상태별 취소/환불/반품 접수 플로우 — **환불 모드 own/mall 분기**(A-10 스위치 연동): 접수는 항상 본 시스템, 인계 모드면 처리만 쇼핑몰 절차 안내 + RF 추적) · **MY-030 결제 내역**(결제 관점 원장 — 승인/취소/환불 행). 마이페이지·게이트웨이 연결. 커머스 화면들은 탐색형 쇼핑몰 UI가 아니라 **원장·투명한 진행** 중심(정체성 유지).
>
> **ERD Ver 4.0 개정 (2026-07-21):** DB 착수 검토 결과 **"ERD 개정까지만 먼저"**(사용자 확정) — 실제 DB 생성·마이그레이션은 개정본 검토 후 **별도 승인**으로 착수하며, 그 전까지 모델링 단계 규칙은 유지된다. DB 위치 = **구글 클라우드(기구성, 사용자 확인)**. 개정 내용([06_db-erd.md](../06_db-erd.md)): Ver 3.0 원칙 3(재고 수량 미보유·상태만 동기화) **폐기** → 재고 단일 원장 = 본 시스템(A-10) / **§10 커머스 원장**(members·orders(항상 생성, ops 스냅샷)·payments·settlements·shipments·refunds·stock_reservations(hold)·stock_movements·후기(구매 인증 강제)·관심 부품) / **§11 공급처 단가표**(suppliers·프리셋 JSONB·일일 rows 스냅샷(diff 원천)·모델↔SKU 매핑(매칭 우선순위 4단계)·products.danawa_code·상품 1:N 공급처 가격) / **§12 상담·견적 스냅샷**("그때 가격으로 다시 보기") / **§13 운영 설정 버전 관리**(스위치 5종 + pricing_settings 카드수수료·마진) / specs에 주변기기 사양 8필드 + 함께 구성 후보 뷰(v_companion_candidates) + 주변기기 필수 사양 매트릭스.
>
> **주변기기(부자재) 취급 확정 (2026-07-21):** **모니터 · 키보드 · 마우스 · 스피커 · 헤드셋 · 웹캠**을 취급 품목에 포함(OS·소프트웨어는 제외 — 사용자 확정). 견적과의 관계 = **본체 견적과 분리된 "함께 구성" 섹션**: 엔진 검증 대상이 아니라 **권장 매칭 근거**(주사율↔GPU 성능, 출력 포트 호환 등)로 제안 — A-02(결정론 엔진)·후보 풀 카운터(본체 부품 26,480)는 그대로 유지. 분류별 필수 사양 세트 상이(모니터: 크기·해상도·주사율·패널·포트). 착수 순서: ① 관리자 분류·검수 확장 ✅(상품 목록 주변기기 5종 + 검수 모니터 케이스) → ② S2 "함께 구성" 섹션 ✅(2026-07-21 — 외부 견적 진단과 클로징 배너 사이, 주변기기 4종 카드: 실재고 뱃지 + "이 견적 기준" 근거(tertiary, 주사율↔GPU·포트 호환·용도 매칭) + 담기 토글·선택 합계. "이미 갖고 계시면 그대로 두세요" 무압박 카피) → ③ S1 대화 인지·S4 장바구니 ✅(2026-07-21 — 팝콘톡 `talkMon()`: 모니터 언급에서 인치·듀얼·보유/구매 의향을 추출해 조건화("32형 듀얼 보유" — 롤플레이에서 발견한 구멍 해소), 듀얼이면 출력 포트 확인 안내 / S2 담기 선택이 localStorage `popcorn-periph`로 S4에 이어져 "함께 구성" 그룹 행 + 요약 라인 + 총액 자동 반영). **주변기기 3단계 로드맵 전체 완료.** 남은 것: 완전 맥락 인지형 응답(이미 말한 게임 되묻기 등)은 LLM 파일럿 착수 시.
>
> **가격 산정 현행 공식 + 단가표 일일 반영 (2026-07-21):** 매입 구조 = 다중 거래처가 **윈윈 플랫폼**에 단품가를 입력(상품 1:N 공급처 가격 — ADM-CSV-010 결정과 일치), **판매가 = 매입가 + 카드수수료 + 마진(기본 0%)**. **매입가 판정 = 단가표 가격 열 중 최저가**(2026-07-21 — 특정 열 고정 매핑이 아님. 통상 공급가가 최저, 결측 시 다음 최저가가 자동 적용). 마진 상세 구조는 여전히 향후 확정. **MSI 가격지도(단가 단속·최저 노출가) 규정은 미적용** — 팝콘PC는 단품을 네이버·다나와 등 외부에 가격 노출하지 않으므로 해당 없음(사용자 확인). 실제 거래처 단가표(MSI_단가표0716, 1일 1회 수신)를 분석해 **ADM-PRC-040 단가표 일일 반영** 화면 신설: 공급처 프리셋(다중 시트·상이 레이아웃·헤더 2줄·그룹 캐리포워드 정규화) → 모델명↔SKU 매핑 기억 → **어제 대비 diff만 표시**(가격 변동/신규/발주 상태 전환, 무변동 자동 통과) → 판매가 자동 재계산 → 일괄 반영(되돌리기·가격 이력 연동).
>
> **커머스 승격 + 운영 모드 스위치 (A-10, 2026-07-16):** 본 시스템이 오픈 단계부터 **회원가입·장바구니·결제/정산·배송·환불을 직접 처리**한다(기존 "주문 원장=쇼핑몰" 결정 번복). 기존 쇼핑몰은 즉시 폐쇄하지 않고 **병행 채널**로 유지하되, 도메인별 **운영 모드 스위치 5종**(회원 연동 · 결제 · 정산 · 배송 · 환불 — 각각 `자체`/`쇼핑몰`)을 운영자 설정으로 분기한다. 원칙: (1) **주문 원장은 모드와 무관하게 항상 본 시스템에 생성** — 인계 모드는 복제 전달일 뿐(스냅샷 재현·감사 체계 유지). (2) 스위치 변경은 관리자 전용 + 설정 버전·작업 기록 필수. (3) **재고 차감 단일 원장 = 본 시스템**, 쇼핑몰 판매분은 API 유입으로 정합 유지, 결제 진행 중 재고는 예약(hold). 전환 시나리오: 1단계 전부 인계(오픈) → 2단계 결제만 자체(병행) → 3단계 전부 자체. 상세 화면 카탈로그는 [`admin-identity.md`](admin-identity.md) 개정판. ⛔ 이에 따라 A-09의 "주문·고객 읽기 전용"과 UX-12의 "쇼핑몰 결제 릴레이 고정"은 개정됨.
>
> **관리자 화면 정체성·카탈로그 확정 (A-09, 2026-07-07 · ⚠️ 일부 A-10으로 개정):** 단일 원천 [`docs/decisions/admin-identity.md`](admin-identity.md). 관리자 = "엔진이 거짓말하지 않도록 재고·사양·규칙을 신뢰 가능한 상태로 유지하는 곳"(쇼핑몰 백오피스 아님). 22화면을 4계층(엔진 코어/가격·매입/관측 창/쇼핑몰 경계/운영 인프라)으로 분류. **주문·고객 = 읽기 전용**으로 확정(기존 미정 "주문 경계" 해소 — 주문은 여기서 생성 안 함, 쇼핑몰 원장).
>
> **아이콘 정책 변경 (D-10, 2026-07-16):** 기존 "단색 currentColor SVG만" 폐기 → **Streamline Ultimate Color**(iconify `streamline-ultimate-color`, **CC BY 4.0 — 출처 표기 필수·상업적 이용 가능**)로 전면 교체. 고객 7화면(Material Symbols)+관리자 21화면(feather) 총 113종 매핑, `mockups/shared/icons/su/` 로컬 보관, `su-icons.js` 런타임 스와퍼(MutationObserver — 동적 렌더 대응)가 교체+**모든 페이지에 출처 표기 자동 삽입**.
>
> **S1 점진 공개 리디자인 진행 중 (UX-17, 2026-07-16 착수 · A안 확정):** 팝콘톡(?mode=talk)부터 "첫 5초엔 대화만" 원칙으로 재설계. **4단계 로드맵**: ①첫 화면=빈 화면+하단 입력창만(placeholder '무엇이 궁금하신가요?', 배너·칩·인사 전부 제거) **✅완료** → ②대화 구멍 메우기(인사 대응 버그·막연형 '컴퓨터 사려고요' 되묻기·조립비/AS 답변) + 조건 첫 발생 시 우측 패널 슬라이드-인('방금 말씀이 반영됐어요' 하이라이트) → ③조건 축적→견적 준비 서사(확정 버튼 등장 타이밍) → ④guided/chat 확산+모바일(하단 요약 바). **예상 키인 10유형 분류 완료**(용도+예산/부품지식/시세/호환/업그레이드/막연형/타사비교/서비스질문/인사/가격의심) — 패널 트리거=조건 생성 유형(1·5·6)만. ②-1 대화 구멍 메우기 ✅완료(인사·감사 프리체크 / 막연형→용도 되묻기 칩→모르겠→예산 되묻기 칩 2단 체인 / 사무·인강 용도 / 조립비 3만원 / A·S 2년 / 다나와 비교→붙여넣기 유도 / 가격 의심→가격의 이유 서사 / 답변 후속 칩 talkRow). ②-2 패널 슬라이드-인 ✅완료(2026-07-21 — talk 초기화 시 패널을 0폭 트랙+투명으로 잠복 / 조건 첫 발생 시 `talkReveal()`이 그리드·칼럼 폭·패널을 0.55s 슬라이드-인 / "방금 말씀이 이렇게 반영됐어요" 콜아웃 5.6초 표시 / 새 조건 칩 앰버 플래시 / 정보성 답변(조립비·AS 등 p 없는 항목)은 패널 미등장 / 패널 용어 talk 한정 고객 언어 개역: "실시간 재고 검증 파이프라인"→"지금 고를 수 있는 부품", "현재 제약 요약"→"지금까지 잡힌 조건", "호환성 5종"→"조립 호환 검사" 등 / rAF 정지 대비 setTimeout 폴백). ③ 견적 준비 서사 ✅완료(2026-07-21 — **견적 재료 = 용도(또는 상황)+예산**: 둘 다 잡히는 순간 1회 "이 정도면 견적을 만들 수 있어요" 선제 제안 + 대화 내 CTA(이 조건으로 견적 받기→S2 / 조금 더 다듬을래요→저소음·화이트·재활용 칩) + 이때 비로소 사이드 확정 버튼 활성화 / 용도만 있고 예산 없으면 답변 뒤 예산 넛지 1회(예산 칩 3개 — "모르겠어요" 체인과 동일 칩) / talk 확정 버튼 잠금 캡션 "용도와 예산을 알려주시면…" / TALKS에 화이트·디자인 항목 보강(우선순위 조건 생성) / 부수 수정: S1 확정 버튼이 스타일만 회색이고 실제 disabled가 아니어서 조건 0개에도 클릭되던 전 모드 공통 버그 → 초기 `disabled` 실제 잠금). ④ guided/chat 확산 + 모바일 ✅완료(2026-07-21 — **점진 공개 패턴 전 모드 공통화**: `panelLatent()`/`panelReveal()`로 일반화, guided·chat도 첫 화면은 패널 잠복(중앙 760px 대화 칼럼)→조건 첫 발생 시 슬라이드-인+콜아웃, 트리거는 `addConstraint` 래퍼 단일 지점(칩·자유입력·시나리오·과제약 전 경로 커버) / 패널 고객 언어 개역도 guided·chat 공통 적용 / **모바일(≤768px)**: 세로 스택 + 조건 첫 발생 시 하단 요약 바("조건 N개 · 후보 M개" 라이브 갱신 — animate·addConstraint 훅) + "조건·견적 보기" 탭 시 패널이 바텀 시트로 등장(견적 확정 버튼 포함), 허브 FAB는 바 위로 이동 / expert 모드는 기존대로 패널 없음). **UX-17 4단계 로드맵 전체 완료.**
>
> **S1 4번째 모드 "팝콘톡" 추가 (UX-15, 2026-07-14):** 기존 3모드(맡김/함께/직접) 위에 **자유 문답 모드 `?mode=talk`** 추가 — 아는 사장님께 묻듯 부품 지식·성능·가격 동향·호환을 LLM과 대화. 앰버(#c77b1f) 아이덴티티, s0-landing 4번째 레인. **A-03 유지**: LLM은 지식 답변+대화 파싱만, 파싱된 조건(예산·용도)은 동일 제약객체에 쌓여 우측 실시간 재고 검증에 반영, 견적 생성은 여전히 엔진. **오프토픽 가드**: PC 무관 질문엔 이유 설명 후 서비스 특성에 맞는 질문 예시 칩으로 유도.
>
> **S1 우측 패널 "예상 예산 범위" 섹션 (UX-16, 2026-07-14):** 실시간 재고 검증과 현재 제약 요약 사이에 추가. 제약객체(대상·용도·예산)에서 용도 티어를 도출해 범위 자동 갱신(예: 사무 40~80 / 보급 게이밍 100~140 / 자녀·최신 160~200 / 4K·하이엔드 200~300). 사용자가 예산을 말하면 범위 바에 마커 + 적정/빠듯/여유 판정. 수치 근거 = 2026-07 시장가 조사(견적왕·다나와·퀘이사존) + 자체 재고가. addConstraint 단일 후킹으로 guided/chat/talk 전 모드 공통(expert는 aside 없음). **같은 라벨 조건은 누적이 아니라 교체**(단일 제약객체 원칙 — 요약 카드 제자리 갱신+플래시, 예산 범위 즉시 재계산). guided 단일 선택은 답한 뒤에도 칩이 살아 있어 **실수 정정 시 재선택 가능**(수정 대화 + 교체 반영, 진행 흐름은 유지).
>
> **관리자 용어집 확정 (UX-14, 2026-07-07):** 모든 관리자(운영자) 화면 라벨의 단일 원천은 [`docs/decisions/admin-terminology.md`](admin-terminology.md). 은어(큐·풀·import·verified·candidate·소싱·릴레이) 퇴출, 운영자 언어로 확정. 핵심: 검수 큐→**상품 검수**, 원본 조회→**상품 원본 자료**, 후보 풀→**추천 가능 재고**, 치명필드/verified→**필수 사양/확인 완료**. 새 화면은 이 표를 따르고, 없는 용어는 표에 먼저 등록 후 사용.

**상태 범례:** ✅ 확정(번복 금지·근거 있음) · 🟡 미정(임의 결정 금지, 사용자 확인 필요) · ⛔ 폐기(과거 방향, 되살리지 말 것)

---

## 1. 정체성 · 아키텍처 (근본, 번복 금지)

| ID | 결정 | 상태 | 근거 |
|----|------|------|------|
| A-01 | 포지셔닝은 **"신뢰 판매자"** — 가격비교 포털이 아니다. 파는 것은 부품이 아니라 안심. 히어로 = "모든 견적에는 이유가 있습니다." | ✅ | 정체성의 근간. 모든 화면 카피·구조가 여기서 파생. |
| A-02 | 추천 = **결정론적 룰/스코어 엔진**이 자체 실재고 위에서 수행. 같은 입력 + 같은 재고 → 같은 결과(재현성). | ✅ | 재현성 확보 + 근거가 창작이 아닌 사실이 되게 함. |
| A-03 | **LLM의 역할은 입력 파싱 + 근거 설명뿐.** 견적을 생성하지 않는다. | ✅ | 신뢰 판매자 정체성의 기술적 근거(환각·창작 차단). |
| A-04 | MVP 3종은 독립 서비스로 병렬 포지셔닝하되 **엔진 ①을 2·3이 공유.** ①(단독 AI 견적)이 코어·최우선 오픈. | ✅ | HANDOFF §1. 현재 모든 목업 대상은 MVP1. |
| A-05 | MVP3(입점 가격비교)는 **외부 크롤링 미사용, 입점사 제공 가격만** 사용. | ✅ | 법적(ToS) 리스크 제거. |
| A-06 | 현재 다루는 GCP 서버 = **개발 서버.** 운영 서버는 별도 시스템으로 추후 구현. | ✅ | HANDOFF §1, GCP_SETUP. |
| A-07 | **다크 톤 채택은 '톤'에 한함 — 정체성 서사는 불변.** 어떤 랜딩·화면도 "3대 AI(ChatGPT·Claude·Gemini)가 견적 생성·합의" 및 "최저가" 카피를 쓰지 않는다. 견적은 결정론 엔진, 카피는 "모든 견적에는 이유가"·검증 파이프라인·근거 리포트. | ✅ 2026-07-02 | 톤 피벗 시 서사 오염 방지(A-01·A-02·A-03·X-01·UX-07 재확인). |
| A-08 | **멀티 AI는 입력 파싱(고객 의도 교차 해석)·근거 설명에만 허용.** 견적 생성·합의·비교·최저가는 금지. main-landing 엔진 섹션 = 5단계로 통합(01 AI 의도 해석 / 02~05 결정론 엔진: 재고→부품 도출→호환성→확정). | ✅ 2026-07-04 | 멀티 AI로 '입력 이해 정확도'는 올리되, 재현성·근거의 사실성은 엔진이 지킴(A-03·A-07 정교화). |

---

## 2. 단계 원칙 (절대 규칙)

| ID | 결정 | 상태 | 근거 |
|----|------|------|------|
| P-01 | **현재 단계는 모델링.** 유일한 산출물은 클릭 가능한 UI 목업. 백엔드/API·DB 스키마·LLM 연동·서버 배포·npm 빌드 도입 **전부 금지.** | ✅ | 과거 UI 미확정 상태로 뒷단부터 짓다 실패 반복. |
| P-02 | 뒷단 착수는 **사용자가 "구현 단계로 간다"를 명시 선언한 뒤에만.** 요청이 이 규칙과 충돌하면 코드를 짜지 말고 먼저 확인한다. | ✅ | CLAUDE.md §0. |
| P-03 | 모델링 순서는 **가장 불확실한 S1(대화형 세션)부터** → 브라우저에서 직접 클릭·타이핑해 검증 → 어색하면 그 자리에서 갈아엎기. | ✅ | S1 통과 시 나머지는 빠르게 따라옴. |

---

## 3. UX 결정 (확정, 근거 있음 — 번복 금지)

| ID | 결정 | 상태 | 근거 |
|----|------|------|------|
| UX-01 | **대화 입력이 주(主), 칩은 가속기.** 칩은 입력창보다 작고 낮은 위계로 종속시킨다. | ✅ | HANDOFF §2. |
| UX-02 | 입력창 **전면**, 세일즈·설득 요소는 모두 **하단.** | ✅ | S0 히어로 = 입력 경험 자체가 후크. |
| UX-03 | **초급/고급 하드분기 삭제** → 하나의 대화 세션에 **기어 2개**(추천질문 ⇄ 직접입력), 사용자가 언제든 강제 전환. | ✅ | 화면 9개(beg/exp 스텝)를 S1 1개로 통합. |
| UX-04 | **기어를 전환해도 이미 파싱된 제약은 유지(상태 보존).** 제약 요약이 단일 원천이고 두 기어·자유입력이 모두 이걸 읽고 쓴다. | ✅ | **S1 최대 구현 함정.** 어기면 "직접입력으로 바꿨더니 방금 고른 게 날아감" 최악 이탈. |
| UX-05 | 결과·근거·비교를 **한 장짜리 스크롤 제안서(S2)**로 통합. 슬라이드로 쪼개지 않고 앵커 탭 이동. | ✅ | HANDOFF §2, S2 프롬프트. |
| UX-06 | **호환성 배지를 가격보다 먼저** 노출. | ✅ | 판매자 약속의 증명. |
| UX-07 | 비교견적은 가격 레이스가 아니라 **정직 프레임**("왜 이 가격/왜 우리가 더 안전한가"). 우리가 비싸도 숨기지 않는다. | ✅ | 정체성 일관. |
| UX-08 | **후보 풀 카운터 = 완성 PC 대수가 아니라 "전체 부품에서 조건 통과한 부품 수".** | ✅ | 개념 교정 완료. HANDOFF §2. |
| UX-09 | 게이트웨이(index) = **내부자 전용 바로가기 허브**(직원/운영자/개발자). 세일즈 아님, 속도·명료 우선. | ✅ | HANDOFF §2. |
| UX-10 | **로그인 강제는 S4(주문 확인)에서만.** 그 전(랜딩·세션·제안·변경)은 게스트로 진행. | ✅ | 이탈 최소화. S0·S4 프롬프트. |
| UX-11 | S3 부품 변경 시 **호환 100% 통과한 대안만 노출**, 스왑으로 위험 발생 시 **결제 잠금(disabled)**, 연쇄 스왑은 정직하게 설명 후 승인 시 자동 동시 교체. | ✅ | "바꿔도 조립 안 되는 구성은 안 나옴." |
| UX-12 | S4 결제 직전 **최종 실재고 재확인** 필수. 조립비·배송비는 총액에 숨기지 말고 별도 행으로 투명 표기. ~~결제는 기존 쇼핑몰 결제 레일로 릴레이 고정~~ → **A-10 개정: 결제는 운영 모드 스위치에 따라 자체 PG / 쇼핑몰 인계로 분기** (릴레이는 '인계 모드'로 존치). | ✅ 개정 2026-07-16 | 신뢰의 마지막 방어선. A-10 참조. |
| UX-13 | **진입 3모드 정의(축 = 누가 운전하나).** ① AI에게 맡길게요 = AI 인터뷰(객관식·타이핑0) → **추천 1구성**(초보·바쁜). ② AI와 같이 정할게요 = 자연어 대화 + 시나리오 → **함께 조율(추천)**(일반). ③ 직접 고를게요 = 사용자가 **부품 직접 지정** → AI는 **검증만**(호환·병목·검수, *추천 안 함*) → **통과/경고 리포트**(매니아). **②와 ③의 경계 = 추천(②) vs 검증(③).** UX-03(기어 2개)을 진입 3모드로 확장·정교화. ③ expert는 기존 '자유텍스트→추천'을 '부품지정→검증'으로 갈아엎음. | ✅ 2026-07-04 | 세 갈래가 겹치지 않게. S0 3레인 카피와 일치. |

---

## 4. 디자인 계약 (확정)

| ID | 결정 | 상태 | 근거 |
|----|------|------|------|
| D-01 | 디자인 시스템 = **Vibrant Horizon.** 토큰 단일 원천 = `design-system/tokens.css`의 `:root`. 임의 HEX/px·인라인 스타일 금지. | ✅ | CLAUDE.md §3. |
| D-02 | 서체 = **Inter(라틴·숫자) + Pretendard(한글) 페어링.** Inter 단독 금지(한글 깨짐). 숫자·가격은 Inter tabular. | ✅ | |
| D-03 | 팔레트: primary `#005ab3`(액션) · `#0082fd`(hover) · tertiary `#853d97`(근거·태그) · error `#b31b25`(위험). | ✅ | primary는 과거 `#0075d5`에서 `#005ab3`로 갱신된 정본. |
| D-04 | ~~아이콘은 단색 currentColor 세트만. 풀컬러 팩 금지~~ → **D-10(2026-07-16)으로 폐기.** 현행 정책 = Streamline Ultimate Color 전면 사용(상단 블록쿼트 D-10 참조). | ⛔ | 2026-07-16 사용자 지시로 번복. 이 행은 기록 보존용. |
| D-05 | 마크업 계약(Stitch 승계): 최상위 `data-screen-id`·`data-domain`, 동적값 `data-bind`, 반복 `data-repeat`, 액션 `data-action`, 예외/상태 UI는 `hidden`. 시맨틱 태그·alt·label·aria 준수. 한국어 UI(기술 약어만 영문). 768px 이하 세로 스택. | ✅ | CLAUDE.md §3. |
| D-06 | **후보 풀 카운터 시각화 = "검증 파이프라인"(안 B).** primary 헤더 밴드 + 실시간 숫자 틱다운·델타 + 검증 3단계 체크리스트(실재고 확인·호환성 5종·조건 반영). 도트 40개·진행바는 폐기. '조건 반영' 단계만 상태 연동(0개일 때 "대기 중"으로 정직 표기). | ✅ 2026-07-02 | 3안 비교 후 선택. "검증 절차 = 증거" 프레이밍이 신뢰 판매자 정체성(A-01)에 밀착. 기존 U-03 대체. |
| D-07 | **디자인 톤 = "Popcorn Light"(랜딩 준용)로 개정.** 사용자가 `Popcorn AI 랜딩.html`의 **밝은 본문 시트 톤**(다크 히어로가 아니라 라이트 콘텐츠)을 다른 화면에 준용하기로 선택 — **다크모드 아님.** 캔버스 `#F0F5F9`, 카드 `#ffffff`, 텍스트 `#111827`·뮤트 `#64748B`, 액센트 블루 `#65A1D4`(hover `#4A90E2`), 경계 `#DCE7F2`. 서체: 헤드라인 **Hanken Grotesk**+Pretendard(한글), 본문 Inter+Pretendard, 수치·라벨 JetBrains Mono. **기존 Vibrant Horizon 색·서체(D-01~D-03)를 이 톤으로 대체**하되 토큰 단일원천·단색 아이콘(D-04)·data-* 마크업(D-05)·검증 파이프라인(D-06)은 유지. | ✅ 2026-07-03 | 사용자 톤 선택(밝은 배경·폰트·폰트사이즈 준용). 서사는 A-07로 불변. |
| D-08 | **디자인 표준 = "Warm Confidence"(S2 신규 디자인)로 개정 — D-07 대체.** 사용자가 디자인 툴로 뽑은 S2(`S2 견적 제안서 standalone`)를 표준으로 채택. 캔버스 크림 `#f1eee8`, 카드 `#fff`·다크카드 `#17171b`, 액센트 블루 `#2b5fd9`, **핵심 색상 블록**(호환=초록 `#2f9e6d` / 검수=파랑 `#2b5fd9` / A/S=보라 `#6a3fbf` / 비교=앰버 `#c77b1f`) + "핵심 N" 배지. 서체 **Pretendard**, 아이콘 **Material Symbols**(단색). 라운드 22px. **워크플로우: 화면별 디자인 툴 .html 출력을 사용자가 제공 → 내가 번들 해제·폰트 CDN 복구·`data-screen-id`·상단바(대화창+3탭)·라우팅·데이터 배선 후 배포.** 서사(A-07)·검증 파이프라인 개념은 유지. | ✅ 2026-07-03 | 사용자가 디자인 툴 산출물 채택. D-04(단색 아이콘)는 Material Symbols(단색)로 충족. |
| D-09 | **관리자(ADM-\*) 화면 = Phoenix 관리자 템플릿(Bootstrap 5.3) 스타일.** 밝은 쿨그레이 캔버스 `#f5f7fa` · primary `#3874ff` · **Nunito Sans + Pretendard(한글)** · 라운드 0.375rem · 표·카드·배지 중심 데이터 밀도 UI. **고객 화면(Warm Confidence, D-08)과 의도적으로 분리** — 운영자 vs 구매자, 목적이 다르니 톤도 다르다. Phoenix 컴파일 자산(theme.min.css + 필요 벤더)을 `mockups/mvp1/admin/`에 복사해 그 위에 제작 — **빌드 시스템(Gulp/Pug/npm) 미도입**, CSS/JS는 정적 자산으로만. `apps/e-commerce/admin` 페이지(products·add-product 등)를 각색 소스로 사용. | ✅ 2026-07-07 | 운영자용 실용·고밀도 UI 필요 + 사용자 선호 스타일(Phoenix) 채택. D-08은 고객 화면 전용으로 범위 한정. |

---

## 5. 미정 결정 (🟡 임의로 정하지 말고 사용자 확인)

| ID | 미정 항목 | 맥락 · 선택지 | 관련 |
|----|-----------|--------------|------|
| U-01 | **success(초록) 토큰 추가 여부** | Vibrant Horizon엔 초록 계열이 없다. 현재는 '통과/정상'을 primary로, '위험'을 error로 표기하고 **항상 텍스트 병기.** `--color-success`를 정식 추가할지, primary 통일을 유지할지. 결정하면 S2·S3·S4에 **일괄** 적용(화면마다 다르면 안 됨). | tokens.css, S2·S3·S4 |
| U-02 | **S2 '커뮤니티 근거' 소스** | 근거 태그 "커뮤니티 최다 추천"의 데이터 출처. 권고안 = **내부 판매/스왑 통계**(외부 커뮤니티 크롤링 아님). 데이터 확보 가능성과 직결. | S2 §3 |
| U-04 | **게임 무게 3분류 대표 게임 라인업** | 가벼운(롤·오버워치) / 인기(배그·로스트아크) / 무거운(사이버펑크·AAA) — 대표작 확정 필요(마케팅·데이터와 합의). | S1 §2 |
| U-05 | **게이트웨이 딥링크 실제 라우팅** | 현재는 Screen 키 전달 "시늉"(toast만). 실제로 화면이 열리려면 앱에 URL/쿼리 파라미터 라우팅(`?screen=…&mvp=…`) 도입 필요 — 다음 이터레이션. | index.html |
| U-06 | **비교견적 방향(S2 §4)** | 가격표 중심(4-1)으로 갈지, 외부 견적 진단 중심(4-2)으로 갈지. 정체성상 진단(4-2)이 우리에게 유리. 최저가 데이터 출처(제휴 API vs 시점 정직표기)와 연동. | S2 §4 |
| U-07 | **히어로 배경 톤(S0)** | 단색 틴트 vs 은은한 그라디언트/일러스트. 라이브 재고 신호 노출 강도. 프리미엄 톤 vs 정보 위주. | S0 적용노트 |

---

## 6. 폐기된 방향 (⛔ 되살리지 말 것)

| ID | 폐기된 것 | 대체 |
|----|-----------|------|
| X-01 | 3사 LLM 병렬로 견적을 **"생성"**하던 구조 | 결정론 엔진을 주엔진으로(validator 승격). LLM은 파싱·설명만 (A-02·A-03). |
| X-02 | 초급/고급을 **별도 화면으로 하드분기**(beg-step1~4 + exp-step1~5, 9개 스텝) | 단일 대화 세션 S1 + 기어 2개 (UX-03). |
| X-03 | 근거 리포트를 LLM에 "이 구성 설명해줘"로 **사후 생성** | 결정론 엔진의 하드필터·점수·가중치 산출값을 바인딩, LLM은 문장 다듬기만 (S2 적용노트 1). |
| X-05 | **Vibrant Horizon 톤**(primary `#005ab3` 청·보라, 캔버스 `#f6f5ff`, Inter 단독) — 기본 톤으로는 폐기 | **Popcorn Light 톤(D-07)**으로 대체(캔버스 `#F0F5F9`·액센트 `#65A1D4`·Hanken Grotesk, 2026-07-03 사용자 선택). 마크업·구조·검증 파이프라인 계약은 승계. |

---

## 7. 인프라 현황 참고 (모델링 중엔 건드리지 않음)

- 리전 `asia-northeast3`(서울) 고정. VM `popcorn-app`(e2-small), Cloud SQL `popcorn-db`(PostgreSQL 16). 상세·민감 식별자는 `docs/infra/GCP_SETUP.md`(로컬 전용).
- **DB 구축 완료(2026-07-23)**: `popcorn_pc` DB + `popcorn_app` 계정 + pg_trgm, Alembic `0001_initial_schema` 적용(테이블 46·뷰 2, ERD 4.0), `db/seed/seed.sql` 투입(상품 13·주문 4·환불 1·설정 프리셋). 개발 PC IP를 승인된 네트워크에 등록(유동 IP — 상세는 GCP_SETUP.md).
- 스키마 변경은 **ERD 개정 → 새 마이그레이션** 순서. API 서버·LLM 연동·운영 배포는 여전히 별도 승인 필요.

---

## 갱신 규칙

1. 미정(🟡)이 확정되면 → 해당 행을 §1~4의 적절한 표로 옮기고 상태를 ✅로, 근거·결정일 기입. §5에서 제거.
2. 확정을 번복하면 → §6(폐기)으로 옮기고 대체 결정을 남긴다. 이유 없이 조용히 지우지 않는다.
3. 이 파일과 목업/프롬프트가 어긋나면 **이 파일이 우선**이며, 목업을 맞춘다.
