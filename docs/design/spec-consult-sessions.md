# 견적 상담 기록 (ADM-ORD-010) — 승인 디자인 계약 · **1a 기준 + 우측 서랍**

> **원안**: Claude Design 프로젝트 `22248237-152f-4f13-8201-54afca75ebe4` 의
> `견적 상담 기록.dc.html` (2안 캔버스). **원안이 아카이브다.**
>
> **사장님 확정(2026-08-14):** *"1a 안을 기준으로 만드는데, 오른편에서 상세 창이 나오는 것은
> 포함시켜줘."*
>
>     기준     1a — KPI 5장 · 방식 칩 · 목록 표 · 페이지네이션
>     바꾼 것  상세를 **행 아래 펼침(1a)이 아니라 우측 서랍(1b)** 으로 낸다
>     가져올 것 1b 서랍의 구성 전부 + 1b 의 「완주 이후 원장이 둘」 안내 띠

## 화면 정보

    화면 ID   ADM-ORD-010
    성격      **조회 전용**
    LNB       상품사양관리 그룹, 「추천 가능 재고 현황」 다음

## 공통 확정

**① 완주 이후 상태는 `orders` · `handoffs` 를 둘 다 본다**
> **orders**(6/25~8/10, A-10 커머스 시절 — 이후 갱신 없음) · **handoffs**(8/11~, 지금 확정
> 범위의 인계 원장 — 실제로 쌓이는 중). **한쪽만 보면 최근 인계가 이 화면에서 사라집니다.**

상단 안내 띠(1b 것)를 **1a 기준 화면에도 넣는다.** 우측 문구: 「우리는 팔지 않고 인계합니다」

**② 페이지네이션 · 필터를 여는 전제로 설계하되, 지금 한계를 숨기지 않는다**
> 지금 서버는 **최신 200건만** 돌려줍니다 — 그 이전 4,219건은 아직 볼 수 없습니다
> (백엔드 확장 대기)

필터·기간 선택도 「백엔드 확장 후 열립니다」로 잠근다.

**③ 스냅샷은 당시 원문 그대로** — **지금 재고로 재계산하지 않는다**
> 재고가 바뀌면 결과가 달라지는 것이 정상이므로, **재현은 「저장된 원문 그대로」를 뜻합니다**

⚠ 스냅샷 형식이 **구형(배열)이어도 화면이 죽지 않게 방어적으로 읽는다.**

**④ 직접(expert) 모드는 다른 모드와 동등하게 두되 0건을 그대로 보인다**
> 직접(expert)은 라벨까지 준비돼 있지만 실사용 **0건**입니다 — 다른 모드와 같은 자리에 두되
> 0을 그대로 보여줍니다.

**⑤ 회원 귀속 없음은 오류가 아니다**
> 회원 귀속이 없는 세션(99.98%)은 오류가 아닙니다 — **실 인증이 붙기 전이라 정상**입니다

## 화면 구조

**KPI 5장** — 전체 세션 / 오늘 / 완주율 / 이탈 / 완주 이후(orders / handoffs)

**방식 칩** — 전체 · 맡김(guided) · 함께(chat) · 팝콘톡(talk) · 직접(expert)
0건인 칩은 흐리게 하되 **숨기지 않는다.**

**목록 표** — 세션 / 시각 / 방식 / 제약 요약 / 대표 견적 / 상태

상태 4종:
```
✓ 완주                 스냅샷 보유
— 이탈                 견적 미생성
완주 → 주문(orders)     옛 원장
완주 → 인계(handoffs)   지금 원장
```
견적 없는 행의 금액은 `—`.

**하단** — 「전체 N건 중 …」 + **위 ②의 200건 한계 경고** + 페이지네이션

## 우측 서랍 (1b 에서 가져온다) — 행을 누르면 열린다

**머리** — 세션 번호 · 상태 배지 · 닫기 / 시각 · 방식 · 회원 · 방문자 키

**① 제약 객체 — 원문 그대로** (칩)
> 상담 시작 시점의 제약을 그대로 저장합니다 — 나중에 이 값이 바뀌지 않습니다.

**② 완주 세션이면 — 티어 스냅샷 3벌 + 대표 스냅샷 부품 구성**
- 가성비형 / 추천형 / 고성능형 카드(총액 · 부품 수 · **대표** 배지)
- 대표 스냅샷의 부품 목록(슬롯 · 상품명 · **당시 가격**)
- 컴패니언 제안도 함께 저장돼 있으면 밝힌다

**③ 이탈 세션이면 — 스냅샷 없음을 그대로 말한다**
> 스냅샷이 하나도 없습니다 — 이탈로 봅니다. 상담은 시작됐지만 견적이 만들어지지 않았습니다.

⚠ **추정을 단정으로 적지 않는다.** 원안이 「예산과 하한이 충돌했을 가능성이 높다」라고 쓰되
**「다만 이 화면은 추정을 단정으로 적지 않습니다」**를 붙였다. 그 태도를 지킨다.

**④ 완주 이후 상태 — 두 원장 모두 확인**
`orders` 한 줄 · `handoffs` 한 줄. 없으면 **왜 없는지**를 적는다
(예: 「연결된 인계 없음 — 견적 자체가 없으므로 인계할 것도 없습니다」).

**⑤ 상담 방식 분포** (가로 막대) — 0건은 점선 테두리 + 「미사용」

**서랍 하단** — 위 ⑤ 회원 귀속 문구

## 경계 (화면 하단)

> 결제·배송·환불은 **쇼핑몰 소관**이라 이 화면에 없습니다 · **인계 상세 관리는 「인계 기록」
> 화면 소관**입니다

## 숫자 규칙

**전부 서버 값**이다. 원안 예시(4,419 / 154 / 93.1% / 303 / 16·3 / 3,488·928·3·0)를 **박지 마라.**
