# 승인 디자인 계약 — 유입 성과 (ADM-HND-030 후보 · 1a 안)

> **사장님 확정 2026-08-15.** 디자인 프로젝트 `5aac72e2-eb72-4712-b21a-6c71f097e83e`
> · 파일 `유입 성과.dc.html` · **1a 안**(끊긴 깔때기를 그대로 그린다).
> 사장님 지시: *"다른 화면도 참조할 거 있으면 쓰도록 해."*
>
> ⚠ **완전 신규다** — 화면 ID·경로가 **아직 확정되지 않았다**(아래 §미정 참조).

    제목      유입 성과
    화면 ID   **ADM-HND-030** (후보 · 미배정)
    경로      **/admin2/funnel-performance** (가칭)
    LNB       「인계 · 성과」 그룹
    성격      **조회 전용** — 쓰기 없음

⚠ 헤더의 화면 ID·경로 배지는 **점선 테두리**로 둔다 — 아직 확정 아님을 화면이 스스로 말한다.

---

# ★ 이 화면의 전부는 «두 구간을 정직하게 가르는 것»이다

    (A) 지금 우리 데이터만으로 잴 수 있는 구간     상담 -> 완주 -> 인계
    (B) 몰 데이터가 있어야 재는 구간               인계 -> 클릭 -> 구매

## ⚠⚠ (B)는 «[향후 사용예정]» 텍스트로만 둔다 — 숫자·차트를 «넣지 않는다»

> **화면을 잘못 지어서가 아니라 «몰 쪽 데이터가 우리에게 없어서»입니다.**

칸마다 **① 무엇을 못 재는가 ② 왜 ③ 무엇이 오면 채워지는가**를 적는다.
리드타임은 **미정**이다(회신 대기) — 지어내지 않는다.

## ⚠⚠ 1a 의 핵심 장치 — 깔때기 가운데가 «끊겨 있다»

    (A) 3단계  →  ⚠ 점선 + 「여기서 끊깁니다」  →  (B) 2단계(점선 테두리 · 회색)

**「왜 아직 몇 건이 팔렸는지 모르는가」가 한눈에 보이게 한다.**

---

# ⚠⚠ 기간 — 이 화면이 가장 조심해야 하는 것

## 인계 기능은 «2026-08-11부터» 있다

> 그 이전 세션은 **인계될 가능성이 애초에 없었습니다.**
> 그래서 전체 기간을 분모로 쓴 비율은 **「성과가 없다」가 아니라
> 「기능이 없던 날을 섞었다」**는 뜻입니다.

## 기간 선택 — 탭 둘 + 직접 고르기

    ㉮ 인계 기능 존재 기간 (2026-08-11 ~)      <- 기본값 후보
    ㉯ 전체 기간
    ㉰ 기간 직접 고르기

## ⚠⚠ ㉯(기능 없던 날 포함)를 고르면 «경고 배너»가 뜬다

> **이 기간에는 인계 기능이 없던 날이 포함돼 있습니다.**
> {시작}~{전날} 세션은 인계될 가능성이 애초에 없었습니다. 그래서 이 기간의 인계율
> **{낮은 비율}**({분자}/{큰 분모})은 실제보다 **{배수}배 넘게 낮게** 보입니다 —
> 기능이 있던 기간 기준은 **{높은 비율}**({분자}/{작은 분모})입니다.

⚠ **배수·비율은 서버가 계산한다.** 화면이 나누지 않는다.

---

# ⚠⚠ 비율에는 «분모»를 병기한다 (P-08)

> 「완주 대비 인계 {비율}」는 **{분자} / {분모}**이고,
> **표본이 {n}개뿐이라 이 비율 자체의 신뢰도가 낮습니다** —
> 기능이 켜진 지 며칠 됐다는 맥락이 비율보다 중요합니다.

## ⚠ 표본이 작으면 «나누지 않는다»

> 인계 총액(우리 견적가 기준) {금액}원 — **{n}건뿐이라 「평균 객단가」로 부르지 않습니다.**
> **나누지 않고 합계와 건수를 그대로 둡니다.**

---

# 골격 — 1a

    ① 기간 탭 줄 (+ 우측에 「인계 기능은 {날짜}부터」 안내)
    ② ⚠ 기간 경고 배너 (조건부)
    ③ **(A) 깔때기** — 3단계 + 끊김 표시 + (B) 2단계 회색
       하단에 분모 병기 문장
    ④ 3단 카드
       ㉮ 인계 건의 티어 분포     ㉯ 인계 시점 가격 재확인     ㉰ 근거 클릭 결합
    ⑤ **(B) 막힌 구간** — 3칸 + 하단 설명

## ④㉮ 인계 건의 티어 분포

    고성능 · 추천 · 가성비 — 가로 막대 + 건수
    ⚠ 하단: 총액과 건수를 «그대로» (평균 금지)
    ⚠ 「건별 상세는 «인계 기록»」

## ④㉯ 인계 시점 가격 재확인

    확인됨 · 가격 변동 · 확인 못함  — 3칸
    ⚠ **요약치만 인용**하고 누르면 「쇼핑몰 동기화」로 간다
    ⚠ 분모(인계 {n}건)를 밝힌다

## ④㉰ ⚠ 근거 클릭 · 부품 교체와의 결합 — 「0」을 «어떻게 말하나»

> 조인할 축(`user_id` · `session_id`)은 **이미 있지만**, 지금 표본에는
> 근거를 클릭하고 인계까지 간 사람도, 부품을 바꾸고 인계까지 간 사람도 **없습니다.**
> 상관을 보여줄 **데이터 자체가 아직 없다**는 뜻입니다 —
> **0을 성과로도 실패로도 쓰지 않습니다.**

## ⑤ (B) 막힌 구간 3칸

    인계된 견적이 결제까지 갔는지     몰이 회신하기 전에는 우리 원장 어디에도 없다
    인계 링크를 실제로 눌렀는지       클릭 추적 자체가 없다
                                     ⚠ 근거 클릭 로그는 «다른 사건»이다(상담 화면 전용)
    다른 상품으로 바꿔서라도 샀는지    간접 전환은 구조적으로 못 잰다

⚠ 각 칸에 **의존 대상**을 작게 적는다(예 「윈윈 API 주문 결과 회신」 · 「추적 경로 신설 필요」).

---

# ⚠ 이 화면이 «하지 않는» 일 — 명시한다

    인계 개별 건 상세        -> 「인계 기록」 (여기서 다시 나열하지 않는다)
    몰 가격 동기화 상세       -> 「쇼핑몰 동기화」 (요약만 인용)
    채널별 유입 분석         -> ⚠ **리퍼러·UTM 테이블 자체가 없다**
    회원 단위 전환           -> ⚠ `member_id` 가 비어 있다

⚠⚠ **옛 자체 결제 원장(`orders`)으로 대신할 수 없다** —
인계는 애초에 거기에 쓰지 않고, 최신 기록도 인계 시작 이전에서 멈춰 있다.

---

# ⚠ 숫자 — 실측이되 «서버 응답만» 쓴다

디자인의 수(**상담 4,419 / 307 · 완주 4,116 / 290 · 인계 세션 2 · 건 3 ·
총액 6,086,700원**)는 **2026-08-13 실측**이다(모형 값이 아니다).
**그래도 화면은 서버 응답만 쓴다** — 하드코딩 금지.

---

# ⚠ 제작자가 «먼저 재야» 하는 것

    · **인계 기능 시작일**이 어디에 기록돼 있나 — 코드? 마이그레이션 날짜?
      -> ⚠ 화면이 「2026-08-11」을 하드코딩하면 안 된다. **서버가 줘야 한다**
    · `consult_sessions` · `quote_snapshots` · `handoffs` 로 (A) 3단계를 낼 수 있나
    · 「완주」의 정의 — **세션에 스냅샷이 하나라도 있으면 완주**가 맞나
    · 인계 건의 **티어**가 어디 있나 · **가격 재확인 결과**가 어디 있나
    · ⚠ `promo_click_logs` ↔ `handoffs` 조인이 **정말 0건인가** (직접 세라)

**⚠ 잰 것을 «수»로 보고에 적어라.**

---

# ★ 사장님 결정이 필요한 것 — 넷 (디자인이 스스로 밝혔다)

    **결정 1**  화면 ID · 경로 확정 (세 화면 접두어를 함께 볼 것)
    **결정 2**  ⚠ **기본 조회 기간** — 「인계 기능 존재 기간」이 기본인가
    **결정 3**  열람 등급 (미로그인 401 은 확정)
    **결정 4**  ⚠⚠ **(A)만으로 지금 열지, (B)를 기다릴지**

⚠ **이 넷은 화면 구조를 바꾸지 않는다** — 정해지면 반영하면 된다. **착수를 막지 않는다.**
⚠ 미정인 동안 헤더 배지는 **점선**으로 두고, 「아직 정해지지 않은 것」 상자에 그대로 적는다.

---

# 담당 파일

    api/admin_ui_funnel.py                  ★ 신설 (화면 라우트)
    templates/admin/funnel.html.j2          ★ 신설
    api/admin_funnel.py                     ★ 신설 (집계 API — 없을 것이다)

⚠ **라우터 자동 등록은 `api/` 평면만 훑는다** — 하위 폴더 금지.
⚠ `api/admin_nav.py` **금지**(기록자 담당 · 검증 통과가 조건).
⚠ 다른 화면 참조는 **우리 것만**(`_admin2_shell.html.j2` · `admin2.css`) — Phoenix 금지.
