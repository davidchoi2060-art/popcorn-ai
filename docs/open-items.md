# 미처리 목록 (open items)

**이 파일이 정본이다.** 「보고했지만 아직 안 한 것」이 여기 없으면 잊힌다.

- 하네스가 **매 보고마다** 갱신한다. 끝난 것은 「끝난 것」으로 옮기고, 새로 나온 것은 더한다.
- 점검자(`sweeper`)가 주기적으로 훑어 **오래 방치된 것**과 **여기 없는데 코드에 남은 것**을 되잡는다.
- 항목마다 **왜 안 했는지**를 적는다. 「막혔다」·「안 골랐다」·「미뤘다」는 다른 상태다.
- 근거(어느 보고에서 나왔나)를 남긴다 — 세션이 끊겨도 되짚을 수 있어야 한다.

최종 갱신: 2026-08-25 (하네스)

---

## 1. 사장님 답을 기다리는 것

**없다** (2026-08-25 오후 기준).

*(2026-08-25 처리됨: 공급처 110888 → **「주식회사 브레인엠엔티」** 확정 · 8000 재시작 · 미처리 관리 구조 확정 · 몰 파일 수령 · 품절 공급처 닫음 · 공급처 117곳 등록 · 클릭나라 별도 등록 · 개발 DB 분리 안 함)*

⚠ 이 줄은 한때 110888을 「주식회사 제이케이온」으로 적고 있었다. **DB 실측값은 「주식회사 브레인엠엔티」**다 — 하네스가 원본의 파일↔값 대응을 반대로 전달한 탓이다. 경위는 `OWNER-DECISIONS.md`에.

---

## 2. 고객에게 영향 있는 것

| 무엇 | 상태 | 근거 |
|---|---|---|
| **「저소음」을 고르면 어떤 예산이든 견적이 안 나온다** | GPU·POWER·CPU쿨러에 `tag_silent=true`인 상품이 **카탈로그 전체에서 0건**. 태그 백필이 필요하다 | 2026-08-24 회귀 제작자 실측 |
| **가성비 티어가 최저가를 보장하지 않는다** | 문구만 정직하게 고쳤다(「가격 오름차순 첫 성립 조합」). 진짜 최저가 탐색은 **비용 측정이 먼저** | 2026-08-24 조사자 |
| 「진행 흐름」이 배포 주소에서 영원히 빈다 | 원천이 로컬 세션 전사본이라 서버엔 없다. **사장님이 「현황판은 로컬로」 결정** — 배포 쪽은 그대로 두기로 함 | 2026-08-25 확인자 |

---

## 3. 운영에 영향 있는 것

| 무엇 | 상태 | 근거 |
|---|---|---|
| **`api/expert.py`가 배분 상한을 풀고도 「유지」라고 말한다** | `:590~593` 호출에 `alloc_capped=False` 한 줄 추가하면 된다. 담당 밖이라 미처리 | 2026-08-24 제작자 |
| **API 단가표 유효기간이 지났다** | claude·codex 계열 `valid_until=2026-08-16`. 지난 뒤 경고만 남기고 같은 표로 계산 — **비용이 실제와 다를 수 있다.** 단가 확인은 사장님 몫 | 2026-08-25 제작자 |
| AI 응답 기록에 계측 지점이 없다 | 질문·답변 텍스트를 남기는 경로가 저장소 어디에도 없다. 선행 조건: ①LLM 연동 재개(2026-07-21 보류) ②운영 도우미 대화 서버 전송 | 2026-08-25 확인자 |
| `api_cost_logs`에 작업 종류 컬럼이 없다 | 「작업 종류별 분해」가 구조적으로 못 채워진다. 스키마 변경 필요 | 2026-08-25 조사자 |
| 비용 화면의 고정 칸 넷 | 상담 1건당 평균 · 배치당 평균 · 작업 종류별 분해 · Claude Code 세션 비용 3행 — **데이터가 쌓여도 절대 안 바뀐다**(조건 분기 자체가 없음) | 2026-08-25 조사자 |
| AI 작업 설정이 아무것도 제어하지 않는다 | 화면이 스스로 배너로 밝힌다. 실행 코드는 `api/llm.py`의 별도 표를 쓴다 — 연동 재개 시 이어야 함 | 2026-08-25 확인자 |
| **점검용 공급처 2건이 운영 목록에 표식 없이 섞여 있다** | `id 95 점검-공급처-A`(활성·brands「테스트브랜드」) · `id 216 화면점검-공급처-임시`(중지). 화면 어디에도 「테스트」 표식이 없어 「abc넷」·「써텍」 같은 실제 축약 상호와 같은 모양으로 보인다. **운영자가 여기에 매입가를 걸면 점검용 더미가 진짜 매입 단가로 견적에 들어간다** — 「시연 상품 23건이 4중 게이트를 통과한」 전례와 같은 계열 | 2026-08-25 확인자 |
| 공급처 117곳에 파서 프리셋이 없다 | 화면 배너 「파서 프리셋이 없는 공급처 120곳」이 실측값. 단가표를 자동으로 읽을 수 없다는 뜻 — 붙이려면 공급처마다 프리셋이 필요하다 | 2026-08-25 확인자 |

---

## 4. 문서가 사실과 다른 것

| 무엇 | 상태 |
|---|---|
| **`product_imports` 「항상 0행」이 3곳** | `api/admin_imports.py` 독스트링 · `templates/admin/catalog_import.html.j2` · `docs/design/req/req-product-bulk-import.md`. **운영자 화면 문구 포함** — 2026-08-24부터 실제로 채워진다 |
| `req-ai-integration.md`에 「한도 되돌리기」가 없다 | 2026-08-25에 기능을 만들었으나 요구사항 정의서 미갱신 |
| `docs/06_db-erd.md`가 `products.supplier`를 「폐기 예정」이라 적음 | 2026-08-25에 그 컬럼을 살려 화면에 노출했다 — 방향이 반대. 사장님 판단 필요 |
| **`docs/decisions/admin-identity.md`(화면 ID 카탈로그)가 14일째 낡았다** | 코드 43개 중 **22개만 등재**, 없는 화면 ID가 8~10개. 손으로 등재해야 갱신되는데 빠뜨려도 개발이 안 막혀 드러나지 않았다 | 2026-08-25 기록자 |
| **`CLAUDE.md`의 「잠금이 지키는 것은 둘뿐」** | 2026-08-24에 **13개**로 늘었다(`api/catalog_ingest.py`). 무조건 덮어쓰는 것은 `status`·`stock_qty`·`data_origin` 셋뿐 | 2026-08-25 기록자 |
| **`docs/design/admin-screen-audit-2026-08-25.md`의 「공급처 5곳」** | 같은 날 117곳을 등록하기 **전** 스냅샷인데 그런 표시가 없다. 운영자 매뉴얼이 이 표를 재료로 쓸 예정이라, 그대로 옮기면 매뉴얼에 틀린 수가 박힌다 | 2026-08-25 확인자 |

---

## 5. 검사 공백

| 무엇 | 왜 위험한가 |
|---|---|
| **작업 현황판 관련 회귀 0건** | 오늘 고친 것이 되돌아가도 자동으로 못 잡는다 |
| **expert 모드 관련 회귀 0건** | 되돌리면 GT710이 재발해도 못 잡는다(2026-08-24 지적, 여전히 미해소) |
| 게이트 체크리스트에 `data_origin` 항목 없음 | 데모 상품이 왜 후보가 아닌지 화면이 못 짚는다. `tests/regression.py`의 `len(gate)==6` 하드코딩을 함께 고쳐야 한다 |
| `mark-read`가 「전부」를 읽음 처리한다 | 특정 id가 아니라 안 읽은 것 전부. 알린 직후 도착한 메시지가 출력 없이 읽힐 이론적 경합 |

---

## 6. 공유 DB 정리 후보

| 무엇 | 상태 |
|---|---|
| `consult_sessions` 8074·8075·8077·8078·8089·8099·8104 | `data_origin='real'`, 2026-08-24 04시대. 제작자 검증 잔재로 추정되나 확증 없어 보류 |
| `.claude/dash-queue.jsonl` 자체검증 줄 1건 | `read:true`라 재알림 없음. 파일 삭제 API가 없어 수동 편집 필요 |

**8137·8174는 손대지 마라** — 다른 작업자의 것으로 판단됨.

---

## 끝난 것 (2026-08-25)

- 견적 캐스케이드 — 가성비가 막혀도 고성능형은 따로 찾는다 (**고객 19명이 못 받던 견적**)
- 공급처 덮어쓰기 차단 + `product_imports` 원본 보관 + 관리자 화면 노출
- 구형·미판매 부품 100건 후보 제외 (후보풀 3,059 → 2,959)
- 시연 주문 10건 표시 교정 (`real` → `demo`, 30행)
- 「N원부터 견적이 가능합니다」 신설
- 모순 토스트 · 거짓 배너 · 제외 사유 통일
- 티어 문구 정직화 (「최고 사양」 → 「가격 내림차순 첫 성립 조합」)
- 팀원 `pathfinder` 한글명 + 누락 경보를 화면에 노출
- AI 응답 기록 모델 필터가 정본(`llm.py PROVIDERS`)을 읽게
- AI 연동 설정 한도 되돌리기(`DELETE /limits/{provider}`)
- 감시자 두 모드 분리(`--service` 하트비트 전용 / 기본 알림+읽음처리) + 중복 방지 + 자동 실행 스크립트
- 회귀 897 → 915건 (「전 티어 불가」 검사 신설)
- **공급처 117곳 등록** (화면 5곳 → 122곳). 엑셀 120곳 → 표기 정리 → `suppliers` 표. 근거 `docs/supplier-consolidation-2026-08-25.md`
- **외부 개발자 온보딩 문서** `ONBOARDING.md` — 오늘 실제 사고를 근거로 「하면 안 되는 것」을 적었다
- 몰 공급처 파일 수령·검사 — **적재하지 않기로 결정**(얻는 것 10건 대비 판매가 939건이 함께 덮인다)
