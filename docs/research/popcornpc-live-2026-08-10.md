# 운영 중인 팝콘PC 쇼핑몰 관리자 실사 — 상품 분류·상세 필드 (2026-08-10)

> `https://popcornpc.co.kr/adm_cate/product_list_multy.html` · 사용자 계정 로그인 상태에서 열람
> **읽기만 했다.** 등록·수정·삭제·저장 버튼은 누르지 않았고, 고객 개인정보는 열람·기록하지 않았다.
> 2026-08-04 판(`legacy-admin-2026-08-04.md`)은 **진단**이었고, 이 문서는
> **"실제로 무엇을 쓰고 있는가"의 실측**이다. 앞 문서의 추정을 사실로 확정한다.

## 0. 한 줄 결론

**우리가 설계한 것보다 훨씬 복잡한 가격 축이 실제로 돌고 있다.**
회원 등급 6단 × 판매 채널 4개 × 채널별 수수료율 × 경쟁사 1·2·3등가 추적.
우리 `pricing` 은 **전역 마진 하나 + 카테고리 노드 마진**뿐이다.

---

## 1. 상품 분류 — 1단계 20개 (실측)

셀렉트 `cate1_order` 의 실제 옵션이다. 2026-08-04 진단(§2.1 "대분류 20개가 다섯 축을 섞고
있다")이 **그대로 확인**됐다.

| 축 | 실제 1단계 노드 |
|---|---|
| ① **상품 분류**(진짜 카테고리) | 컴퓨터주변기기 · 기타주변기기 · 모니터 · 소모품 · 음향＊방송장비 · 키보드,마우스외 · 케이블/변환젠더 · 쿨링/튜닝용품 · 노트북 · 가전제품[린클음식물처리기 / 비비딕벌레퇴치기] · 화장품/미용 |
| ② **완제품 유형** | ai 슈퍼컴퓨터 · 프리미엄 피씨 · 가상화폐 채굴기 · 알레오코인 채굴기/ 부품 |
| ③ **마케팅·채널** | 베스트7주력부품 · 네이버노출 |
| ④ **내부 업무** | 고객님 개인결제 · 내부관리용 |
| ⑤ **생명주기** | 삭제대기 |

**①만 카테고리다.** 나머지 9개는 속성·플래그여야 할 것이 트리 1단에 올라와 있다.
그리고 ①에도 `가전제품[...]`처럼 **노드 이름에 상품명이 박혀 있는 것**이 있다.

### 깊이 — 셀렉트는 4단(`cate1~4_order`)인데 실제로는

2·3·4단계 셀렉트는 **옵션이 라벨 하나뿐**(1단계를 고르면 AJAX 로 채워지는 구조)이다.
목록 행이 보여 준 실제 경로는 **2단**이었다:

    컴퓨터주변기기 > 제작,서비스 (조립관련)

즉 **틀은 4단인데 실사용은 얕다.** 2026-08-04 조사의 "미매핑 1,458건 중 95%가 3차에서 났다"와
같은 그림이다 — 깊이를 강제해 두고 채우지 못한 상태.

> **우리 판단(`category-axis-2026-08-09.md`)을 뒤집지 않는다.** 오히려 근거가 늘었다:
> 4단 틀을 먼저 만들어 두면 **채우지 못한 칸이 미매핑으로 남는다.**

---

## 2. 상품 상세 — 실제로 쓰는 필드 (목록 한 행에서 실측)

목록이 상품 하나에 대해 실제로 보여주는 것들이다. 우리 스키마와 대조한다.

### 2.1 식별·분류

| 필드 | 우리에게 |
|---|---|
| `pd_no`(상품번호) | `product_code` ✅ |
| 카테고리 경로 | `categories` ✅ |
| 품명 | `product_name` ✅ |
| **스펙**(텍스트 한 덩어리) | `product_specs`(구조화) — **우리가 더 낫다** |
| 홍보(HTML) | 없음 |
| 검색키워드 | 없음 |
| **장부 상품코드 · 장부 상품명** | 없음 — 회계 연동 축 |
| 상품바코드 · 상품대표코드 | 없음 |
| **상품위치 · 상품창고 · 본창고** | 없음 — **실물 창고 관리 축** |

### 2.2 상태·플래그

`판매중 · 품절 · 단종` / `주력 · 주력아님` / `성인상품` / `중고` /
`DB재수집금지` / `1Box포장가능수` / `인증번호`

우리는 `products.status` 하나로 판매중·품절·단종을 표현한다. **주력 여부는 없다.**

### 2.3 채널 노출 제어 — 우리에게 통째로 없는 축

`오픈마켓 제외` · `다나와 제외` · `오픈마켓 상하단 제외` · `네이버쇼핑 제외`

상품마다 **어느 채널에 내보낼지**를 끈다. 우리 운영 전환 스위치(A-10)는 **시스템 전역**이라
상품 단위 노출 제어가 없다.

### 2.4 담당자

행마다 담당자를 고르는 셀렉트가 있다(이규수·최정진·성향숙·조충기·이미숙·김민철·장현욱·
이성열·이종건·이순애 등 **실제 사원 이름**). 우리는 `admin_operators` 가 **작업 기록의 주체**일
뿐, **상품에 담당자를 매는 축이 없다.**

### 2.5 외부 연동 표시

`다나와` · `다나와(간편)` · `에누리` · `네이버` · `견적왕` · `샵링커` · `윈셀링`
그리고 검색 축에 **`다나와번호`** 가 있다(`s1_cate` 옵션 7종:
상품명 · 제조사 · **스팩** · 지정업체명 · **다나와번호** · 상품코드 · 메모).

> 우리도 `danawa` pcode 를 쓰지만 **제안 수집용**이다(A-18). 저쪽은 **판매 채널 연동**이다.

---

## 3. 가격 — 여기가 가장 크게 다르다

한 상품에 **판매 채널(몰) 4개**가 붙어 있고, **채널마다 수수료율이 다르다**:

| 채널 | 법인 | 수수료 |
|---|---|---|
| `basic` | 주식회사 팝콘컴퓨터 | **2.585%** |
| `kjwang` | 주식회사 팝콘컴퓨터 | **7.7%** |
| `kjwang1` | 주식회사 팝콘피씨 | **4.4%** |
| `kjmart` | 주식회사 팝콘피씨 | **0%** |

**채널마다** 아래를 각각 갖는다:

- **회원 등급 6단 가격**: `시중가 · 일반회원 · 프리미엄 · VIP · VVIP · 지도가`
- `가격 · 마진 · 예정가` / `마진% · 할인% · 적립금 · 추천`
- **`카드원가`** — 카드 수수료를 반영한 원가
- **경쟁사 순위가**: `일등가 · 이등가 · 삼등가` (각각 우리 대비 차이 %)
- 배송·정책 플래그: `판/고 · 딜/노 · 기/배 · 무/배 · 착/가`
- `에누리` · `판매가로그`(가격 변경 이력)
- `견적왕 (카드 최저가 / 현금 최저가)`
- 메모 — 예: `다나와[카드몰] 적용` / `다나와[현금몰] 적용`

그리고 상품 단위로 **`지도가(카)` · `지도가(현)`** (카드가/현금가 지정가)가 따로 있다.

### 우리와의 격차

| | 저쪽(운영 중) | 우리 |
|---|---|---|
| 판매 채널 | **4개, 채널별 수수료율** | 운영 전환 스위치(전역) |
| 회원 등급 가격 | **6단** | 없음 — 단일 `sale_price` |
| 원가 | **카드원가**(수수료 반영) | `product_supplier_prices.cost_price` |
| 경쟁사 추적 | **1·2·3등가 + 격차%** | 없음 |
| 가격 이력 | 채널별 `판매가로그` | `product_price_history` ✅ |
| 마진 | 채널×등급마다 | 전역 12% + 카테고리 노드 |

---

## 4. 제조사·원산지 — 자유입력이 축적된 결과

**제조사 셀렉트 2,368개.** 실제 운영의 규모다.
그런데 **같은 회사가 여러 표기로 들어 있다**:

- `(주)이지넷유비쿼터스` / `이지넷유비쿼터스` / `이지넷`
- `브리츠` / `브리츠인터내셔널` / `브리츠전자` / `브릿츠인터내셔날` / `Britz`
- `GIGABYTE` / `GIGBYTE` / `기가바이트`
- `써클코리아` / `써클코리아 외 2제조사` / `써클코리아, 앱코, 좋은디에스아이`

마지막 형태가 특히 위험하다 — **"외 2제조사"·쉼표 나열이 제조사 값 자체**로 들어가 있다.

**원산지 133개**도 같다: `영국` 2회 · `원산지` 3회 · `국내`/`국내산`/`국산`/`국내제조`/`대한민국` ·
`포루투칼`/`포르투갈` · `이태리`/`이탈리아` · `타이완`/`대만` · `인디아`/`인도` ·
`상세설명참조` · `설명글참조` · `원료원산지하단참조`.

> 우리 `products.maker` 도 자유 텍스트다(실측 620종). **같은 병의 초기 단계**다.
> 저쪽은 그게 2,368개까지 자란 모습이고, **우리가 지금 손대면 훨씬 싸다.**

---

## 5. 이 실사가 우리 설계에 주는 것

### 5.1 확인된 것 (기존 판단이 옳았다)

- **4단 카테고리를 서둘러 만들 이유가 없다.** 저쪽은 틀만 4단이고 실사용은 2단이며,
  강제한 결과가 미매핑 1,458건이었다.
- **카테고리에 다섯 축을 섞으면 안 된다** — 실물로 재확인.
- 사양을 **구조화한 것**(`product_specs`)은 우리가 낫다. 저쪽은 텍스트 한 덩어리라
  "스팩"이 검색 대상일 뿐이다.

### 5.2 우리에게 없어서 **새로 생각해야 하는 것**

| 축 | 왜 필요한가 |
|---|---|
| **회원 등급별 가격** | 실제로 6단을 운영 중이다. 우리 `sale_price` 단일 값으로는 이 사업을 못 옮긴다 |
| **채널별 가격·수수료** | 법인 2개 × 몰 4개가 이미 돌고 있다. A-10 운영 전환 스위치는 전역이라 이걸 표현 못 한다 |
| **경쟁사 1·2·3등가** | 가격 결정의 실제 입력값이다. 우리 `판매가 재산정`은 원가×마진만 본다 |
| **카드원가** | 카드 수수료가 원가에 들어간다. 우리 마진 계산에 그 개념이 없다 |
| **상품 단위 채널 노출 제어** | 다나와 제외·네이버 제외 같은 상품별 스위치 |
| **창고·상품위치** | 실물 재고 관리 축. 우리는 수량만 안다 |
| **담당자** | 상품마다 사람이 붙어 있다 |
| **제조사 정규화** | 2,368개가 된 모습을 봤다. 우리 620종일 때 손대는 게 싸다 |

### 5.3 아직 못 본 것

- 상품 **등록/수정 폼의 전체 필드 목록** — 상세 페이지가 팝업(`product_change.php`)이라
  이번에 못 열었다. **다음에 확인할 1순위.**
- 2·3·4단계 카테고리의 실제 내용(1단계를 골라야 채워지는 구조)
- 옵션(`추가옵션관리`) 구조
- 오픈마켓 스케줄

---

## 6. 사용자에게 확인이 필요한 것

1. **회원 등급 6단(시중가·일반·프리미엄·VIP·VVIP·지도가)을 팝콘AI 에도 가져가는가?**
   가져가면 `sale_price` 단일 값 설계를 바꿔야 한다 — P-08(재설계 허용) 범위다.
2. **채널 4개(basic·kjwang·kjwang1·kjmart)와 법인 2개를 팝콘AI 가 다루는가?**
   아니면 팝콘AI 는 **자체 채널 하나**로 시작하는가?
3. **경쟁사 1·2·3등가 추적**을 우리도 하는가? (다나와·에누리·견적왕 연동 전제)
4. 담당자·창고·장부코드 축을 가져갈 것인가?
