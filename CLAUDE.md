# CLAUDE.md — 팝콘PC AI (속도 최적화판)

<!-- 이 파일은 매 턴 컨텍스트에 통째로 로드된다. 짧게 유지한다. -->
<!-- 상세 배경은 여기 넣지 말고 docs/ 파일로 미룬다(필요할 때만 읽게). -->

## 프로젝트 한 줄
자체 부품 풀 기반 PC 견적·판매 시스템. 정체성 = **신뢰 판매자**("조립 불가 PC는 안 나온다 + 왜 이 부품인지 설명"). 히어로: "모든 견적에는 이유가 있습니다."
**A-10(2026-07-16): 커머스 승격** — 회원·장바구니·결제·정산·배송·환불을 본 시스템이 직접 처리. 기존 쇼핑몰은 병행 채널, **운영 모드 스위치 5종**(자체/쇼핑몰)으로 분기. 주문 원장은 항상 본 시스템.

## 지금 단계 = 모델링 + DB·API 구현 (2026-07-23 개정)
- **목업 = 스펙.** UI 목업(HTML/CSS/JS)이 1차 산출물이며, 손으로 검증된 화면 범위가 구현 범위를 정의한다.
- **DB 구축 완료(2026-07-23)**: Cloud SQL `popcorn_pc`에 스키마(테이블 46+뷰 2)·시드 적용됨. 스키마 단일 원천 = `docs/06_db-erd.md` Ver 4.0, 실행본 = `db/`(Alembic). 접속 정보 = `docs/infra/GCP_SETUP.md`(로컬 전용). 스키마 변경은 **ERD 개정 → 새 마이그레이션** 순서.
- **로컬 API 슬라이스 승인(2026-07-23)**: 스택 = FastAPI(`api/`), 방식 = **화면 단위 수직 슬라이스**(DB→API→목업 바인딩 관통, 이음새 검증 후 다음 화면). 첫 슬라이스 = ADM-PRD-010 상품 목록 읽기 1본. 목업은 API 실패 시 더미 폴백(Firebase 검토 배포 무영향). 새 화면 연동은 슬라이스 단위로 계획 승인 후 진행.
- **여전히 금지(별도 승인 필요):** 실제 LLM 연동(보류 결정 2026-07-21: 착수 시 팝콘톡 한정·공개 노출·Opus 4.8) · 운영 배포(서버 구동은 로컬 전용).
- 충돌 요청이 오면 코드 짜지 말고 먼저 물어라. **비밀값(DB 비밀번호·API 키)은 리포·설정·메모리에 절대 저장 금지.**

## 작업 방식 (속도 핵심 — 이게 느림을 잡는다)
1. **탐색 최소화.** 작업 전 전체 코드베이스를 훑지 마라. 필요한 파일만 열어라. 어떤 파일을 볼지 모르면 먼저 물어라.
2. **한 번에 하나.** 큰 작업을 한 요청에 몰지 마라. "S2 전체" 대신 "S2 섹션1"처럼 쪼개라.
3. **계획 먼저, 코드 나중.** 파일을 만들기 전 3~5줄로 접근을 요약하고 승인받아라(Plan Mode 선호).
4. **끝나면 자체 검증.** 목업은 브라우저에서 열리는지·JS 콘솔 에러 없는지 스스로 점검 후 보고.
5. **말은 짧게.** 턴 사이 설명은 최소, 최종 보고는 요점만. 장황한 서술 금지.
6. **세션은 일회용.** 결정은 이 파일/`docs/decisions/decision-log.md`에 기록. 긴 세션에 기억을 의존하지 마라. 컨텍스트가 길어지면 `/clear` 후 재시작.

## 베타 배포 상태 (슬라이스 41 — 2026-07-26 가동)
- 접속 `http://34.47.124.184/admin/login.html` · GCP `popcorn-app`(asia-northeast3-b) · Cloud SQL `popcorn-db`.
- **nginx Basic Auth가 사이트 전체를 봉쇄한다** — 로그인이 dev 어댑터라 이게 유일한 실질 방벽이다. 걷어내려면 실 OAuth 먼저.
- 앱은 `127.0.0.1:8000`에만 바인드(외부 직접 접근 불가) · 방화벽 80·22만.
- 배포 갱신: 로컬 커밋·푸시 → 서버에서 `git pull` + `systemctl restart popcorn-api`(절차 `deploy/README.md`).
- **DB 복구 절차 = `deploy/RESTORE.md`** (2026-07-27 리허설 통과). 자동 백업 매일 21:00 UTC·7건, **PITR 로그 7일** — 최근 7일 임의 시점으로 되돌릴 수 있다. **원본을 덮어쓰지 않는다**: `backups restore`가 아니라 `instances clone --point-in-time`으로 복제본을 만들어 검증 후 전환한다(복제 약 4분). 운영 인스턴스는 삭제 보호 ON.
- **서버 코드 직접 수정 금지** — 배포 타깃이다(전역 규약).

## 데이터 규모 (슬라이스 39 — 실카탈로그 적재 완료)
- products **22,838건**(real 22,808 · demo 30). 원천 = `최종_정제완료_상품데이터.csv` 24,303행 + `db_product_specs.csv` EAV 339,650행.
- **호환 규칙 8종**(슬라이스 43: 케이스가 보드 규격을 수용하는가 추가). 규칙은 `compat_rules` 단일 원천.
- **추천 후보 3,087건** · 검수 대기 **6,490건**(판매중 1,720). 적재 `tools/catalog_import.py --origin real|demo` · 재파싱 회수 `tools/respec.py --dry`.
- **선호 태그 818건**(화이트 763 · 저소음 55) — 백필 `tools/backfill_tags.py`. **명시 표현만** 인정(무소음·저소음·정숙·사일런트·SILENT / 화이트·WHITE·백색). 추론 금지 — 지어낸 태그는 '조용하게'라고 말한 이유를 배신한다. respec과 분리한 이유: respec에게 `false`는 '이미 있는 값'이지만 태그에서 false는 '아직 판정 안 함'이다.
- **데모/실데이터는 `data_origin`으로 구분**한다. 데모를 올려 테스트해도 실운영 데이터와 섞이지 않는다.
- 목록·검수 큐는 **서버 페이지네이션**이다(전 행 반환 금지). 화면은 '전체 N건 중 이 페이지'를 명시한다.
- 견적 응답 2.1초(후보 3,046행 로드가 주된 몫 — 탐색 자체는 0.02초). `DFS_NODE_CAP`=2,000,000.
- **로그 문자열은 ASCII 기호만** — 서버 stdout이 cp949라 em-dash·화살표가 요청을 500으로 만든다(슬라이스 40).

## 화면 (대상 = MVP1)
- 고객: 게이트웨이·main-landing·S0~S5·MY(마이페이지/주문 내역·환불 접수/결제 내역) 완료. **S1 `mockups/mvp1/s1-session.html` = 품질 기준 파일** (4모드 + UX-17 점진 공개 + 주변기기 인지).
- 관리자 인증(슬라이스 37): `mockups/admin/login.html`에서 **회사 소셜 계정 → 신청(대기) → owner 승인 → 권한(조회/운영자/관리자)**. 비밀번호 미저장. 현재 신원 확인은 **dev 어댑터(입력 이메일 신뢰) — 로컬 전용·공개 배포 차단 사유**. 작업 기록 주체 = 로그인 운영자.
- **상품 일괄 등록(ADM-CSV-010)은 실제 적재한다**(슬라이스 50): 업로드 → **드라이런** → 확인 → 적용. `POST /api/admin/catalog-import/dryrun|apply`, **owner 전용**. 적용 시 드라이런이 보고한 `expect_ok`를 되돌려 보내야 한다(다른 파일을 확정하는 사고 차단). 되돌리기는 없다 — 화면이 그 사실을 먼저 말한다.
- **적재 로직 단일 원천 = `api/catalog_ingest`**. CLI(`tools/catalog_import.py`)와 업로드 API가 같은 `build_plan`/`apply_plan`을 부른다 — 두 번 구현하면 "왜 이 상품이 안 올라왔지?"에 답할 수 없다.
- **적재는 채우기만 하고 지우지 않는다**: `product_specs`는 `COALESCE(EXCLUDED.col, 기존)`. EAV 없이 마스터만 올렸다가 사양이 지워져 후보 -1·검수 +169가 된 사고가 있었다(슬라이스 50). 필수 사양 판정도 **새 값 ∪ 기존 값**으로 한다.
- **드라이런은 건수가 아니라 영향을 말한다**: 신규·갱신·추천 후보 진입/이탈·검수 새로 회부(중복 뺀 실측). "검수 회부 198건"이라 말했는데 실제 증가가 0이면 헛수를 말한 것이다.
- **카테고리 문자열이 어긋나면 상품이 `ETC`로 강등되어 후보에서 사라진다** — 드라이런의 `pool_out`이 이걸 미리 알린다(회귀로 고정).
- 검수 큐 정합: 이미 값이 채워진 대기 행은 `tools/prune_reviews.py`로 **'처리' 전이**(삭제 금지). 슬라이스 50에서 117건 정리.
- **검수 깔때기(A-17)**: ① 자동 회수(파서·분류 교정) → ② 외부 제안(다나와) → ③ 사람 검수(원칙). 못 채운 부품은 추천에서 빠진 상태로 둔다(4중 게이트가 이미 그렇게 동작 — 선택지가 아니다).
- **오분류가 사양 부족보다 크다**(슬라이스 51): 시스템 팬이 CPU쿨러로, 베어본이 메인보드로, UPS·AC케이블이 파워/케이스로 올라가 있었다(160건 → 검수 436건 해소). 판정 근거는 **스펙 원문의 분류 토큰만**(`catalog_map.is_accessory`) — 상품명으로 보면 모듈러 파워의 '케이블', SSD의 '연장가이드'까지 걸려 진짜 부품을 떨어뜨린다. 교정 도구 `tools/reclassify.py`.
- **다나와 수집은 제안까지만**(A-18): `tools/danawa_fetch.py` → `product_reviews.suggested_value`. **정본에 바로 넣지 않는다** — 남의 페이지 값이 견적 근거가 되면 정체성이 무너진다. `origin_value`가 비어 있으므로 일괄 확정 대상도 아니다(사람 확인 강제). robots 허용 경로 `/info/`만, 1.5초 간격, 연속 실패 5회면 중단, 원문은 `.cache/`(gitignore).
- **pcode 오매핑이 실재한다**: 우리 메인보드에 랜케이블 pcode(제목 유사도 0.16). 유사도 0.45 미만이면 사양을 쓰지 않고 '코드 불일치'로 회부한다.
- **적재가 오분류를 막는다**(`map_part_type` — 슬라이스 51 마감): 분류 판정은 **원문의 `:`가 없는 앞쪽 토큰**만 본다(키-값은 사양이지 분류가 아니다). 이 한 줄로 '액세서리'가 3번째 토큰이라 놓치던 것들이 잡히고, 모듈러 파워의 '케이블' 오검출은 안 생긴다. `wrong_slot`은 SSD 자리의 램(DDR+CL)을 잡되 **슬롯을 자동으로 바꾸지 않는다**(etc로 내리고 사람이 결정).
- **어휘를 넓힐 때 오검출을 먼저 센다**: '허브랙'을 넣었더니 진짜 랙마운트 케이스 3건이, '시스템쿨러'만 봤더니 진짜 수랭 AIO 1건이 걸렸다. 원천은 '시스템쿨러'를 CPU쿨러 대분류로도 쓴다 → **냉각 방식(수랭·공랭) 표시가 있으면 CPU쿨러**. 회귀가 8종(걸러야 할 것 3 · 지켜야 할 것 5)을 고정한다.
- **다나와 분류로 우리 오분류를 잡는다**(`tools/reclassify.py --danawa`, 캐시만 사용·재수집 없음): 라이저 케이블이 케이스로, 기계식 키보드·스위치허브가 메인보드로, 노트북 램이 SSD로, 시스템 팬이 CPU쿨러로 올라가 있었다(49건 → 검수 147건 해소). **부정 목록으로만 판정한다** — 긍정 목록("이 분류여야 한다")은 오탐 318건을 만든다. head는 분류가 아니라 상품 특성인 경우가 흔하다(케이스 "미니ITX" · 보드 "AMD(소켓AM5)" · GPU "RTX 5050" · SSD "6.4cm(2.5형)").
- **`cooler_tdp`는 다나와에도 없다** — 판매중 대기의 최대 덩어리(324건)는 크롤링으로 못 채운다. 사람 검수 또는 제조사 자료가 필요하다.
- 검수 큐(ADM-PRD-020)는 **큐 구성**을 먼저 말한다: 대기 6,542 중 일괄 확정 가능 **1** · 단건 검수 **2** · 원문에 값이 없어 사람이 넣어야 하는 것 **6,487**. respec은 소진됐다(추가 회수 2건). '판매중·재고 있는 것만' 필터(1,720)가 실제 작업 목록이다.
- 근거 클릭은 **수집된다**(슬라이스 49): S2 부품 행 클릭 → `POST /api/promo-click` → `promo_click_logs`. `user_id`는 회원이 아니라 **익명 방문자 키**(FK `users`) — member_id를 넣으면 FK 위반으로 조용히 롤백된다.
- 관리자: `mockups/admin/` **29화면 완료** (Phoenix 한글 셸, 28파일 — 재고 입고 ADM-SRC-020 포함) — A-10 커머스(운영 전환·주문 관리·환불·결제 정산·배송·후기·회원) + 단가표 일일 반영(ADM-PRC-040) 포함.
- **A-10 카탈로그 전 화면 목업 완료(2026-07-23)**. 카탈로그 = `docs/decisions/admin-identity.md`.

## 관리자 화면 규약 (슬라이스 53·54)
- **상품 관리(ADM-PRD-010)가 상품 작업의 유일한 진입점**이다. 좌측 메뉴에 편집 화면을 따로 두지 않는다 — 대상 없이 여는 편집 화면은 저장이 안 되는데 운영자는 모른다. 상세는 `product-edit.html?code=`, 등록은 `?new=1`.
- **단건 수정은 고친 필드를 잠근다**(`locked_fields`). 안 그러면 다음 적재가 덮어써 어제 고친 값이 오늘 되돌아간다(A-16의 짝). **되돌릴 때 잠금도 함께 되돌린다** — 값만 원복하고 잠금이 남으면 적재가 영영 못 채운다.
- 상세에서 **사양·분류는 고치지 않는다**. 사양은 검수 화면(근거와 함께 확정), 분류는 슬롯을 바꾸는 일이라 재적재 소관.
- **목록은 전부 서버 페이지네이션이다.** 재고 입고가 15,259건 + 카탈로그 22,838건을 매 요청 전량 보내고 있었다(슬라이스 54). 셀렉트 후보는 **검색어가 있을 때만** 조회한다. 회귀는 한 페이지 길이가 아니라 `total`을 대시보드와 대조한다.
- **쓰기 요청은 자동으로 진행·완료를 표시한다**(`shared/ui-progress.js`, 전 43화면). fetch를 감싸 GET·인증 폴링을 뺀 나머지에 상단 바를 띄우고, 완료 문구는 **서버가 준 `verdict`/`note`를 그대로** 쓴다. 기존 하단 토스트와 위치가 달라 겹치지 않는다.
- 선택지(분류 라벨·상태값)는 **서버에서 받는다**(`/api/admin/product-meta`). 화면에 하드코딩했다가 등록 셀렉트에 3종만 있어 파워·케이스·메모리를 등록할 수 없었다.

## 디자인 계약
- 토큰 단일원천 = `design-system/tokens.css` `:root`. 임의 HEX/px 금지.
- 서체: Inter(라틴·숫자) + Pretendard(한글). 숫자는 tabular.
- 색: primary `#005ab3`/hover `#0082fd`, tertiary `#853d97`, error `#b31b25`. **초록 없음** → 통과=primary, 위험=error, 항상 텍스트 병기.
- **아이콘: Streamline Ultimate Color (CC BY 4.0, 출처 표기 필수)** — 2026-07-16 사용자 지시로 단색 정책 폐기. 파일=`mockups/shared/icons/su/`, 매핑=`_map.json`, 런타임 스와퍼=`mockups/shared/su-icons.js`(전 페이지 주입, 출처 표기 자동 삽입). 새 아이콘 필요 시 iconify에서 받아 `ms-`/`ft-` 규칙으로 추가.
- 마크업: `data-screen-id`/`data-domain`/`data-bind`/`data-repeat`/`data-action`, 예외는 `hidden`. 한국어 UI, 768px 세로 스택.

## 화면 정직성 (슬라이스 47·48 — 가장 중요한 규칙)
- **화면이 숫자를 지어내지 않는다.** 후보 수·가격·건수·규칙 수는 서버 응답만 쓴다. 목업 시절 연출(예: `poolN*0.6`, '호환성 5종', 완화 칩 '+1,240개')이 LIVE 경로에 남아 서버가 모르는 수를 말하면 '모든 견적에는 이유가 있습니다'가 무너진다.
- 서버가 반영하지 않는 조건은 **화면도 세지 않는다**(용도는 후보 수를 안 바꾼다). 읽지 못한 입력은 '조건으로 못 읽었다'고 밝힌다. 완화 칩도 `effects[].applied=false`인 제약은 내밀지 않는다(풀어도 그대로여서 거짓 기대를 준다).
- **후보 수 > 0 ≠ 견적 가능.** 슬롯 하나가 0이면 조립이 불가능하다. 판정은 `/api/candidates/count`의 `buildable`·`empty_slots`·`verdict`만 쓴다. 슬롯은 **엔진과 같은 정의**로 센다 — 쿨러는 공랭·수냉이 한 슬롯이라 part_type으로 세면 AIO 0을 빈 슬롯으로 오판한다.
- **서버가 내려준 판정을 화면이 버리지 않는다.** `budget.verdict`(within/over/over_by)를 안 쓰면 120만원 예산에 3,025만원 구성을 내밀며 초과를 감춘다(슬라이스 48 실제 사례).
- 목업 검증 시 **브라우저 캐시를 먼저 의심**한다 — 패치가 '효과 없음'으로 보이는 흔한 원인이다.

## 상태 관리 원칙 (S1에서 확립)
- 제약은 **객체 하나가 단일 원천**. 칩·자유입력·파싱이 전부 그 하나만 갱신.
- 기어/뷰 전환은 상태를 리셋하지 않는다.
- 후보 풀 카운터 = 완성품 대수 아님. **전체 부품(26,480개)에서 조건 통과 부품 수.**

## 회귀 세트 (A-13 관계식 전환 — 리팩터링 전후 필수)
- 실행: API 서버(8000) 띄운 뒤 `.venv/Scripts/python tests/regression.py` (실패만 보려면 `--quiet`)
- **160건 전량 불변식.** 고정 기대값(FIXED)은 **폐지**했다 — 베타 실주문 1건이 재고를 줄이자 고정값 5개가 동시에 깨졌고, 그때마다 손으로 숫자를 맞추면 회귀가 '거짓 안심'이 된다.
- 원칙: **같은 사실을 두 경로로 구해 일치를 본다.** ① API가 말하는 수 ↔ **DB가 세는 수**(후보 풀·활성 규칙·검수 대기) ② 응답 내부 정합(**부품 가격 합 = 총액**, 규칙 수 = 판정 수, `over_by` = 총액 − 상한) ③ 누적돼도 성립하는 관계(티어 서열 `가성비 ≤ 추천 ≤ 고성능`, 추천은 예산 이내이며 상한의 90% 이상, 가성비는 전 예산 공통).
- **검증됨**: 재고 3건을 0으로 내려도 123/123 통과(값 변화만 알림) · `total`에 +100 버그를 심으면 13건이 즉시 실패.
- **값 변화 알림은 실패가 아니다.** 주요 수치를 `tests/.regression-snapshot.json`(gitignore)에 적어두고 바뀌면 보고한다 — 관계식이 못 잡는 "가중치를 조용히 바꾼" 변화를 사람이 알아채라는 신호다.
- 스크립트가 **시드 owner(`admin@popcornpc.local`)로 먼저 로그인**한다(슬라이스 37). 고객 그룹은 회원 세션을 따로 갈아탄다(슬라이스 38). DB에 닿지 못하면 원천 대조만 건너뛰고 그 사실을 알린다 — 조용히 통과시키지 않는다.
- **회귀는 정본을 쓰지 않는다**(슬라이스 50 전례): 업로드 검사가 apply까지 돌려 시드 상품을 테스트 값으로 덮었다(P-1001이 ETC·1000원으로 강등 → 손으로 복구). 검증 수단이 검증 대상을 오염시키면 안 된다 — 쓰기 경로는 **가드(400·409·404)만** 확인한다.
- **실패는 전부 코드 문제다** — 값을 맞추지 말고 고친다.
- 엔진·규칙·게이트·원장을 건드리는 슬라이스는 **작업 후 이 세트를 돌리고 결과를 보고**한다.

## 엔진 규칙을 늘릴 때 (슬라이스 46 전례)
- 호환 규칙에 새 필드를 쓰면 **그 필드를 싣는 곳을 전부** 늘려야 한다: `recommend._load_pool` · `swap.SPEC_COLS` · 추천 뷰. 하나라도 빠지면 NULL 불통과로 **조용히** 견적/대안이 0이 된다(500이 아니라 '결과 없음'으로 보인다).
- 그래서 규칙·엔진을 건드린 슬라이스는 회귀를 돌린다 — 스왑 대안 0건은 회귀가 잡는다.

## 원장·되돌림 규약
- 되돌림은 삭제가 아니라 **역방향 행/상태 전이**. undo 로그의 역참조 키는 **`ref_log_id`**(활동 로그가 이 키로 '되돌려짐' 배지를 파생한다 — 다른 이름을 쓰면 회귀의 `되돌림 행 수 = 원본 수` 불변식이 깨진다. 슬라이스 44 전례).

## 명령 관례
- 목업 확인: `mockups/` 하위 .html을 브라우저로 직접 열기(빌드 없음).
- 정적 서버 필요 시: `python -m http.server`. API 서버: `.venv` + `uvicorn api.main:app`(루트 `requirements.txt` 한정 pip 허용, npm 금지). DATABASE_URL은 루트 `.env`(gitignore)만.

## 미정 (임의 결정 금지 — 물어라)
success 토큰 추가 / S2 커뮤니티 근거 소스 / 후보 풀 시각화 형태 / 게임 무게 대표게임 / 게이트웨이 실제 라우팅.

## 첫 세션 시작 시
`HANDOFF.md`와 이 파일과 `docs/decisions/decision-log.md`만 읽고 현 상태를 5줄로 요약한 뒤, 다음 작업을 물어라. 전체 폴더를 스캔하지 마라.
