# CLAUDE.md — 팝콘PC AI (속도 최적화판)

<!-- 이 파일은 매 턴 컨텍스트에 통째로 로드된다. 짧게 유지한다. -->
<!-- 상세 배경은 여기 넣지 말고 docs/ 파일로 미룬다(필요할 때만 읽게). -->

## 프로젝트 한 줄
자체 부품 풀 기반 PC 견적·판매 시스템. 정체성 = **신뢰 판매자**("조립 불가 PC는 안 나온다 + 왜 이 부품인지 설명"). 히어로: "모든 견적에는 이유가 있습니다."
**A-10(2026-07-16): 커머스 승격** — 회원·장바구니·결제·정산·배송·환불을 본 시스템이 직접 처리. 기존 쇼핑몰은 병행 채널, **운영 모드 스위치 5종**(자체/쇼핑몰)으로 분기. 주문 원장은 항상 본 시스템.

## 지금 단계 = 모델링 + DB·API 구현 (2026-07-23 개정)
- **목업 = 스펙.** UI 목업(HTML/CSS/JS)이 1차 산출물이며, 손으로 검증된 화면 범위가 구현 범위를 정의한다.
- **DB 구축 완료(2026-07-23)**: Cloud SQL `popcorn_pc`에 스키마(테이블 46+뷰 2)·시드 적용됨. 스키마 단일 원천 = `docs/06_db-erd.md` Ver 4.0, 실행본 = `db/`(Alembic). 접속 정보 = `docs/infra/GCP_SETUP.md`(로컬 전용). 스키마 변경은 **ERD 개정 → 새 마이그레이션** 순서.
- **로컬 API 슬라이스 승인(2026-07-23)**: 스택 = FastAPI(`api/`), 방식 = **화면 단위 수직 슬라이스**(DB→API→목업 바인딩 관통, 이음새 검증 후 다음 화면). 첫 슬라이스 = ADM-PRD-010 상품 목록 읽기 1본. 목업은 API 실패 시 더미 폴백(Firebase 검토 배포 무영향). 새 화면 연동은 슬라이스 단위로 계획 승인 후 진행.
- **여전히 금지(별도 승인 필요):** 실제 LLM 연동(보류 결정 2026-07-21: 착수 시 팝콘톡 한정·공개 노출·Opus 4.8) · 운영 배포(서버 구동은 로컬 전용).
- 충돌 요청이 오면 코드 짜지 말고 먼저 물어라. **비밀값(DB 비밀번호·API 키)은 리포·설정·메모리에 절대 저장 금지.**

## 작업 방식 (속도 핵심 — 이게 느림을 잡는다)
1. **탐색 최소화.** 작업 전 전체 코드베이스를 훑지 마라. 필요한 파일만 열어라. 어떤 파일을 볼지 모르면 먼저 물어라.
2. **한 번에 하나.** 큰 작업을 한 요청에 몰지 마라. "S2 전체" 대신 "S2 섹션1"처럼 쪼개라.
3. **계획 먼저, 코드 나중.** 파일을 만들기 전 3~5줄로 접근을 요약하고 승인받아라(Plan Mode 선호).
4. **끝나면 자체 검증.** 목업은 브라우저에서 열리는지·JS 콘솔 에러 없는지 스스로 점검 후 보고.
5. **말은 짧게.** 턴 사이 설명은 최소, 최종 보고는 요점만. 장황한 서술 금지.
6. **세션은 일회용.** 결정은 이 파일/`docs/decisions/decision-log.md`에 기록. 긴 세션에 기억을 의존하지 마라. 컨텍스트가 길어지면 `/clear` 후 재시작.

## 베타 배포 상태 (슬라이스 41 — 2026-07-26 가동)
- **이 서버 = 개발/스테이징이다. 운영은 새로 세운다**(2026-07-29 확정 · decision-log **I-01**). 승격하지 않는다 — 베타의 테스트 잔재가 운영 원장이 되면 나중에 구분할 수 없다(슬라이스 78 owner 테스트 계정 · 실주문 `ORD-84222` 전례). 운영 DB는 **원장을 비우고** 시작하되 **카탈로그·규칙은 이관**한다(사람 검수 값은 원천 CSV로 재현 안 됨). 그래서 **여기서는 부작용 있는 검사를 해도 되고, 운영 서버에서는 절대 안 된다.**
- 정본 도메인 `popcornai.co.kr`은 **운영 몫**이다. 개발엔 서브도메인(`dev.`)을 붙인다 — 나중에 옮기면 인증서·OAuth 리다이렉트·쿠키를 전부 다시 손봐야 한다.
- 접속 `http://34.47.124.184/admin/login.html` · GCP `popcorn-app`(asia-northeast3-b) · Cloud SQL `popcorn-db`. **이름은 그대로 둔다**(역할은 문서가 정한다 — 리소스명 변경은 재생성을 요구한다).
- **접속 = `https://admin.popcornai.co.kr`** (2026-07-28 HTTPS 전환 · Let's Encrypt). 정본 `popcornai.co.kr`은 여전히 운영 몫이다.
- **접근 정책: 관리자·고객 모두 열려 있다**(2026-08-09 사용자 결정으로 개정 · 절차 `deploy/README.md §7`). Basic Auth는 폐기했다 — certbot이 설정을 고치며 `auth_basic`이 사라진 것을 발견하고, 복구 대신 경로별 분리를 택했다(2026-07-30).
  - 관리자: 열림. **비밀번호 인증(슬라이스 70) + `/api/admin/*` 미들웨어**가 막는다.
  - 고객: **2026-08-09에 열었다.** 2026-07-30~08-09 사이에는 nginx가 10경로를 404로 막고 있었다(`/mvp1/` `/api/auth/` `/api/my/` `/api/candidates` `/api/recommend` `/api/swap/` `/api/orders` `/api/ops` `/api/showcase` `/api/promo-click`). 되돌리는 방법은 `deploy/nginx-popcorn.conf`에 주석으로 보존돼 있다.
- **⚠ 고객 인증은 여전히 dev 어댑터다 — 열면서 감수한 구멍이다.** `api/customer_auth.py`의 검증은 `if "@" not in email` 한 줄이 전부다(비밀번호·OAuth·인증메일 없음). **아무나 남의 이메일을 입력해 그 사람 세션을 받고 `/api/my/`로 주문·결제·환불·후기를 볼 수 있다.** 개방 시점 실회원은 6명(전부 테스트 계정)이었다. **테스터에게 실제 개인정보를 넣지 말라고 공지해야 하고, 실 OAuth(카카오·네이버·구글)가 유일한 해소다** — 붙일 자리는 `api/admin_profile.PROVIDER_KO` + `_verify_google()`.
- **`/api/auth/`와 `/api/admin/auth/`는 다른 경로다.** nginx 접두어 매칭은 URI 시작부터 본다 — 나중에 고객 경로를 **다시 막을 때** 이 구분을 어기면 관리자 로그인까지 죽어 아무도 못 들어온다.
- **nginx 설정 정본 = `deploy/nginx-popcorn.conf`이고 서버 실제와 일치해야 한다.** 2026-07-30까지 리포는 `server_name _;` HTTP 설정을 들고 있었고 서버는 certbot이 고친 HTTPS로 돌았다 — 리포를 복사하면 돌아가는 설정을 덮어썼다. 고칠 때는 **백업 → 로컬 수정·push → 서버 pull → `nginx -t` → reload**(P-06: 서버에서 직접 고치지 않는다).
- 앱은 `127.0.0.1:8000`에만 바인드(외부 직접 접근 불가) · 방화벽 80·22만.
- 배포 갱신: 로컬 커밋·푸시 → 서버에서 `git pull` + `systemctl restart popcorn-api`(절차 `deploy/README.md`).
- **DB 복구 절차 = `deploy/RESTORE.md`** (2026-07-27 리허설 통과). 자동 백업 매일 21:00 UTC·7건, **PITR 로그 7일** — 최근 7일 임의 시점으로 되돌릴 수 있다. **원본을 덮어쓰지 않는다**: `backups restore`가 아니라 `instances clone --point-in-time`으로 복제본을 만들어 검증 후 전환한다(복제 약 4분). 운영 인스턴스는 삭제 보호 ON.
- **서버 코드 직접 수정 금지** — 배포 타깃이다(전역 규약).

## 데이터 — **수를 여기 적지 않는다** (2026-08-12 개정)

> **현재 수는 재서 안다: `.venv/Scripts/python scripts/state.py`**
>
> 이 파일에 적으면 낡는다. 실제로 2026-08-12에 여기 적혀 있던 셋이 **전부 틀렸다** —
> products 22,838→**22,840** · 추천 후보 3,087→**3,307** · 검수 대기 6,490→**5,156**.
> **문서는 자기가 낡았다고 말해 주지 않는다.** 그래서 다음 세션이 그걸 사실로 읽고,
> 같은 일을 다시 하고, 일주일 뒤엔 처음부터 하는 느낌이 든다(사용자 지적 2026-08-12).
>
>     적지 않는다   지금 몇 건인가 — products · 후보 · 검수 대기 · 규칙 수 · 화면 수
>     적는다        왜 그런가 · 무엇을 조심하나 · **과거에 무슨 사고가 있었나**
>
> 아래 교훈에 박힌 과거 수치는 남긴다 — 「그때 그랬다」는 낡지 않는다.

- **⚠ 원천이 둘이다**(2026-08-16 발견 — 이 줄이 오래 ①만 적고 있었다). 어느 쪽으로 들어왔는지
  모르면 사고를 되짚을 수 없다.

      ① CLI 원천    `최종_정제완료_상품데이터.csv` + `db_product_specs.csv`(EAV).
                    경로는 `tools/catalog_import.py` 가 하드코딩해 갖고 있다(`E:\GDRIVE\...`).
                    적재 `--origin real|demo` · 재파싱 회수 `tools/respec.py --dry`
      ② 몰 직수령    사장님이 팝콘PC(윈윈) 관리자에서 **직접 내려받은** 파일.
                    2026-08-15 에 24,721건이 이 경로로 들어왔고 **그날 사고 전부가 여기서 비롯됐다**
                    (완제품 340건 강등 · 상품명 대괄호 841건 · 중고 67건 혼입).
                    파일은 `docs/상품다운_*.csv|xlsx`, **git 에 넣지 않는다**(`.gitignore` · 16MB↑).
                    ⚠ **내려받는 절차가 어디에도 적혀 있지 않다** — 지금은 사장님께 다시 청하는 수밖에 없다.

  두 경로 다 `api/catalog_ingest` 의 `build_plan`/`apply_plan` 을 지난다(적재 로직은 여전히 단일 원천).
- **호환 규칙은 `compat_rules` 단일 원천이고 회귀가 DB와 대조한다** — 문서에 8종이라 적힌 채
  수랭 규칙이 늘어 있었다(2026-07-30 발견). **수를 문서에 박으면 정확히 이렇게 어긋난다.**
- **선호 태그** — 백필 `tools/backfill_tags.py`. **명시 표현만** 인정(무소음·저소음·정숙·사일런트·SILENT / 화이트·WHITE·백색). 추론 금지 — 지어낸 태그는 '조용하게'라고 말한 이유를 배신한다. respec과 분리한 이유: respec에게 `false`는 '이미 있는 값'이지만 태그에서 false는 '아직 판정 안 함'이다.
- **데모/실데이터는 `data_origin`으로 구분**한다. 데모를 올려 테스트해도 실운영 데이터와 섞이지 않는다.
- 목록·검수 큐는 **서버 페이지네이션**이다(전 행 반환 금지). 화면은 '전체 N건 중 이 페이지'를 명시한다.
- 견적 응답 2.1초(후보 3,046행 로드가 주된 몫 — 탐색 자체는 0.02초). `DFS_NODE_CAP`=2,000,000.
- **로그 문자열은 ASCII 기호만** — 서버 stdout이 cp949라 em-dash·화살표가 요청을 500으로 만든다(슬라이스 40).

## 화면 (대상 = MVP1)
- 고객: 게이트웨이·main-landing·S0~S5·MY(마이페이지/주문 내역·환불 접수/결제 내역) 완료. **S1 `mockups/mvp1/s1-session.html` = 품질 기준 파일** (4모드 + UX-17 점진 공개 + 주변기기 인지).
- 관리자 인증(슬라이스 37): `mockups/admin/login.html`에서 **회사 소셜 계정 → 신청(대기) → owner 승인 → 권한(조회/운영자/관리자)**. 비밀번호 미저장. 현재 신원 확인은 **dev 어댑터(입력 이메일 신뢰) — 로컬 전용·공개 배포 차단 사유**. 작업 기록 주체 = 로그인 운영자.
- **상품 일괄 등록(ADM-CSV-010)은 실제 적재한다**(슬라이스 50): 업로드 → **드라이런** → 확인 → 적용. `POST /api/admin/catalog-import/dryrun|apply`, **owner 전용**. 적용 시 드라이런이 보고한 `expect_ok`를 되돌려 보내야 한다(다른 파일을 확정하는 사고 차단). 되돌리기는 없다 — 화면이 그 사실을 먼저 말한다.
- **적재 로직 단일 원천 = `api/catalog_ingest`**. CLI(`tools/catalog_import.py`)와 업로드 API가 같은 `build_plan`/`apply_plan`을 부른다 — 두 번 구현하면 "왜 이 상품이 안 올라왔지?"에 답할 수 없다.
- **적재는 채우기만 하고 지우지 않는다**: `product_specs`는 `COALESCE(EXCLUDED.col, 기존)`. EAV 없이 마스터만 올렸다가 사양이 지워져 후보 -1·검수 +169가 된 사고가 있었다(슬라이스 50). 필수 사양 판정도 **새 값 ∪ 기존 값**으로 한다.
- **드라이런은 건수가 아니라 영향을 말한다**: 신규·갱신·추천 후보 진입/이탈·검수 새로 회부(중복 뺀 실측). "검수 회부 198건"이라 말했는데 실제 증가가 0이면 헛수를 말한 것이다.
- **카테고리 문자열이 어긋나면 상품이 `ETC`로 강등되어 후보에서 사라진다** — 드라이런의 `pool_out`이 이걸 미리 알린다(회귀로 고정).
- **⚠ 잠금(`locked_fields`)이 지키는 것은 매입가·판매가 «둘»뿐이다**(2026-08-15 실사고). 같은 UPSERT의
  `part_type`·`category_group`·`ai_candidate_yn`·`review_required_yn`·`status`·`maker`·`model_name`·
  `market_price`·`supplier`·`danawa_code`·`spec_source_text`·`data_origin` 은 전부 무조건 `EXCLUDED` 다.
  **운영자가 상품 상세에서 손으로 분류를 고쳐도 잠기지 않는다**(`change_part_type` 이 `locked_fields` 를
  건드리지 않는다). 그래서 **두 가지가 조용히 되돌아간다**:
  - **분류** — `0031_built_pc_axis.py` 가 완제품·베어본을 ETC에서 올려놓은 뒤 2026-08-15 몰 적재가
    340건을 다시 ETC로 내렸다. **그 마이그레이션 자체가 「판매 분류에만 살면 적재가 되돌린다」고 예고했다.**
  - **검수 회부** — 123034 는 2026-08-10 검수로 보내졌는데(검수 행은 지금도 `대기`) 적재가
    `review_required_yn` 을 재계산해 **후보로 되돌렸다.** 즉 **「사람이 검수로 뺐다」는 사실이 적재마다 지워진다** —
    §검수 깔때기(A-17)의 「못 채운 부품은 빠진 상태로 둔다」가 전제부터 깨진다.
  - ⚠ **위 `pool_out` 은 이걸 못 잡는다**: 「지금 후보인 것이 빠지는가」만 보므로, 원래부터 후보가 아니던 것
    (`category_group='etc'`)이 **잘못된 라벨을 받는 것**은 관측 범위 밖이다.
  확인법: `api/catalog_ingest.py` UPSERT 에서 `locked_fields` 를 참조하는 줄이 몇 개인지 세면 된다(2개).
- 검수 큐 정합: 이미 값이 채워진 대기 행은 `tools/prune_reviews.py`로 **'처리' 전이**(삭제 금지). 슬라이스 50에서 117건 정리.
- **검수 깔때기(A-17)**: ① 자동 회수(파서·분류 교정) → ② 외부 제안(다나와) → ③ 사람 검수(원칙). 못 채운 부품은 추천에서 빠진 상태로 둔다(4중 게이트가 이미 그렇게 동작 — 선택지가 아니다).
- **오분류가 사양 부족보다 크다**(슬라이스 51): 시스템 팬이 CPU쿨러로, 베어본이 메인보드로, UPS·AC케이블이 파워/케이스로 올라가 있었다(160건 → 검수 436건 해소). 판정 근거는 **스펙 원문의 분류 토큰만**(`catalog_map.is_accessory`) — 상품명으로 보면 모듈러 파워의 '케이블', SSD의 '연장가이드'까지 걸려 진짜 부품을 떨어뜨린다. 교정 도구 `tools/reclassify.py`.
- **다나와 수집은 제안까지만**(A-18): `tools/danawa_fetch.py` → `product_reviews.suggested_value`. **정본에 바로 넣지 않는다** — 남의 페이지 값이 견적 근거가 되면 정체성이 무너진다. `origin_value`가 비어 있으므로 일괄 확정 대상도 아니다(사람 확인 강제). robots 허용 경로 `/info/`만, 1.5초 간격, 연속 실패 5회면 중단, 원문은 `.cache/`(gitignore).
- **pcode 오매핑이 실재한다**: 우리 메인보드에 랜케이블 pcode(제목 유사도 0.16). 유사도 0.45 미만이면 사양을 쓰지 않고 '코드 불일치'로 회부한다.
- **적재가 오분류를 막는다**(`map_part_type` — 슬라이스 51 마감): 분류 판정은 **원문의 `:`가 없는 앞쪽 토큰**만 본다(키-값은 사양이지 분류가 아니다). 이 한 줄로 '액세서리'가 3번째 토큰이라 놓치던 것들이 잡히고, 모듈러 파워의 '케이블' 오검출은 안 생긴다. `wrong_slot`은 SSD 자리의 램(DDR+CL)을 잡되 **슬롯을 자동으로 바꾸지 않는다**(etc로 내리고 사람이 결정).
- **어휘를 넓힐 때 오검출을 먼저 센다**: '허브랙'을 넣었더니 진짜 랙마운트 케이스 3건이, '시스템쿨러'만 봤더니 진짜 수랭 AIO 1건이 걸렸다. 원천은 '시스템쿨러'를 CPU쿨러 대분류로도 쓴다 → **냉각 방식(수랭·공랭) 표시가 있으면 CPU쿨러**. 회귀가 8종(걸러야 할 것 3 · 지켜야 할 것 5)을 고정한다.
- **다나와 분류로 우리 오분류를 잡는다**(`tools/reclassify.py --danawa`, 캐시만 사용·재수집 없음): 라이저 케이블이 케이스로, 기계식 키보드·스위치허브가 메인보드로, 노트북 램이 SSD로, 시스템 팬이 CPU쿨러로 올라가 있었다(49건 → 검수 147건 해소). **부정 목록으로만 판정한다** — 긍정 목록("이 분류여야 한다")은 오탐 318건을 만든다. head는 분류가 아니라 상품 특성인 경우가 흔하다(케이스 "미니ITX" · 보드 "AMD(소켓AM5)" · GPU "RTX 5050" · SSD "6.4cm(2.5형)").
- **`cooler_tdp`는 다나와에도 없다** — 판매중 대기의 최대 덩어리(**307건**, 2026-08-13 실측. 이전에 적혀 있던 324는 오기)는 다나와 크롤링으로 못 채운다. 실측 근거: `field_name='cooler_tdp'` 검수행 1,065건 중 **다나와 제안 마커가 붙은 것 0건**. 사람 검수 또는 제조사 자료(웹 사양 채움)가 필요하다.
  - 넓게 보면 **엔진이 읽는 17개 사양 필드 중 `cooler_tdp`가 결측률 1위(1,066/1,374 = 77.6%)**다. 다나와 자체의 성공률도 낮다 — 크롤 자격 20,570건 중 제안이 한 번이라도 붙은 상품은 **425건(2.1%)**.
- 검수 큐(ADM-PRD-020)는 **건수가 아니라 큐 구성**을 먼저 말한다: 일괄 확정 가능 / 단건 검수 / **원문에 값이 없어 사람이 넣어야 하는 것**. 마지막 덩어리가 압도적이라 「대기 N건」만 보면 할 수 있는 일을 오판한다. respec은 소진됐다. '판매중·재고 있는 것만' 필터가 실제 작업 목록이다.
- 근거 클릭은 **수집된다**(슬라이스 49): S2 부품 행 클릭 → `POST /api/promo-click` → `promo_click_logs`. `user_id`는 회원이 아니라 **익명 방문자 키**(FK `users`) — member_id를 넣으면 FK 위반으로 조용히 롤백된다.
- 관리자: `mockups/admin/` (Phoenix 한글 셸) — A-10 커머스(운영 전환·주문 관리·환불·결제 정산·배송·후기·회원) + 단가표 일일 반영(ADM-PRC-040) 포함. **화면 수는 `scripts/state.py` 가 센다**(`data-screen-id` 가 있는 것만 화면이다 — `_demo-products.html` 은 템플릿 잔재라 아니다). 회귀도 파일 수와 대조한다 — 문서에 31로 적힌 채 두 화면이 늘어 있던 전례가 있다.
- **A-10 카탈로그 전 화면 목업 완료(2026-07-23)**. 카탈로그 = `docs/decisions/admin-identity.md`.

## 관리자 화면 규약 (슬라이스 53·54)

- **★ 화면 UI 를 받으면 «정의서 링크와 다음 차례»를 함께 올린다 (2026-08-13 사장님 지시).**
  목차 정본 = `docs/design/req/INDEX.md`(LNB 순서 · 상태 · 클릭 링크).
  사장님이 위로 스크롤해 찾지 않게, 하네스가 매번 **그 화면의 정의서 + 다음 두세 개**를
  링크로 붙인다. 화면이 지어지면 목차의 상태를 함께 갱신한다.

- **★ 디자인 원안이 있는 화면은 «치수까지» 검증한다 (2026-08-13 사장님 지적).**
  «동작이 된다»로 통과시키지 않는다. 원안(`docs/design/dc-*.html`)은 인라인 스타일에
  치수가 박혀 있으므로, 확인자가 `getComputedStyle()`·`getBoundingClientRect()` 실측값과
  **한 줄씩 대조표**를 만든다: 행 높이·상하 padding·글자 크기·행간·요소 간 gap·구분선.
  판정은 일치 / 허용 오차(±1px) / **어긋남**이고, 어긋난 것은 **원인 선택자까지** 짚는다.
  계기: 목록 행 간격이 원안보다 벌어진 채 «동작 통과»로 넘어갈 뻔했다.

- **★ 좌측 메뉴(LNB) 동작 규격 — admin2 전 화면 공통 (2026-08-13 사장님 확정).**
  근거 = 사장님이 Claude Design 에서 확정한 화면 `docs/design/dc-review-screen.html`.
  **항목 구성(IA)은 이 규격의 대상이 아니다** — 정본은 여전히 `api/admin_nav.py`(8그룹 37항목,
  P-09). 디자인의 11항목 사이드바는 목업용 축약이었고, 규칙으로 삼는 것은 **동작**뿐이다
  (2026-08-13 사장님 확인).

      펼침 폭      212px          접힘 폭   64px      전환    width .16s ease
      접기 단추    메뉴 최하단 「메뉴창 접기」 · 아이콘 « / »  (접히면 라벨 숨김)
      접힘 표시    아이콘만 남기고 각 항목에 `title` 툴팁 — 아이콘만으로 이름을 확인한다
      브랜드       펼침 `admin2` -> 접힘 `a2` (가운데 정렬)
      선택 유지    사이드바 클릭은 화면의 선택 상태를 풀지 않는다 (`data-keep`)

  **`data-keep` 규약:** 바깥 클릭으로 선택 해제·상세 닫힘을 구현하는 화면에서, 선택을
  유지해야 하는 영역(사이드바·상세 서랍·필터 바·목록 행)에 `data-keep="1"` 을 단다.
  루트 클릭 핸들러는 `closest('[data-keep]')` 이면 아무것도 하지 않는다 — 필터를 만지다
  상세가 닫히는 사고를 막는 장치다.

- **★ 이 화면 작업은 «재구축»이다 — 스키마 변경까지 포함한다 (2026-08-13 사장님 확정).**
  사장님 원문: *"지금 화면들을 만드는 것은 DB 설계 구조변경까지 감안해서 하는 거니까 —
  재구축으로 생각해야 함."* P-08(2026-08-10)이 이미 «재구축은 증분이 아니라 재설계이고
  DB 폐기·스키마 재설계까지 열려 있다»고 확정했는데, 이번 admin2 물결에도 **그대로 적용**된다.

  **그래서 바뀌는 판단:**

      옛 스키마에 억지로 맞추지 않는다   합성 키·의미 겹치기·컬럼 재활용으로 «표를 안 만들고»
                                          우회하는 것은 이 단계에서 미덕이 아니다.
      맞는 표를 새로 만든다              마이그레이션 + ERD 개정 + DBA 적용이 정규 경로다.
      다만 근거를 적는다                  왜 기존 표로 부족한지를 보고에 남긴다(무근거 신설 금지).

  **그래도 바꾸지 못하는 것**(P-08 그대로): **A-01·A-02·A-03**(정체성·결정론 엔진·LLM 역할)과
  **원장·되돌림 규약**은 근거가 기록된 결정이라, 바꾸려면 *왜 그 근거가 더는 유효하지 않은지*를
  먼저 적는다. 그리고 **개발 DB는 버려도 되지만**(I-01 — 운영은 새로 세운다) **카탈로그·규칙·
  사람이 검수로 채운 값은 이관 대상**이므로, 그것을 지우는 변경은 이관 계획과 함께 낸다.

- **★ 관리자 UI는 `/admin2` 아래 전면 신규 개편이다 (2026-08-13 사장님 지시 — P-09의 귀결).**
  구 `/admin/*.html`은 **백업용 동결** — 수정 금지, 사용자 안내·화면 간 링크·버튼 연결
  금지. 필요한 기능이 구화면에만 있으면 **그 기능을 admin2에 새로 짓는 것**이지,
  구화면을 고치거나 그리로 보내는 것이 아니다 (실례: 검수 승인을
  /admin/review-queue.html로 안내했다가 지적받음 — /admin2/reviews 신규 구축으로 해소).
- **상품 관리(ADM-PRD-010)가 상품 작업의 유일한 진입점**이다. 좌측 메뉴에 편집 화면을 따로 두지 않는다 — 대상 없이 여는 편집 화면은 저장이 안 되는데 운영자는 모른다. 상세는 `product-edit.html?code=`, 등록은 `?new=1`.
- **보기와 고치기를 나눈다**(슬라이스 55): 상품명 클릭 = `?code=` **읽기 전용**, 목록의 `[수정]` = `?code=&edit=1` **편집**. 읽기 모드의 `[수정]`은 페이지 이동 없이 그 자리에서 전환하고 `[취소]`는 서버 값으로 되돌린다. **저장하면 읽기로 복귀**(연속 오조작 방지). 읽기 전용 화면을 따로 만들지 않는다 — 같은 정보를 두 곳에서 관리하면 한쪽만 고치는 사고가 난다.
- **등록 폼에 예시 값을 박아두지 않는다**: 마크업에 남아 있던 다른 상품명·가격이 신규 등록의 기본값이 되어 그대로 저장될 뻔했다(사용자 지적). 분류도 첫 항목 자동 선택 대신 '분류를 선택하세요'를 둔다. 회귀가 마크업의 `value=`를 검사한다.
- **단건 수정은 고친 필드를 잠근다**(`locked_fields`). 안 그러면 다음 적재가 덮어써 어제 고친 값이 오늘 되돌아간다(A-16의 짝). **되돌릴 때 잠금도 함께 되돌린다** — 값만 원복하고 잠금이 남으면 적재가 영영 못 채운다.
- **분류는 상세에서 고친다**(슬라이스 67 — 규약 개정). 예전엔 "슬롯을 바꾸는 일이라 재적재 소관"으로 잠갔는데, 적재가 잘못 넣은 분류(미분류 5,149건)를 바로잡을 길이 없어 그 상품들이 영영 추천에서 빠졌다. 대신 **눈 감고 바꾸게 하지 않는다**: `preview`로 영향(새로 필요한 사양·빠지는 사양·추천 진입/이탈)을 먼저 보여주고 확인받는다. `products`와 `product_specs`의 분류를 함께 옮긴다 — 어긋나면 적재·검수가 서로 다른 종류로 본다.
- **목록 필터 선택지는 서버가 준다**(`product-meta`의 `used_parts`·`makers`). 화면이 현재 페이지에서 뽑으면 한 페이지에 없는 값은 아예 안 나온다 — 상세에서 고른 분류가 목록 필터엔 없었다. 제조사 필터는 **서버 파라미터**여야 한다(예전엔 현재 페이지 안에서만 걸러 22,841건 중 100건에만 먹었다).
- **공급처별 매입가는 실데이터다**(`GET/POST/DELETE /products/{code}/suppliers`). 표가 마크업 하드코딩이라 신규 등록 화면에도 남의 값(용산도매A 352,000)이 떴다. 같은 (상품, 공급처)는 한 행(`ON CONFLICT`) — 두 번 넣으면 어느 값이 맞는지 모른다.
- **등록 전에 닮은 상품을 보여준다**(`api/dedupe`, 임계 0.97). `danawa.title_similarity`를 쓰면 안 된다 — 모델명 토큰 부스트 때문에 MP600 PRO **NH**와 **XT**가 1.000이고, 0.9 이상이 61.6%라 등록할 때마다 창이 뜬다. 중복 판정은 **다른 점을 지우지 않아야** 한다: 부스트 없이, 용량·크기 수치가 어긋나면 점수를 깎는다(6GB vs 8GB → 0.467). 되묻는 동안 **등록되지 않는다**.
- **중복 정리는 편입이 먼저, 삭제는 마지막**(슬라이스 69). products를 참조하는 테이블이 15개고 주문에 걸린 상품(33종)을 지우면 **과거 주문서가 깨진다**. 편입은 재고·공급처를 원본으로 옮기고 중복을 `삭제대기`로 내린다(원장 보존·되돌리기 가능). 재고 이동도 **원장 두 행**으로 남긴다 — 수량만 고치면 어디서 왔는지 알 수 없다. 삭제는 주문 이력이 있으면 409로 거부하고, 다른 참조가 있으면 `force`를 요구한다.
- 상세에서 **사양은 고치되 근거를 남긴다**. 사양 값 입력은 검수 행을 함께 해소한다(슬라이스 57).
- **목록은 전부 서버 페이지네이션이다.** 재고 입고가 15,259건 + 카탈로그 22,838건을 매 요청 전량 보내고 있었다(슬라이스 54). 셀렉트 후보는 **검색어가 있을 때만** 조회한다. 회귀는 한 페이지 길이가 아니라 `total`을 대시보드와 대조한다.
- **쓰기 요청은 자동으로 진행·완료를 표시한다**(`shared/ui-progress.js`, 전 43화면). fetch를 감싸 GET·인증 폴링을 뺀 나머지에 상단 바를 띄우고, 완료 문구는 **서버가 준 `verdict`/`note`를 그대로** 쓴다. 기존 하단 토스트와 위치가 달라 겹치지 않는다.
- 선택지(분류 라벨·상태값)는 **서버에서 받는다**(`/api/admin/product-meta`). 화면에 하드코딩했다가 등록 셀렉트에 3종만 있어 파워·케이스·메모리를 등록할 수 없었다.

## 디자인 계약
- **톤 = Popcorn Light**(D-07, 2026-07-03 사용자 선택). Vibrant Horizon은 폐기됐다(decision-log X-05).
- 토큰 단일원천 = `design-system/tokens.css` `:root`. 임의 HEX/px 금지.
- **여기에 HEX를 다시 적지 않는다.** 토큰 이름(`--primary`·`--error`·`--font-head`)으로 쓰고 값은 파일을 본다 — 이 문서가 `#005ab3`(폐기된 Vibrant Horizon 색)을 들고 있다가 실제 토큰(`#65A1D4`)과 어긋난 채 오래 남아 있었다(2026-07-29 발견).
- 서체: 본문 `--font`(Inter+Pretendard) · 제목 `--font-head`(Hanken Grotesk) · **숫자 `--font-num`(JetBrains Mono)**.
- **U-01 확정(2026-08-08 사용자 결정): success 토큰을 둔다.** 값은 새로 고르지 않고
  고객 화면이 이미 쓰던 13종을 그대로 옮겼다(169회 · 겉보기 불변). 초록은 고객 화면의
  기존 용법(검증 통과·안전)에 한정했고, **당시엔 admin2가 없어 관리자 화면은 전부 계속
  통과=`--primary`였다.**
  **admin2 예외(2026-08-15 사장님 확정 — decision-log A-45):** admin2 팔레트는 초록을
  쓴다. U-01의 「관리자 화면은 `--primary`」는 **admin2 가 생기기 전 규칙**이다 —
  `mockups/shared/admin2/admin2.css`가 이미 `--success-dark`·`--state-done`을
  `--rv-green`으로 정의해(2026-08-13 승인 디자인에서 뽑은 팔레트) 「완료·통과·연결됨·
  가격 내림」류에 쓰고 있다. **구 `/admin/*.html`(백업용 동결)은 이 예외 밖** — 여전히
  `--primary`다.
  **바뀌지 않는 것**: **어느 쪽이든 항상 텍스트 병기** — 색만으로 통과/실패를 말하지
  않는다(색약 배려). **차트에는 여전히 초록을 쓰지 않는다** — 주문 상태·분류 건수는
  통과/실패가 아니라 공정 단계다(회귀가 지킨다).
- **아이콘: Streamline Ultimate Color (CC BY 4.0, 출처 표기 필수)** — 2026-07-16 사용자 지시로 단색 정책 폐기. 파일=`mockups/shared/icons/su/`, 매핑=`_map.json`, 런타임 스와퍼=`mockups/shared/su-icons.js`(전 페이지 주입, 출처 표기 자동 삽입). 새 아이콘 필요 시 iconify에서 받아 `ms-`/`ft-` 규칙으로 추가.
- 마크업: `data-screen-id`/`data-domain`/`data-bind`/`data-repeat`/`data-action`, 예외는 `hidden`. 한국어 UI, 768px 세로 스택.
- **★ Phoenix 는 더 이상 참조하지 않는다 (2026-08-14 사장님 지시 — 이 항목이 아래 둘을 덮는다).**

  사장님 원문: *"앞으로는 피닉스 디자인은 참고하지 말고, 내가 주는 화면 UI 대로 해줘."*

      유일한 원천   사장님이 주시는 승인 디자인 (Claude Design 프로젝트)
      금지          Phoenix 템플릿에서 마크업·컴포넌트·레이아웃을 가져오는 것
                    「옆 화면을 베낀다」 · 「템플릿 검증 후 혼합」 같은 옛 절차
      남는 것       공용 셸(`_admin2_shell.html.j2`)과 `admin2.css` — 우리 것이다

  **왜 바뀌었나:** Phoenix 를 기준으로 삼던 시절의 규약은 *「우리에게 디자인이 없다」*는
  전제 위에 있었다. 지금은 사장님이 화면마다 확정 디자인을 주신다 — 전제가 사라졌다.
  Phoenix 를 계속 섞으면 **승인받지 않은 배치가 화면에 들어간다.**

  ⚠ 아래 두 항목(Phoenix 지도 · ①→②→③ 재구축 절차)은 **역사 기록으로 남긴다.**
  왜 그렇게 했는지가 다음 판단의 재료라 지우지 않지만, **지금 따르는 규칙이 아니다.**

- ~~**Phoenix 원본 지도 = `docs/design/phoenix-template-map.md`**~~ (폐기 2026-08-14) — (v1.22.0, `D:\phoenix_Templet\`). 새 화면·컴포넌트·vendor가 필요하면 **옆 화면을 베끼지 말고 여기서 좌표를 찾는다.**
- **P-08 재구축은 증분이 아니라 재설계다**(사용자 확정 2026-08-10): **DB 폐기·스키마 재설계·화면 개념 폐기까지 열려 있다.** 개발 DB 는 원래 버려도 되는 곳이다(I-01 — 운영은 새로 세운다). 다만 **A-01/A-02/A-03(정체성·결정론 엔진·LLM 역할)과 원장·되돌림 규약은 근거가 기록된 결정**이라, 바꾸려면 *왜 그 근거가 더는 유효하지 않은지*를 먼저 적는다. 그리고 **화면 하나를 끝낼 때마다 검증을 통과해야 다음으로 간다** — 실패 자원 0 · 콘솔 에러 0 · 화면 수치 ↔ API 대조 · 경계 입력(미로그인·빈 상태·서버 실패) · **비율에는 표본 병기**. 상세는 `decision-log` P-08.
- **재구축 화면(`/admin2/*`)은 순서가 규약이다**(사용자 확정 2026-08-09): **① 콘텐츠 기획 → ② 템플릿 검증(하나로 안 되면 혼합) → ③ 표출**. 반대로 하면 **포맷이 내용을 지시하고**, 그 칸을 채우려고 데이터를 만들게 된다 — `26,480`·`호환성 5종`이 그렇게 생겼다. 포맷은 **카드 수가 아니라 도메인 일치**로 고른다(2026-08-09 실수에서 얻음). ③에서는 **원본 마크업 그대로**, 바꾸는 것은 자산 경로와 제목뿐이고, 못 채우는 칸은 지우지 않고 `[향후 사용예정]` 텍스트로 두되 **숫자·차트는 남기지 않는다.** 실데이터 연결 전이면 **상단에 「레이아웃 견본」 배너**를 단다. 절차 상세는 위 지도 문서 §재구축 절차.
  - **셸(좌측 메뉴·상단바)은 템플릿이 아니라 우리 화면(`activity-logs.html`)에서 가져온다** — 템플릿 `starter.html`은 좌측 메뉴가 Phoenix 데모 전체다.
  - **자산 대조표를 먼저 본다**: 템플릿엔 있고 우리엔 없는 파일을 참조하면 404이고 브라우저는 **조용히 넘어간다**. `assets/css/theme.css`(비압축)를 참조한 세 화면이 스타일 없이 이틀 돌았다(슬라이스 100). 쓸 파일은 `theme.min.css`·`user.min.css`뿐이고 vendor는 템플릿 41개 중 우리에게 10개만 있다.
  - 회귀 `[30] 화면 자산`이 **참조한 CSS·JS가 실제로 있는지** 검사한다.

## 화면 정직성 (슬라이스 47·48 — 가장 중요한 규칙)
- **랜딩의 'AI 교차 해석' 문구는 유지한다**(2026-08-08 사용자 결정). 실제 LLM 연동은
  보류 상태라 지금은 동작하지 않지만, 오픈 시점에 붙일 예정이라 사전 홍보로 둔다.
  **이건 이미 판단이 끝난 항목이다 — 감사에서 다시 '결함'으로 올리지 않는다.**
  (숫자를 지어내지 않는다는 규칙과는 별개다: 저건 계획의 표현이고, 후보 수·가격·규칙 수는
   여전히 서버 값만 쓴다.)
- **화면이 숫자를 지어내지 않는다.** 후보 수·가격·건수·규칙 수는 서버 응답만 쓴다. 목업 시절 연출(예: `poolN*0.6`, '호환성 5종', 완화 칩 '+1,240개')이 LIVE 경로에 남아 서버가 모르는 수를 말하면 '모든 견적에는 이유가 있습니다'가 무너진다.
- 서버가 반영하지 않는 조건은 **화면도 세지 않는다**(용도는 후보 수를 안 바꾼다). 읽지 못한 입력은 '조건으로 못 읽었다'고 밝힌다. 완화 칩도 `effects[].applied=false`인 제약은 내밀지 않는다(풀어도 그대로여서 거짓 기대를 준다).
- **후보 수 > 0 ≠ 견적 가능.** 슬롯 하나가 0이면 조립이 불가능하다. 판정은 `/api/candidates/count`의 `buildable`·`empty_slots`·`verdict`만 쓴다. 슬롯은 **엔진과 같은 정의**로 센다 — 쿨러는 공랭·수냉이 한 슬롯이라 part_type으로 세면 AIO 0을 빈 슬롯으로 오판한다.
- **서버가 내려준 판정을 화면이 버리지 않는다.** `budget.verdict`(within/over/over_by)를 안 쓰면 120만원 예산에 3,025만원 구성을 내밀며 초과를 감춘다(슬라이스 48 실제 사례).
- **화면이 «판정»도 지어내지 않는다 — 게이트 조건을 일부만 베끼지 않는다** (2026-08-18 실사고).
  재고 입고 목록이 후보 진입 판정을 `reviewed && priced && has_specs` **셋**으로 조합했는데,
  원천 `v_recommendation_candidates` 의 조건은 **여덟**이었다(`ai_candidate_yn`·`category_group`·
  `part_type`·`data_origin`·`status` 를 못 봤다). **입고 대기 15,725행 중 3,876행에 「후보 진입
  가능」이라 말했고 그 행들은 뷰에 영영 못 들어간다.** 조합식은 문법도 타입도 맞아 **콘솔이
  조용했고**, 회귀는 건수만 대조해 잡지 못했다. **판정은 뷰·엔진과 «같은 원천»을 실조회한다** —
  술어를 다른 모듈에 다시 적지 않는다(`api/admin_pool.py` 가 이미 그렇게 갈라졌다 — 그 파일
  주석의 3,219 대 3,242). **원천이 아직 답할 수 없는 것은 판정하지 않는다**: 품절 행은 뷰의
  `status='판매중'` 을 입고 자체가 바꾸므로 그 false 로 나머지 게이트를 알 수 없다 — 「진입 불가」도
  「진입 가능」도 둘 다 지어내는 것이라 **`unknown`(「확정 후 판정」)로 둔다.**
  위 「숫자를 지어내지 않는다」의 **다른 얼굴**이다 — 저건 수, 이건 판정.
  ⚠ **한 행이 두 원천으로 말하지 않는다**(같은 날 후속 결함): 라벨만 새 판정으로 바꾸고 그 옆
  사유 문장을 옛 원천에 둔 탓에 **한 행 안에서 「추천 후보 제외」와 「입고 시 추천 가능 진입」이
  동시에** 떴다. 라벨과 사유는 같은 판정에서 나온다.
  ⚠ **실조회만으로는 부족하다 — 판정 «정의»도 한 표현에 모은다**(같은 날 U-17 에서 얻음).
  원천을 실조회해도, **조회 조건 · 집계(칩·배지의 수) · 행 표시**가 각자 술어를 적으면 다시
  갈라진다. 화면과 서버가 따로 `재고 < 안전재고` 를 비교하고 있었고, 그대로 두면 **칩의 수와
  행의 태그가 어긋나는 날**이 온다. 셋이 **같은 SQL 표현 하나**에서 나오게 하고 재비교를 지웠다.
  판정 항목을 늘릴 때 물을 것은 「원천을 봤나」가 아니라 **「이 정의를 적은 자리가 몇 곳인가」**다.
- 목업 검증 시 **브라우저 캐시를 먼저 의심**한다 — 패치가 '효과 없음'으로 보이는 흔한 원인이다.
- **첫 화면도 예외가 아니다**(슬라이스 59): main-landing·S0이 "실시간 재고 검증"·"검증 통과 견적"이라 써 붙이고 API를 한 번도 안 불렀다 — 재고 26,480개(어느 값과도 불일치) · 호환성 5종(실제 8종) · RTX 4060 Ti 645,000원(실제 428,000원). `data-bind`만 있고 아무도 바인딩하지 않은 자리가 오래 남아 있었다. 정직성은 견적 화면만의 규칙이 아니다.
- **랜딩용 수는 `GET /api/showcase`**(읽기 전용·5분 캐시). 견적 API를 그대로 쓰면 방문마다 `consult_sessions`가 쌓여 원장이 오염된다 — 회귀가 그것까지 검사한다. 후보 수는 **S1 카운터와 같은 정의**를 쓴다(다르면 다음 화면에서 갑자기 줄어 보인다). 대표 구성은 **조건을 밝힌다**(`SHOWCASE` 상수 = 게임·150만원) — 조건 없는 'TOP PICK'은 근거가 없다.

## 상태 관리 원칙 (S1에서 확립)
- 제약은 **객체 하나가 단일 원천**. 칩·자유입력·파싱이 전부 그 하나만 갱신.
- 기어/뷰 전환은 상태를 리셋하지 않는다.
- 후보 풀 카운터 = 완성품 대수 아님. **전체 부품에서 조건 통과 부품 수**이고, 그 수는
  `/api/candidates/count` 가 준다 — 화면이 세지 않는다.
  (2026-08-12까지 이 줄에 «26,480개»가 박혀 있었다. 바로 위 §화면 정직성이 **그 수를
  «어느 값과도 불일치하는 지어낸 값»으로 지목**하고 있는데도 정의 자리에 남아 있었다.
  문서에 수를 박으면 결국 자기 모순까지 간다.)

## 회귀 세트 (A-13 관계식 전환 — 리팩터링 전후 필수)
- 실행: API 서버(8000) 띄운 뒤 `.venv/Scripts/python tests/regression.py` (실패만 보려면 `--quiet`)
- **전량 불변식이다.** ⚠ **건수는 여기 적지 않는다 — 돌리면 마지막 줄이 말해 준다.** 슬라이스마다 늘어나는
  수라 문서에 박으면 반드시 어긋난다(2026-08-16 실제로 이 파일 두 곳과 `CANON.md` 가 **서로 다른 세 수**를
  정본처럼 들고 있었다). 고정 기대값(FIXED)은 **폐지**했다 — 베타 실주문 1건이 재고를 줄이자 고정값 5개가 동시에 깨졌고, 그때마다 손으로 숫자를 맞추면 회귀가 '거짓 안심'이 된다.
- 원칙: **같은 사실을 두 경로로 구해 일치를 본다.** ① API가 말하는 수 ↔ **DB가 세는 수**(후보 풀·활성 규칙·검수 대기) ② 응답 내부 정합(**부품 가격 합 = 총액**, 규칙 수 = 판정 수, `over_by` = 총액 − 상한) ③ 누적돼도 성립하는 관계(티어 서열 `가성비 ≤ 추천 ≤ 고성능`, 추천은 예산 이내이며 상한의 90% 이상, 가성비는 전 예산 공통).
- **검증됨**: 재고 3건을 0으로 내려도 123/123 통과(값 변화만 알림) · `total`에 +100 버그를 심으면 13건이 즉시 실패.
- **값 변화 알림은 실패가 아니다.** 주요 수치를 `tests/.regression-snapshot.json`(gitignore)에 적어두고 바뀌면 보고한다 — 관계식이 못 잡는 "가중치를 조용히 바꾼" 변화를 사람이 알아채라는 신호다.
- 스크립트가 **시드 owner(`admin@popcornpc.local`)로 먼저 로그인**한다(슬라이스 37). 고객 그룹은 회원 세션을 따로 갈아탄다(슬라이스 38). DB에 닿지 못하면 원천 대조만 건너뛰고 그 사실을 알린다 — 조용히 통과시키지 않는다.
- **회귀는 정본을 쓰지 않는다**(슬라이스 50 전례): 업로드 검사가 apply까지 돌려 시드 상품을 테스트 값으로 덮었다(P-1001이 ETC·1000원으로 강등 → 손으로 복구). 검증 수단이 검증 대상을 오염시키면 안 된다 — 쓰기 경로는 **가드(400·409·404)만** 확인한다.
- **실패는 전부 코드 문제다** — 값을 맞추지 말고 고친다.
- **⚠ 「이름」이나 「문자열 모양」으로 «동작»을 추정하는 검사는 원천 표기가 바뀌는 날 조용히 빠져나간다**
  (2026-08-15 몰 카탈로그 24,721건 적재로 **하루에 둘이 동시에 드러났다**).
  - `[26]` 재고 원장 검사가 `"stock_before" in ci` 라는 **글자 존재 확인**이었다 — 절대값 회귀도
    부호 반전도 통과시켰다. 슬라이스 98 사고(원장에 절대값 누적)의 유일한 사전 방어선이 **아무것도 안 잡고 있었다.**
    뺄셈식 구조 파싱으로 교체(커밋 `8413eb7`).
  - `[42]` 상품명 검사의 게이트가 `LIKE '회원가입%'`, 즉 **문자열 «시작»** 일치였다. 표기가
    `[회원가입…]` 으로 바뀌자 **후보 400건을 한 건도 못 잡았다.** `%회원가입%` 로 넓히고
    「`display_name()` 결과에 잔해가 남지 않는다」를 신설.
  - **둘 다 드러나기 전까지 «통과» 상태였다.** 검사가 무엇을 «세는지»가 아니라 무엇을 «증명하는지»를 본다.
- **⚠ `.env` 스위치가 켜져 있으면 회귀가 «거짓 통과»한다 — 실패보다 위험하다**
  (2026-08-20, 제작자가 코드를 읽어 판정 · 실행하지 않음). 고객 쓰기 잠금
  (`POPCORN_CUSTOMER_WRITE_LOCK`)을 켜면 `[6]`·`[7]`의 소유권 검사 셋 중 둘이
  **기대값과 같은 상태코드(403)를 낸다 — 그런데 그 검사가 증명하려던 것(소유권
  검사·타인 주문 차단)은 코드 경로를 타지도 못한 채다.** 잠금 게이트가
  `require_member()` 바로 뒤, DB 접근보다 앞에 있어 body 내용과 무관하게 항상
  403을 먼저 돌려주기 때문이다(`api/my_account.py`·`api/my_orders.py`의
  `write_locked()` 호출부). `tests/regression.py` 안에 이 스위치 이름은
  **전무하다** — 회귀는 이 스위치의 존재를 모른다.
  - **게이트가 검사 대상보다 앞에 있으면, 검사는 도달하지도 못한 채 기대값과
    우연히 일치한다.** 코드가 기대와 다른 값을 내면 사람이 의심하지만, **같은
    값이 우연히 나오면 아무도 의심하지 않는다** — 그래서 「통과」 둘이 「실패」
    하나보다 위험하다.
  - **회귀가 스위치의 존재를 모르면, 통과 결과만 보고는 진짜 통과와 거짓 통과를
    구분할 방법이 없다.** 이번에는 제작자가 코드를 직접 읽어 가려냈지만, 코드를
    안 읽고 초록 불만 보면 다음에도 똑같이 못 가린다.
  - **실제로 로컬에서 터질 뻔했다.** `.env`에 이 스위치가 **중복 두 줄**로 들어가
    있었다(누가 언제 넣었는지 불명). 터지지 않은 이유는 우연이었다 — 로컬 서버
    프로세스가 그 수정보다 먼저 떠 있어 `load_dotenv()`(프로세스 시작 시 1회만
    읽음)가 아직 그 값을 안 문 상태였다. **재기동했으면 그 순간부터 회귀가
    조용히 거짓 통과를 냈을 것이다.** 하네스가 로컬 `.env`에서 그 두 줄을
    걷었다 — 회귀 격리용 `POPCORN_TEST_HEADER_ENABLED`는 그대로 남겼다.
  - **로컬에 시험용 `.env` 스위치를 켠 채 두지 않는다** — 다음 재기동까지
    잠복한다.
  - ⚠ **배포 서버의 이 잠금은 그대로 켜져 있다**(사장님 승인분 · 의도대로
    동작하는 것을 확인자가 실측했다) — **걷은 것은 로컬뿐이다.**
  - **처방은 아직 없다** — 회귀가 이 스위치를 읽게 할지, 켜진 상태를 별도로
    검사할지는 결정되지 않았다.
  - 확인법: `grep -n "write_locked()" api/my_account.py api/my_orders.py`
    (게이트 위치) · `grep -c "write_locked\|WRITE_LOCK_REASON\|CUSTOMER_WRITE_LOCK"
    tests/regression.py`(0이면 이 상태가 아직 유효) · `grep -c
    "^POPCORN_CUSTOMER_WRITE_LOCK" .env`(로컬은 0이어야 한다 — 있으면 재발).
- 엔진·규칙·게이트·원장을 건드리는 슬라이스는 **작업 후 이 세트를 돌리고 결과를 보고**한다.

## 빈 상태에서 시작 (슬라이스 81 — 처음 관통시킨 길)

- **절차 단일 원천 = `docs/07_운영-시작-순서.md`.** 0단계(alembic) → 부트스트랩 로그인 →
  마진·운영방식 → 공급처 → 상품 → 검수 → 가격 → 재고 → **슬롯별 후보 확인** → 고객 흐름.
- **`alembic upgrade head`만으로 운영 규칙이 전부 선다**(0019): 호환 8 · 사양 항목 29 ·
  용도 하한 21 · 가격 정책 · 운영 전환 5, 더미 0건. 격리 스키마로 관통 검증했다.
- **`db/seed/*.sql`은 개발용 더미 전용.** 상품 30종·주문·회원이 딸려 온다 — 실제 개업에 쓰면
  가짜 주문이 원장에 남는다. 그래서 운영 규칙을 시드에서 마이그레이션으로 옮겼다.
- **호환 규칙이 비면 엔진은 실패하지 않고 조용히 전부 통과시킨다**(규칙 없는 슬롯 = 독립).
  빈 DB에서 조립 불가 조합이 견적으로 나간다는 뜻이다 — 규칙은 선택이 아니라 구축의 일부다.
- **낡은 시드를 그대로 옮기지 마라**: `seed_0013`의 쿨러 소켓은 `socket`/`eq`인데 엔진은
  슬라이스 39부터 `socket_list`/`contains`다. 베꼈다면 새 서버에서 **모든 쿨러가 탈락**했다.
  운영 DB의 현재 정의와 한 글자씩 대조해 옮겼고, 회귀가 "활성 규칙 ⊆ 부트스트랩"을 지킨다.
- **첫 관리자는 `ADMIN_BOOTSTRAP_EMAILS` → 첫 로그인이 비밀번호 없이 통과**(그 한 번만).
  들어오자마자 비밀번호를 설정해야 한다 — 계정 행이 생긴 뒤로는 `password_not_set`으로 막힌다.
  막히면 `tools/set_admin_password.py`.
- 페이지를 모집단으로 쓰는 불변식은 언젠가 터진다: 되돌림 1:1 검사가 창 밖으로 밀린 원본 때문에
  145 대 144로 실패했다. **관계는 전체에서 보고, 페이지에서는 개수가 아니라 근거를 본다.**

## 사양 항목 (슬라이스 56 — 화면에서 스키마를 늘린다)
- **단일 원천 = `spec_field_defs` 테이블.** 필드 목록이 코드 상수 다섯 곳(적재·대안·후보 로드·검수 승인·필수 판정)에 흩어져 있던 것을 데이터로 옮겼다. `api/spec_fields`가 그것을 읽고, 코드는 이 모듈만 부른다.
- **상품 스펙 관리(ADM-PRD-050) · owner 전용**: 항목을 추가하면 한 번에 넷이 일어난다 — ① `product_specs` 컬럼(ALTER) ② 메타 행 ③ 엔진 사양이면 **추천 뷰 재생성** ④ **마이그레이션 파일 기록 + 버전 스탬프**. ④가 없으면 서버 재구축·백업 복구 때 그 컬럼이 사라진다.
- **뷰는 끝에 컬럼 추가만 된다.** `CREATE OR REPLACE VIEW`는 순서 변경·삭제를 거부한다("cannot drop columns from view") — 기존 정의를 그대로 두고 마지막에 한 줄만 끼운다. 컬럼을 지우려면 `DROP VIEW` → `CREATE VIEW`가 필요하다(뷰가 컬럼을 잡고 있다).
- **삭제는 열지 않는다.** 컬럼을 지우면 값이 함께 사라지고 되돌릴 수 없다. downgrade도 메타만 지우고 컬럼은 남긴다. 쓰지 않을 항목은 `part_types`를 비워 감춘다.
- **필수 지정은 적용 대상 안에서만.** 쓰지도 않는 종류에 필수를 걸면 그 종류 상품이 전부 영원히 검수 대기가 된다.
- **사양 값은 상품 상세에서 넣는다**(슬라이스 57): `PATCH /api/admin/products/{code}/specs`. 검수 큐는 **검수 행이 있는 필드만** 다루므로 화면에서 새로 만든 항목은 그 경로로 영영 못 채운다. 입력하면 ① 값 반영 ② **고친 사양 잠금** ③ 그 필드의 검수 대기 해소 ④ 필수가 다 차면 게이트 통과(추천 후보 진입)까지 한 번에. 되돌리기는 값·잠금·게이트 상태를 함께 되돌린다.
- **필수 사양 판정도 메타가 원천**(`admin_products.required_fields`). 상수를 쓰면 화면에서 '필수로 지정'해도 게이트가 몰라 **아무 효과 없는 버튼**이 된다. 상수는 DB를 못 읽을 때의 폴백으로만 남는다.
- 메타에 있는데 실제 컬럼이 없으면(캐시가 앞서거나 DB에서 직접 지운 경우) 화면이 죽지 않고 `ghosts`로 알린다.
- **④는 배포 서버에서 실패한다**(슬라이스 81 실사고): 앱 계정은 리포 폴더에 쓸 수 없다(써서도 안 된다 — working tree drift). `cpu_gpu`가 그렇게 됐다 — ①~③이 커밋된 뒤 파일 쓰기에서 500이 나 **운영자에겐 실패로 보였는데 컬럼은 이미 생겼다.** 되돌릴 수도, 이력을 남길 수도 없었고 하루 뒤 빈 DB 관통(29 대 28)에서 발견했다. 지금은 파일을 못 써도 요청을 실패로 만들지 않고, 스탬프도 찍지 않으며(없는 리비전을 가리키면 다음 배포가 멈춘다), 목록이 `unrecorded`로 계속 표시한다. 회귀가 그 목록이 비어 있는지 지킨다 — **개발 PC에서 마이그레이션을 채워야 해소된다**.

## 용도 하한 (슬라이스 58 — 화면이 한 약속을 서버가 지킨다)
- **단일 원천 = `usage_floors` 테이블**(`api/usage_floors`). 후보 카운터와 견적 엔진이 **같은 규칙**을 읽는다.
- 배경: S1 화면은 오래전부터 용도 칩에 "저사양 GPU 제외(프레임 미달)"·"램 32GB↑ 우선"이라 적어 왔는데 **서버는 용도로 아무것도 하지 않았다**(`_apply_one`이 "구성 단계에서 반영"이라 답하고 구성 단계에도 없었다). 그 결과 "롤·배그 하려고요"에 **GT710 2GB**가 나왔다 — 저소음 태그 + 최저가를 정확히 만족한 결과다. 규칙대로 동작했지만 용도를 배신했다.
- **GPU는 성능 지표가 없다.** `required_power_watt`를 계층 근사로 쓴다(300W 등급 2.8만~19만 · 550W 등급 29.7만~83만 · 750W 등급 105만~256만으로 단조). **전력 등급이지 성능이 아니라는 사실을 근거 문구가 먼저 밝힌다.** RAM·SSD는 `capacity_gb`가 100% 채워져 있어 그대로 쓴다.
- **근거에는 숫자가 있어야 한다**: "고사양 게임 GPU 등급"이 아니라 "그래픽카드 550W 이상".
- **하한과 예산이 충돌하면 불성립을 말한다**(게임 40만원 = 전 티어 불가). 지금까지는 GT710을 내밀며 성립한 척했다.
- **고성능 = 예산 무시가 아니다**(`HIGHEND_CAP_X=1.5`): 상한 없이 가격 내림차순으로 두었더니 120만원 예산에 램 931만원 + 데이터센터 SSD 492만원을 얹은 **1,741만원**이 나왔다(GPU는 그대로 RTX 4060인 채로). 예산의 1.5배 총액 + 부품별 배분 상한을 함께 건다.
- **가성비형은 배분율을 받지 않는다**: 최저가를 고르는 티어에 "비싼 부품 차단" 상한은 무의미한데, 슬롯 후보를 예산마다 다르게 만들어 **150만원이 70만원보다 비싼** 가성비 구성을 냈다(회귀가 잡았다).
- **배분율이 조합을 막으면 푼다**(추천·고성능): 50만원 사무용에서 RAM 배분 10%(5만원)가 DDR4 램(최저 84,900원)을 전부 걸러 견적이 불성립했다. 균형을 못 지킬 바엔 균형을 포기하고 견적을 내되 **총액 상한은 지키고 근거에 그 사실을 적는다**.
- **노트북 메모리(SO-DIMM)는 데스크톱 견적에 들어오면 조립 불가다**(102건 교정). 판정은 원문 분류 토큰만 본다 — **'노트북'만으로는 안 된다**: 원천은 M.2 SSD를 "PC / 노트북"으로 적는데 그건 겸용이라는 뜻이다. `SO DIMM` 명시 또는 '노트북' 있고 'PC' 없음(실측 오탐 0).
- **서버가 준 `reasons`를 화면이 버리지 않는다**: S2가 엔진이 적어 보낸 이유를 한 줄도 안 보여주고 있었다("모든 견적에는 이유가 있습니다"인데).
- **`budget.cap`은 null일 수 있다**('200만원 이상'·'AI 추천 예산'). 상한 없는 예산에도 고성능 상한은 필요하다 — 없으면 3,025만원이 나온다. 기준선은 **추천 구성 × 1.5**이고 그 기준을 근거에 밝힌다.

## 검증이 흔적을 남긴다 (슬라이스 78 — 실사고)
- 인증을 검사하면서 **없는 이메일로 로그인을 시도**했더니 그게 신청으로 접수됐다(설계대로 동작한 것). 그 신청이 승인 목록에 남았고, 나중에 승인되어 **owner 권한 계정**이 됐다(`nobody-zz@example.com`). 활동 0건인 테스트 잔재라 삭제했다.
- **검사는 부작용이 없어야 한다.** 로그인·등록·주문처럼 무언가를 만드는 경로를 두드릴 때는 ① 만들어지는 것이 무엇인지 알고 ② 검사 끝에 반드시 치운다. 회귀가 정본을 쓰지 않는 것과 같은 이유다(슬라이스 50 전례).
- 실주문 검증(`ORD-84222`)도 같은 부류였다 — 그때는 재고 원장까지 건드렸고 정리에 별도 작업이 필요했다.

## 화면이 죽으면 조용히 다른 것을 판다 (슬라이스 58 후속 — 실사고)
- `cap=null`을 `toLocaleString()`에 넘겨 TypeError가 났고, **그 지점에서 LIVE 스크립트가 통째로 멈췄다.** 뒤에 있던 근거 렌더·티어 저장·복원이 전부 실행되지 않아, 고성능형(3,025만원)을 고른 사용자의 장바구니에 **추천형(274만원)이 담겼다.**
- **화면이 죽는 것보다 나쁜 건, 죽은 줄 모르고 다른 구성으로 결제까지 가는 것이다.** 콘솔 에러는 사용자에게 보이지 않는다.
- 그래서 **LIVE 렌더는 한 덩어리로 두지 말고, 서버 값이 null일 수 있는 자리마다 가드**한다. 수정이 '효과 없어' 보이면 그 줄까지 도달했는지부터 의심하라 — 앞선 티어 수정이 옳았는데도 안 먹은 이유가 이것이었다.
- **검증은 정상 경로만 타면 안 된다**: 예산을 숫자로 준 경우만 확인해서 놓쳤다. 상한 없는 예산·구성 불가·티어 null 같은 **경계 입력**을 같이 돌린다.
- 회귀가 화면 계약을 마크업으로 검사한다(브라우저 없이): `cap!=null` 가드 존재, 티어 복원 분기 존재, 새 견적의 티어 초기화.

## 목록 화면 규약 (2026-08-05 · UI 개편)

- **일괄 선택은 장식이 아니다.** 체크박스만 붙이면 아무 일도 못 하는 버튼이 된다.
  `data-bulk-select` 를 쓰는 화면은 **일괄 동작을 부르는 코드가 있어야 한다**(회귀가 지킨다).
  이 기준으로 두 화면은 **붙이지 않기로** 했다: 가격 검토 대기(파생 큐가 0건 — 누를 것이 없다) ·
  재고 입고(수량이 상품마다 달라 하나를 일괄 적용하면 거의 항상 틀린다).
- **일괄 동작은 자격 없는 대상을 막지 않고 사유와 함께 건너뛴다.** 선택은 사람이 하고,
  왜 빠졌는지 모르면 같은 선택을 반복한다. 상한도 숨기지 않는다.
- **상태 기계를 일괄용으로 다시 쓰지 않는다.** 주문 전이는 `_advance_one` 하나를 단건·일괄이
  함께 부른다 — 원장이라 갈라지면 이력이 서로 다른 말을 한다. 로그를 건마다 남기면
  **되돌리기도 기존 것을 그대로 쓴다.**
- **열 수를 함께 고친다**: 체크박스 열을 넣으면 `colgroup`·`thead`·행·빈 상태 `colspan` 이
  전부 하나씩 늘어야 한다. 어긋나면 표가 조용히 뒤틀린다(회귀가 대조한다).
- **파이썬으로 JS 를 만들 때 문자열 안 `\n` 을 조심한다.** 실제 개행이 되면 JS 문자열이
  줄을 넘겨 **인라인 스크립트 전체가 SyntaxError 로 죽는다.** 하루에 두 번 겪었고
  (products·orders) 두 번 다 화면은 멀쩡해 보였다 — **브라우저 콘솔이 유일한 발견 수단이었다.**

## 단일 원천 — 같은 것을 두 벌 두지 않는다 (2026-08-05 대청소)

이 프로젝트가 가장 자주 걸리는 병이다. 하루에 **네 건**을 더 찾았고, **넷 다 이미 갈라져 있었다.**

- **좌측 메뉴** — 36화면에 복제(672KB). `ai-cost` 엔 '운영 전환 설정'이, `policy-weights` 엔
  '용도 하한'이 **빠져 있어 그 화면에서는 갈 길이 아예 없었다.**
  → 정본 `mockups/shared/admin-menu-data.js`, `admin-menu.js` 가 그린다.
  **그리는 시점이 중요하다**: 문서 끝에서 로드되므로 `DOMContentLoaded` 를 기다리지 않고
  즉시 그린다 — `feather.replace()`(phoenix docReady)와 `su-icons` 가 아직 안 돌았기 때문이다.
  기다렸다 그리면 **아이콘이 하나도 안 붙는다.**
- **디자인 토큰** — `design-system/tokens.css` 를 **아무 화면도 읽지 않고 있었다**(서버가
  `mockups/` 만 마운트해 닿을 길이 없었다). `var(--font-num, monospace)` 12곳이 전부 폴백으로
  떨어져 숫자가 Courier New 로 그려졌다. 서버가 `/design-system` 을 서빙하고 화면은
  `../../design-system/tokens.css` 로 참조한다(HTTP·`file://` 양쪽에서 같은 파일).
  `vslot-demo` 의 인라인 사본은 이미 `--font-head` 에서 'Noto Sans KR' 이 빠져 있었다.
- **부품 어휘의 표시 축** — 쿨러 공랭·수랭은 **화면에서만** 합친다(A-28). 접는 방식은 견적
  슬롯과 같은 `slot_of` 하나를 쓴다.
- **마진 상속** — `pricing.resolve_margins` 한 곳에서만 계산한다(A-29). 화면은 서버가 준
  `margin_effective`·`margin_source` 를 그대로 쓴다.
- **상품명의 두 얼굴**(A-38 ⑤ · 2026-08-10) — `product_name` 은 **마켓에 내보내는 판매용
  제목**이라 판매조건 꼬리가 붙는다("… 회원가입 계좌이체 맞춤할인 2.5%" · 22,840건 중 868건).
  견적은 "왜 이 부품인가"를 말하는 자리라 그 꼬리가 근거를 묻는다. **컬럼을 두 벌 두지 않고
  파생한다** — 단일 원천 `api/product_name.display_name`, 부르는 곳은 **고객 응답 경로**이고
  운영자 화면은 원천을 본다(마켓 노출을 관리하는 자리다). ⚠ **호출처 수는 여기 적지 않는다** —
  `grep 'display_name(' api/*.py` 로 잰다(「8곳」이라 박아 둔 채 늘어나 있었다).
  `(정품)`·`(벌크)`·`(멀티팩)` 은 유통 형태라 **부품 정보이므로 지우지 않는다.**
  자르면 이름이 없어지는 상품(123034 — 원천이 통째로 광고)은 **자르지 않는다**: 빈 이름을 내미느니
  광고를 내미는 게 낫고, 그건 검수 소관이다 — ⚠ **다만 「검수 소관」은 규약이지 처리됐다는 증명이 아니다.**
  123034 는 2026-08-10 실제로 회부됐는데 2026-08-15 적재가 그 플래그를 덮어 **후보로 되돌렸다**
  (2026-08-16 실측 · §데이터 「잠금은 가격 둘뿐」 참조).
  ⚠ **판매조건 꼬리의 «모양»은 원천이 바꾼다**: 2026-08-15 몰 적재로 `회원가입 … 2.5%` 가
  `[회원가입 … -2.5%]`(대괄호로 감쌈)가 됐고, 「회원가입부터 지운다」던 정규식이 **앞의 `[` 를 남겼다**(841건).

회귀가 넷 다 지킨다: 화면이 메뉴를 다시 품지 않는다 · 토큰을 다시 정의하지 않는다 ·
`taxonomy.py` 밖에서 부품 어휘를 정의하지 않는다 · 상속을 `pricing.py` 밖에서 구현하지 않는다.

## 실패를 삼키지 않는다 (2026-08-05 사용자 신고)

"공급처 매입가 입력 시 오류" — 원인이 셋 겹쳐 있었고, **셋째가 첫째를 오래 숨겼다.**

- 목록 API 는 `id` 로 주는데 화면이 `s.supplier_id` 를 읽어 `value="undefined"` →
  `+value` 가 **NaN** → JSON 에서 null → **저장할 때마다 422**.
  → **화면이 서버 응답의 키를 추측하지 않는다.** 같은 개념이 API 마다 다른 이름일 수 있다
    (상품 상세는 `supplier_id`, 목록은 `id` 였다).
- `cost_price` 는 NOT NULL 인데 API 는 `int | None` 을 받고 입력창은 "비워 두면 가격 미확인"
  이라 안내했다 → 비우면 **500**. **지킬 수 없는 약속을 화면에 적지 않는다.**
- `ui-progress.errText` 가 422 의 `detail`(**배열**)을 처리하지 못해 **"오류 422"** 만 떴다.
  이제 어느 항목이 왜 틀렸는지 적는다. 화면도 같은 헬퍼(`window.popcornProgress.errText`)를 쓴다.
- **`if(!res.ok) return;` 을 쓰지 않는다.** 저장을 눌러도 아무 일도 안 일어나는 화면이 된다.
  공용 진행 바가 말해 주는 자리만 예외이고, 그 근거를 주석에 남긴다.

## 감사 문서보다 코드가 먼저다 (2026-08-07 실사고)

`docs/design/admin-screen-audit-2026-08-06.md` 가 "고칠 것 셋"을 들고 있었는데
**셋 다 이미 고쳐져 있었다.** 문서만 믿고 ②를 다시 고치다가 **없던 결함을 하나 만들었다** —
버튼 열을 96px 로 줄여 100행 중 76행에서 [검수]+[수정] 두 버튼이 넘쳤다(실측 필요폭 119px).

- **"고칠 것" 목록을 보면 그 코드를 먼저 연다.** 문서는 코드보다 먼저 낡는다.
- **고쳤다고 적을 때는 확인법을 함께 적는다** — 코드에서 한 줄로 되짚는 방법.
  "고침"만 적힌 문서는 다음 사람이 못 믿어서 결국 코드를 다시 읽는다.
- **판단이 갈리는 것은 '결함'으로 적지 않는다.** 맞바꿈이면 선택지와 근거를 적고 비워 둔다 —
  결함으로 적으면 다음 사람이 운영자에게 묻지 않고 한쪽으로 정해 버린다.
  (상품 목록의 가로 439px 넘침이 그런 항목이다 — 세로로 접으면 이름이 3~4줄이 된다.)
- **판정과 수치를 섞어 적지 않는다.** 판정(양호/보완)은 오래가고 수치는 하루면 틀린다.

## 브라우저 점검 계정 (2026-08-07)

화면을 브라우저에서 확인하려면 세션이 필요한데, 세션 쿠키가 `HttpOnly` 라 **서버가 심어야**
한다 — 즉 로그인 요청을 브라우저 안에서 보내야 하고 그때 비밀번호가 기록에 남는다.
그래서 **공용 `ADMIN_PW` 를 쓰지 않고 점검 전용 계정을 따로 둔다.**

    .venv/Scripts/python tools/ui_check_account.py          # 활성화(없으면 만든다)
    .venv/Scripts/python tools/ui_check_account.py --off     # 정지 + 비밀번호 무효화

**브라우저로 화면을 열 때는 `dev-login` 을 쓴다**(2026-08-11): `.env` 에
`UI_CHECK_DEV_LOGIN=1` 을 두고 `GET /api/admin/auth/dev-login` 을 한 번 열면
서버가 점검 계정 세션을 심는다. **비밀번호가 요청·응답·화면 어디에도 나오지 않는다** —
그래서 자동화가 화면을 직접 확인할 수 있다.
`.env` 스위치 + **localhost 요청만** + 계정 활성, 셋이 동시에 맞아야 동작한다.
**운영에는 이 변수를 두지 않는다**(절차 `deploy/README.md`) — 열리면 누구나 관리자가 된다.

- 값은 `.env` 의 `UI_CHECK_EMAIL` · `UI_CHECK_PW`(gitignore). **리포에 적지 않는다.**
- **끌 때도 지우지 않는다** — 이 계정의 작업 기록이 원장에 남아 있어 삭제하면 FK 가 깨진다.
  정지 + 비밀번호 무효화가 맞다(되돌림이 삭제가 아니라 역방향 전이인 것과 같은 규칙).
- **`status` 는 한글 `'활성'` 이다.** `'active'` 로 넣으면 로그인은 200 을 주는데
  `resolve_session` 이 상태를 보고 세션을 버려서 계속 401 이 난다(오늘 여기서 시간을 버렸다).
- **개발 서버 전용.** 운영 서버에서는 만들지 않는다.
- **⚠ 점검 계정의 «이름 길이»가 화면을 바꾼다** (2026-08-18 실사고). 점검 계정
  `UI점검 · 관리자` 는 상단바에서 85.3px, 사장님 실계정 `davidchoi2060 · 관리자` 는
  **129.2px — 44px 더 길다.** 그래서 `/admin2/ai-response-log` 는 **사장님 계정에서만**
  768px 에서 헤더가 두 줄이었고, 짧은 이름으로만 검증한 탓에 **아무도 모르고 있었다.**
  **폭이 빠듯한 자리(헤더·툴바·표 머리)를 검증할 때는 짧은 이름으로 통과시키지 않는다** —
  실계정 길이를 DOM 으로 넣어 다시 잰다(읽기 전용). 「내 화면에서는 됩니다」가 이렇게 생긴다.

## 브라우저에서 실제로 열어 봐야 보이는 것 (2026-08-05)

코드만 읽어서는 안 보였고, 화면을 열어 콘솔·네트워크를 보고서야 나왔다:

- **유령 List** — `list.js` 는 0건 표에서 예외를 던진다. `ui-list.js` 의 유령 제거가
  **fetch 응답 뒤**에 돌아 docReady 에 먼저 훑는 `phoenix.js` 를 막지 못했다. 36화면에서
  로드마다 uncaught 였다. → `ui-list.js` 가 docReady 에 sweep 한다(`phoenix.js` 보다 먼저 로드됨).
  **행이 있는 표는 건드리지 않는다** — 던지던 것은 빈 표뿐이다.
- **RTL 404** — 우리에게 없는 시트를 36화면이 참조했다. 회귀가 그것만 `IGNORE` 하고 있었는데
  **틀린 예외였다**(평범한 `<link>` 라 브라우저가 항상 받는다). 링크만 지우면 인라인 스크립트가
  `null` 에 `setAttribute` 를 걸어 TypeError 가 난다 — 링크·전환 스크립트·설정 토글을 함께 걷는다.
- **외부 CDN** — 글리프 7종 때문에 아이콘 폰트를 통째로 받고 있었다. 폰트·아이콘은 전부
  자체 호스팅한다(A-30). 새 화면에 외부 링크를 넣지 않는다 — 회귀가 막는다.

## 엔진 규칙을 늘릴 때 (슬라이스 46 전례)
- 호환 규칙에 새 필드를 쓰면 **그 필드를 싣는 곳을 전부** 늘려야 한다: `recommend._load_pool` · `swap.SPEC_COLS` · 추천 뷰. 하나라도 빠지면 NULL 불통과로 **조용히** 견적/대안이 0이 된다(500이 아니라 '결과 없음'으로 보인다).
- 그래서 규칙·엔진을 건드린 슬라이스는 회귀를 돌린다 — 스왑 대안 0건은 회귀가 잡는다.

## 원장·되돌림 규약
- 되돌림은 삭제가 아니라 **역방향 행/상태 전이**. undo 로그의 역참조 키는 **`ref_log_id`**(활동 로그가 이 키로 '되돌려짐' 배지를 파생한다 — 다른 이름을 쓰면 회귀의 `되돌림 행 수 = 원본 수` 불변식이 깨진다. 슬라이스 44 전례).

- **재고는 원장 밖에서 바뀌지 않는다**(슬라이스 97·98). `products.stock_qty`를 고치는 모든 경로가 `stock_movements`를 함께 남긴다 — 재고 입고(`inbound`) · 상세 수정(`adjust`/`manual`) · 카탈로그 적재(`adjust`/`catalog`, **절대값이 아니라 델타**). 불변식 **`stock_qty = SUM(qty_delta)`**를 회귀가 전 상품에서 검사한다. 어긋나면 원장을 안 남기는 경로가 새로 생긴 것이다 — 값을 맞추지 말고 그 경로를 고친다.
- **모르는 과거는 지어내지 않는다 — 기초 재고로 기록한다**(슬라이스 98). 적재가 원장 없이 넣어둔 재고 7,594건이 있었는데, 과거 입고 시점을 우리는 모른다. 그럴듯한 `inbound`를 날짜까지 만들면 지어낸 값이다. 회계의 **기초 재고**를 쓴다: `ref_kind='opening'` · `movement_type='adjust'`. **`inbound`로 쓰지 않는다** — 입고를 주장하지 않는다는 뜻이고, 언제 들어왔는지 모른다는 사실까지 함께 기록하는 것이다(회귀가 지킨다).

## 텔레그램 (폰 ↔ PC)

- **"텔레그램 시작"** 이라고 하면 폴링 루프를 띄운다. **규칙 정본 = `docs/telegram-rules.md`**
  (명령 표·2단계 확인·답장 길이·안전이 거기 있다). **"텔레그램 종료"** 는 PC·폰 어디서든 즉시 종료.
- **권한은 전부 열려 있다**(2026-08-11 사용자 결정 — 원격PC가 주 경로이고 폰은 급할 때,
  그리고 폰에서 끊을 수 있다는 판단). 다만 **되돌릴 수 없는 일은 2단계**로 한다:
  되읽어 주고 → `승인 배포` 처럼 **고유명사를 포함한 문장**을 받아야 실행 →
  **30분 지난 승인은 무효**. 「ㅇㅇ」가 주머니에서 잘못 날아가도 아무 일이 없어야 한다.
- **오래 걸리는 것(회귀 8분 등)은 붙잡지 말고 백그라운드로 돌린 뒤 끝나면 알린다.**
  붙잡으면 5분 자동 종료에 걸려 결과를 못 받는다.
- 자격증명은 **프로젝트 `.env`**(전역 환경변수 아님). 스크립트가 `.env` 를 먼저 읽는다 —
  전역에 다른 프로젝트 봇 토큰이 남아 있으면 **엉뚱한 봇으로 조용히 나가기 때문**이다
  (실제로 이 PC에 `@sola_pipeline_bot` 토큰이 남아 있었다. 2026-08-11 정리함).
- 긴 배치의 알림 배선은 `scripts/notify_telegram.py --run <명령>`. PowerShell 파이프를
  거치면 **한글이 전부 `?`** 가 된다(PS 5.1이 프로그램 사이를 ASCII로 넘긴다).

## 명령 관례
- 목업 확인: `mockups/` 하위 .html을 브라우저로 직접 열기(빌드 없음).
- 정적 서버 필요 시: `python -m http.server`. API 서버: `.venv` + `uvicorn api.main:app`(루트 `requirements.txt` 한정 pip 허용, npm 금지). DATABASE_URL은 루트 `.env`(gitignore)만.

## 미정 (임의 결정 금지 — 물어라)
S2 커뮤니티 근거 소스 / 후보 풀 시각화 형태 / 게임 무게 대표게임 / 게이트웨이 실제 라우팅.

## 화면 작업의 경계 — 팀은 «요구사항»까지만 (2026-08-13 사장님 지시)

**에이전트는 UI 기획·설계(레이아웃·화면 구조·디자인 안)를 하지 않는다.**
화면 작업을 맡으면 **요구사항 정의서**만 만들어 올린다. UI 설계는 사장님(또는 사장님이
지정한 디자인 경로)이 그 정의서를 입력으로 결정한다.

정의서에 담는 것 — 이 일곱이 전부다:

    ① 화면 성격      무엇을 하는 자리인가 (작업 화면 · 조회 화면 · 관문 등)
    ② 제목 · 화면 ID  ADM-xxx-0nn · 경로(/admin2/...)
    ③ 정의           이 화면이 없으면 무엇이 안 되는가 (존재 이유)
    ④ 데이터         쓰는 API·테이블·필드 (실측 근거와 함께 · 지어내지 않는다)
    ⑤ 기능           할 수 있는 일 목록 (동사로)
    ⑥ 인터랙션       입력·선택·저장·이동의 흐름과 결과
    ⑦ 상태 · 예외    빈 상태 · 실패 · 권한 없음 · 경계값

**적지 않는 것:** 화면 배치·컴포넌트 선택·색·와이어프레임·"이렇게 보이면 좋겠다".
그건 설계이고, 이 단계의 산출물이 아니다.

### 문서·화면 어휘 표준 (2026-08-13 사장님 지시)

**요구사항 정의서의 문구는 UI 설계자가 화면에 그대로 옮겨 쓴다.** 그래서 한국 업무
시스템(관리자 도구)의 표준 용어로 쓴다. 문서를 만들 때마다 문장가가 전수 검사한다.

    ① 명칭       화면·기능은 명사형. 서술문·의문형·구어체 금지
    ② 동작       등록 · 수정 · 삭제 · 조회 · 검색 · 저장 · 취소 · 확인 · 닫기 · 초기화
                 적용 · 승인 · 반려 · 보류 · 일괄 적용 · 내려받기 · 올리기 · 되돌리기
    ③ 데이터·상태 목록 · 상세 · 이력 · 원장 · 대기 · 처리 · 완료 · 실패 · 사용 · 미사용
                 활성 · 비활성 · 필수 · 선택 · 단건 · 일괄 · 조회 조건 · 정렬 · 권한 등급
    ④ 도메인     검수 · 제안 · 후보 · 게이트 · 잠금 · 인계 · 사양 · 호환 · 하한 (정본 어휘 · 임의 변경 금지)
                 기술 약어(CPU/GPU/DDR5/TDP/API/CSV)는 영문 유지
    ⑤ 문체       평서·간결체 「~한다」. 감탄·반문·비유 금지. 「우리」 같은 대화체 주어를 빼고 객체 중심
    ⑥ 화면 문구   항목명·버튼·상태·안내·오류는 특히 엄격히 — 명사형 항목명, 시스템 안내체로 일관

**구어·은유 표현 금지**(재기·훑기·갈아엎기·집어넣기 등). 교훈 문서(이 파일·커밋 메시지)는
예외 — 거기서는 겪은 말로 적는 것이 오히려 정확하다.

⚠ **계약서·정의서 자신이 이 규약을 어기면, 위반이 화면 코드로 전파된다** (2026-08-19 실증).
`spec-my-profile.md`·`req-my-profile.md` 가 2026-08-17~18 작성 당시 이 규약을 안 지켰고,
제작자가 그 문구를 화면에 그대로 옮겨 **위반이 코드로 번졌다.** 문안검사가 나중에 문자열
197개 전수 판정으로 잡았고, 고치고 나니 이번엔 **계약서 쪽이 낡아** 계약자가 교정된 코드를
어긋남으로 잡을 상태가 됐다(원안은 승인 시점의 기록이라 갱신하지 않는다 — 그 차이를
계약 요약이 명시한다).

**그래서 계약서·정의서를 쓸 때 이 표를 «먼저» 통과시킨다** — 「설계자가 그대로 옮겨 쓴다」는
것이 그 문서의 존재 이유이기 때문이다. 화면을 다 지은 뒤 문구를 훑으면, 같은 문구를
**코드·계약서·정의서 세 곳에서** 고쳐야 한다. 경위는 `decision-log.md` **T-15**.

## 제작팀 — 한 명이 아니라 여럿이 짓는다 (2026-08-13 사장님 지시)

**바뀐 것:** 제작자는 「한 번에 하나」가 아니라 **「한 파일에 하나」**다.
겹치지 않으면 여럿을 동시에 띄운다. 사장님 지적: *"이렇게 일하다보면 제작자에게 너무
일이 몰리는 것 아닌가 — 제작자를 팀으로 승격시키고 중첩되지 않도록."*

**왜 바꿀 수 있게 됐나:** 병목의 원인은 제작 속도가 아니라 **공유 파일 충돌**이었다
(`api/main.py`·`api/admin_ui.py`에 화면마다 라우트를 등록해야 했다). 셸 정본화 +
라우터 자동 등록으로 그 접점이 사라지면, 화면 하나당 «자기 템플릿 + 자기 API 모듈»만
건드린다.

**동시에 띄우는 규칙 넷:**

    ① 파일 소유권을 지시에 적는다   각 제작자에게 «네가 쓸 파일» 목록을 준다.
                                    담당 밖은 읽어도 되지만 고치지 않는다.
    ② 정본 파일은 언제나 한 명       공통 셸 · tokens · admin_nav · CANON · CLAUDE.md ·
                                    decision-log 는 동시에 두 명이 못 만진다.
    ③ git checkout·restore 금지      남의 미커밋 작업이 사라진다(전례 있음).
    ④ 합류 후 중복 검사              물결이 끝나면 점검자가 «같은 것이 두 벌 생기지
                                    않았나»를 훑는다 — 이 저장소의 지병이다.

**★ 팀원 모델을 하네스가 덮어쓰지 않는다 (2026-08-18 사장님 확정 — 실사고).**

`.claude/agents/*.md` 의 `model:` 이 정본이다(대부분 sonnet). 하네스가 `Agent` 호출에
`model` 을 얹어 opus 로 올리지 않는다.

    한도에 걸리면   더 «싼» 쪽으로 간다 — haiku 로 한 번 더, 그래도 막히면 멈추고 보고한다.
                    비싼 쪽으로 자동으로 올라가지 않는다.
    하네스 자신     Opus 5. 지휘·판단만 하므로 토큰을 적게 쓴다.
    Fable 5         **사장님이 켤 때만.** Opus 의 «2배» 가격이다($10/$50 대 $5/$25).

**왜**: 2026-08-18 에 하네스가 「한도가 모델별이겠지」라고 짐작해 팀원을 전부 opus 로
덮어썼고, **30분에 50달러가 나갔다.** 짐작이 틀렸고, 그 짐작의 비용을 사장님이 냈다.
검증을 여섯 번 돌린 것도 겹쳤다 — §갈래 ② 「회귀를 화면마다 돌리지 마라」와 같은 병이다.

**동시 인원은 3명 안팎이 상한이다.** 그 이상은 파일 충돌보다 **검증이 먼저 막힌다**
(확인자가 화면당 치수·동작·회귀를 봐야 한다). 제작을 늘리면 확인자도 함께 늘린다.

**★ 제작자 3명이면 확인자는 2명이다 (2026-08-14 사장님 확정 — 이 규칙을 지키지 않아 사고).**

바로 위 줄에 「제작을 늘리면 확인자도 함께 늘린다」고 적어 두고 **제작 3 : 확인 1 로 굴렸다.**
그 결과 확인자 하나가 미커밋 21개 항목을 **한 번에** 봐야 했고, 28분이 걸렸고,
**세션 내내 스크린샷을 한 장도 못 찍었다**(브라우저 창이 합성되지 않아 `visibilityState`가
계속 `hidden`이었다). DOM·CSSOM 실측으로 대체해 결과 자체는 믿을 만했지만,
**눈으로 봐야 잡히는 종류는 그 검증에 아예 들어가지 않았다.**

    제작 1~2명   확인 1명
    제작 3명     확인 2명
    제작 4~5명   확인 2명 + 계약 1명
    제작 6~7명   확인 2명 + 계약 1명 — 다만 **배치가 아니라 파이프라인**으로 (아래)

**검증이 밀리면 제작을 멈춘다.** 지어 놓고 못 본 것이 쌓이는 편이 더 비싸다.

**★ 상한 7 · 검증은 파이프라인으로 (2026-08-14 사장님 확정).**

**자리를 늘리는 것만으로는 안 된다.** 제작자들이 다 끝날 때까지 기다렸다가 확인자를 몰아
붙이면 5든 7이든 마지막에 똑같이 막힌다. 실제로 그렇게 해서 확인자 하나가 **미커밋 21개를
한 번에** 봤고 28분이 걸렸다.

    바꾼 것   제작자 하나가 끝나면 **그 화면에 즉시 확인자를 붙인다** — 다음 제작을
              기다리지 않는다. 확인자 자리가 비면 바로 다음 완료분으로 넘어간다

**★ 검증은 인원을 늘리는 것이 아니라 «세 갈래로 가르는» 것이다 (2026-08-14 사장님 확정).**

사장님 지적: *"이렇게 되면 또 확인자가 병목이 생기는거 아닌가?"* — 그대로 두면 생긴다.
다만 **「제작 12 면 확인 12」가 답이 아니다.** 실측이 다른 것을 가리킨다:

    확인자   21개 항목 · 28분 · 도구 호출 185 · 발견 결함 **1**
    계약자   화면 4종 · 15분 · 도구 호출  77 · 발견 결함 **10** + 문서 6 + 하네스 오기 3

확인자가 오래 걸린 것은 검증이 어려워서가 아니라 **한 사람이 21개를 몰아 봤기 때문**이고,
그 시간의 상당 부분이 **제작자가 스스로 잴 수 있는 것**이었다.

**갈래 ① 자기 검증 — 제작자가 짓자마자 잰다**

    콘솔 에러 0 · 404 자원 0 · 외부 CDN 0 · `:root` 재정의 0

제작자에게 Bash 가 있다. **확인자를 기다릴 이유가 없다.** 지시서에 넣고 보고에 수치를 적게 한다.

**갈래 ② 회귀 — 화면별이 아니라 «물결에 한 번»**

⚠ **회귀를 화면마다 돌리지 마라.** 900건 가까이 되고 한 번에 8분이다. 화면 12개에 붙이면
**96분이 같은 검사를 반복하는 데 들어간다.** 회귀는 저장소 전체를 보는 검사라
화면별로 돌릴 이유가 없다.

    .venv/Scripts/python tests/regression.py --quiet

**물결(동시에 돌린 제작 묶음)이 끝나는 시점에 한 번** 돌린다. 값 변화 알림은 실패가 아니지만
**왜 변했는지 설명이 되는지**는 본다. 실패는 전부 코드 문제다 — 값을 맞추지 말고 고친다.

⚠ **예외: 엔진·규칙·게이트·원장을 건드린 화면은 그 자리에서 한 번 더 돌린다.**
견적 결과가 바뀌는 변경은 물결 끝까지 기다리면 원인 지목이 어려워진다.

**갈래 ③ 눌러야 아는 것 — 확인자 (화면별, 짧게)**

    실제 클릭 · hover · 탭 전환 후 상태 유지
    경계 입력 (빈 상태 · 미로그인 401 · 서버 실패 500 · null · 0건)
    치수 실측 (getComputedStyle · getBoundingClientRect ↔ 승인 원안 대조표)
    독립된 눈 — 「이게 깨지면 무엇이 조용히 넘어가나」

**★ 개발 서버 검증은 «눌러 봐도» 된다 (2026-08-15 사장님 확정 — decision-log T-12,
I-01 의 적용이지 새 결정이 아니다).**

**「누를 수 없어서 못 봤다」는 여기서는 정당한 사유가 «아니다»** — 적용·저장·항목
추가·스키마 변경(ALTER)을 직접 눌러 확인한다. 여전히 지키는 것 셋(개발/운영 문제가
아니다 — 상세·근거는 T-12): ㉮ 우리 DB 밖으로 나가는 요청(외부 시스템·메일·결제,
되돌릴 수 없다) ㉯ 회귀가 지금 쥐고 있는 표본(`tests/.regression-snapshot.json`
확인 없이 정본 값을 바꾸지 않는다 — 번호를 외워 피하지 않는다, 회전한다) ㉰ 무엇을
했는지 전부 기록(상품코드·건수·log_id·만든 행). **§검증이 흔적을 남긴다(슬라이스
78)·§회귀 세트의 "회귀는 정본을 쓰지 않는다"(슬라이스 50)는 폐기가 아니다** —
슬라이스 78 은 "무엇이 만들어지는지 모르고 두드리지 말라"였지 "누르지 말라"가
아니었고, 슬라이스 50 은 **회귀**(자동·반복 검사) 얘기라 사람이 한 번 누르는
확인자 검증과는 다른 대상이다.

**갈래 ④ 계약 대조 — 계약자 (묶어서)**

계약 대조는 **문서 읽기라 배치가 유리하다** — 첫 작업이 4화면을 15분에 봤다.
**3~4화면씩 묶어** 돌린다. 화면마다 하나씩 띄우지 마라.

**인원 산정 — 물결 12개 기준**

    확인자 3~4  화면을 나눠 맡는다 (한 명이 21개를 몰아 보지 않게)
    계약자 2~3  3~4화면씩 묶어서
    회귀   1회  물결 끝에

**★ 결함은 «담당 파일별로 묶어서» 돌려준다.**

12개 검증 보고가 동시에 오면 **읽고 결함을 담당별로 재분류하는 것이 하네스 병목**이 된다.
확인자·계약자가 처음부터 **파일 경로 기준으로 묶고**, ⓐ(문구만 고치면 됨) / ⓑ(설계를 다시
그려야 함)를 **판정해서** 돌려주면 그 시간이 사라진다. ⓑ 는 사장님께 올라간다(P-12).

**★ 상한 12 (2026-08-14 사장님 확정 — 「나중에 줄이더라도」).**

**파일 겹침 걱정은 실측으로 해소됐다.** 하네스가 「`/admin2/products` 와 `/admin2/` 가 같은
모듈이라 못 넣는다」고 판단했는데 **틀렸다.** 조사자 실측:

    커밋 bd087b0 (2026-08-14 18:40)  api/admin_ui.py 한 파일이 다섯 화면을 서비스하던 것을
                                      admin_ui_home · products · spec_standard · build_map · dash
                                      + 공용 admin_ui_common 으로 이미 쪼갰다
    지금                              admin_ui_*.py 19개 = 화면 19개, **겹침 0곳**
                                      템플릿도 화면당 1개(include 0건, 상속만)
                                      파일끼리 서로 import 하는 것도 0건

**그래서 화면 단위 병렬이 실제로 가능하다.** 남은 공유 지점은 셋뿐이고, 이건 화면 수와
무관하게 **항상 한 명**이다(CANON §1 그대로):

    api/admin_ui_common.py                  render() — 19개 전부가 쓴다
    api/admin_nav.py                        NAV — 3곳이 쓴다
    templates/admin/_admin2_shell.html.j2   셸 — 템플릿 18개 전부가 상속한다

⚠ **라우터 자동 등록은 `api/` 바로 아래 1단계만 훑는다**(`pkgutil.iter_modules`, 비재귀).
파일명 규칙은 없고 제외는 `main` 과 `_` 로 시작하는 것뿐이다. **하위 폴더로 쪼개면 안 잡힌다** —
잡히게 하려면 `main.py`(공유 파일)를 고쳐야 하므로, **화면 모듈은 `api/` 평면에 둔다.**

**이제 진짜 상한은 검증과 하네스의 계약 요약 속도다.** 파일이 아니다.

**★ 상한을 3 → 5 로 올린다 (2026-08-14 사장님 확정 — 병목이 제작이 아니었다).**

3 으로 잡았던 근거는 「확인이 먼저 막힌다」였는데, **실측해 보니 막고 있던 것은 하네스였다.**
한 세션에 승인 디자인 **293.8KB 를 손으로 옮겨 적었고**(서버에 이미 있는 내용이다),
지시서마다 같은 공통 규약 1,500자를 다시 적었다. 그동안 제작 자리 3개 중 평균 1개가 놀았다.

    없앤 것 ①   제작자·계약자가 DesignSync 로 승인 디자인을 **직접** 가져온다
                 하네스는 좌표 세 줄만 준다 — 프로젝트 ID · 파일명 · 확정 안
    없앤 것 ②   공통 규약을 `docs/design/MAKER-CHECKLIST.md` 하나로 뺐다
                 지시서는 그 파일을 가리키고 **그 화면 고유 사항만** 적는다

**파일이 겹치지 않는 한 5 개가 굴러간다.** 겹치면 수를 줄이는 것이 아니라 **묶는 방식을 바꾼다**
(화면 단위로 자르면 대개 안 겹친다).

**앞단을 조사자에게 미리 시킨다.** 제작자가 화면 하나에 쓰는 시간의 상당 부분이
«데이터 실측·기존 API 파악»이다. 조사자가 먼저 재두면 제작자는 짓기만 한다.

## 팀 운용 — 하네스는 지휘만 한다 (2026-08-13 사용자 지시)

하네스(주 세션)가 코드·화면을 직접 고치다가 오판했다(대시보드 주석 오탐 — 검색 출력의
표시 왜곡을 결함으로 읽었다). 사용자 지시: *"하네스 니가 혼자 다하는 건 무리야.
에이전트 팀을 돌려서 제대로 확인해야지."*

- **하네스는 짓지 않는다.** 구현·수정 = 제작자(maker) · 검증 = 확인자(checker) ·
  조사 = 조사자(investigator) · 데이터 쓰기 = DBA · **문서·기록 = 기록자(archivist)**.

- **★ 정본 문서도 하네스 전용이 아니다 — 「한 번에 한 명」이 규칙이다 (2026-08-14 사장님 지시).**

  사장님 원문: *"니가 하는 일을 에이전트들이 할 수 있도록 하는게 중요하다고 생각함."*

  전에는 결정 로그·목차·화면 ID 카탈로그를 **하네스만** 고쳤다. 그래서 하네스가
  문서 정리를 하는 동안 지휘가 멈췄다. 제약은 **「하네스여야 한다」가 아니라
  「동시에 둘이 만지면 안 된다」**였다 — 그건 담당을 지정하면 풀린다.

      기록자(archivist)가 맡는다   decision-log · INDEX · admin-identity · HANDOFF
                                   · 커밋 메시지 작성과 커밋
      하네스가 지정한다             누가 그 파일을 들고 있는지. 동시에 둘은 안 된다
      하네스가 남긴 것              **지휘 · 사장님 보고 · 결정 확정 · 위험 판단**
                                   그리고 **사장님께 무엇을 여쭐지 고르는 일**

  ⚠ **결정을 「만드는」 것은 여전히 사장님이다.** 기록자는 확정된 것을 적고 낡은 것을
  잡을 뿐, 미정을 임의로 메우지 않는다(§화면 작업의 경계 그대로).

- **★ 하네스가 손으로 하던 넷을 기록자에게 넘긴다 (T-02 · 2026-08-14 사장님 지시).**

  사장님 원문: *"에이전트들을 잘 관리하고 귀하가 하는 일을 최대한 줄이는게 중요합니다.
  정말, 전체 관리만 가능하도록."*

      ① 커밋 전부          하네스는 `git commit` 을 하지 않는다. 하네스는 «무엇이 끝났고
                           무엇이 검증됐는지»만 알려주고, 커밋 단위·메시지·실행은 기록자가 정한다
      ② `api/admin_nav.py` 정본이지만 편집은 기계적이다(라벨·href). 기록자가 든다
                           ⚠ **href 연결은 검증이 끝난 화면만** — 검증 안 된 화면을 메뉴에
                              노출하면 운영자가 먼저 들어간다. 하네스의 「검증 통과」 통보가 조건
      ③ `CLAUDE.md` 초안   규약이 바뀌면 기록자가 초안을 쓰고 **하네스가 확정**한다
      ④ 수정 지시서 초안   확인자·계약자가 결함을 파일별로 묶어 ⓐ/ⓑ 판정해 오면,
                           **누구에게 무엇을 줄지는 파일 경로가 이미 정한다.**
                           기록자가 담당 파일별 초안을 만들어 두면 하네스는 띄우기만 한다

  **못 넘기는 것 — 도구 제약이지 판단 문제가 아니다 (2026-08-18 둘 → 하나):**

      승인 디자인 가져오기   ~~`DesignSync` 가 서브에이전트에서 차단돼 있다(세션 정책).
                            하네스만 원안을 열 수 있어 계약 요약(`docs/design/spec-*.md`)도
                            하네스가 쓴다.~~ **2026-08-18 실측으로 풀렸다(하네스 확정).**
                            general-purpose 서브에이전트가 `DesignSync get_file` 로
                            `재고 입고.dc.html` 을 받아 `docs/design/dc-stock-inbound.html` 로
                            저장했다(`truncated:false`). 새 권한이 아니다 — 아래 「없앤 것 ①」
                            (제작자·계약자가 직접 가져온다)이 이미 사장님 확정인데 도구 제약으로
                            성립하지 못하던 전제가 성립하게 됐다는 사실 기록이다. 절차는
                            `docs/design/MAKER-CHECKLIST.md` §1. 어느 세션에서 다시 막히면
                            그때만 하네스 경유로 돌아간다.
      에이전트 띄우기        서브에이전트는 다른 서브에이전트를 못 띄운다.
                            지휘는 **구조적으로** 하네스 몫이다

  **그래서 하네스에게 남는 것은 이것뿐이다:**

      사장님과의 대화 · 결정 확정
      에이전트 띄우기와 배분
      승인 디자인 수신 · 계약 요약 작성 (도구가 풀리면 이것도 넘어간다)
      위험 판단 — 되돌릴 수 없는 것 앞에서 멈추는 일

  **근거: 하네스가 직접 고치면 그 자리가 병목이 된다.** 실측 — 승인 디자인 293.8KB 를 손으로
  옮겨 적는 동안 제작 자리 3개 중 1개가 놀았고, 하네스 지시서 오기 셋이 계약자에게 잡혔고,
  T-01 오기 하나가 기록자에게 잡혔다. **하네스가 만지는 것이 줄수록 하네스가 틀릴 자리도 준다.**

- **★ T-03 — 자기 권한을 넓히는 문서 변경은 사장님을 거친다 (2026-08-15 사장님 확정).**

  발단은 커밋 `4d260ed`다 — 그 커밋에서 **기록자가 「정본 문서도 하네스 전용이 아니다」·
  「T-02」 두 항목, 즉 자기 자신(기록자)의 권한을 넓히는 내용을 `CLAUDE.md` 에 직접
  적었다.** 하네스가 사장님께 **㉮ 그대로 유지 / ㉯ 하네스가 재작성 / ㉰ 되돌림** 셋을
  올렸고, 답은 **㉮ 유지**였다 — 내용 자체는 사장님 뜻과 같았기 때문이다. 다만 **방식**은
  별도로 확정됐다:

  > 에이전트가 자기 권한을 늘리는 문서 변경은 반드시 사장님을 거친다.
  > 하네스가 중계하지 않는다.

  **무슨 뜻인가**: 에이전트(기록자를 포함해 누구든)가 자신 또는 다른 에이전트의
  **권한·범위를 넓히는** 문서 변경을 만들면, 하네스가 "타당해 보인다"고 판단해 대신
  승인하지 않는다 — **사장님이 원문을 직접** 봐야 한다. 권한을 줄이거나 실무 절차만
  바꾸는 변경(담당 파일 재배정 등)은 이 규칙의 대상이 아니다.

  ⚠ **이 항목 자체가 이 규칙의 첫 적용 사례이자 예외 사례다.** 이 문장도 에이전트
  (기록자)가 쓰고 있다 — 그런데 ① 하네스의 중계·판단이 아니라 **사장님이 원문 그대로
  직접 승인**했고 ② **권한을 늘리는 게 아니라 줄이는 방향**이다(에이전트가 스스로에게
  권한을 더 못 주게 막는 규칙이므로). 그래서 이 기록은 T-03 이 금지하는 사례가 아니라
  **T-03 을 처음 지킨 사례**다.

  **확인법**: `git show --stat 4d260ed` 로 그 커밋이 `CLAUDE.md`·`decision-log.md`
  두 파일만 건드렸는지 보고, 이 항목이 그 커밋을 되돌리지 않고 **규칙만 추가**했는지 대조한다.

- **★ 확인은 세 축이고, 서로 대신하지 못한다 (2026-08-14 · 계약자 신설).**

      확인자(checker)        도는가            콘솔 · 회귀 · 경계 입력 · 치수
      검증자(crosschecker)   실물과 맞는가      제조사 · 다나와 · 실제로 팔린 구성
      계약자(designchecker)  약속과 같은가      승인 디자인 · 정의서 · 결정 로그 · 규약

  **계약자가 보는 결함은 코드가 멀쩡히 도는 결함이다** — 그래서 확인자가 못 잡는다.
  콘솔은 깨끗하고 회귀는 통과하는데 화면이 사실이 아닌 말을 하는 상태다.
  이 자리는 그동안 **하네스가 손으로 메우고 있었고**, 실제로 하네스가 두 번 틀렸다
  (「셸만」 넷 → 실측 둘 · 정본을 `spec_field_defs` 31행이라 오기 → 실제 `std.spec_defs` 49행).
  **자기가 쓴 지시서를 자기가 검증하면 자기 오기를 못 본다.**

  ⚠ **검증자(crosschecker)를 계약자로 바꾸지 않았다.** 그 역할은 중복이 아니라 휴면이다 —
  NZXT Kraken 의 sTR5 미지원 · RTX 5090 1000W · `소켓1700` 표기 탓에 쿨러 234개가 조용히
  탈락하던 것을 잡은 것이 그 팀원이다. 데이터 작업으로 돌아가면 다시 필요하다.
- **★ 길잡이(pathfinder) 신설 — 화면 «사이»를 본다 (2026-08-19 사장님 확정).**

      확인자(checker)        도는가             콘솔 · 회귀 · 경계 입력 · 치수
      계약자(designchecker)  약속과 같은가       승인 디자인 · 정의서 · 결정 로그 · 규약
      문안검사(writer)       말이 맞나           문자열 전수 판정
      길잡이(pathfinder)     고객이 왜 멈추나     흐름 · 이탈 지점 · 빠진 단계 · 거짓 약속

  **셋은 화면 «하나»를 본다. 길잡이는 화면 «사이»를 본다** — 화면 하나하나는 멀쩡히 돌고
  약속과도 같은데 이어 붙이면 고객이 길을 잃는 상태, 그것이 길잡이가 잡는 결함이다.
  **관리자 화면은 담당이 아니다** — 고객단(랜딩→S0~S5→인계 · MY)만 본다.

  **산출물은 점검 보고 + 요구사항 정의서(7항목)까지다** — 배치·컴포넌트·색은 그리지
  않는다. §화면 작업의 경계(2026-08-13)에 **고객단 예외를 두지 않는다**(2026-08-19
  사장님 확정 — 선택지 「정의서까지 / 흐름 설계까지 / UI 설계안까지」 중 첫째).

  **판정 근거는 셋 다다**(2026-08-19 사장님 확정): ① 정체성 「신뢰 판매자」·정본 화면
  S1(4모드 · UX-17) ② 실제 고객 행동 데이터(`handoffs`·유입 성과 — 지금은 원장이 얕다,
  얕으면 얕다고 적는다) ③ 동종 서비스 대조(베끼지 않고 **우리가 빠뜨린 단계**를 찾는
  용도 — 기준은 여전히 ①).

  정의 정본 `.claude/agents/pathfinder.md`(미러 `E:\DEV\.claudegents\`). 근거·경위는
  `decision-log.md` **T-14**. 왜 지금인가 — 팀이 관리자 화면 쪽으로 기울어 고객단은
  확인자가 「도는가」만 봤고, 흐름 정본 `screen-flow.md`(08-12)·고객 감사(08-06)가 둘 다
  낡은 채였다.
- **사용자가 지시한 작업은 «제작 → 검증» 체인을 통과해야 "완료"라고 보고한다.**
  제작자가 지었다는 보고만 받고 사장님께 "됐다"고 말하지 않는다 — 확인자가
  실제 화면·실측으로 검증한 뒤에 말한다. (2026-08-13 실례: "대시보드에 보인다"고
  보고했는데 실제로는 하네스 카드가 둘이었다 — 서버 응답만 보고 화면을 안 봤다.)
- **결함 발견도 팀으로**: 하네스가 증상을 직접 판독하지 말고, 조사자/확인자에게
  재현·측정을 시키고 그 결과로 제작자에게 넘긴다.

### 검증 결과는 사장님을 거치지 않고 바로 제작자에게 간다 (2026-08-13 사장님 지시, 두 번째)

*"검수 결과 나오면 제작자한테 바로 고치게 해줘."* — 같은 지시를 두 번 받았다. 규약으로 고정한다.

- 확인자가 결함을 내면 **묻지 말고 즉시 제작자에게 넘긴다.** 사장님 결재를 기다리지 않는다.
  결함 목록을 사장님께 보고하는 것과 수정을 착수시키는 것은 **동시에** 한다.
- **파일 소유권이 겹치지 않으면 제작자 여럿을 동시에** 띄운다(§제작팀 규약). 결함을
  담당 파일별로 묶어 배분한다 — 한 파일은 언제나 한 명.
- 수정이 끝나면 **같은 확인자에게 재검증**을 시킨다(새 확인자를 띄우면 앞의 맥락이
  사라진다). 재검증 통과 전에는 "완료"라고 보고하지 않는다.
- **예외 — 사장님께 먼저 물어야 하는 결함**: ⓐ 화면 설계·배치를 바꿔야 풀리는 것
  (팀은 요구사항까지만 — §화면 작업의 경계) ⓑ 결정 로그를 뒤집어야 하는 것
  ⓒ 정본 스키마를 폐기·재설계해야 하는 것. 이 셋은 수정 지시 대신 선택지를 올린다.

## 첫 세션 시작 시 — **재고 나서 말한다** (2026-08-12 개정)

**① 먼저 잰다. 문서보다 이게 앞이다.**

```bash
.venv/Scripts/python scripts/state.py
```

코드(HEAD·미커밋·미푸시) · 붙어 있는 곳(DB 호스트·마이그레이션·로컬/배포 서버) ·
**배포가 닿았나** · 운영 스위치 · 데이터 · 화면 수 · **문서 신선도**가 한 번에 나온다.

**② 그다음 「안 낡는 것」만 읽는다.**

    docs/decisions/decision-log.md    확정·미정 (결정은 낡지 않는다)
    이 파일                            규약·교훈 (수는 없다)
    HANDOFF.md                        지금 무엇을 하던 중이었나 (의도만 · 숫자 없음)
    .claude/CANON.md                  정본이 어디 있나

**③ 팀원 점호 — 약속하지 말고 증명한다** (2026-08-12 사용자 불신 표명에 따라 신설)

`.claude/agents/` 의 팀원은 **세션 시작 때 등록**된다(수는 여기 적지 않는다 — `ls .claude/agents/*.md` 로 센다. 「일곱」이라 적혀 있던 것이 2026-08-19 실측 열하나였다). 등록됐다고 믿지 말고 잰다:

    ① 조사자(investigator)에게 1분짜리 시험 작업을 시킨다
       — "scripts/state.py 를 돌리고 결과 요약만 돌려줘" 면 충분하다
    ② 확인자(checker)에게 브라우저 도구가 잡히는지 묻는다
       — 정의가 mcp__Claude_Browser__* 를 싣는데, 서브에이전트에서 실제로
         잡히는지는 **한 번도 검증된 적이 없다** (형식 검사만 통과한 상태)
    ③ 실패한 이름이 있으면: general-purpose 에게 그 정의 파일을 읽혀 대행시키고
       (2026-08-12 실측으로 동작 확인된 폴백), 그 사실을 사장님께 보고한다

점호 결과(등록 O/X · 브라우저 도구 O/X)를 **첫 보고에 포함**한다.

**④ 현황판 감시자를 다시 세운다** — Monitor 는 세션과 함께 죽는다. 안 세우면
대시보드에서 보낸 말이 조용히 쌓이기만 한다(2026-08-12 실사고 — 22:29 메시지 유실):

    Monitor(persistent): .venv/Scripts/python scripts/dash_watch.py
    (로컬 큐 5초 · 배포 서버 큐 30초 · 메시지를 받으면 처리 후 mark-read)

**⑤ 현 상태를 5줄로 요약하고 다음 작업을 묻는다.** 전체 폴더를 스캔하지 마라.

### 왜 이 순서인가

전에는 문서 셋을 읽고 시작했다. 그런데 **문서는 자기가 낡았다고 말해 주지 않는다.**

    HANDOFF.md 가 20커밋 뒤처진 채 범위를 정반대로 말했다
    감사 문서의 「고칠 것 셋」이 셋 다 이미 고쳐져 있었고, 믿고 고치다 없던 결함을 하나 만들었다
    이 파일이 폐기된 색 #005ab3 을 들고 있었다
    이 파일의 데이터 수 셋이 전부 틀려 있었다 (2026-08-12 실측으로 발견)

사용자 지적(2026-08-12): *"컨텍스트가 날아가면 압축돼서 과거 이력을 제대로 파악 못 하고,
서버·PC 구성도 현행화가 안 돼 있어서 같은 일을 반복하고, 일주일 지나면 다시 처음부터 하는
느낌이 든다."* — **원인은 「안 적어서」가 아니라 「적어서」다.**

### 작업 흐름 (사용자 확정 2026-08-12)

    로컬 PC 화면 확인  ->  git push  ->  서버 배포  ->  **배포 확인**

배포 확인은 `scripts/verify_deploy.py` — 바뀐 파일을 **서버에서 받아 해시 비교**하고
운영 스위치를 로컬·서버 양쪽에서 받아 대조한다.

> ⚠ **`.env` 의 DATABASE_URL 이 Cloud SQL 이라 로컬과 배포 서버가 DB 를 공유한다.**
> 로컬에서 운영 스위치를 바꾸면 **배포 서버 고객 화면이 즉시 바뀐다.** 코드는 배포해야 따라간다.
> 2026-08-12 에 여기서 당했다 — 서버가 「인계 모드인데 인계를 못 하는」 상태가 됐다.
