# 팝콘PC AI — 온보딩 가이드

첫날 30분 안에 읽는 것을 목표로 한다. 다 담지 않았다 — **사고를 막는 것**과 **어디를 보면 되는지**만 담았다. 나머지는 아래 「문서 지도」가 가리키는 곳에 있다.

---

## 시작하기 전에 — 이거 하나는 꼭 알고 시작한다

> **로컬 PC와 배포된 사이트가 같은 데이터베이스 하나(Cloud SQL `popcorn-db`)를 본다.**
>
> 지금 이 서비스는 **오픈 베타**다(지인 대상으로 `www.popcornai.co.kr`를 열어 둔 상태).
> 그런데 별도의 "진짜 운영 DB"가 없다 — **2026-08-25, 사장님이 로컬(개발) DB와 배포
> DB를 분리하지 않기로 확정했다.** "나중에 분리를 검토한다"가 아니라 **"지금은 안
> 나눈다"는 확정**이다(이유: 아직 정식 고객이 없는 오픈 베타 기간이라서). 이 문서에서
> "운영 DB"라고 말하면 바로 이 하나뿐인 DB를 가리킨다.
>
> **그래서 로컬 PC에서 실행한 스크립트가 실제로 지금 사이트를 쓰는 사람들의 데이터를
> 지울 수 있다.** 이건 비유가 아니라 실제로 일어난 일이다.

### 최근 실제로 일어난 사고

- **공급처 정보 4,113건이 지워졌다(2026-08-15).** CSV를 적재하는 코드가 "칸이
  비어 있다"와 "값을 지우라는 뜻이다"를 구분하지 못해서, 빈 칸이 하나라도 있는
  CSV를 올릴 때마다 기존 값이 통째로 NULL로 덮였다. Cloud SQL의 시점 복구 창은
  **7일**인데 문제를 알아챈 게 9일 뒤라 그 창을 이미 넘겼다. 다른 자료로 되짚어
  채운 건 143건뿐이고, 나머지 3,956건은 원래 판매처(몰)에도 남아 있지 않아
  **영영 복구 불가**로 판정됐다(2026-08-25 기준). 코드 결함 자체는 막았지만
  (`46925d1`, 2026-08-24 — `locked_fields` 보호 대상을 2개에서 13개 컬럼으로
  넓혔다), **이미 지워진 값은 코드를 고쳐도 돌아오지 않는다.**
- **로컬 서버(포트 8000)가 엉뚱하게 종료됐다.** 작업자가 "포트 8000을 쓰는
  프로세스"를 짐작만으로 죽였는데 다른 프로세스였다. 서버는 다시 띄웠지만 그
  프로세스가 메모리에 들고 있던 상태(캐시 등)는 복구할 수 없었다. **포트 번호만
  보고 프로세스를 단정하지 않는다** — 끄기 전에 `netstat -ano | findstr :8000`
  같은 명령으로 포트와 PID를 대조한다.
- **회귀 테스트가 며칠 새 여러 번 오탐했다** — 다른 사람(또는 다른 AI 에이전트
  세션)이 같은 순간에 같은 표를 건드려서다. 이 저장소는 사람과 AI 에이전트
  여럿이 **같은 DB를 동시에** 쓴다.

### 공유 DB에서 일하는 규칙

- **테스트 데이터는 자기가 만든 것만 지운다.** 남의 것으로 보이면 손대지 말고
  물어본다. (`docs/open-items.md`가 정체가 불확실한 상담 세션 7건을 "다른 작업자
  것으로 추정 — 손대지 마라"로 남겨 둔 것이 실제 사례다.)
- **회귀가 실패하면 일단 두 번 돌려 본다.** 동시 작업 때문에 생긴 오탐일 수 있다.
  두 번 다 같은 자리에서 실패하면 그때부터 진짜로 의심한다.
- **뭔가를 바꾸기 전에 드라이런으로 몇 건이 바뀌는지 먼저 센다.** 이 저장소
  전역의 습관이다 — 상품 일괄 등록도 업로드-드라이런-적용 3단계 중 가운데가
  드라이런이다.
- **되돌리는 방법을 먼저 적어 두고 시작한다.** "무엇을 어떻게 바꿨는지"와
  "어떻게 원복하는지"를 작업 전에 문장으로 써 두면, 사고가 나도 대응할 수 있다.
- **테스트 트래픽과 실제 트래픽을 가르는 장치가 있다** — 요청 헤더
  `X-Popcorn-Test` (+ `.env`의 `POPCORN_TEST_HEADER_ENABLED`, localhost 전용,
  `api/recommend.py`)와 `consult_sessions.data_origin='test'` 표시. 자동화
  스크립트를 짤 때는 이 표식이 남게 한다. 다만 **이 표식을 읽어서 집계에서
  실제로 빼는 화면은 아직 없다** — 표시만 정직하고, 걸러내는 건 별도 작업으로
  남아 있다.

---

## 1. 이 프로젝트가 무엇인가

**팝콘PC AI**는 PC 조립·판매 쇼핑몰 "팝콘PC"가 만드는 AI 견적 상담 채널이다. 고객이
예산과 용도를 말하면, 사람이 만든 규칙과 실제 재고 위에서 도는 **결정론적 룰/스코어
엔진**이 호환되는 부품 조합을 골라 견적을 낸다. 생성형 AI(LLM)는 고객 입력을
이해하고 근거를 사람 말로 풀어 쓰는 역할만 하고, **견적 자체를 만들지는 않는다** —
같은 조건이면 언제나 같은 결과가 나와야 하기 때문이다(재현성). 정체성은 "신뢰
판매자"이고, 이 정체성이 "화면이 숫자를 지어내지 않는다"처럼 이 저장소 전역의 여러
규칙의 근거다.

고객은 랜딩 → **S0**(용도·예산 입력 관문) → **S1**(대화형으로 조건을 모으는 화면,
4가지 모드) → **S2**(견적 제안서 — 부품마다 "왜 이 부품인가"를 근거와 함께 보여줌)
→ **S3**(부품 하나씩 바꿔보기) → **S4**(확인) → **S5**(완료) 순서로 걷는다.

한때는 팝콘AI가 회원가입·결제·배송·환불까지 직접 처리할 계획(2026-07-16 확정, A-10)
이었지만 **2026-08-11에 뒤집혔다.** 지금은 팝콘AI가 **신규 고객을 데려오는 채널일
뿐**이고, 견적이 끝나면 **"팝콘PC 쇼핑몰로 인계"**한다. 결제·배송·환불·회원관리는
전부 쇼핑몰(몰) 소관이다. 관리자(운영자) 화면은 `/admin2` 아래 전면 재구축
중이며(옛 `/admin/*.html`은 동결 백업), 운영자가 하는 일의 본질은 "엔진이 거짓말하지
않도록 재고·사양·규칙을 사실로 유지하는 것"이다.

---

## 2. 절대 하면 안 되는 것

DB 공유 얘기는 위에서 이미 했다 — 여기부터는 나머지다. 전부 **실제로 사고가 났거나
사고로 이어지는 규칙**만 골랐다(스타일 규칙은 뺐다).

| 하면 안 되는 것 | 왜 |
|---|---|
| **서버 코드를 직접 고친다** | 배포 서버는 "배포 타깃"이지 편집 대상이 아니다(결정 **P-06**). 서버가 리포와 어긋나면(working tree drift) 다음 배포가 그 차이를 못 알아채고 되던 걸 덮어쓴다 — 실제로 인증서 발급 도구(certbot)가 서버 nginx 설정을 자동으로 고쳤는데 그걸 모른 채 리포 내용으로 되돌렸다가, 잘 돌아가던 설정을 며칠간 덮어쓴 적이 있다. **로컬에서 고치고 커밋·푸시 → 서버는 `git pull`로만 반영**한다 |
| **`/api/handoff`를 이유 없이 누른다** | 고객이 S4에서 "구매하기"를 누르면 호출되는, **외부 쇼핑몰의 가격을 재확인하는 되돌릴 수 없는 경로**다. 확인·테스트 목적이라도 실제로 누르지 말고 코드 리뷰(diff)로 대신한다 |
| **DB 예외의 상세 내용을 그대로 출력·로그에 남긴다** | `psycopg2` 트레이스백을 그대로 노출하면 **DB 접속 문자열(비밀번호 포함)이 그 안에 실려 나간다.** 최근 며칠 사이에만 최소 세 번 이 실수가 났다. 에러는 잡아서 사람이 읽을 메시지로 바꿔 낸다 |
| **`popcornpc.co.kr`(실제 쇼핑몰)에 쓰기 요청을 보낸다** | 이 도메인은 우리 것이 아니다 — **GET만 허용, 폼 제출·장바구니 담기·저장 전부 금지**다 |
| **비밀값을 코드·커밋·문서에 남긴다** | DB 접속 문자열·비밀번호·API 키는 로컬 `.env` 하나뿐이다(`.gitignore` 대상). 필요한 값은 사장님/팀에 요청해서 각자 `.env`에 채운다 — `.env.example`이 어떤 이름의 값이 필요한지는 보여주지만 값 자체는 없다 |
| **`stock_qty`(재고 수량)를 원장 없이 고친다** | 재고를 바꾸는 모든 경로는 `stock_movements`에 사유와 함께 행을 남겨야 한다. 그래야 `재고 = 이동 합계`라는 불변식이 성립하고, 회귀가 이걸 전 상품에서 검사한다 |
| **화면에 서버가 안 준 숫자·판정을 적는다** | 랜딩·S0 화면이 "실시간 재고 검증"이라 써 붙이고는 **API를 한 번도 호출하지 않은 채** 재고 26,480개(어느 실제 값과도 불일치) · 호환성 5종(실제 8종) · 특정 GPU 645,000원(실제 428,000원)을 화면에 박아 둔 적이 있다. 후보 수·가격·규칙 수·"만들 수 있어요" 같은 판정은 **항상 서버 응답값 그대로**만 쓴다 |
| **LLM을 실제로 연동한다** | 2026-07-21에 보류 결정이 났고, 별도 승인 전까지는 그대로 유효하다 — 화면에 자리는 있어도 실제 호출은 별도 승인 사안이다 |

그 밖에 알아 두면 좋은 것: 이 저장소는 여러 AI 에이전트(제작자·확인자·기록자 등)가
사람과 동시에 코드를 만지는 방식으로 개발돼 왔다. 커밋이 짧은 간격으로 여러 건
올라오거나 DB 값이 예고 없이 바뀌어 있어도 — 대부분 다른 세션이 작업 중인 것이다.
이상하면 먼저 `git log`와 `HANDOFF.md`부터 본다.

---

## 3. 처음 30분에 할 것

**1) 저장소 받기**

GitHub 비공개 저장소다 — `https://github.com/davidchoi2060-art/popcorn-ai.git`.
접근 권한은 사장님/팀에게 요청한다.

**2) 로컬 서버 띄우기**

로컬 실행 설정의 정본은 `E:\DEV\.claude\launch.json`의 `"api"` 항목이다. 요약하면:

```
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt      # mac/Linux는 .venv/bin/pip
# .env.example을 .env로 복사하고, DATABASE_URL 등 실제 값을 요청해서 채운다(커밋 금지)
.venv/Scripts/python -m uvicorn api.main:app --port 8000
```

프런트엔드는 빌드가 없다 — 목업(`mockups/`)은 브라우저로 HTML을 직접 열고,
관리자 화면(`templates/admin/`)은 서버가 Jinja2로 렌더링한다. **npm은 쓰지
않는다.**

포트 8000과 8002는 다른 세션(A/B 비교용)이 항상 쓸 수 있다고 보고 건드리지
않는다. 자기 확인용으로 새 서버가 필요하면 8103~8110 사이에서 비어 있는 포트를
쓰고, 띄우기 전에 `netstat`로 정말 비어 있는지 확인한다.

**3) 관리자 화면 들어가기**

`http://localhost:8000/admin2/login`. 계정이 없으면 `.env`의
`ADMIN_BOOTSTRAP_EMAILS`에 자기 이메일을 넣고, 로그인 화면의 소셜 버튼으로 첫
신청을 하면 **비밀번호 없이 자동으로 owner 권한**이 나온다(이 첫 한 번만).
들어가자마자 비밀번호 설정을 요구받는다 — 막히면
`.venv/Scripts/python tools/set_admin_password.py`를 쓴다. (지금 소셜 로그인은
실제 인증을 하지 않는 dev adapter다 — 화면 자신이 그렇다고 밝힌다.)

**4) 회귀 돌리기 — 이 저장소의 안전망**

서버를 8000에 띄운 채로:

```
.venv/Scripts/python tests/regression.py          # 실패만 보려면 --quiet
```

오늘 기준 900건 안팎이다(정확한 수는 실행 결과 마지막 줄이 말해 준다 — 자꾸
늘어나는 수라 문서에 고정해 두지 않는다). 전부 "값"이 아니라 "관계"를 검사하는
불변식이다 — 예를 들어 부품 가격의 합이 총액과 같은지, DB가 세는 후보 수와
API가 답하는 후보 수가 같은지. **실패는 전부 코드 문제다** — 기대값을 손으로
맞춰서 통과시키지 않는다. 엔진·규칙·게이트·재고 원장을 건드렸으면 반드시
돌린다.

---

## 4. 무엇을 고칠 때 무엇을 먼저 봐야 하나

과하게 나열하지 않았다 — 자주 만지게 될 네 곳만 적는다.

| 만지려는 것 | 먼저 볼 것 |
|---|---|
| 견적 엔진 (`api/recommend.py`, `api/candidates.py`) | `docs/decisions/decision-log.md`의 **A-02**(왜 결정론 엔진인가, 재현성)와 고성능 티어 상한(`HIGHEND_CAP_X`) 관련 결정 · `CLAUDE.md` §용도 하한. 규칙을 넓히면 새 필드를 싣는 모든 곳(`_load_pool`·`SPEC_COLS`·추천 뷰)을 같이 늘려야 한다 — 하나라도 빠지면 조용히 후보가 0이 된다 |
| 호환 규칙 · 필수 사양 | `docs/design/req/req-compat-rules.md`, DB의 `compat_rules` 테이블(정본은 문서가 아니라 이 테이블이다 — 회귀가 문서와 대조한다) |
| 관리자 화면 (`templates/admin/*.j2`, `api/admin_ui_*.py`) | 그 화면의 요구사항 정의서(`docs/design/req/req-*.md`) · `docs/design/req/INDEX.md`(목차) · `CLAUDE.md` §문서·화면 어휘 표준(문구 규약 — 서술문·의문형·구어체 금지) · `.claude/CANON.md` §1(정본 목록, 좌측 메뉴는 `api/admin_nav.py` 하나뿐) |
| DB 스키마 | `docs/06_db-erd.md`(정본 ERD, Ver 4.0)를 먼저 고치고 그다음 `db/migrations/`(Alembic)에 새 마이그레이션을 만든다 — 순서가 "ERD 개정 → 새 마이그레이션"이다. 거꾸로 하면 문서와 실행본이 갈라진다 |
| 카탈로그 CSV 적재 (`api/catalog_ingest.py`) | 이 파일의 `UPSERT` SQL 주석부터 읽는다. `locked_fields`가 보호하는 컬럼과 무조건 덮어쓰는 컬럼(`status`·`stock_qty`·`data_origin`)이 나뉘어 있다 — 위에서 말한 공급처 4,113건 사고가 바로 여기서 났다 |

---

## 5. 지금 진행 중인 것

이 문서에는 옮겨 적지 않는다 — 옮기는 순간 낡기 시작한다. 아래 세 파일이 항상
최신이다(팀이 보고할 때마다 갱신한다):

- **`HANDOFF.md`** — 지금까지 뭘 했고, 뭐가 막혀 있고, 다음은 뭔지
- **`OWNER-DECISIONS.md`** — 사장님이 직접 정해야 하는 것만 (안 정해지면 뭐가 막히는지도 적혀 있다)
- **`docs/open-items.md`** — 그 밖의 미처리 목록 (왜 아직 안 했는지까지)

---

## 6. 문서 지도

| 문서 | 담당 |
|---|---|
| `~/.claude/CLAUDE.md` (이 PC 전역) | 서버·배포·포트 등 이 PC의 모든 프로젝트 공통 규약 |
| `popcorn-ai/CLAUDE.md` | 이 프로젝트의 작업 규약 + 그동안의 사고와 교훈이 쌓인 곳(가장 길고, 가장 자주 참고하게 된다) |
| `.claude/CANON.md` | "이미 정본이 있으니 새로 만들지 마라" 목록 한 장 + 절대 규칙 |
| `HANDOFF.md` / `OWNER-DECISIONS.md` / `docs/open-items.md` | 위 5번 참조 |
| `docs/decisions/decision-log.md` | 결정 이력 — 확정(`A-xx`) · 미정(`U-xx`) · 폐기(`X-xx`). 6천 줄이 넘는다 — 처음부터 읽지 말고 필요한 ID로 검색한다 |
| `docs/decisions/admin-identity.md` | 관리자 화면 ID(`ADM-*`) 카탈로그 — **아래에서 낡음을 설명한다** |
| `docs/design/req/INDEX.md` | 화면 목차(좌측 메뉴 순서·상태) |
| `docs/06_db-erd.md` | DB 스키마 설계서 — **아래에서 낡음을 설명한다** |

### 지금 확인된 낡은 문서 (2026-08-25 기준)

- **`docs/decisions/admin-identity.md`가 절반 가까이 낡았다.** 지금 실제로 살아
  있는 관리자 화면은 43개(`api/admin_ui_*.py` 파일 수, 공용 헬퍼 1개 제외)인데,
  이 문서에 이름과 근거가 개별로 등재된 것은 약 22개뿐이다. 반대로 이 문서에는
  등재돼 있는데 지금 화면이 없는 "유령 ID"도 약 10개 있다 — 대부분 2026-07-16에
  잡았다가 2026-08-11에 뒤집힌 "결제·배송·환불·회원관리를 직접 처리한다"는 옛
  계획의 흔적이다(예: 주문 관리·결제·정산·배송 관리·환불·클레임·회원 관리 —
  지금은 전부 쇼핑몰 소관이라 화면 자체가 없다). **확인법**: `ls api/admin_ui_*.py`
  (실제 화면 목록)와 이 문서의 "확정된 화면 정의" 절 아래 소제목들을 나란히
  놓고 대조한다.
- **`docs/06_db-erd.md`가 `products.supplier` 컬럼을 "폐기 예정"이라 적고 있는데,
  2026-08-25에 오히려 그 컬럼을 살려 상품 관리 화면(`templates/admin/products.html.j2`,
  "공급처(원문)" 필드)에 노출했다.** 문서와 코드가 반대 방향을 가리키고 있다 —
  어느 쪽이 맞는지는 아직 정해지지 않았다(`OWNER-DECISIONS.md` #5 참조, 사장님
  판단 대기).
- **`popcorn-ai/CLAUDE.md` 86번째 줄** — "잠금(`locked_fields`)이 지키는 것은
  매입가·판매가 «둘»뿐이다(2026-08-15 실사고)"라고 적혀 있는데, 2026-08-24
  커밋(`46925d1`)으로 **13개 컬럼**으로 넓어졌다(무조건 덮어쓰는 건
  `status`·`stock_qty`·`data_origin` 세 컬럼뿐이다). 위 "공급처 4,113건" 사고의
  재발 방지책이 바로 이 확장이다. `CLAUDE.md`는 하네스 담당이라 이 문서(온보딩
  가이드)는 낡았다는 사실만 적어 둔다 — **확인법**: `api/catalog_ingest.py`에서
  `locked_fields ?` 문자열이 몇 번 나오는지 센다.

이 셋 말고 낡은 곳이 더 있을 수 있다 — **이 저장소의 문서는 스스로 낡았다고
말해 주지 않는다.** 뭔가 이상하면 문서보다 코드와 `scripts/state.py`(실제 화면·
데이터 수를 재는 스크립트)를 믿는다.
