# -*- coding: utf-8 -*-
"""usage_floors 이름·낱말 정정 -- 「시청」과 「방송 송출」을 가른다 (사장님 확정 2026-08-17 · 파일만 · 적용 보류).

■ 왜 -- 병의 뿌리는 낱말이 아니라 «이름»이었다
  0057 보고가 남긴 겹침: 「넷플릭스 스트리밍」이 stream 으로 접혀 RAM 32 · SSD 1000 ·
  GPU 550W 가 걸렸다(실측). 그 뿌리는 **「방송·스트리밍」이라는 이름 하나에 송출과 시청이
  같이 들어 있다**는 것이다.
  사장님 확정: **「시청용과 방송 송출용으로」** -- 이름부터 그렇게 부른다.
      송출  stream  「방송·스트리밍」 -> **「방송 송출」**   RAM>=32 · SSD>=1000 · GPU>=550W
      시청  media   「영화·미디어」   -> **「영화·시청」**   RAM>=8  · SSD>=500
  이름이 분명하면 ① 고객이 고를 때 헷갈리지 않고 ② **모델이 `usage_label` 을 보고 판단하므로
  AI 도 헷갈리지 않고** ③ `match_terms` 싸움이 줄어든다.
  ⚠ **하한 값은 한 줄도 건드리지 않는다.** 바꾸는 것은 `usage_label` 과 `match_terms` 뿐이다.

■ ★ 이름 판정 -- 후보를 «실측으로» 걸렀다
  ㉠ 송출 = **「방송 송출」** (「방송·송출」이 아니다)
     · 가운뎃점은 기존 관례상 **다른 두 가지를 잇는** 자리다(사무·인강 · 주식·트레이딩).
       「방송」과 「송출」은 거의 같은 뜻이라 ·로 이으면 동의어 나열이 된다.
       수식 관계는 **띄어쓰기** 형태가 관례다 -- 「고사양 게임」 · 「AI 작업」.
     · 불변식(라벨이 자기 match_terms 로 잡힌다) 유지: 「방송 송출」 ⊃ 「방송」 ✓
  ㉡ 시청 = **「영화·시청」** (⚠ 「영상 시청」은 **실측으로 기각**했다)
     · **「영상 시청」을 쓰면 화면 예산대가 틀어진다.** `mockups/mvp1/s1-session.html`
       `pickTier()` 의 첫 줄이 `/영상|편집|크리에이터|프리미어|다빈치/` 라, 「영상」이 든
       라벨은 **영상편집 예산대(150~250만원)** 로 접힌다. 넷플릭스 보려는 사람에게
       150만원대 예산을 띄우는 셈이다. 지금 「영화·미디어」는 그 정규식에 안 걸려 기본
       범위(40~300)로 떨어진다 -- **이름을 잘못 고르면 지금보다 나빠진다.**
     · 「영화·시청」은 그 정규식 어디에도 안 걸린다(실측) · 「시청」이 들어가 사장님 지시와 맞고
       · 불변식 유지: 「영화·시청」 ⊃ 「영화」 ✓ (새 낱말을 안 만들어도 자기가 잡힌다)
     · 대안 「시청·감상」도 성립한다(「감상」을 낱말로 두면 자기매칭 ✓). 다만 「영화」가
       고객이 가장 많이 쓰는 말이라 이름에 남기는 쪽을 골랐다.
  ⚠ **행 라벨(`label` 컬럼)은 바꿀 필요가 없다.** 관례가 「합성 라벨의 앞부분」인데
    (사무·인강 -> "사무 메모리") 앞부분이 둘 다 그대로다: 방송 송출 -> 「방송」 ·
    영화·시청 -> 「영화」. 즉 "방송 메모리" · "영화 저장 용량" 이 그대로 맞다.

■ ★ 방식 판정 -- **낱말도 가른다. 다만 sort_order 는 한 줄도 건드리지 않는다.**
  순서 조정(㉯·㉰)은 **구조적으로 막혀 있다**(실측):
      · 「스트리밍 시청」이 stream 보다 먼저 잡히려면 media 가 stream(sort 6) «앞»에 와야 한다.
      · 그런데 media 는 video(영상편집, sort 9) «뒤»에 있어야 한다 -- media 의 「영화」가
        앞서면 **「영화 편집」이 media 로 접힌다**(지금은 video 로 맞게 간다).
      · video(9) 는 stream(6) 뒤다. 즉 **media < stream 과 media > video 를 동시에 만족할 수
        없다** -- 셋을 다 옮겨야 하고 「고사양 게임 > 게임」 같은 기존 서열을 흔든다.
  그래서 순서가 아니라 **낱말**로 가른다. 겹치는 낱말을 없애면 순서 다툼이 사라진다.

■ ★ 애매한 셋 -- **「못 가른다」고 적는다. 어느 쪽으로도 보내지 않는다.**
      스트리밍 · 스트리밍용 PC · 스트리밍 하려고요
  그 말만으로는 송출인지 시청인지 알 수 없다. 세 갈래를 다 따져 봤다:
      · 시청(가벼움)으로 보내면 -- 송출하려는 사람이 GPU 하한 없이 뽑힌다.
        슬라이스 58 이 고친 «롤·배그에 GT710» 과 같은 방향의 사고다.
      · 송출(무거움)으로 보내면 -- 넷플릭스 보려는 사람에게 RAM 32 · SSD 1000 · GPU 550W 가
        걸리고 근거 문구가 **「인코딩 기준 32GB 이상」**이라 말한다. 인코딩을 안 하는 사람에게
        그것은 **거짓 근거**이고 값도 크게 오른다.
      · 근거가 없으므로 **하한을 걸지 않는다**(`applied:false`). CANON §2-3 「모르는 것을
        지어내지 않는다」 · `passes()` 의 「값을 모르면 불통과」와 같은 결이다.
  ⚠ **동작 변경이다** -- 지금은 「스트리밍」 단독이 stream 으로 접힌다. 바꾸는 것이 목적이므로
    숨기지 않고 적는다. 되살리려면 「스트리밍」을 stream 에 되돌린다(downgrade 가 그렇게 한다).
  ⚠ 이름이 분명해졌으므로 **화면 칩은 영향이 없다** -- 칩은 라벨(「방송 송출」·「영화·시청」)을
    보내고 둘 다 자기 낱말로 잡힌다. 영향을 받는 것은 **자유 문장(팝콘톡)** 뿐이다.

■ 낱말 -- «신호»만 남긴다
  송출(stream): 방송 · 송출 · 생방송 · 트위치 · 아프리카TV · 치지직 · 올릴 영상 · 업로드
  시청(media) : 영화 · 넷플릭스 · 미디어 감상 · 영상 시청 · 스트리밍 시청 · 유튜브 시청 ·
                유튜브 볼 · 감상 · 시청용
  ⚠ **「라이브」는 일부러 넣지 않았다** -- 「라이브**러리**」가 부분일치로 걸려 개발 문장이
    방송으로 접힌다(`'라이브' in '라이브러리'`). 「라이브 방송용」은 「방송」이 이미 잡는다.
  ⚠ 「아프리카」가 아니라 **「아프리카TV」** -- 두 글자 「아프리카」는 방송과 무관한 문장에도 걸린다.
  ⚠ **바깥 「시청」은 넣지 않았다** -- 「강의 시청」(0057 의 web)의 부분문자열이라, media(23)가
    web(25)보다 앞이므로 **「강의 시청 위주」가 media 로 끌려간다.** 0057 이 맞게 만든 것을
    깨는 낱말이다. 그래서 「시청용」·「스트리밍 시청」처럼 **붙어 있을 때만** 시청으로 읽는다.

■ ★ 0057 을 고치지 않고 «여기서 UPDATE» 한다 -- 판정과 근거
  0057 이 아직 미적용이라 그 파일의 `media` 라벨을 직접 고치는 선택지가 있었다. 고르지 않았다:
    ① **DBA 가 지금 0057 을 드라이런 중이다.** 발밑에서 파일을 바꾸면 그가 잰 것이 무효가 되고,
       그가 어느 파일을 쟀는지조차 흐려진다.
    ② 0057 의 검증 포인트는 「새 행 등록 · 기존 행 UPDATE 0건」이다. 거기에 이름 변경을 섞으면
       **무엇이 무엇을 바꿨는지 갈라 볼 수 없다** -- 0058 을 따로 두라는 지시와 같은 이유다.
    ③ `usage_label` 은 «값»이다. 값을 고치는 정상 경로는 UPDATE 이고, downgrade 로 정확히
       되돌릴 수 있다(아래 `_BEFORE`). 파일을 고치면 되돌릴 지점이 사라진다.
  ⚠ 따라서 **DBA 를 멈출 필요가 없다.** 0057 은 그대로 재면 된다.

■ ⚠⚠ 이 마이그레이션은 파일만 만든다 -- DB 에 적용하지 않는다 (DBA 소관)
  0054~0057 과 같다. 검증은 **0057 -> 0058 을 한 트랜잭션에서 차례로 돌리고 ROLLBACK**
  했다(실제 적용 순서와 같게). alembic_version 은 0056 그대로, 표에 실제 변화 0.

■ downgrade -- **되돌린다(지우지 않는다)**
  0057 은 «우리가 넣은 새 행»이라 삭제가 답이었지만, 여기서는 **기존 행을 고친다.**
  원래 값을 이 파일에 그대로 적어 두고(`_BEFORE`) 복원한다.
      stream 원래값  「방송·스트리밍」 · ["방송", "스트리밍"]                      (0016)
      media  원래값  「영화·미디어」   · ["영화","넷플릭스","미디어 감상","영상 시청"] (0057)
  ⚠ 운영자가 화면에서 이 값을 고친 뒤 downgrade 하면 그 수정이 이 값으로 덮인다.

Revision ID: 0058
Revises: 0057
"""
import json

import sqlalchemy as sa
from alembic import op

revision = "0058"
down_revision = "0057"
branch_labels = None
depends_on = None

# usage_key -> (usage_label, match_terms). 같은 usage_key 의 «모든 행»이 같은 값을 갖는다
# (0016·0057 이 그렇게 넣었고 `_rows()` 가 행마다 읽는다) — 한 행만 고치면 갈라진다.
_AFTER = {
    # 송출 신호만 남긴다. 「스트리밍」을 뺀다 — 시청 문장까지 끌고 오던 낱말이다.
    "stream": ("방송 송출",
               ["방송", "송출", "생방송", "트위치", "아프리카TV", "치지직",
                "올릴 영상", "업로드"]),
    # 시청 신호를 더한다. 「스트리밍 시청」처럼 «붙어 있을 때만» 시청으로 읽는다.
    "media": ("영화·시청",
              ["영화", "넷플릭스", "미디어 감상", "영상 시청",
               "스트리밍 시청", "유튜브 시청", "유튜브 볼", "감상", "시청용"]),
}

# downgrade 가 복원할 원래 값 — 지우는 것이 아니라 «되돌리는» 마이그레이션이라 여기 적어 둔다.
_BEFORE = {
    "stream": ("방송·스트리밍", ["방송", "스트리밍"]),
    "media": ("영화·미디어", ["영화", "넷플릭스", "미디어 감상", "영상 시청"]),
}

_SQL = ("UPDATE usage_floors"
        " SET usage_label = :ul, match_terms = CAST(:t AS JSONB)"
        " WHERE usage_key = :k")


def _apply(mapping) -> None:
    conn = op.get_bind()
    for key, (label, terms) in mapping.items():
        conn.execute(sa.text(_SQL),
                     {"k": key, "ul": label,
                      "t": json.dumps(terms, ensure_ascii=False)})


def upgrade() -> None:
    _apply(_AFTER)


def downgrade() -> None:
    _apply(_BEFORE)
