# 팝콘PC AI — 프로젝트 인수인계 문서

> 이 문서는 Claude Code(Windows)가 이 프로젝트를 이어받아 작업하기 위한 인수인계서다.
> 작업 시작 전 이 문서와 `CLAUDE.md`, `docs/decisions/decision-log.md`를 먼저 읽는다.
> 최종 갱신: **2026-07-25** / 단계: **모델링 + DB·API 수직 슬라이스 구현**
> ⚠️ 아래 1·2·4·5절(정체성·UX·디자인)은 유효하나 **3·6·8절의 진척 서술은 구본** —
> **진행 이력의 정본은 `docs/decisions/decision-log.md`**(슬라이스 1~18 전 기록)다.

---

## 0. 30초 요약 (가장 먼저 읽을 것 — 2026-07-26 현행)

- **무엇을 만드나:** 자체 실재고 부품 기반 PC 견적·판매 시스템(팝콘PC AI). 정체성 **"신뢰 판매자"** + A-10 커머스 승격(주문·결제·환불·정산·배송 직접 처리).
- **지금 단계:** 목업=스펙 원칙 아래 **화면 단위 수직 슬라이스**(DB→API→목업 바인딩)로 구현 중. Cloud SQL 구축 완료, FastAPI(`api/`) 로컬 전용.
- **진행률: 슬라이스 40까지 완료.** 고객 축 S0~S5·MY 4화면(주문·결제·마이페이지·**후기 쓰기 MY-040**) + **관리자 29화면 전수 정리**(실데이터 19 + 엔진 규칙 노출 3 + 원천 준비 중 정직 표기 2 + 나머지 유지). T1~T10 트랜잭션 성문·실현. 결정론 엔진 v1 가동.
- **실카탈로그 적재 완료(슬라이스 39)**: products **22,838건** · **추천 후보 3,046** · 검수 대기 **7,415건**. 데모/실데이터는 `data_origin`으로 구분(데모 재적재 가능).
- **범위 = 내부 직원 베타**(사용자 확정 2026-07-26): 고객 미오픈이므로 PG·통신판매업·법정 표시·택배사 연동은 범위 밖. 남은 것은 규모 대응·직원 접근·백업.
- **알려진 한계 1건**: 고성능 티어 3,025만원(서버용 RAM이 카탈로그에 실재 — 설계대로지만 실용성 판단 필요).
  추천 100만원 '불가'는 슬라이스 40에서 해소 — 원인은 탐색 전략이 아니라 노드 상한값이었다.
- **회귀 세트 = 검증 수단**: `.venv/Scripts/python tests/regression.py` **60건**(`(F)` 고정 기대값 / `(I)` 불변식). 엔진·규칙·게이트·원장·인증을 건드렸으면 돌리고 결과를 보고한다.
- **고객 인증 도입(슬라이스 38 — 공개 배포 차단 사유 1순위 해소)**: `/api/my/*`가 `?email=`로 아무나 타인 데이터를 볼 수 있던 구멍을 닫음. **회원 경계 = 세션**. 관리자와 다른 정책 3가지: **승인 게이트 없음**(첫 로그인 = 가입) · **절대 14일·유휴 없음** · 권한 등급 없음. **게스트 유지**(상담·추천·주문은 로그인 불필요) · 주문 귀속은 세션 우선, 없으면 게스트 upsert.
- **관리자 인증 도입(슬라이스 37)**: `admin/login.html` → 회사 소셜 계정으로 신청(대기) → **owner 승인 + 권한 부여**(조회/운영자/관리자). **비밀번호 미저장**(신원은 소셜 제공자). 세션 = HttpOnly 쿠키·절대 8시간/유휴 30분, 정지 시 세션 즉시 무효. **작업 기록 주체가 로그인 운영자로 바뀜**(그전엔 전 행 '관리자' 고정). 신원 확인은 아직 **dev 어댑터(입력 이메일 신뢰) — 로컬 전용·공개 배포 차단 사유**.
- **화면 배지 어휘 3종(규약)**: `더미 데이터`(폴백) / `실데이터 · Cloud SQL` / **`원천 준비 중`**(저장 경로 자체가 없는 화면 — 0을 성과처럼 보여주지 않기 위한 구분).
- **고객↔관리자 양방향 루프 2종 폐합**: 매핑 요청(관리자)→동의(고객)로 계정 연결 확정 / 후기 작성(고객)→노출·신고 처리(관리자).
- **부품 라이프사이클 전 구간 폐합:** 단가표 수신(실파일 파싱) → 편입 → 검수 → 가격 승인 → 입고 → **추천 풀 진입(S1 20)** → 견적·주문 → 결제·배송 → 환불·정산 → 감사 기록.
- **게이트 정본 4중**(슬라이스 23~25에서 확정·관측 화면화): ① 사양 행 존재(product_specs — 추천 뷰가 조인) ② 검수 통과(review_required=false ∧ ai_candidate) ③ 판매가 산정 ④ 재고>0 ∧ status='판매중'. 위반 사유는 `추천 가능 재고 현황`(candidate-pool)에서 배타 버킷으로 집계된다.
- **다음 후보(자율 진행 승인됨 — 권장 순서):** ① **슬라이스 40 = 엔진 탐색 최적화**(eq/contains 규칙을 인덱싱해 DFS 폭발을 막는다 — 추천 100만원 불가 해소) ② 검수 큐 대량 처리 도구(7,415건을 직원 2~3명이 뚫는 방법 — AI 보강 필요성 판단) ③ 베타 배포(GCP VM · systemd · nginx · **백업 리허설 1회**) ④ 실제 OAuth 연동(카카오·네이버·구글).
- **미정으로 남아 사용자 확인이 필요한 것:** U-02 S2 커뮤니티 근거 소스(cite_s2 후기를 쓸지·내부 판매 통계를 쓸지) / success(초록) 토큰 추가 여부 / 게임 무게 3분류 대표 게임.
- **주의:** 원천이 없는 화면은 실데이터화보다 **정직 표기 + 이관 기록**이 맞다(슬라이스 21·28 전례). 이관 항목은 decision-log 각 슬라이스 항목 말미에 누적돼 있다.

---

## 1. 프로젝트 정체성

**팝콘PC AI** — 기존 팝콘PC 쇼핑몰(서울 용산)에 붙는 부품 기반 AI 견적·판매 시스템.

**핵심 포지셔닝: "신뢰 판매자"** (가격 비교 포털이 아님)
- 파는 것은 부품이 아니라 안심: "조립 불가능한 PC는 우리에게선 안 나온다 + 왜 이 조합인지 다 설명한다."
- 히어로 헤드라인: **"모든 견적에는 이유가 있습니다."**

**핵심 아키텍처 사상 (반드시 유지):**
- 추천 = **결정론적 룰/스코어 엔진**이 자체 부품 풀 위에서 수행. 같은 입력 + 같은 재고 → 같은 결과(재현성).
- **AI(LLM)의 역할은 입력 파싱 + 근거 설명뿐.** 견적을 생성하지 않는다.
- 이유: 재현성 확보 + 근거가 창작이 아닌 사실이 되게 함(신뢰 판매자 정체성의 근거).
- (과거 3사 LLM 병렬로 견적을 "생성"하던 구조는 폐기. validator를 주엔진으로 승격하는 방향.)

**3개 MVP (독립 서비스로 병렬 포지셔닝, 엔진 ①을 2·3이 공유):**
1. 팝콘PC 단독 AI 견적 — **가장 먼저 오픈, 코어. 현재 모든 목업의 대상.**
2. AI 제안 + 용산 도매 최저가
3. 입점 부품 가격비교 포털 — **외부 크롤링 미사용, 입점사 가격만**(법적 리스크 제거 완료)

**환경 구분:** 지금 다루는 GCP 서버 = **개발 서버**. 운영 서버는 별도 시스템으로 추후 구현.

---

## 2. 확정된 UX 결정 (번복 금지, 근거 있음)

- 대화 입력이 **주**, 칩은 대화 안에서 빠르게 답하는 **가속기**.
- 입력창 **전면**, 세일즈 요소는 **하단**.
- 초급/고급 하드분기 **삭제** → 하나의 대화 세션에 **기어 2개**(추천질문 ↔ 직접입력), 사용자가 언제든 강제 전환.
- **기어를 전환해도 이미 파싱된 제약은 유지**(상태 보존). 이게 S1의 최대 구현 함정.
- 결과·근거·비교를 **한 장짜리 스크롤 제안서(S2)**로 통합.
- 호환성 배지를 **가격보다 먼저** 노출.
- 비교견적은 가격 레이스가 아니라 **정직 프레임**("왜 이 가격/왜 우리가 더 안전한가").
- 후보 풀 카운터 = **완성 PC 대수가 아니라 "전체 부품(26,480개)에서 조건 통과 부품 수"**. (개념 교정 완료)
- 게이트웨이(index) = **내부자 전용 바로가기 허브**(직원/운영자/개발자). 세일즈 아님, 속도·명료함 우선.

---

## 3. 화면 목록과 진척 상태

| 화면 | 화면 ID | Screen 키 | 파일 | 상태 |
|------|---------|-----------|------|------|
| 내부자 게이트웨이 | INT-GATE-010 | internal-gate | `mockups/index.html` | 초안 완료(딥링크는 라우팅 시늉) |
| S0 랜딩·입력게이트 | FR-LND-010 | landing | `mockups/mvp1/s0-landing.html` | **재정비 필요**(풀컬러 아이콘 문제) |
| **S1 대화형 견적 세션** | FR-REC-010 | rec-session | `mockups/mvp1/s1-session.html` | **완료(진짜 도는 버전)** ★심장 |
| S2 견적 제안서 | FR-REC-020 | rec-result | (프롬프트만) | 목업 미착수 |
| S3 부품 변경 | FR-REC-030 | rec-detail | (프롬프트만) | 목업 미착수 |
| S4 주문 확인 | FR-REC-040 | rec-cart | (프롬프트만) | 목업 미착수 |

각 화면의 상세 설계는 `docs/prompts/`의 S0~S4 프롬프트 md 참조.

### S1 목업에 실제 구현된 3대 핵심 (검증됨)
1. **상태 단일 원천:** `session.constraints` 객체 하나. 칩·자유입력·파싱·완화가 전부 `setConstraint()`만 경유. 기어 전환 시 constraints 미변경(=조건 유지).
2. **실시간 후보 풀 카운터:** 답변마다 부품 수 틱다운(26,480→…) + 도트 40개 축소 + 진행바 + 델타 표시.
3. **단색 currentColor 인라인 SVG 아이콘:** 풀컬러 팩(fluent-color/streamline) 전량 제거. 토큰 색 상속.

---

## 4. 디자인 계약 (반드시 준수)

**디자인 시스템: Vibrant Horizon.** 토큰 단일 원천 = `design-system/tokens.css`의 `:root`.
- 모든 목업은 이 파일을 링크해 토큰만 사용. 임의 HEX/px, 인라인 스타일 지양.
- 서체: **Inter(라틴·숫자) + Pretendard(한글)** 페어링. Inter 단독 금지(한글 깨짐). 숫자·가격은 Inter tabular.
- primary `#005ab3`(액션) / `#0082fd`(hover) / tertiary `#853d97`(근거·태그) / error `#b31b25`(위험).
- **success(초록) 토큰 부재** → '통과/정상'은 primary로, '위험'은 error로, **항상 텍스트 병기**(색 의존 금지).
- 라운드: 칩 pill, 카드 1rem. 앰비언트 그림자.
- **아이콘: 단색 currentColor 세트만**(Lucide 권장 또는 인라인 SVG). 풀컬러 팩 금지(토큰 색 통제 불가·톤 불일치).

**마크업 계약 (Stitch 규약 승계):**
- 최상위 `data-screen-id`, `data-domain`. 동적값 `data-bind`, 반복 `data-repeat`, 버튼·링크 `data-action`, 예외/상태 UI는 `hidden`.
- 시맨틱 태그, `alt`, `label` 연결, 접근성(aria) 준수. 한국어 UI(기술 약어만 영문). 768px 이하 세로 스택.

---

## 5. 폴더 구조

```
E:\DEV\popcorn-ai\
├─ CLAUDE.md                  Claude Code 지침(0번 규칙: 모델링 단계·뒷단 금지)
├─ README.md
├─ HANDOFF.md                 이 문서
├─ docs/
│   ├─ prompts/               S0~S4 설계 프롬프트 md
│   ├─ design/                DESIGN.md (Vibrant Horizon 원문)
│   ├─ decisions/             decision-log.md (확정/미정)
│   └─ infra/                 10_infra-deploy-gcp.md, GCP_SETUP.md
├─ design-system/
│   ├─ tokens.css             :root 토큰 (단일 원천)
│   ├─ fonts/                 Inter, Pretendard
│   └─ icons/                 단색 SVG 아이콘
├─ assets/images/             더미 이미지
└─ mockups/
    ├─ index.html             게이트웨이
    ├─ mvp1/                   S0~S4 목업
    └─ shared/                 공통 CSS/JS(토큰 링크, 라우팅 시늉)
```

**새 목업은 `mockups/`에만.** 서버/백엔드/DB 폴더는 이 단계에서 생성하지 않는다.

---

## 6. 인프라 현황 (개발 서버, GCP 서울)

> 상세 절차·명령어는 `docs/infra/10_infra-deploy-gcp.md`, 민감 식별자는 `docs/infra/GCP_SETUP.md` 참조.

- **리전:** asia-northeast3(서울) 고정 (지연 + PIPA).
- **VM:** Compute Engine `popcorn-app`, e2-small, Ubuntu 24.04. Nginx/Node20/PM2/Redis/certbot 설치 완료.
- **DB(A안):** Cloud SQL `popcorn-db`, PostgreSQL 16, db-g1-small, 자동 백업+PITR, 삭제방지. **연결 검증 완료(`SELECT 1` 성공).**
- **연결 방식:** 개발 검증은 공개 IP + 승인된 네트워크(VM IP /32) + `sslmode=require`. (운영 전 Auth Proxy로 전환 권장 — Auth Proxy는 VM 액세스 스코프 이슈로 보류.)
- **미완:** DB 앱 유저·확장 생성(`popcorn_app`/`pg_trgm`/`GRANT ON SCHEMA public`)은 **모델링 이후**로 미룸.

**중요:** 인프라는 "붙는다"까지만 검증된 상태다. 모델링 단계에서는 인프라를 더 건드리지 않는다.

---

## 7. 미해결 결정 (임의로 정하지 말고 사용자 확인)

- success(초록) 토큰 추가 여부
- S2 '커뮤니티 근거' 소스 (권고: 내부 판매/스왑 통계 — 외부 커뮤니티 크롤링 아님)
- 후보 풀 축소 시각화 형태(현재 도트+바) 유지/변경
- 게임 무게 3분류(가벼운/인기/무거운) 대표 게임 라인업 확정
- 게이트웨이 딥링크의 실제 라우팅(현재 시늉만) — 앱에 URL/쿼리 파라미터 도입 필요

---

## 8. 다음 작업 (우선순위)

1. **S1 최종 판정** — 브라우저에서 직접 클릭·타이핑. 대화 자연스러움/후보 풀 축소 체감/기어 전환 혼란 여부. 어색하면 그 자리에서 갈아엎기.
2. **S0 재정비** — S1 품질 기준(단색 아이콘·토큰 정합)으로 랜딩 목업 다시. 기존 S0는 풀컬러 아이콘·How박스 배경 누락 결함 있음.
3. **S2 목업** — 결과+근거+비교 한 장 제안서. 근거는 엔진 산출값 바인딩 전제(더미).
4. **S3·S4 목업.**
5. (모델링 완료·사용자 선언 후) 뒷단 착수: DB 유저 생성 → 카운트 API → 앱 라우팅 → 배포.

**작업 원칙:** 가장 불확실한 것부터, 이음새를 하나씩 검증. 한 번에 하나. 미정은 임의로 메우지 말고 질문.

---

## 9. Claude Code(Windows) 시작 방법

1. 이 인수인계 묶음을 `E:\DEV\popcorn-ai\` 구조대로 배치(아래 배치표 참조).
2. PowerShell에서:
   ```
   cd E:\DEV\popcorn-ai
   claude
   ```
3. 첫 실행 시 브라우저 OAuth(유료 플랜/ API 키 필요). **PowerShell에서 실행**(Git Bash 금지 — TTY 이슈). 이미지 붙여넣기는 **Alt+V**.
4. Claude Code가 `CLAUDE.md`를 읽고 모델링 단계 규칙을 따르는지 확인.

### 배치표
| 파일 | 놓을 위치 |
|------|-----------|
| HANDOFF.md | `E:\DEV\popcorn-ai\` (루트) |
| CLAUDE.md | 루트 |
| README.md | 루트 |
| tokens.css | `design-system\` |
| decision-log.md | `docs\decisions\` |
| s1-session.html | `mockups\mvp1\` |
| index.html | `mockups\` |
| 10_infra-deploy-gcp.md | `docs\infra\` |
| GCP_SETUP.md | `docs\infra\` |
| S0~S4 프롬프트 md | `docs\prompts\` |
| DESIGN.md | `docs\design\` |
