# HANDOFF — 2026-09-21 아침 재개용

> 수는 적지 않는다. 재는 법: `.venv/Scripts/python scripts/state.py`

---

## 지금 어디까지 왔나

**배포 완료 · HEAD `0051901` · 로컬=서버 일치 · 회귀 1,188건 중 1,187 통과**
(남은 1건은 알려진 파워 105053 품절 — 견적 무영향)

어제~오늘 마이그레이션 0095~0108 을 전부 커밋·배포했다.

---

## ★ 아침에 볼 것 — 「칸 없음」이 너무 많다 (사장님 지적)

`/admin2/grid` 를 열면 빈 자리가 많이 보인다. **표가 잘못된 게 아니라,
격자가 아직 안 채워진 자리를 처음으로 «보이게» 만든 것이다.**

실측(2026-09-20 밤):
```
비게임   용도 12종 x 예산대 7구간 = 84 가능 / 실제 34칸   (40%)
게임     등급·해상도 18 x 6구간 = 108 가능 / 실제 34칸   (31%)

영상편집 4/7   방송·단순사무·캐드·사진·조판·3D렌더링·AI 3/7
개발·음악·복합사무·주식 2/7
```

### 가려야 할 것 — 이게 핵심이다

**「일부러 비운 자리」와 「아직 안 만든 자리」가 표에서 똑같이 보인다.**

    일부러 비움   사무용에 700만원 칸이 없다  -> 사장님 확정 상한 200만. 맞다
    안 만듦       개발이 2구간뿐이다        -> 실판매 표본 0이라 근거가 없어 안 만든 것

지금 화면은 둘 다 「칸 없음」이다. **운영자가 구분할 수 없다.**
0106 이 variant(고성능)에 대해 한 일 — 사유를 DB 에 남기고 화면이 말하게 하는 것 —
을 **칸 자체에도** 해야 한다는 뜻일 수 있다. 다만 사장님 판단 자리다.

### 선택지 (사장님께 올릴 것)

    ㉮ 사유를 붙인다      「이 용도는 이 예산대를 두지 않습니다 — 근거」를 칸에도
                          (0106 의 grid_variant_omissions 와 같은 모양)
    ㉯ 안 보이게 한다     해당 없는 자리는 표에서 아예 지운다(열을 용도마다 다르게)
    ㉰ 칸을 채운다        근거가 있는데 안 만든 자리만 골라 생성
                          -> 어느 자리에 근거가 있는지 먼저 실측해야 한다

**㉰ 이 필요한 자리를 먼저 재는 것이 순서다** — 시장 표본이 있는데 칸이 없는 조합.
근거 자료: `D:/Hermes-Workspace/pc_tier_ladder.json`(실판매 234벌 7단계) ·
`office_pc_market.json`(사무용 iGPU 신품 262벌) · `game_budget_measure.json`

---

## 그 밖에 남은 것

### 사장님 판단 대기
- **게임 등급 미확정 11종** — `D:/Hermes-Workspace/grade_pending.md`.
  7종이 두 패턴으로 묶여 규칙 한 줄이면 풀린다:
  ① 공식이 60fps 목표인데 손님이 고주사율을 원하면 어느 쪽인가
  ② 권장사양이 없으면 최소 기준으로 낮게 잡는가

### 확인 못 한 것 (정직하게)
- **브라우저 도구가 오늘 7회 전부 420초 타임아웃.** 픽셀·768px 리플로우를 눈으로 못 봤다.
  node/jsdom 으로 실제 템플릿을 렌더해 마크업 전수 대조로 대신했다.
  사장님이 직접 열어보시는 것이 지금 유일한 육안 검증이다.

### 문서 낡음 (사장님 승인 필요해 못 고침)
`CLAUDE.md` 27·29행이 「정본 도메인은 운영 몫, 개발엔 dev.」라고 적혀 있는데 사실이 아니다.
실제는 2026-08-21 사장님 확정대로:
```
popcornai.co.kr        고객 전용 (관리자 경로 404 차단)
admin.popcornai.co.kr  관리자 통로
```
`dev.` 는 DNS 가 없다. 이 낡은 줄 때문에 하네스가 이미 끝난 일을 다시 꺼내 물었다.

---

## 오늘 한 일 (커밋 8건)

```
0051901  관리자 격자 화면 둘이 축 전환을 못 따라가 비어 있었다
482f211  조립공임 30,000원 별도 표시 + 회귀 불변식 [55] 개정
5998cbd  게임 등급 54종 확정 — PC방 상위 9종이 0/9 에서 8/9 로
105c0bd  디자인을 조판과 사진·후보정으로 가른다(0108)
bc957df  조건부 규칙을 엔진에 연결하고 AI 하한을 와트에서 VRAM 으로(0107)
b4e80c8  사무·주식에 고성능 카드를 두지 않는다 — 사유를 스키마가 강제(0106)
e761c5f  격자 예산대 축 — 롤 하는 고객에게 219만원을 내밀던 결함(0105)
7351fa5  화면이 「고성능을 안 만든 사유」를 말한다
```

### 이 중 기억할 만한 것

- **롤 고객에게 219만원을 내밀고 있었다.** 원인 둘 다 측정으로 확정했다 —
  등급이 엔진에 도달하지 못했고(18칸이 5하한으로 뭉개짐),
  하한이 가격을 정하지 못했다(하한을 없애도 가격이 그대로였다).
  지금은 921,100 / 1,499,900 / 2,250,000 세 장이 나온다.
- **AI 하한 650W 는 근거 없는 값이었다.** VRAM 12GB 로 바꾸니 가격이 오르지 않고
  **내려갔고 불성립이 해소됐다**(AI 200만 가성비 -572,100원).
- **design 에 GPU 하한을 넣지 않은 판단** — 넣으면 가장 가벼운 고객에게 40만원을
  더 받는 결정이 된다. 대신 사진·후보정으로 갈라 그쪽에만 넣었고,
  조판 쪽은 값이 한 원도 안 바뀐다(개명이라 구조로 보장).

### 회귀에서 배운 것

- `[55]` 불변식이 **규칙 쪽이 틀렸다** — 「확정된 게임은 제안 표에 없다」는
  승인이 한 번도 일어나지 않은 상태에서만 성립했다. 막으려던 건 겹침이 아니라 덮어쓰기다.
- `[59]` 는 **「칸 수가 0이 아니다」를 검사하지 않는다** — 좌표가 겹치면
  1칸만 그려져도 0은 아니다. cell_id 를 렌더 결과와 전수 대조한다.
  음성 검사로 실효성 확인(옛 축으로 되돌리니 9건 FAIL).

---

## 서버

로컬 API :8000 이 떠 있어야 회귀가 돈다.
```
cd /d/DEV/popcorn-ai && .venv/Scripts/python -m uvicorn api.main:app --port 8000
```
배포 갱신은 `deploy/README.md` 의 한 줄(pull + pip + alembic + restart).
