# HANDOFF — 2026-09-22 재개용

> 수는 적지 않는다. 재는 법: `.venv/Scripts/python scripts/state.py`

---

## 지금 어디까지 왔나

**두 슬라이스가 main 에 커밋·푸시까지 끝났다.** 클라우드 컨테이너(기록자 작업 환경)에는
`.env`·DB·SSH 가 없어 그 뒤 단계는 아직 실행되지 않았다.

    ① S2 게임 견적 근거 — reasons 와 섞지 않고 `game_context` 형제 필드로 분리
       (결정 로그 A-137, 커밋 b6ab16e)
    ② 게임 이름 별칭 표 `talk_game_aliases`(0113) — "롤"처럼 짧은 줄임말이
       게임명으로 이어지지 않던 것을 고친다 (결정 로그 A-138, 커밋 03fdc74)

둘 다 확인자가 정적 검사·node 가짜 document 실렌더·음성 검사(게이트를 끄고 FAIL
확인 후 원복)로 통과 판정했다. **실행하지 않은 것**은 사장님 PC/서버 몫이다:

    git pull  ->  alembic upgrade head (0113 적용)  ->  회귀 전체 실행  ->  서버 배포

alembic 을 돌리기 전에는 `talk_game_aliases` 표 자체가 없다 — "롤" 견적은 여전히
안 된다.

---

## 미정 — 사장님 결정 대기 (결정 로그 §5, 임의로 채우지 않는다)

    U-59  "롤이랑"처럼 조사가 붙은 줄임말 — 별칭 정확 키 밖
    U-60  피파·스타·와우 별칭 — 86종 목록에 있는지 DB 확인 후 2차 판단
    U-61  고객 화면에 내부 필드명(`game.grade_src`) 그대로 노출
          (`mockups/mvp2/app.js:691`)
    U-62  회귀 `[62]`의 DB/서버 항목 4건이 기존부터 FAIL — 이번 두 슬라이스와 무관
          (다음 세션이 이번 탓으로 오인하지 않도록 기록)

---

## 문서 낡음 — 하네스 몫으로 보고됨(기록자는 고치지 않음)

`CLAUDE.md`가 아직 S1(`mockups/mvp1/s1-session.html`)을 「품질 기준 파일」로 적고
있는데, 사장님 확인(2026-09-22): *"S1~S4까지는 일단 안쓰는데"* · *"mvp2 를
사용하고 있는데"* · *"이것만 지금 사용하고 있어.. s1~ 이거는 백업하라고 시켰는데.
아주 예전에."* — 실제 사용 화면은 mvp2 뿐이다. `CLAUDE.md` 초안 필요.

---

## 그 직전 세션(2026-09-21)에 남아 있던 것 — 아직 사장님 확인 전

`/admin2/grid` 의 「칸 없음」이 「일부러 비운 자리」인지 「아직 안 만든 자리」인지
화면이 구분하지 못한다는 지적과 선택지 셋(사유를 붙인다 / 안 보이게 한다 / 근거
있는 칸만 채운다)이 아직 사장님 결정을 못 받았다 — 이번 세션은 이 항목을 건드리지
않았다. 상세는 git 이력의 이전 HANDOFF 버전을 참조.
