# 팝콘PC AI — 인수인계 (현재 상태)

> **최종 갱신: 2026-08-16**(`state.py` 의 「그 뒤 90커밋」 경보를 받고 **내용 대조**로
> 점검했다 — 그 수는 저장소 전체 커밋 수라 그 자체로 낡음의 증거는 아니지만, 대조해
> 보니 실제로 어긋난 항목이 있었다: §4 「S4/S5 고객 화면이 아직 자체 결제」는
> 2026-08-12 부터 이미 틀려 있었다(`s4-cart.html` 이 그날부터 `/api/handoff` 를
> 부른다) — 정정했다. §5 「다음 작업 후보」도 절반 가까이 이미 끝나 있어 표시를
> 갱신했다. §4 에 **몰 카탈로그 재적재 사고**(2026-08-15~16, 지금 대응 진행 중)를
> 새로 적었다. §0~3 본문(범위·인프라)은 그대로다 — 그 범위 자체는 안 바뀌었다.
> 전체 재정리는 여전히 별도 작업)
>
> ### 문서 역할 분담 — 같은 사실을 두 곳에 적지 않는다
>
> | 문서 | 담당 | 갱신 시점 |
> |------|------|-----------|
> | `CLAUDE.md` | **작업 규칙 + 축적된 교훈**(사고 기록). 매 턴 컨텍스트에 로드된다. | 규칙·교훈이 생길 때 |
> | `docs/decisions/decision-log.md` | **확정 / 미정 / 폐기된 결정** | 결정이 바뀔 때 |
> | `.claude/CANON.md` | **정본 목록 + 절대 규칙**. 에이전트가 일 시작 전에 읽는다 | 정본이 늘 때 |
> | **`HANDOFF.md`(이 파일)** | **지금 어디까지 왔나 · 무엇이 막혀 있나 · 다음은 무엇인가** | 작업 묶음이 끝날 때 |
>
> 이 파일은 **상태만** 적는다. 정체성·UX 결정·디자인 계약은 위 문서들이 원천이고 여기서는 링크만 한다.
>
> ### ★ 이 파일에 숫자를 적지 않는다 (2026-08-11 개정 — 사용자 지시)
>
> 2026-08-10 판이 20커밋 뒤처진 채 남아 있었고, **낡은 게 아니라 틀려 있었다**:
> 검수 대기 5,152(실제 3,235) · 추천 후보 3,308(실제 3,219) · 회귀 795(실제 861) ·
> 관리자 계정·공급처·화면 수까지 전부 어긋났다. 그리고 §0 은 여전히 «내부 직원 베타,
> 고객 오픈은 나중» 이라 말하고 있었다 — **그날 확정한 범위와 정면으로 반대**였다.
>
> **틀린 인계는 인계가 없는 것보다 나쁘다.** 새 세션이 그걸 사실로 읽고 일을 시작한다.
>
> 그래서 **세는 수는 여기 적지 않고 «어디서 보는지»만 적는다.** CLAUDE.md 가 HEX 값을
> 빼고 토큰 이름만 남긴 것과 같은 처방이다. 판정(양호/막힘/대기)은 오래 가지만
> 수치는 하루면 틀린다 — **둘을 섞어 적지 않는다.**
>
> | 알고 싶은 것 | 어디서 보나 |
> |---|---|
> | 상품·후보·검수 대기 | `GET /api/admin/candidate-pool` · 대시보드 |
> | 활성 호환 규칙 | `compat_rules` (회귀가 문서와 대조) |
> | 회귀 건수 | `.venv/Scripts/python tests/regression.py` 마지막 줄 |
> | 화면 수 | `mockups/admin/*.html` · `templates/admin/*.j2` |
> | 재고 정합 | 회귀 `stock_qty = SUM(qty_delta)` |
> | 인계 실적 | `handoffs` · `handoff_items` |

---

## 0. 30초 요약

- **무엇:** 자체 부품 풀 기반 PC 견적 시스템. 정체성 **"신뢰 판매자"** — 추천은 결정론적
  룰/스코어 엔진이 하고, LLM 은 입력 파싱과 근거 설명만 한다(견적을 생성하지 않는다).
  근거는 `decision-log §1`.

- **★ 범위가 바뀌었다 (2026-08-11 사용자 확정 — 이전 판과 정반대다):**
  **팝콘AI 는 신규 고객 유입 채널이다. 판매하지 않는다.**

      고객 -> 용도·조건 -> 호환성 판정 -> 견적 -> **팝콘PC 쇼핑몰로 인계**
      결제 · 배송 · 환불 · 회원관리 = 쇼핑몰 소관

  이전 판(A-10 커머스 승격, 2026-07-16)은 **뒤집혔다.** 그때는 회원·장바구니·결제·정산·
  배송·환불을 본 시스템이 직접 처리하기로 했었다. 지금은 **넘기고 끝난다.**
  운영 IA 가 54 -> 35 항목으로 줄었고(주문·클레임·배송설정·매출세무·고객 축이 빠졌다),
  고객 화면도 13 -> 7 로 줄어든다(MY 4화면 제거 · S4/S5 는 인계로 재작성).

- **쇼핑몰 연동 — 실측으로 확인된 것:**
  · 몰 상품코드 `pd_no` **== 우리 `products.product_code`** (매핑표가 필요 없다)
  · 담기 경로 `POST /shop/order_basket_action.php`
  · 조립비도 몰 상품이다 (`pd_no=92983` 「제작 공임1」)
  · 플랫폼 = **윈윈소프트**. API 5종을 요청해 둔 상태 —
    `docs/design/winwin-api-request-2026-08-11.md`

- **가격 원천:** 팝콘PC(윈윈)의 판매가가 기준이다. **직접 소싱한 경우만** 우리가 덮어쓰고
  `locked_fields` 로 잠근다(동기화가 못 건드린다). 지금은 엑셀 주기가 곧 가격 오차라,
  **인계 직전에 몰 가격을 다시 확인**하고 다르면 고객에게 알린다.

- **단계:** 화면 단위 수직 슬라이스(DB->API->화면 관통). 재구축 화면은 `/admin2/*` 에
  **병행 신축** — 기존 `/admin/*.html` 은 그대로 살아 있다.

- **여전히 금지(별도 승인 필요):** 실제 LLM 연동(보류 2026-07-21) · 운영 배포.
  **실서비스 `popcornpc.co.kr` 는 읽기 전용**(GET 만 · 폼 제출 금지).

- **검증 수단:** 서버 띄우고 `.venv/Scripts/python tests/regression.py`.
  전량 관계식 불변식이고 **고정 기대값은 폐지**했다. 엔진·규칙·게이트·원장을 건드렸으면
  돌리고 결과를 보고한다. 상세는 `CLAUDE.md §회귀 세트`.
  · `REGRESSION_KEEP_SESSIONS=1` — 브라우저에 로그인해 둔 세션을 안 끊는다
  · `REGRESSION_BASE=http://127.0.0.1:8001` — 8000 을 남이 잡고 있을 때
    (안 쓰면 **남의 낡은 서버를 검사하면서 내 코드를 판정한다**)

- **지금 하던 일(2026-08-17, 기록자 갱신):** 몰 카탈로그 재적재 대응은 §4 맨 위 그대로
  진행 중(다음 세션이 재확인). 그와 별개로 **미커밋으로 남아 있던 마이그레이션 셋이
  커밋됐다**(0057 용도 하한 +3 · 0058 스트리밍 시청/송출 분리 · 0059 환율 표 신설,
  근거는 `decision-log.md` A-59~A-61) + **재고 입고 보류 API**(`stock_inbound_holds`,
  A-58의 「미해결」 항목 중 서버 쪽) + **용도 어휘 정본화**(`GET /api/usages` 신설,
  AI 파서가 `usage_floors.match_terms`를 직접 읽는다 — 「배그」 오판정 해소).
  ⚠ **화면(admin2 라우트)은 아직 없다** — 재고 입고 화면·AI 비용 화면의 원화 환산
  표시는 이번 커밋 범위 밖(A-58·A-61 「미해결로 남긴다」 참조). 0057~0059가 DB에
  실제 적용됐는지는 이 기록자 세션은 확인하지 못했다(문서·커밋만 다루는 역할 —
  `alembic_version` 재확인은 다음 세션 몫).

- **지금 하던 일(2026-08-18, 기록자 갱신):** 위 2026-08-17 물결(A-62~A-66)이 커밋되고
  **A-66(구 관리자 화면 처분 — `mockups/admin/` 동결 이동 + door 축소 + 회귀 헬퍼)이
  오늘 커밋됐다.** 오늘의 새 물결은 **재고 입고 화면(ADM-SRC-020, 사장님 선택)
  착수** — `api/admin_ui_stock_inbound.py`·`templates/admin/stock_inbound.html.j2`·
  `docs/design/dc-stock-inbound*` 를 제작자가 짓는 중(이 기록자 세션 시점 기준
  아직 미커밋 · 검증 전이므로 메뉴에 연결하지 않는다 — §2 규약 그대로).
  **↑ 위 마지막 줄은 같은 날 안에 낡았다(지우지 않고 이어 적는다).** 확인자 3차 검증이
  결함 0으로 통과하고 회귀가 전건 통과해 **T-02 ②의 조건(검증 통과 통보)이 충족됐고**,
  기록자가 `api/admin_nav.py`에 「재고 입고」를 재등재하고 `docs/design/req/INDEX.md`에
  행을 세웠다(A-58 「미해결」 셋 중 둘 해소 — 남은 하나는 `admin-identity.md`의
  **구 화면 정의 개정**이고 별건이다). **아직 커밋 전이다** — 하네스가 회귀를 한 번 더
  돌린 뒤 기록자가 물결별로 나눠 커밋한다. ⚠ **재고 입고 화면의 「대기 유형 필터」는
  확정만 되고 구현 전이다**(U-17 → A-68 ㉰ · 다음 물결). ⚠ **회귀를 다시 돌리기 전에
  API 서버를 재기동한다** — 지금 떠 있는 uvicorn 은 `--reload` 없이 떴으므로 바뀐 IA를
  싣지 않았고, 회귀 [41]은 «서버가 그린 LNB»와 «지금 코드의 IA»를 대조한다.
  **↑ 「다음 물결」은 같은 날 왔다(지우지 않고 이어 적는다).** 대기 유형 필터가
  **지어졌고 검증을 통과했다** — 확인자 4차 결함 0 · 회귀 전건 통과 · 커밋 `95acd6b`.
  서버가 `kind`(`all`·`zero`·`low`)를 받고 유형별 건수와 기준 문장을 함께 준다.
  기본값이 필터 도입 전 모집단과 같아 대시보드·매입 견적은 안 고쳤다(A-68 ㉰ ✅ 블록).
  **다음 사람이 볼 것**: 안전재고 기준 **쓰기 경로는 여전히 0건**이라 「안전재고 미달」
  유형의 모집단이 작다(A-58 ㉡ — 상품 상세 소관, 아직 없다) · `admin-identity.md` 의
  **구 화면 정의 개정**도 여전히 남아 있다(별건).

- **2026-08-18 저녁 — 여기서 멈췄다(사용량 한도 · 다음날 14:00 리셋).** 커밋된 것은 전부
  배포됐다(`01b1ac6` = 로컬·원격·서버 동일). **미커밋으로 남은 것 둘 묶음**:

      ① 검증까지 끝난 것 — 커밋만 하면 된다
         templates/admin/_admin2_shell.html.j2   상단바 「허브」 단추(새 탭 · data-keep)
         mockups/shared/admin2/admin2.css        그 스타일 + ≤768px 열간격·padding 회수
         mockups/mvp1/index.html                 허브 화면의 지어낸 값·죽은 링크 제거
         CLAUDE.md                               §브라우저 점검 계정에 「이름 길이」 함정 추가
         -> 확인자 6차 통과(육안 포함) · 회귀 미실행 · 커밋 미실행.
         ⚠⚠ **「커밋만 하면 된다」가 아니다 — 마지막 실측에서 결정 대기가 하나 남았다.**
            실제 조건(스크롤바 있음 · 실계정 이름 · 768px)에서 **헤더가 나빠진 화면 4**:
            `products` 52->71.5 · `categories` 52->72.5 · `mall-sync` 66->67.5 ·
            `ops-settings` 67->67.5. 앞 둘은 **허브 이전에 한 줄이던 화면**이다.
            (1450px 은 네 조건 모두 40/40 불변 — **데스크톱은 무해**하다.)
            원인은 목록이 아니라 수 하나다: **허브의 순 비용 +15.2px**(열간격 27px 회수 뒤 순증).
            그보다 여유가 적던 화면이 넘어간다. `category-mapping` 은 **0.7px 로 버틴다** —
            헤더에 무엇이든 더하면 가장 먼저 깨질 화면이다(아바타 링크 작업이 특히 위험).
            **사장님 결정 대기**: ㉮ 그대로 둔다(좁은 폭에서만 두 줄) ㉯ ≤768px 에서 허브를
            숨긴다 ㉰ 좁은 폭에서 `.a2-roles` 를 줄인다. **정해지기 전엔 커밋하지 않는다.**
            ⚠ 확인자 미검증분: 위 수치의 이름 축은 **DOM 문구 치환**이다 — `products`·
            `categories` 를 실계정 세션으로 한 번 재는 것이 남았다(`compat-rules` 는 그렇게 갈랐다).
            ⇒ **2026-08-19 풀렸다(지우지 않고 이어 적는다)** — 사장님이 ㉯를 택했다.
            ①은 커밋 완료. 상세는 아래 「지금 하던 일(2026-08-19)」.

      ② 짓다 만 것 — 「내 정보」(ADM-SYS-022)
         docs/design/spec-my-profile.md          계약 완성(1a 확정 · 사장님 확정 둘 반영)
         docs/design/dc-my-profile.html|-README  승인 원안(1a·1b 한 파일 · 1a 만 정본)
         docs/design/dc-support.js               원안 부속
         api/admin_ui_my_profile.py              신규 · 미완
         templates/admin/my_profile.html.j2      신규 · 미완
         api/admin_profile_photo.py              사진 정책 노출 GET 추가 · 미완
         -> 제작자가 자기 점검 중 결함 둘을 찾았다고 보고하고 끊겼다. **그 둘이 무엇인지
            기록에 없다** — 다음 세션이 그 에이전트를 잇거나 처음부터 자기 점검을 다시 한다.

- **오늘 배운 것 — 「재는 조건」을 안 적으면 수치가 거짓말한다** (하루에 세 번 당했다):
  ㉠ 고정 1초 대기 -> 아직 안 채워진 헤더를 재고 「고쳤다」 ㉡ 짧은 이름 점검 계정 ->
  실계정에서만 나는 결함을 못 봄(44px 차이) ㉢ `--hide-scrollbars` -> 가용폭 15px 낙관.
  셋 다 **수치는 맞고 조건이 빠져** 결론이 틀렸다. 계측기는 스크래치패드 `cdp_sweep.py`.

- **지금 하던 일(2026-08-19, 기록자 갱신):** 어제저녁 멈췄던 미커밋 두 묶음 중
  **①이 풀리고 커밋됐다.** 사장님이 위 「사장님 결정 대기」에서 **㉯(≤768px 에서
  허브를 숨긴다)**를 택했다 — 열간격 회수(10px→7px)는 유지 확정, 그래서 좁은
  폭은 허브 도입 «전»보다 27px 여유가 는다. 확인자가 남겨 뒀던 「미검증분」
  (실계정 세션 재측정)까지 마쳐 결함 0을 다시 확인했다. 등재 =
  `docs/decisions/decision-log.md` **A-69**. 커밋 = `5377b24`(상단바 허브 단추
  신설 + ≤768px 숨김, 셸+`admin2.css`) · `e24dd4b`(허브 화면 자체의 지어낸 값·
  죽은 링크 제거, `mockups/mvp1/index.html`) · `31fbdc2`(`CLAUDE.md` — 점검 계정
  이름 길이 함정 + 팀원 모델 상향 금지 규약 명문화).
  **②(「내 정보」 ADM-SYS-022)는 여전히 그대로 미완이다** — 제작자가 자기 점검
  중 찾았다는 결함 둘의 정체는 오늘도 기록에 없다. 다음 세션이 그 에이전트를
  잇거나 자기 점검을 처음부터 다시 한다(파일 목록은 위 「② 짓다 만 것」 그대로,
  전부 여전히 미커밋).

  ⇒ **같은 날 안에 이어졌다(지우지 않고 이어 적는다)** — ②(「내 정보」 ADM-SYS-022)
  **재점검이 끝났다.** 위에서 「그 에이전트를 잇거나 자기 점검을 처음부터 다시
  한다」고 적어 둔 것 중 **후자를 택했다** — 확인자(동작)·계약자(계약)·문안검사(문구)
  3축을 독립으로 새로 돌렸다. **「제작자가 말한 결함 둘」의 정체는 끝내 확인하지
  못했다** — 그 자리를 메운 것은 그 결함을 되짚는 것이 아니라 **전수 재점검**이다.
  이 "모른 채 남음"을 해소로 읽지 말 것 — 무엇이었는지는 여전히, 그리고 앞으로도
  기록에 없을 가능성이 크다.

  **발견 요약**: 동작 결함 2건(HEAD 사전 확인이 실제로는 안 걸리는 사문화 상태 ·
  `editable` 필드가 raw 로 노출) · 계약 결함 3건(제작자가 계약 유보를 넘어
  `GET /api/admin/my-profile/photo-policy` 를 임의 신설 → **같은 날 A-70 으로 추인
  해소** · 서버 `note` 문장 미표시(정적 문구로 이원화) → 미정 **U-19** · 이 화면이
  `docs/design/req/INDEX.md` 에 아예 없던 것 → **오늘 등재로 해소**, 아래 참조) ·
  문구 결함 다수(세 문체 혼재 약 66곳 · 대화체 주어 2곳 · 값-설명 오독 조합 등,
  그중 셋을 **U-20~U-23** 으로 등재). **동작 골격 자체는 튼튼하다** — PATCH 경계값
  18종 · 권한 상승 차단(422) · 사진 매직바이트 판정 · 1MB 경계 전부 통과했다.
  ⚠ **회귀 스위트는 이 화면을 다루지 않는다** — 사진 API 를 회귀가 아예 검사하지
  않는다(coverage gap 으로 남긴다 — 코드 파일이라 기록자는 못 고친다).

  ⚠ **FastAPI 0.139.2 가 GET 라우트에 HEAD 를 자동으로 추가하지 않는다** — 이
  화면만의 문제가 아니라 **앱 전체 GET 라우트가 HEAD 요청에 404** 인 프레임워크
  레벨 사실이다. 외부 모니터링이 HEAD 로 헬스체크하면 거짓 장애로 보일 수 있다.
  별도 조사 배지가 발행돼 있다 — 이 화면의 결함 목록과 섞지 않는다.

  **교정 물결**(문체 통일 · raw 노출 정리 · 사문화된 HEAD 확인 걷어내기)**이
  제작자에게 가 있다 — 검증·커밋 전.** 계측 라우트 목록(스크래치패드
  `cdp_sweep.py`)에 `/admin2/my-profile` 을 추가하는 건(어제 확인자 발견, 위
  「다음 사람이 볼 것」 참조)이 아직 남아 있으면 **그대로 유지**한다 — 오늘
  손대지 않았다.

  **기록자가 오늘 등재한 것**: `decision-log.md` **A-70**(사진 정책 API 추인) ·
  **U-19~U-23**(서버 `note` 표시 방향 · 「로그인 잠금」 배지 · 임시 비밀번호 배너
  문구 · 「비밀번호 설정」 값-설명 오독 · 「기기」 한 낱말 두 개념 — 전부 선택지만
  적었다 · 임의 결정 없음) · `spec-my-profile.md` §미해결 ④ 갱신 · `INDEX.md`
  「0. 로그인 — LNB 밖」과 같은 결로 **「0B. 내 정보 — LNB 밖」 신설**(상태: 지어졌으나
  미커밋 · 검증 전 · 교정 물결 진행 중). **커밋은 아직 하지 않았다** — 제작·검증
  체인이 끝난 뒤 별도 지시로 한다.

  **하네스가 오늘 실측해 해소를 확인한 것**(이 기록자 세션은 문서·커밋만 다루고
  DB에 닿지 않으므로, 하네스가 잰 값을 그대로 옮겨 적는다 — 재확인 방법은 각
  항목에 있다):
  · **몰 카탈로그 재적재는 실제로 다시 돌았다.** 상세·확인법은 §4 「몰 카탈로그
    재적재」의 2026-08-19 정정 참조(`csv_import_jobs` job_id 27).
  · **A-31 불변식(완제품 분류인데 `part_type='ETC'`) 위반이 298건 → 0건**이다 —
    같은 §4 정정 참조.
  · **상품 119253 가격 이력은 지금 어긋나 있지 않다 — 그런데 이것은 판정이 아니다.**
    재적재가 새 값을 덮어써 어긋남을 가렸을 뿐이고, **「PATCH 경로가 이력을 안
    남긴다」는 의심은 여전히 미판정이다.** 상세는 §4 「원장 결함 의심」의
    2026-08-19 정정 참조 — **이 구분(지금 맞는 것 ≠ 원인이 밝혀진 것)을 흐리지
    말 것.**
  · **특허는 출원이 끝났다.** §5 항목 2의 2026-08-19 정정 참조 — 「메일 발송
    대기」는 낡은 상태다.

  **다음 사람이 볼 것**:
  · `scripts/state.py`가 「화면 수 ≠ 메뉴 항목 수(41 vs 40)」라고 알릴 수 있는데
    **결함이 아니다.** 미커밋 `templates/admin/my_profile.html.j2`(② 묶음)가 화면
    파일 집계엔 잡히고 검증 전이라 메뉴엔 없어서다(§2 규약 — 검증이 끝난 화면만
    메뉴에 연결). ② 가 커밋·검증되면 41=41로 맞는다.
  · **계측 sweep 의 커버리지 공백**(확인자 2026-08-19 발견) — `cdp_sweep.py`가
    쓰는 라우트 목록이 40개뿐이라 **41번째 화면 `/admin2/my-profile` 이 빠진 채
    「40/40 통과」를 말한다.** 이번엔 확인자가 수동으로 재서 정상(52px 1줄)이었지만,
    다음 sweep 전에 목록에 추가해야 한다. 이 목록은 스크래치패드 파일이라
    **저장소 밖이고, 그래서 더 쉽게 잊힌다.**

- **같은 날 팀원이 늘었다 — pathfinder(길잡이) 임명.** 사장님이 2026-08-19 새 팀원
  **길잡이**를 임명했다. 정의 정본 `.claude/agents/pathfinder.md`(하네스 작성) · 미러
  `E:\DEV\.claude\agents\pathfinder.md`. **팀 11명**(확인법 `ls .claude/agents/*.md | wc -l`).
  고객단(랜딩→S0~S5→인계 · MY)을 고객의 눈으로 걸어 「고객이 왜 멈추나」를 찾는 역할 —
  확인자(도는가)·계약자(약속과 같은가)·문안검사(말이 맞나)와 달리 **화면 «사이»**를 본다.
  산출물은 점검 보고 + 요구사항 정의서(7항목)까지, 배치·색은 그리지 않는다(§화면 작업의
  경계 — 고객단도 예외 없음, 2026-08-19 사장님 확정). 등재 `decision-log.md` **T-14**.
  **첫 임무 후보** — 낡은 흐름 정본 재측정(`docs/design/screen-flow.md`, 2026-08-12 이후
  약 200커밋 · `scripts/state.py` 가 「의심」 표시) · 고객 감사 재측정
  (`docs/design/customer-screen-audit-2026-08-06.md`). **아직 착수 지시 전이다** — 하네스
  배정 대기. ⇒ **같은 날 다른 과제가 먼저 배정되고 끝났다**(아래 「같은 날 첫 임무가
  배정되고 끝났다」 참조) — 이 후보 둘은 아직 미착수라 지우지 않고 「다음 후보」로 남긴다.

- **같은 날 첫 임무가 배정되고 끝났다 — 「견적 AI 재검토」(기록자 갱신).** 위 「첫 임무
  후보」 둘이 아니라 **하네스가 다른 과제를 배정했다** — 「고객에게 견적 AI 를 통해
  물어보는 부분」 전면 재검토(범위는 **고객 경험만**, 엔진 원칙 A-01~03 불변). 길잡이가
  세 인물상(처음 온 사람 · 부품 아는 사람 · 예산 빠듯한 사람)으로 **375px 우선**으로
  걸었고, 조사자 둘이 구조·데이터를 실측했다.

      확정 넷   A-71(expert 모드를 엔진에 두 단계로 연결) · A-72(expert 쿨러 슬롯 추가) ·
                A-73(S2 표시 = 고른 구성 + 서버 대안 둘) · A-74(특정 부품 요청은 「정직
                표시」가 먼저 — 같은 날 앞선 ㉯ 확정을 뒤집었다, 경위는 A-74 본문에 있다)
      미정 넷   U-24(guided 6문항 중 3문항이 후보를 안 바꾼다) · U-25(예산 범위를 대화에서
                밝힐지) · U-26(chat·talk 에 예산 탈출구가 없다) · U-27(팝콘톡 고정 통계
                문구) — 전부 선택지만 적었고 임의 결정 없음
      기존 갱신 U-16 범위가 넓어졌다 — 근거 없는 출고 약속이 s4/s5 뿐 아니라 S1 팝콘톡·
                S2 배지·main-landing 하단에도 있다(지우지 않고 이어 적었다)
      결함 둘   (결정 아님 · 제작자에게 이미 갔거나 갈 것) 모바일 반응형 누락
                (`main-landing.html` `@media` 0건 · 375px 에서 가로 스크롤) · 죽은
                마크업(`#s1-side-expert` 하위 5개 요소에 JS 가 값을 쓴 적이 없다)

  **상태: 1단계(expert 서버 실판정 + S2 연결) 착수 대기 · 2단계(실재고 선택 교체)는
  길잡이 요구사항 정의서 제출 → 사장님 설계 확정이 먼저다.** 등재 `decision-log.md` **A-71~A-74** ·
  **U-24~U-27** · U-16 갱신. 흐름 정본 `docs/design/screen-flow.md` 에는 **이번에 측정된
  것만**(S1→S2 가 4갈래고 expert 는 안 닿는다) 정정 표시를 달았다 — 문서 전체 재측정은
  여전히 별도 물결이다(바로 위 「첫 임무 후보」 첫째 항목이 그 대상으로 그대로 남아 있다).

  ⇒ **다음 날 배포까지 끝났다(지우지 않고 이어 적는다)** — 위 「상태: 1단계 착수 대기」는
  이 줄로 낡았다. 2026-08-20 새벽 커밋 `e53e854`로 1단계(서버 실판정 + S2 연결)가 실제로
  지어지고 배포됐다(서버 HEAD 대조 `ead6dd3`) — 검증 사실(쿨러 규칙 4행이 체크 목록에
  뜨고 불통과 2건 재현 · 「모른다」가 「통과」로 새지 않음)은 `decision-log.md`
  **A-71~A-73**에 이어 적힌 「2026-08-20 갱신」 블록에 있다. 2단계(실재고 선택)는 여전히
  정의서 단계다(`docs/design/req/req-expert-part-picker.md`, 길잡이 작성) — 그 정의서가
  남긴 결정 둘이 미정 **U-29**·**U-30**으로 등재됐다.

- **2026-08-20 — 회귀가 실제 상담 원장을 오염시키고 있던 것을 발견하고 막았다.**
  `tests/regression.py`가 두드리는 로컬 API가 배포 서버와 같은 Cloud SQL을 봐서, 회귀를
  돌릴 때마다 `consult_sessions`(6,893건 중 6,732건 · 97.7%)를 비롯한 여러 원장에
  회귀가 만든 행이 실제 데이터처럼 쌓이고 있었다. **이 오염 위에서 낸 고객 행동 통계가
  이미 한 번 사장님께 보고된 뒤에 발견됐다** — 4모드 구조를 재려던 근거가 무효였다는
  뜻이라 미정 **U-28**로 남겼다(「측정 대기」가 아니라 「오염 때문에 애초에 측정 불가
  상태였다」로 구분해 적었다). 차단 처방(헤더 `X-Popcorn-Test` + `.env` 스위치 +
  localhost 이중 게이트, `consult_sessions.data_origin='test'`)은 **지어지고 배포됐다**
  (커밋 `ead6dd3`). **과거에 이미 쌓인 오염분은 이번 처방의 대상이 아니다** — 「확실한
  것만 표식」이 사장님 확정이고 **별도 작업**(DBA 소관 · 드라이런 먼저)이다. 등재
  `decision-log.md` **A-75**(가릴 수 없는 표본까지 함께 적어 뒀다 — guided 436건·
  chat 1,023건은 표식 대상에서 가려낼 수 없다). ⚠ **대시보드·세션 목록·퍼널 화면 셋
  (`api/admin_dashboard.py`·`api/admin_sessions.py`·`api/admin_funnel.py`)은 여전히
  `data_origin`을 안 읽는다** — 이번 처방은 표식을 다는 것까지고, 표식을 읽어 집계에서
  빼는 것은 다음 작업이다. 회귀 스위트 자체도 아직 `/api/expert/build`를 부르지 않아
  그 경로의 게이트는 배포 후 단발 확인 1건으로만 확인된 상태다.

  ⇒ **과거분 표식이 실제로 적용됐다(같은 날 안에 이어졌다 — 지우지 않고 이어
  적는다)** — 2026-08-20, **사장님이 직접 실행**했다. 집행 도구는
  `tools/mark_test_sessions.py`(하네스가 새로 작성 — DBA 가 준비했다던 스크립트는
  실제로는 없었다, Claude Code 자체 안전 분류기가 에이전트의 DB 반영 동작을 막아
  못 만들었다). 이 도구는 대상을 하드코딩하지 않고
  `docs/data-ops/2026-08-20_consult-sessions-test-mark.md` 의 session_id 목록을
  읽고, 문서가 적은 기대 건수와 실제 목록이 다르면 멈춘다 — **문서가 원천**이다.

      패턴 6개 전부   기대 5,296건 = 실제 적용 5,296건 (정확히 일치)
      적용 전후        real 6,900 -> **1,604** · test 20 -> **5,316**
      딸린 하위 원장    (조인으로 따라온 것 · 표식하지 않았다)
                       quote_snapshots 14,554/19,049 · swap_event_logs 392/401 ·
                       handoffs 4/5 · **orders 0/20(무영향 확인됨)**

  ⚠ **「정리됐다」로 읽지 말 것** — 남은 `real` 1,604건이 전부 고객은 아니다. 하네스
  실측 구성: 미판정 1,458(가릴 수 없어 «일부러» 안 찍은 것 — guided 436·chat 1,023) ·
  출처 미상 39(리포 전수 grep에 없음 · 그중 10건이 실제 주문으로 이어졌다) · 잔여
  103(어느 패턴에도 안 걸림 · 아직 안 봤다) · 오늘 검증 5(6948~6952, 위 「이 목록
  뒤에 생긴 검증 세션」과 같은 다섯 건). **진짜 고객 상담의 «최대치»가 6,900에서
  1,604로 좁혀진 것뿐**이고 실제 수는 여전히 모른다 — 「1,604명이 왔다」로 읽으면
  이번 사고를 반복한다.
  ⚠ 위 네 구성 항목을 더하면(1,458+39+103+5=1,605) 총계(1,604)와 1건 어긋난다 —
  **기록자는 이 산식을 스스로 재확인할 DB 접근이 없어, 하네스가 보고한 값을 그대로
  옮겨 적었을 뿐이다.** 다음에 이 수를 근거로 쓰는 사람은 재보고 쓴다.
  ⚠ **아직 효과가 없다** — `consult_sessions.data_origin`을 읽는 코드가 0곳이라
  대시보드·퍼널·상담 세션 목록은 표식 전과 똑같이 전부를 센다(신규 미정 **U-34**).
  ⚠ **아직 안 막힌 구멍** — 헤더 차단은 회귀 스크립트 경로만 덮는다. 사람이 브라우저로
  직접 확인하는 트래픽은 여전히 `real`로 들어온다 — 오늘만 다섯 건(신규 미정
  **U-35**). 상세·되돌림 절차는
  `docs/data-ops/2026-08-20_consult-sessions-test-mark.md`.

- **2026-08-20 — 「내 정보」(ADM-SYS-022)가 커밋·배포까지 끝났다.** 어제(08-19) 「지어졌으나
  미커밋」이던 것이 커밋 셋(`922e7d0` 화면 신설 · `6023cba` 문구 교정 약 90곳 ·
  `d4573d2` 401 구조화)으로 이어져 **서버에 반영됐다**(HEAD `ead6dd3` 대조 완료).
  `docs/design/req/INDEX.md` **0B** 행의 「지어졌으나 미커밋 · 검증 전」은 이 사실로
  낡았다 — 기록자가 오늘 갱신했다(아래 §2 참조). ⚠ **다만 이 화면은 아직 어디서도
  링크되지 않는다** — 상단바 셸(`templates/admin/_admin2_shell.html.j2`)에 아바타
  드롭다운 항목이 없다(기록자 grep 확인 — `my-profile` 참조가 화면 자신의 템플릿·
  백엔드 라우트뿐이고 활성 셸 템플릿에는 0건). 검증은 이미 통과했으니(커밋 메시지
  참조) 「검증 통과 전엔 안 잇는다」(T-02 ②)의 조건은 충족됐다 — **링크를 잇는 일
  자체가 남아 있다.** 화면 파일이라 기록자는 손대지 않는다.

- **2026-08-20 — 길잡이 재검토에서 남은 결함 넷(다음 물결 대상, 결정 아님)**:
  ㉠ **S2 헤더가 375px에서 겹친다** — 배지가 폭 12px로 짜부라져 버튼과 겹친다.
  확인자가 대조해 **이번 물결(견적 AI 재검토) 탓이 아님을 확인**했다 — 평범한 guided
  견적으로도 재현된다. ㉡ **모바일 반응형 누락** — `main-landing.html`은 `@media`
  0건, `s0-landing.html`은 2건뿐이다(기록자 재확인 — 하네스 전달값 "1건"과 달라
  직접 세어 정정한다). ㉢ **chat 모드에 같은 회색 칩 문제가 남아 있다** — `NUDGES`
  (`s1-session.html:461`)의 「성능」 등 회색 칩은 이번 물결에서 **guided만** 고쳐졌다.
  ㉣ **저마진 캡션** — 마진이 5%여도 화면은 「넉넉합니다」라고 말한다. 임계값이 이
  화면에 없어 제작자가 임의로 정하지 않은 상태다(사장님 판단 대기 성격 — 미정 표로
  올리진 않았다, 다음 물결에서 제작자에게 갈 결함 목록).

- **2026-08-20 — Expert 모드가 엔진의 「탐색되지 않은 구석」을 처음으로 드러냈다**
  (결정 아님 · 다음 세션 참고). guided·chat·talk 는 DFS(백트래킹)가 애초에 호환 안
  되는 조합을 탐색에서 제외하므로, 그 실패 경로 코드는 **실사용자에게 노출된 적이
  없었다.** expert 는 고객이 조합을 직접 골라 그 경로를 처음 통과시킨다 — 오늘 함께
  배포된 「None → —」 문구 교정(`api/recommend.py:363-370`, "라디에이터 장착 불통과
  · 3열 ≤ 케이스 **None**열"이 새던 것)이 그렇게 드러난 첫 사례다. **앞으로도 expert
  가 먼저 드러낼 결함이 있을 가능성이 높다.**

  **같은 날 확인된 회귀 커버리지 공백 넷**(결정 아님 · 다음 세션 참고):
  ① `tests/regression.py`의 `[41] 재구축 화면 계약` 검사가 쓰는 `SCREENS` 목록
  (5645행)이 **admin2 41화면 중 4개**(`/admin2/`·`/products`·`/spec-standard`·
  `/build-map`)뿐이다 — 이 물결에서 새로 생긴 화면(허브·내 정보 등)은 이 계약 검사를
  한 번도 안 받았다.
  ② 계측기 라우트 목록(`cdp_sweep.py`, 저장소 밖 스크래치패드)이 41개 중 40개뿐인
  것은 2026-08-19에 이미 적어 둔 그대로다(위 「다음 사람이 볼 것」 참조) — 오늘도
  안 고쳤다.
  ③ 아이콘 검사(`su-icons.js`의 MS 맵 대조)는 **마크업의 리터럴 `ms-` 문자열만**
  잡고, JS 변수로 조립해 주입하는 아이콘 이름은 못 잡는다.
  ④ **`resFail`·`skipOverlayCode`(`templates/admin/my_profile.html.j2:614`, 오늘
  401 구조화(`d4573d2`)가 만든 그 함수)를 회귀가 전혀 건드리지 않는다** —
  `tests/regression.py`의 my-profile 관련 검사는 전부 **구 화면
  `mockups/admin/my-profile.html`**을 대상으로 하고 있어(49·2725·3196·3294·3997·
  4078행), 새 admin2 화면의 프런트엔드 로직은 API 경로가 같다는 이유로 간접 커버될
  뿐 **직접 검사된 적이 없다.**

- **2026-08-20 — 같은 함정에 다시 걸렸다: 서버 재기동을 빠뜨리면 「코드는 맞는데
  실동작은 옛것」이 된다.** 정적 파일(HTML·CSS·JS)은 매 요청 디스크에서 다시
  읽히지만 **파이썬(FastAPI/uvicorn)만은 재기동해야 새 코드를 싣는다** — 오늘도
  확인자가 이걸 놓쳐 헛수고했다. 2026-08-18에 이미 한 번 똑같이 겪고 여기(바로 위
  §「대기 유형 필터」 항목 부근)에 적어 뒀던 경고인데, **같은 세션 묶음 안에서도
  두 번째 겪을 만큼 잊기 쉽다** — 다음 세션은 서버를 새로 띄우거나 재기동 여부부터
  확인하고 검증을 시작한다.

- **2026-08-20 — 인계 직전 검증이 가격만 보고 품절은 안 본다는 것이 드러났다**
  (검증자 실증 · 결정 대기). 「판매중」으로 든 후보 표본 125건 중 2건이 실제로는
  품절이었다(표본 기준 — 그 시점 전체 후보 3,059건에 그대로 대입하면 약 49건
  추정이지만 표본이 작아 폭이 넓다). 원장 컬럼 `handoffs.soldout_cnt`가 있는데
  INSERT 가 항상 리터럴 0이라 이 문제가 화면에 드러난 적이 없었다. 뒷받침 사실 —
  재고값 자체가 오래됐다(마지막 일괄 갱신 2026-08-16 09:41 UTC 이후 자동 동기화
  없음 · `stock_qty`는 사실상 「있었다/없었다」 이진 플래그, 그 시점 후보 3,059건
  중 3,058건이 값 1). 제작자가 "보이게 하는 것"까지 짓고 있고, **품절이면 인계를
  막을지**는 신규 미정 `decision-log.md` **U-32**로 등재했다.

- **2026-08-20 — 사실 기록(결정 아님)**:
  · **가격은 깨끗해졌다** — 표본 125건 중 124건이 몰 실가와 일치, 「우리가 더
    싸게 안내」 0건. 08-11~12 인계에서는 실제로 있었다(i9-14900 -51,500원 ·
    ASUS PRIME Z790 -5,100원). 08-15/16 재적재가 그 문제를 줄인 것으로 보이나
    실 트래픽 표본은 2건뿐이라 단정하지 않는다.
  · **인계 5건 중 신뢰할 것은 1건**이다(위 A-75 관련 단락의 「신뢰할 수 있는
    인계는 1건뿐」과 같은 수치) — 나머지 4건은 회귀 오염으로 화면이 낼 수 없는
    값을 갖고 있다.
  · **고객 인증 실측**(확인자) — 이메일만 알면 그 사람 세션을 받고, 없는 이메일은
    그 자리에서 회원이 생성된다(`CLAUDE.md` §베타 배포 상태에 이미 적힌 dev
    어댑터 구멍과 같은 것 — 오늘 노출 범위를 다시 쟀다). **읽기 노출**은 주문·
    결제·배송·후기 대상이다. **이름·연락처·주소는 `/api/my/*`로 안 샌다**
    (관리자 경로만 읽는다 — 노출 범위가 그 서술보다 좁다). **쓰기(남의 이름으로
    후기·환불 접수)는 코드상 가능**하다 — 소유권 검사 자체는 정상인데 세션이
    피해자 것이라 정당하게 통과한다. 회원 중 2명은 실제 사람일 가능성이 있어
    확인자가 건드리지 않았다.
  · **원장 표식은 2026-08-20 실제로 적용됐다** — 상세는 위 §「회귀가 실제 상담
    원장을 오염시키고 있던 것을 발견하고 막았다」의 이어 적은 단락과
    `docs/data-ops/2026-08-20_consult-sessions-test-mark.md`.

- **2026-08-20 — 기록자가 오늘 등재한 것**: `decision-log.md` **A-76**(안정성
  테스트 3시간 — 2026-08-17 확정인데 결정 로그에 누락, 문안검사 발견 · 근거
  `api/orders.py:30`·`api/handoff.py:39~43`) · **T-16**(그 누락의 교훈 — 코드
  주석만으로는 결정 로그가 모른다) · **A-77**(인계 후에도 조립·출고·A/S 는
  팝콘PC 소관 — 2026-08-11 결정에 이어 적음, 근거 `s5-complete.html:216-217`) ·
  **A-78**(오픈 전 문구 정리 범위 「틀린 것만 먼저」) · **U-32~U-35**(품절 인계
  누락 · 근거 없는 배지·후기 문구 · 표식을 읽는 화면 없음 · 사람 확인 트래픽
  미차단) · **U-16 갱신**(「24시간」 항목 제외, 「왕복 택배비」 소관을 A-77 로
  분리). 안전판 문서 `docs/data-ops/2026-08-20_consult-sessions-test-mark.md`
  (DBA 작성 · 오늘 실행 결과를 채웠다)도 함께 커밋했다.

- **2026-08-20 — 전력 여유 판정은 짓지 않기로 확정됐다(같은 날 안에 이어졌다).**
  사장님이 처음엔 「서버가 판정하게 한다」로 확정했는데, 제작자가 착수 전 조사에서
  ① 근거 있는 임계값이 저장소 어디에도 없고(실제 기준은 이미 있는 `power` 규칙과
  같은 100% 경계 하나뿐) ② 그 규칙을 확장하면 회귀 불변식 네 곳이 깨지고
  ③ 실측(게임 표본 8건 중 5건이 정확히 100%)이 "엔진이 PSU 요구치를 딱 맞추는
  최저가를 고른다"는 구조적 사실을 보여줘 임계값만 정해서는 안 풀린다는 것을
  찾았다. 사장님이 그 보고로 「지금대로 둔다 — 오픈 뒤로」로 바꿨다 — 화면은
  「확인 완료」로 중립하게 계속 말한다(거짓말이 아니다). **하네스가
  `docs/design/req/req-compat-rules.md` §비범위를 안 읽고 지시서를 썼다는 것도
  함께 드러났다** — 그 문서가 "여유율% 같은 파라미터 개념 자체가 없다"고 이미
  경고하고 있었다. 등재 `decision-log.md` **A-79**(경위 전문) · 미정 **U-36**
  (임계값 자체 + 정해지면 회귀 넷을 같은 물결에서 고쳐야 한다는 조건).

- **지금 하던 일(2026-08-20, 기록자 갱신 ③) — 오픈 전 정직화 물결, 커밋·푸시
  완료.** 위 A-76~A-79·U-32~U-36 등재 뒤로 제작팀이 손본 여러 API·고객 목업
  파일이 확인자 회귀 전건 통과(896/896)를 받고 물결별로 나뉘어 커밋됐다(T-02
  ①·주제별 분리 — 품절 인계 표시(U-32) · 후보 카운터 배분 재시도 · 고객 쓰기
  잠금 신설 · MY 화면군 실데이터화 · chat·talk 정직화 · Expert 주석 · 화면 계약
  · 반응형 · 문구 정직화 두 건). **무엇이 어느 커밋에 있는지는 `git log
  --oneline`이 원천이다** — 여기 다시 나열하지 않는다(수·목록을 이 파일에
  박으면 다음 물결에서 낡는다).

  **Expert 경계 사실 확인이 끝났다.** A-71(2026-08-19 확정)의 "1단계는 예시
  21개를 그대로 유지한다"와 실제 배포 코드(`GET /api/expert/slots`가 이미
  실재고 표본을 낸다)가 어긋나 있던 것을 등재했다(`decision-log.md` **A-71
  갱신 ②**). 조사자 실측으로 **이미 배포돼 고객에게 노출 중이지만 실제 고객
  사용은 0건**(유일한 `real` 세션은 제작자의 빌드 확인)이라는 것까지 확정했다 —
  되돌리든 유지하든 지금은 전환 비용이 거의 없다는 판단 재료를 남겼다.
  U-29·U-30(2단계 결정 항목)에도 전제가 갈렸다는 표시를 남겼다. **사장님 확인
  대기**이고 기록자는 임의로 어느 쪽으로도 정하지 않았다.

  **오염 차단의 남은 구멍(U-35)에는 새 번호를 달지 않았다.** 같은 사실(사람이
  손으로 확인하는 트래픽 다섯 건이 여전히 `real`로 잡힌다)이 이미 U-35로
  등재돼 있어(2026-08-20 앞선 등재), 처리 전례(과거분 표식은 DBA 준비 + 사장님
  직접 실행)만 그 항목에 보탰다 — 중복 ID를 만들 뻔한 것을 결정 로그의 기존
  항목 대조로 피했다.

  **서버가 로컬보다 뒤처져 있었다(마지막 확인 시점).** 서버 HEAD 는 오늘 새벽
  A-75 오염 차단 커밋까지만 갖고 있었고, 로컬 HEAD 는 그보다 여러 커밋 앞서
  있었다. 오늘 배포는 그 뒤처진 분과 오늘 커밋분을 함께 올리게 된다 —
  **서버=로컬로 단정하지 않는다**, 배포 뒤 `scripts/verify_deploy.py`로
  다시 잰다.

  **`main-landing.html` 의 `data-screen-id` 부채(`screen-flow.md` §⑥, 2026-08-12
  부터 추적)는 오늘 해소됐다.** 처음엔 "회귀 영향을 먼저 재고 다음 물결에서
  처리한다"고 미뤘는데(그 판단의 근거였던 "회귀·`state.py`가 이 속성으로 화면
  수를 센다"는 애초에 **고객(mvp1) 화면에는 적용되지 않았다** — `state.py`의
  `data-screen-id` 필터링은 `mockups/admin/*.html` 동결 화면 전용이고, mvp1
  집계·`tests/regression.py`의 mvp1 순회는 속성과 무관하게 파일 수만 센다,
  기록자 직접 확인), 미루는 진짜 이유는 파일이 다른 작업으로 미커밋 상태라는
  것뿐이었다. 제작자가 확인자 회귀 통과(896/896)와 함께 그 파일에
  `data-screen-id="FR-LND-030" data-domain="landing"` 을 붙였다(`FR-LND-020` 은
  인증 모달용으로 이미 예약돼 있어 030 을 씀, 중복 검사 통과) — 반응형 수정과는
  다른 결이라 커밋을 갈랐다. `docs/design/screen-flow.md` §⑥ 항목 ③도 이 사실에
  맞게 고쳐야 하는데 그 문서는 기록자 담당 밖이다 — 다음에 손대는 사람이 갱신한다.

- **지금 하던 일(2026-08-21, 기록자 갱신) — 밤사이 셋 커밋·푸시(사장님 취침 중,
  배포는 하네스 몫으로 남김).** ① `GET /` 신설 — 404였던 것을 `/mvp1/main-landing.html`
  307 리다이렉트로 고쳤다(지인 오픈 도메인 `www.popcornai.co.kr`의 첫 화면). ②
  「오늘」 KST 기준 교정 — `scripts/status.py`·`api/admin_dashboard.py`·
  `api/admin_sessions.py`·`api/admin_sourcing.py` 넷이 UTC `CURRENT_DATE`를
  써 KST 09:00 이전엔 「오늘」이 어제 구간과 섞이고 있었다(실측: 상담
  `CURRENT_DATE` 술어 4건 vs KST 반개구간 0건). `api/timeutil.kst_day_range()`로
  통일했다 — 회귀 `[4834]`가 왜 이 결함을 못 잡았는지(동등성 검사가 「같은
  잘못을 공유하는 두 값」을 구분 못함)를 `CLAUDE.md` §회귀 세트 네 번째 사례로
  남겼다(기록자 초안 — **하네스 확정 필요**, T-02 ③). ③ 텔레그램 일일 현황은
  서버가 아니라 로컬 PC 작업 스케줄러로 방향이 바뀌었다(사장님: "토큰을 서버에
  안 넣습니다") — 준비 문서 `deploy/telegram-daily-status.md` 신규, 등록은
  하네스가 이미 함(`PopcornAI-DailyStatus`, 매일 09:30 KST).

  **결정 아님, 다음 세션 참고할 것 둘**(2026-08-21 밤사이 조사에서 함께 드러남):
  `api/admin_sourcing.py`의 `_confirmed_at_ready()` 독스트링이 "마이그레이션
  0053이 DB에 미적용"이라 적고 있는데, 제작자가 `information_schema`로 확인한
  결과 `confirmed_at` 컬럼이 **이미 있다** — 문서만 낡았다(코드는 이번 물결에서
  안 고쳤다, 이 작업과는 다른 결함이다). `api/admin_funnel.py`의 응답 필드
  `history_bounds.sessions_min`은 여전히 죽은 필드다(2026-08-20 A-81 항목에
  이미 기록 — 오늘 재확인만 됐다, 새 사실 아님).

- **결정 아님, 다음 세션 참고 — `admin2.css`의 「768px 잔존 2줄」 주석이 낡았다**
  (2026-08-21, 아바타 링크화 검증 중 제작자 발견). 제작자가 아바타를 `<a>`↔`<span>`
  으로 맞바꿔가며 헤더 높이를 대조해 이번 변경과 무관함을 확인하는 과정에서,
  `/admin2/consult-sessions`·`/admin2/funnel-performance` 가 768px에서 **이미**
  2줄이었다는 것이 드러났다(두 경우 다 `<a>`·`<span>` 높이 diff 0px — 오늘 커밋
  탓이 아니다). 그런데 `mockups/shared/admin2/admin2.css`의 2026-08-19 스윕
  주석은 "잔존 2줄은 mall-sync·ops-settings 둘뿐"이라 적고 있다 — **실제로는
  넷이다.** 원인(각 화면 `header_extra`)은 특정되지 않았다 — 지어내지 않는다.
  `admin2.css`는 전 화면이 상속하는 디자인 토큰 정본이라 이 기록자 세션은
  손대지 않았다(주석 한 줄도 정본이라는 판단) — 다음에 그 파일을 만지는 사람이
  주석을 실측과 맞추고, 원인도 그때 조사한다. 확인법: `grep -n "잔존"
  mockups/shared/admin2/admin2.css`(줄번호는 안 박는다 — 오늘도 여러 곳이
  같은 이유로 낡았다).

- **결정 아님, 다음 세션 참고 — certbot 이 서버 설정을 고친 뒤 리포로 되가져오는
  절차가 문서에 없다**(2026-08-21, www·apex 도메인 인증서 발급 준비 중 제작자
  발견 — `deploy/README.md` 전문과 `certbot` 전수 grep으로 확인). README §7
  「설정을 바꾸는 절차」는 **로컬→서버 방향만** 다룬다. 유일한 관련 기록은
  `deploy/nginx-popcorn.conf` 머리말(5~8행 부근)인데, 그건 2026-07-30 사고
  (certbot이 `auth_basic`을 지워 사이트가 이틀 공개됐던 사고) 뒤 슬라이스 99가
  **손으로 옮겨 적은 사실**일 뿐 절차서가 아니다 — "이런 사고가 있었다"는 기록과
  "이번엔 이렇게 하면 된다"는 절차는 다르다.

  **이번엔 그 공백을 우회했다**(README에 새 절차를 만들지 않았다 — 지시가 아니었고
  이 세션도 만들지 않는다): certbot을 `--nginx`(설정을 고침) 대신 `certonly
  --webroot`(설정을 안 건드림)로 돌리기로 했다. 서버 설정을 안 건드리면 되가져올
  것 자체가 없다 — 공백을 메우는 대신 이번 상황에서 그 공백에 안 걸리는 경로를
  골랐다는 뜻이다. **README §7 은 안 고쳤다** — 로컬→서버 절차 문서에 "certbot이
  거꾸로 고칠 때"를 끼워 넣는 것은 절차 신설에 해당한다고 판단해 손대지 않았다.
  다음에 `--nginx`(설정을 고치는 경로)를 다시 써야 하는 상황이 오면, 그때 이 공백을
  실제로 메울지 사장님·하네스가 판단한다.

- **결정 아님, 다음 세션 참고 — 세 가지(2026-08-21, 사장님 요청·길잡이 반응형
  물결 커밋 과정에서 함께 드러남)**

  ① **`f4b36af`(2026-08-20, "고객 화면 반응형 375~820px")가 고친 것은 세 파일
  (main-landing·s0-landing·s2-result)뿐이었다** — 그 커밋 메시지 자체는 이미
  파일 셋을 정확히 적고 있었다(기록자 재확인). 그런데 S3(s3-detail.html)·
  S4(s4-cart.html)는 애초에 대상이 아니었던 채로 하루를 넘겼고, 오늘에야
  길잡이가 375px에서 S3 811px·S4 767px(기기 폭의 2배)를 재발견해 별도 커밋
  (`47d1e19`)으로 확장했다. **앞으로 "반응형을 했다"고 보고할 땐 파일 목록을
  함께 적는다** — "반응형 했다"만 남으면 다음 사람이 전체로 오해한다.

  ② **A-07("최저가" 카피 금지)을 검사하는 회귀 항목이 없다.** `tests/regression.py`
  전수 grep 결과 "최저가" 매치 2건은 전부 엔진 내부 "가성비 = 예산과 무관한
  최저가 조합" 로직 주석이고 화면 카피 검사가 아니다 — 2026-07-02 확정 이후
  지금까지 자동으로 지켜지지 않고 있었다(오늘 `index.html`의 위반을 계약자가
  손으로 잡아 `0770dd9`로 고침). 회귀에 이 검사를 추가할지는 이 세션 담당 밖이라
  적지만 않는다.

  ③ **`s3-detail.html:227`의 닫기 버튼도 같은 문제일 가능성이 높다 — 미확인,
  고치지 않았다.** `font-family:'Material Symbols Outlined'`를 쓰는데(코드로
  직접 확인) 오늘 main-landing 팝업 조사에서 이 폰트가 이 저장소 어디에도
  로드되지 않는다는 것이 canvas measureText 대조로 증명됐다 — 같은 패턴이면
  아이콘이 아니라 글자 "close"가 뜬다. 제작자는 담당 밖이라 값도 확인하지
  않았다(기본 숨김 드로어 안이라 상태를 바꿔야 보인다). **⚠ 확인자가 앞서
  "s3의 닫기는 회색으로 제대로 렌더된다"고 보고한 것과 배치되지 않는다** —
  그 보고는 색만 본 것이고 모양(글자냐 아이콘이냐)은 안 봤다. 같은 사실을
  두 사람이 다르게 본 것이 아니라 한 사람이 절반만 본 것이다. 담당이 정해지지
  않았으니 고치지 않는다 — 다음에 이 버튼을 여는 사람이 실제 렌더를 확인한다.

- **지금 하던 일(2026-08-21, 기록자 갱신 ③) — swap 500·확인 보류 오표시·admin2
  브랜드 물결, 커밋 완료(푸시는 하네스 몫으로 남김).** 다섯 성격이 다른 변경을
  주제별로 나눠 커밋했다(근거: CANON "무관한 셋을 한 커밋에 묶지 마라" — 백엔드
  가용성 결함 / 프런트 정직성 결함 / 자산 결측 / 브랜드 코드 / 브랜드 문서가 각각
  되돌림 단위가 달라야 한다고 판단):

      a0f8055  swap candidates/apply — 부품 미선택 슬롯이 있으면 500이 나던 것을 고친다
      b751870  S3·S4 — 호환 판정 「확인 보류」를 「통과」로 그리던 결함을 고친다
      92d0d19  아이콘 5종 결측 해소
      2f19716  admin2 셸 — 브랜드 「팝콘 AI」 확정 반영 + 화면ID 배지 제거
      993853a  CLAUDE.md — admin2 LNB 브랜드 표시를 「팝콘 AI」/「팝콘」으로 정정

  **무엇이 있었나** — 오늘 사장님 요청으로 S1 부품 선택란 기본값이 「선택 안
  함」이 되면서 `api/swap.py`가 빈 슬롯에서 KeyError·AttributeError로 500을
  내고 있었다(거의 모든 고객이 S3에서 기능 정지 — 상세·재현 세션 번호는 커밋
  `a0f8055`). 같은 원인으로 S3·S4가 서버의 3값 판정(true/false/null) 중 null
  (확인 보류)을 통과로 그리고 있던 것도 함께 드러나 고쳤다(`b751870`) — 배너·
  카드 색이 상태를 따라가는 것(빨강/주황/초록)은 사장님 2026-08-21 확정.
  admin2 브랜드는 사장님이 2026-08-13 확정(`admin2`/`a2`)을 같은 날(08-21)
  두 번 뒤집어 지금 값(펼침 「팝콘 AI」/접힘 「팝콘」)이 됐다 — 경위 전문은
  `decision-log.md` **A-82**(같은 물결, 바로 다음 커밋에 있다).

  **⚠ 다음 세션이 속을 자리 — 둘, 커밋 메시지에도 있다(중복 기록)**
  ① **`X-Popcorn-Test` 헤더가 swap 경로에서는 아무 효과가 없다.** `api/swap.py`에
  그 헤더·`data_origin` 참조가 0건이고, `quote_snapshots`·`swap_event_logs`에는
  애초에 `data_origin` 컬럼이 없다(0033 마이그레이션이 `consult_sessions`에만
  두고 나머지는 파생시키기로 설계함). 오늘 검증이 안전했던 것은 세션이 애초에
  test로 표시된 채 만들어졌기 때문이지, 이 헤더가 swap 호출을 보호해서가
  아니다. **실 고객 세션에 이 헤더를 얹어 `/api/swap/apply`를 불러도 원장이
  보호되지 않는다** — 다음에 이 경로를 회귀 트래픽 격리(A-75) 대상에 넣을지는
  사장님 판단이 필요하다(아직 미정 항목으로 등재하지 않았다 — 등재 여부 자체가
  판단이라 임의로 만들지 않았다).
  ② **회귀 896/896 통과는 오늘 swap 500 수정의 증거가 아니다.** `tests/regression.py`
  의 부품 교체 섹션이 쓰는 세션은 전부 `/api/recommend`가 만든, 처음부터
  8슬롯이 찬 것뿐이다 — "빈 슬롯이 남은 채 교체"는 회귀에 없다. 다음에 이
  영역을 다시 만지는 사람은 회귀 통과를 그 시나리오의 증거로 읽지 말 것.

  **⚠ 미해결 셋, 사장님 판단 대기 — decision-log U-40~U-42로 새로 등재**(임의로
  채우지 않고 맥락·선택지만 적었다):
  · U-40 — S4 부품 0개인데 검증 카드(「호환 9종 중 9종 확인 보류」+ 칩 9개)가
    「선택된 부품이 없습니다」와 같은 화면에 동시에 뜬다. 금액은 안 지어내지만
    판정 상태를 지어낸다.
  · U-41 — S4 검증 카드 안 혜택 3행(전문가 조립·당일 출고·A/S보증)이 고정
    색이라 카드가 빨강이어도 초록 그대로다.
  · U-42 — 관리자 브라우저 탭 제목이 「팝콘PC 관리자」로 남아 있다 — 사이드바는
    「팝콘 AI」(A-82)다. A-82 확정은 `<title>` 태그를 다루지 않았다.

  **⚠ 미해결 — 작은 것, 결정 로그에는 안 올렸다(선택지가 갈리는 판단이 아니라
  그냥 안 고친 것이라서)**: `s3-detail.html:662`의 구어체 「준비 중이에요」
  (CANON §2-7 위반, 담당 밖) · `_admin2_shell.html.j2`·`login.html.j2`의
  `<title>팝콘PC 관리자`(→ U-42로 승격했으니 위 항목 참조, 여기 중복 등재
  안 함) · MCP 브라우저 패널이 `data:` URL에 고정돼 이번 확인자가 못 썼다
  (Playwright로 우회— §3 「로컬 브라우저 자동화 도구」 절 참고, 재현 스크립트는
  여전히 리포 밖).

  **검증(하네스가 모은 팀원 보고 — 기록자는 DB·브라우저에 안 닿는 역할이라
  재실행하지 않았다. 되짚는 법은 각 커밋 메시지에 있다)**: 회귀 896/896(위 ②
  주의사항과 함께 읽을 것) · swap candidates 5세션 200 · apply 실호출 200 ·
  S3/S4 각 4세션 × 3폭 실측 · 배너 3색 실측값(S3 `rgb(192,57,43)`/
  `rgb(153,92,0)`/`rgb(28,122,77)` · S4 `#fbecec`/`#fbf3e2`) · 브랜드 접기
  단추 실클릭 확인 · 아이콘 404=0 · 가로 넘침 0(375·768·1440) · 오프라인
  전환 Playwright 재현.

- **오늘 수치는 여기 적지 않는다** — 위 머리말의 「어디서 보나」 표를 쓴다.

- **지금 하던 일(2026-08-22, 기록자 갱신) — S1 물음 구조 재설계 방향 확정 + 결함 셋
  수정·배포.** 오늘 사장님과의 대화로 S1(상담 세션) 재설계 방향이 크게 갈렸고, 같은
  날 별도로 예산 파서·조건 칩·쿨러 TDP 매핑 결함 셋이 고쳐져 배포까지 끝났다.
  **아래에서 "정한 것"과 "지은 것"을 나눠 적는다 — 섞어 읽으면 다음 세션이 카드
  구조·문답 개편이 이미 화면에 있다고 오해한다. 카드 구조·문답 없애기·용도 갈래·
  CPU 하한은 전부 착수 전이다.**

  **정한 것(결정만 — 화면은 안 바뀌었다).** 확정 11건은 번호·제목만 적는다, 상세는
  `decision-log.md`(§7 규약 — 같은 사실을 두 곳에 적지 않는다):

      A-83  S1 대화 설계 원칙 — LLM 역할 재확인 · 유도 질문 금지 · 대화 범위 확장
      A-84  S1 진입 구조 — 「간단하게 / 자세하게」 두 길로 가른다
      A-85  게임 목록·게이밍 사양 산정 규칙 — ⚠ 아래 참조, 같은 날 보류로 뒤집혔다
      A-86  엔진 조건 확장 + 후보 풀 확장을 함께 간다
      A-87  품절 재고는 단종 확정 — 재입고를 기다리지 않는다
      A-88  오래된 등록 데이터 정리 — 방향만 확정, 실행은 축 신설부터(보류)
      A-89  윈셀링 OPEN API는 사양 데이터원에서 제외한다
      A-90  화면이 「조건 수집기」로 답하지 않는다
      A-91  미리 만든 조건 버튼 5종을 없앤다 — 오늘 실제로 지어진 것 중 하나(아래 참조)
      A-92  용도를 사양·예산 범위가 보이는 카드로 좁힌다
      A-93  예산 범위 계산을 이력으로 쌓는다

  **⚠ A-85는 같은 날 확정된 뒤 같은 세션 안에서 보류로 뒤집혔다.** 게임 목록 다중
  선택·최고사양 기준·표 우선 조회 다섯 항목이 사장님 확정을 받았는데, 실제 화면
  시연을 본 사장님이 "너무 짜여진 각본 같아서 요즘의 LLM 구조와 안 맞는다"고 판단해
  같은 세션에서 보류로 돌렸다. **이 목록만 보고 "게임 카드 시나리오가 확정됐다"로
  읽지 말 것** — 지금 살아 있는 것은 A-90~A-93(물음 구조가 지켜야 할 원칙 넷)이지
  구체적 게임 시나리오가 아니다. 상세·경위는 `decision-log.md` A-85 본문(같은 날
  두 번 이어 적힘).

  **미정 11건**도 번호·제목만:

      U-43  「자주 하는 게임」 응답의 용도
      U-44  두 길(간단하게/자세하게)의 게임 선택 축
      U-45  대화 개방 범위의 경계
      U-46  게임 직접 입력 단계 포함 여부
      U-47  「단종」 상태값 부재
      U-48  다른 용도의 갈래를 무엇으로 나눌지
      U-49  하위 갈래가 실제로 사양을 다르게 걸어야 한다는 제약을 어떻게 지킬지
      U-50  용도 갈래 확장이 `usage_floors`에 행만 늘리면 되는지
      U-51  예산 이력을 담을 표 설계
      U-52  무관한 금액 언급이 예산을 덮어쓴다 — 화면이 AI 판정과 무관하게 정규식
            결과를 병합한다(오늘 배포는 진행, 고치는 방법은 미정 — 아래 참조)
      U-53  「예산 20만원 올리기」 버튼 수정이 도달 불가능한 코드에 적용됐다 — 죽은
            코드인지 되살릴지 미정

  **지은 것(커밋·배포까지 끝남) — "정한 것"과는 별개로 결함 셋만 고쳤다:**

      99eb7d2  api/candidates.py — 예산 상한 정규식이 2~3자리로 못박혀 있던 결함
      0fc601a  s1-session.html — 예산 파서 네 곳(talkBudget·budgetSaid·완화 버튼·
               liveParse)의 2~4자리 상한 제거(위 결함과 짝)
      5f4d97d  s1-session.html — 미리 만든 조건 버튼 5종 제거(A-91)
      0123553  api/danawa.py — 쿨러 TDP 매핑 누락 해소
      532ef6e  decision-log.md·req-customer-s1-session.md — 위 확정·미정 등재 +
               S1 첫 요구사항 정의서 신설

  **배포(하네스 실행·보고 — 이 기록자 세션은 서버·DB에 안 닿는 역할이라 재실행하지
  않았다)**: `f5887c4..532ef6e`를 `origin/main`에 푸시한 뒤 `gcloud compute ssh
  popcorn-app --zone asia-northeast3-b`(좌표는 §3 인프라 참조)로 서버에서 `git
  pull` + `pip install` + `alembic upgrade head`(스키마 변경 없음) + 재기동까지
  한 줄로 실행했다고 보고받았다. `systemctl is-active` = `active` ·
  `scripts/verify_deploy.py --since HEAD~5` = 어긋난 것 없음(화면 파일 해시
  로컬=서버 · 운영 스위치 5종 일치 · 경로 6개 정상) · 회귀 전건 통과·실패 0.

  **화면 실측(기록자가 직접 grep으로 재확인 — 재현 가능)**: 없앤 조건 칩 5종은
  서버 응답에 없다. 「화이트로」가 코드에 2곳 남아 있는데 둘 다 칩이 아니다 — TIP
  안내 문구(`mockups/mvp1/s1-session.html:150`)와 값 합집합 규칙을 설명하는 코드
  주석(`:1569`)이다 — 의도적으로 남긴 것이다. 확인법: `grep -n "화이트로"
  mockups/mvp1/s1-session.html`.

  **오늘 실측으로 드러난 사실(수치 없이, A-86·A-87 본문 참조)**: 지금 후보를 가장
  크게 깎는 것은 품절이다. 재고 0인 상품 중 나머지 게이트를 다 충족하는 큰 덩어리는
  「곧 들어올 품절」이 아니라 **단종**으로 확정됐다(A-87). 엔진이 더 받을 수 있는
  조건을 넓혀도, 지금 실제 병목은 **CPU와 쿨러 쪽 사양 결측**이다.

  **다음 사람이 이어서 할 것(이번 세션에서 실제로 남은 것만 — 지어내지 않는다)**:

      ① 게임 3갈래 GPU 문턱값 확정 — 하네스가 550/600/700을 제안했고 사장님이
         "맞아"라고 답했으나, 그 값 자체의 확정인지 방향에 대한 동의인지는 안
         갈렸다(하네스 판단 필요)
      ② CPU 하한 축 선택 — 가격이냐 TDP냐. TDP로 가면 `api/candidates.py` 두
         곳을 함께 고쳐야 카운터가 안 깨진다
      ③ 다른 용도 갈래(U-48)
      ④ 예산 이력 표 설계(U-51)
      ⑤ 다나와 제안 실제 적재 — 코드는 됐고(오늘 커밋 `0123553`) 도구 실행은 안
         됐다. `tools/danawa_fetch.py`는 DB 쓰기라 별도 승인 소관

  ~~**막힌 것에 한 줄 추가** — `api/talk.py`의 `_BUDGET_NUM`이 아직 옛 패턴이라 다섯
  자리 이상 예산이 서버에서 잘린다(담당자 미배정).~~ **→ 같은 날 해소됐다(아래).**

  **그 뒤에 지은 것(2026-08-22 늦은 오후 · 커밋됨, 배포는 아직)**:

      c91b9d1  api/talk.py·s1-session.html — 예산 파서 **마지막 구멍**을 메우고
               금액 표기를 천단위 콤마로 바꿨다(**A-94** 사장님 지시).
               위 「막힌 것 한 줄」이 이것이다 — `99eb7d2`가 "남은 구멍"으로
               명시했던 자리다. 옛 패턴은 「12,000만」을 「000만원」으로 읽어
               **상한이 0원**이 됐고, 후보 0개라 고객에게는 "조건에 맞는 부품이
               없습니다"로 보였다(`api/candidates.py`의 「3000만원 -> 0원」과 같은
               병리). 서버만 고치면 한 화면에 같은 예산이 두 모양으로 보여
               화면 8곳(`commaNum`)을 함께 맞췄다.

               검증은 **회귀가 아니라 실행 경로로** 했다 — 회귀는 LLM을 부르지 않아
               이 경로를 지나지 않는다(검증은 못 하고 원장 오염만 남는다).
               정규식 폴백 경로(LLM 미호출)와 서버 AI 경로(사장님 승인 아래 실호출 ·
               `stored:false`라 원장 미적재) 양쪽에서 「1,500만원」을 확인했고,
               콤마가 붙은 값을 화면이 다시 읽어 범위 판정까지 정확했다.

---

## 1. 어디까지 왔나 — 슬라이스 지도

전체 이력은 `git log`가 원천이다(슬라이스마다 원인·고친 것·검증을 커밋 메시지에 남긴다).
여기서는 **덩어리**만 적는다.

| 구간 | 무엇을 했나 |
|------|-------------|
| 1~18 | 목업 → DB 스키마(Cloud SQL, 테이블 46+뷰 2) → 첫 수직 슬라이스 |
| 19~36 | 관리자 화면 실배선 · 4중 게이트 확정 · 원장/되돌림 규약 · T1~T10 트랜잭션 |
| 37~38 | **인증 도입** — 관리자(승인 게이트·권한 3등급) / 고객(세션 경계) |
| 39~43 | **실카탈로그 적재**(22,838건) · 엔진 탐색 최적화 · 호환 규칙 8종 |
| 44~46 | 운영 전환 스위치 실배선 · S3가 조용히 깨져 있던 것 발견 |
| 47~49 | **화면 정직성** — S1이 후보 수를 지어내던 것 제거, 견적 가능 여부를 서버에서 받는다 |
| 50~52 | 업로드 적재(드라이런) · 검수 깔때기 · 다나와 제안 543건 승인 |
| 53~57 | 상품 관리 전면(단건 상세·수정·등록·중복 판정) · **사양 항목을 화면에서 늘린다** |
| 58~60 | **용도 하한** — 화면이 한 약속을 서버가 지킨다 · 첫 화면 정직성 |
| 61~71 | 운영자 작업 패널 · 검수 UX · 분류 변경 · 중복 편입 · **관리자 비밀번호 인증** |
| 73~78 | 비밀번호 발급 · 마진 정책 · 용도 하한 관리 화면 · 운영자 화면 전수 검증 |
| 81~86 | **빈 상태에서 시작하는 길 관통** · 수랭 규칙 · 스펙 관리 부품 축 · 페이징 사고 |
| 87~88 | 붙여넣기 사고 — 증상 1건 수정 후 **마크업 파싱으로 전수 감사**(회귀에 고정) |
| 89~90 | 문서 셋 역할 분리(중복이 stale의 원인) · **서버 역할 확정 I-01**(승격 아니라 신설) |
| 91~94 | HTTPS 설정 · **내 정보 화면**(죽은 링크 30개) · **공급처 관리**(빈 상태에서 막혀 있던 길) · **대시보드 운영 시작 체크리스트** |
| 95~98 | **상품 게이트 체크리스트**(막힌 이유를 전부 한 번에) · `사양 항목 정의` 이름 정정 · **검수 연속 입력** · **재고 = 원장 합 관통**(기초 재고) |
| 99 | nginx 설정을 서버 실제와 맞추고 **고객 경로만 404 차단** |
| 100 | **내 정보 화면을 사실에 맞춘다** — 시각이 9시간 어긋나 보이던 것(서울시각 렌더 모듈) · 세션 21개인데 끊을 수단이 없던 것 · 비밀번호가 화면에 그대로 보이던 것 · 빈 값에 이유를 적는 것 |
| 101 | **세 화면이 스타일 없이 이틀 돌았다** — 템플릿에만 있는 CSS를 참조해 404(브라우저는 조용히 넘어간다). 셸까지 입히고 **Phoenix 원본 지도**(`docs/design/phoenix-template-map.md`)를 만들어 재발을 막았다 |
| 102 | **승인 화면이 "본인 확인을 마친 신청"이라 적고 있었다** — 확인한 적이 없다(dev 어댑터). 승인 판단의 근거를 서버가 실제 상태에서 파생하게 바꾸고, 승인자 열의 `—` 하나가 덮고 있던 세 사실을 갈랐다 |
| 103 | 이 문서 갱신 — 그 과정에서 **문서의 수 셋이 실제와 어긋난 것**을 찾아 고치고, 다시 낡지 않게 **회귀가 문서와 실제를 대조**하게 했다(`[31] 문서 정합`) |
| 104~ | **카테고리 축 신설**(0027~0030) — 판매 분류 트리·상품 매핑·판매가 재산정·**마진 정책을 트리에**(A-29). 기존 임대 관리자(윈윈소프트)와 도매꾹 상품공급사센터를 조사해 물려받지 않을 것을 먼저 정했다(`docs/research/`) |
| ↑ | **Phoenix 템플릿 실사용** — 사용자 지적("라이선스 산 UI를 전혀 못 쓰고 있다")으로 treeview·bulk-select·data-list·echarts·choices를 실제로 붙이고 공용 헬퍼 2개(`ui-list.js`·`ui-choices.js`)로 모았다 |
| ↑ | **지어낸 것을 걷어냈다** — 더미 폴백 행(37화면) · 유령 List 예외(36화면) · RTL 404(36화면×2) · 외부 아이콘 CDN(37화면) |
| ↑ | **단일 원천 정리 4건** — 부품 어휘(쿨러 표시 축 A-28) · **좌측 메뉴**(36화면 복제 672KB → 8KB 정본, 끊겨 있던 길 2개 복구) · **디자인 토큰**(tokens.css를 아무도 안 읽고 있었다) · **폰트 자체 호스팅**(외부 CDN 0건, A-30) |
| ↑ | **완성품 축**(0031) — 완제품 318·베어본 104를 `part_type` 으로 옮겼다(A-31). 엔진 영향 0 |
| ↑ | **전 프로세스 관통 점검** — 상품·공급처·카테고리·마진·가격·재산정(7,239건 반영→되돌리기)·검수·주문·조회 20종을 실제 데이터로 등록→수정→삭제→되돌리기까지 97건 확인 |
| **8/10** | **A-38 운영 시스템의 실제 축을 그대로 가져간다**(사용자 확정 5건 · 옛 번호 A-11 — 2026-08-14 중복 해소) · 견적 표시용 상품명을 파생으로 걷는다(판매조건 꼬리) · 이름 없는 부품을 검수로 회부 |
| **8/10~11** | **조립 보증 표준을 `std` 스키마에 세우고 실물로 검증**(P-08) — 호환 관계 22쌍 · 부품별 사양 표준. 완제품 208종 구성과 제조사 자료로 대조했다. **제조사 확인 3건**: NZXT Kraken 360 은 sTR5 미지원(그 조합은 조립 불가) · RTX 5090 은 1000W 필요 · Thermalright PA 120 SE 는 LGA1700/1851 지원. 그리고 **`소켓1700` 표기 때문에 LGA1700 이 쿨러 234개에서 빠져 있었다** — 조용히 견적에서 탈락하던 것 |
| **8/11** | **텔레그램 연동**(폰 <-> PC · Monitor 기반 · 권한 전부 열되 되돌릴 수 없는 일은 2단계) |
| **8/11** | **dev-login** — 자동화가 비밀번호 없이 화면을 직접 확인한다(`.env` 스위치 + localhost + 계정 활성, 셋 동시) |
| **8/11** | **조립 사양 표준(ADM-STD-010)** · **조립 호환 조감도(ADM-STD-020)** 두 화면. 조감도는 **이 저장소에서 유일하게 Phoenix 를 쓰지 않는다** — 첫 판이 같은 데이터를 네 번 말하며 지도가 화면의 1/4 이라, 사용자가 Claude 디자인으로 그린 원안을 옮겼다(3안 탭 전환) |
| **8/11** | **오픈 범위 확정 + 쇼핑몰 인계 실측** — `pd_no` == `product_code` · 담기 경로 · 조립비 상품. 가격 표본에서 불일치가 나와 **인계 직전 재확인**을 넣기로 했다 |
| **8/11** | **가격 원천·IA 확정** — 몰 기준 + 소싱 시 잠금으로 덮어씀 · 운영 IA 54 -> 35 |
| **8/11** | **S4 인계**(`api/handoff.py`) — 결제·재고차감·배송지 없이 넘긴다. 원장은 `handoffs`(주문이 아니므로 `orders` 를 쓰지 않는다) |
| **8/11** | **시연용 상품이 고객 견적에 들어가고 있었다** — 후보 뷰가 `data_origin` 을 읽기만 하고 거르지 않았다. 인계를 만들다 몰 가격이 0원으로 와서 드러났다. **인계가 없었으면 오픈 후 고객이 발견했을 것** |
| **8/11** | **LNB 54 -> 35** · 재구축된 화면에만 링크 · 대분류 아코디언 · 「인계·성과」 축 신설 |
| **8/11** | **에이전트 구조 착수** — `.claude/CANON.md`(정본 계약) · `.claude/agents/surveyor.md` |
| **8/13~14** | **제작팀을 구조화** — 하네스는 지휘·확인만, 제작·검증은 팀이 한다. admin2 화면을 물결 단위로 병렬 재구축(파일 소유권을 나눠 동시에 여럿이 짓는다). 검증을 확인자·검증자·계약자 세 갈래로 나눴다(도는가 / 실물과 맞는가 / 약속과 같은가). Phoenix 템플릿 참조를 끊고 사장님이 주는 승인 디자인만 쓴다 |
| **8/15** | **몰(윈윈) 카탈로그를 다시 적재**하다가 잠금(`locked_fields`)이 가격 두 필드만 지키는 구조적 결함을 발견했다 — 완제품 분류·검수 회부·상품명이 조용히 되돌아갔다. 같은 날 회귀 게이트 두 곳이 이름·문자열 모양만 보고 있었다는 것도 드러나 관계 검사로 고쳤다 |
| **8/16** | 위 사고에 대한 사장님 결정(잠금 확장·중고 부품 분류·서버류는 완제품·제품 이미지 사용 허용·개발 DB 쓰기 개방 — **몰 분류 저장 방식은 미정**)이 나왔고, 그걸 반영하는 제작·재적재가 지금 진행 중이다. 상세는 **§4 「몰 카탈로그 재적재」** |
| **8/17** | 운영 도우미 잠금 뒤집힘(A-55)·LLM 실연동 확장(A-56)·팝콘톡 어휘 관문 폐기(A-57)·재고 입고 IA 재검토 착수(A-58) · **용도 하한 +3 및 스트리밍 시청/송출 분리(A-59·A-60)·환율 표 신설(A-61)·재고 입고 보류 API(A-58 서버 쪽)·용도 어휘 정본화(`GET /api/usages`)를 기록자가 미커밋 상태로 발견해 커밋**(마이그레이션 0057~0059 포함) — 각 화면 배선은 아직 없다 |

---

## 2. 화면 현황

> 수는 적지 않는다. `mockups/mvp1/*.html` · `mockups/admin/*.html` · `templates/admin/*.j2` 를 세라.

### 고객 — `mockups/mvp1/` · **범위 축소로 13 -> 7 이 된다**

| 화면 | 파일 | 새 범위에서 |
|------|------|------|
| 랜딩 | `main-landing.html` | **남김** — 실데이터(`GET /api/showcase`) |
| S0 입력 게이트 | `s0-landing.html` | **남김** |
| **S1 대화형 견적** | `s1-session.html` | **남김 · 품질 기준 파일** (4모드 + UX-17 점진 공개) |
| S2 견적 제안서 | `s2-result.html` | **남김** — 근거·클릭 수집 |
| S3 부품 변경 | `s3-detail.html` | **남김** |
| S4 주문 확인 | `s4-cart.html` | **재작성 완료(2026-08-12)** — 배송지·결제수단·재고차감을 걷고 «견적 확인 -> 가격 변동 고지 -> [팝콘PC에서 구매하기]» 로. §4 「S4/S5」 정정 참조 |
| S5 완료 | `s5-complete.html` | **재작성 완료(2026-08-12)** — 주문완료가 아니라 인계완료(HO 번호 · 몰로 이동) |
| MY 4화면 | `my-page`·`my-orders`·`my-payments`·`my-review-write` | **뺌** — 회원 원장은 쇼핑몰이 갖는다 |
| 내부자 게이트웨이 | `index.html` | 정리 대상(고객 화면 아님) |
| 데모 | `vslot-demo.html` | 정리 대상 |

> **MY 를 빼면 회원가입·로그인이 견적 흐름에서 빠진다** — 익명으로 견적 받고 몰에서
> 로그인한다. 이탈 지점이 하나 준다. 그리고 `api/customer_auth.py` 의 **dev 어댑터
> 구멍이 통째로 사라진다**(지금은 `if "@" not in email` 한 줄이 검증의 전부다).

**API 도 함께** — 뺌: `my_account` · `my_orders` · `my_payments` /
대체: `orders.py` -> `handoff.py` / 재검토: `customer_auth.py`

### 관리자 — 병행 신축

- **기존 `mockups/admin/*.html`** — 그대로 살아 있다. Phoenix 한글 셸.
  `_demo-products.html` 은 `data-screen-id` 가 없는 템플릿 잔재라 화면이 아니다.
- **재구축 `/admin2/*`** (Jinja2 서버 렌더) — 8/11 시점엔 넷뿐이었으나 8/13부터
  물결식 병렬 제작으로 크게 늘었다. **지금 몇 개가 지어졌는지는 여기 세지 않는다**
  — 화면별 상태·검증 근거는 목차 정본 `docs/design/req/INDEX.md`를 본다(이 문서도
  한때 실측과 어긋난 적이 있다 — 지음/미지음은 그 안에서도 실측으로 확인한다).
  **LNB 링크 조건이 세졌다**(2026-08-13 이후) — 「재구축됐으면 연결」이 아니라
  **「확인자 재검증까지 통과해야 연결」**이다. 지어졌어도 검증 대기인 화면은
  메뉴에 없다 — 안 본 화면에 운영자가 먼저 들어가지 않게 하기 위함이다.
- IA 정본 = `api/admin_nav.py`. 화면 카탈로그·ID 규약 = `docs/decisions/admin-identity.md`.

**화면 배지 어휘 3종:** `실데이터 · Cloud SQL` / `연결 안 됨` / `원천 준비 중`.
~~`더미 데이터`~~ 는 폐기했다 — 서버에 못 닿으면 아무것도 지어내지 않고
"서버에 연결되면 표시됩니다"라고만 말한다.

---

## 3. 인프라 · 배포

- GCP `popcorn-app`(asia-northeast3-b, 고정 IP `34.47.124.184`) · Cloud SQL `popcorn-db`.
- **접속 좌표 보강(2026-08-22, 기록자 — 배포 중 하네스 실측을 옮겨 적음)**: 프로젝트
  `project-045e861b-da1d-423a-b84` · 접속 명령 `gcloud compute ssh popcorn-app
  --zone asia-northeast3-b`. 위 고정 IP(`34.47.124.184`)는 `admin.popcornai.co.kr`
  뿐 아니라 **`www.popcornai.co.kr`도 같은 IP를 받는다.**
  **⚠ `deploy/README.md:26`은 지금도 "VM 외부 IP · SSH 접속 = GCP 콘솔에서 확인"
  이라고만 적고 있다** — 좌표가 문서에 없어서 오늘도 배포 직전에 사장님도 하네스도
  이 좌표를 다시 찾아야 했다. 그 README를 이 좌표로 갱신할지는 절차 문서 개정이라
  **하네스 소관**이다 — 기록자는 README 본문을 고치지 않았다, 좌표는 이 파일에만
  남긴다. 확인법: `grep -n "GCP 콘솔에서 확인" deploy/README.md`.
- **접속(웹) = `https://admin.popcornai.co.kr`**(2026-07-28 HTTPS 전환 완료 · Let's Encrypt).
- **접근 정책(2026-08-09 개정): 관리자·고객 모두 열려 있다.** Basic Auth는 폐기했다 —
  certbot이 설정을 고치며 `auth_basic`이 사라진 것을 발견하고 복구 대신 경로별 분리를 택했다.
  관리자는 비밀번호 인증(슬라이스 70) + `/api/admin/*` 미들웨어가 막는다.
  **고객 경로 10종은 2026-07-30~08-09 동안 nginx가 404로 막다가 사용자 결정으로 걷었다** —
  베타 테스터가 실제로 AI 상담 → 견적 → 구매까지 해보게 하기 위해서다.
  되돌리는 블록은 `deploy/nginx-popcorn.conf`에 주석으로 보존. 절차 `deploy/README.md §7`.
  **⚠ 열면서 dev 어댑터 구멍도 함께 열렸다 — 아래 §4 참조. 테스터에게 실제 개인정보를
  넣지 말라고 공지해야 한다.**
- 앱은 `127.0.0.1:8000`에만 바인드 · 방화벽 80·22만.
- **배포:** 로컬 커밋·푸시 → 서버에서 `git pull` + `systemctl restart popcorn-api`. 절차 `deploy/README.md`.
  **서버 코드 직접 수정 금지** — 배포 타깃이다(전역 규약). working tree drift가 나면 자동 배포가 깨진다.
- **백업·복구:** `deploy/RESTORE.md` (2026-07-27 리허설 통과). 자동 백업 매일 21:00 UTC·7건 + **PITR 7일**.
  복구는 원본을 덮어쓰지 않는다 — `instances clone --point-in-time`으로 복제본을 만들어 검증 후 전환한다.
- **도메인:** 5개 등록(`docs/99_domain.md`).
  **I-01로 배정이 갈렸다** — 정본 `popcornai.co.kr`은 **운영 서버 몫으로 남겨 둔다.**
  현재(개발) 서버는 **`admin.popcornai.co.kr`**을 쓴다(2026-07-28 적용됨).
  ~~`dev.` 서브도메인~~은 쓰지 않는다 — 2026-07-29 문서에 그렇게 적었지만 실제로는
  `admin.`이 이미 돌고 있었다(`dev.` 하나만 조회하고 "DNS 아직"이라 보고한 오류).
  정본을 개발에 붙였다가 운영으로 옮기면 인증서·OAuth 리다이렉트 URI·쿠키를 전부 다시 손봐야 한다.
- `COOKIE_SECURE=1` 서버에 적용됨(HTTPS 전환과 함께).
- **마이그레이션 head = `0031`** (0027 카테고리 · 0028 수수료 정밀도 · 0029 쿨러 노드 병합 ·
  0030 노드별 마진 · **0031 완성품 축**).
- **정적 서빙이 둘이다**(2026-08-05): `mockups/`(캐치올) + **`/design-system`**.
  토큰 파일은 "단일 원천"으로 만들어 두고 **아무 화면도 못 읽고 있었다** — 서버가 `mockups/`만
  마운트해서 닿을 길이 없었기 때문이다. 화면은 `../../design-system/tokens.css`로 참조한다
  (브라우저가 루트 위로 안 올라가므로 HTTP·`file://` 양쪽에서 같은 파일을 가리킨다).
- **외부 네트워크 의존 0건**(2026-08-05): 폰트(Pretendard 변수 + Hanken/Inter/JetBrains)와
  아이콘을 전부 `mockups/shared/`에서 내준다. 사내망·폐쇄망에서도 글꼴이 그대로다.
  전부 SIL OFL 1.1이고 라이선스 원문을 함께 둔다.

> **이 서버의 역할이 정해졌다(2026-07-29 · decision-log I-01):**
> **현재 서버 = 개발/스테이징으로 남는다. 운영은 새로 세운다**(승격하지 않는다).
> 베타 기간의 테스트 잔재가 운영 원장이 되는 것을 막기 위해서다 — 슬라이스 78의 owner 테스트 계정,
> 실주문 `ORD-84222`가 전례다. 운영 DB는 **원장을 비우고** 시작하되 **카탈로그·규칙은 이관**한다
> (사람이 검수로 채운 값은 원천 CSV로 재현되지 않는다). 대상 표는 I-01에 있다.
> 그래서 **이 서버에서는 부작용 있는 검사를 해도 된다** — 오염돼도 되는 곳이 됐다.
> 반대로 운영 서버가 서면 그곳에서는 절대 하지 않는다.

### 로컬 브라우저 자동화 도구 — 있는 줄 알면 헛수고한다 (2026-08-15 확인)

제작자 둘의 보고가 어긋났다 — 한쪽은 "Playwright 패키지가 이 PC 어디에도 없다",
다른 쪽은 "이미 캐시된 Playwright 를 썼다"고 했다. **기록자 실측으로 정정한다.**

**`playwright` 패키지는 설치돼 있지 않다.** `.venv` site-packages · 시스템 Python
(`py`·`python` 양쪽) `pip show playwright` · 전역 `npm ls -g --depth=0` ·
`where playwright` · pipx · `popcorn-ai`·`E:\DEV` 상위 2단계 `node_modules` 를
전부 확인했고 **어디에도 없다.** 있는 것은 **브라우저 바이너리 캐시뿐**이다
(`%LOCALAPPDATA%\ms-playwright\chromium-1234` 등) — 이 캐시가 정확히 어디서 왔는지는
모른다(다른 도구·다른 프로젝트의 흔적일 수 있다). **캐시가 있다고 패키지가 있는
것은 아니다** — `import playwright` 는 이 캐시만으로는 안 되고, 다음 사람이 "있다"고
믿고 시작하면 그 자리에서 막힌다.

**그래도 브라우저 실측 자체는 됐다** — 셸(`admin2.css`) 반응형 수정 확인자는
**Edge 를 CDP(Chrome DevTools Protocol)로 직접 구동**해 우회했다: Playwright
패키지 없이 브라우저를 원격 디버깅 모드로 띄우고 그 포트로 직접 붙는 방식이다.
Playwright 설치 없이 화면을 실측해야 하면 이 방법을 쓴다 — **재현 스크립트는
리포에 없다**(그 확인자 세션에서 그때그때 만든 것이라, 다음에 쓰는 사람이 절차를
다시 세워야 한다).

**확인법**: `.venv/Scripts/python -m pip show playwright` · `npm ls -g --depth=0` ·
`where playwright` 셋 다 "없음"을 돌려주면 이 기록이 아직 유효하다.

---

## 4. 지금 막혀 있는 것

> **범위 축소로 사라진 항목이 있다.** 이전 판의 「법정 표시 41종」·「PG·통신판매업·택배사」는
> **우리가 팔지 않으므로 발생하지 않는다** — 쇼핑몰 소관이다. 아래에서 걷어냈다.

| 항목 | 내용 | 급한가 |
|------|------|--------|
| **윈윈소프트 API 회신 대기** | 인계에 필요한 5종을 요청해 뒀다(`docs/design/winwin-api-request-2026-08-11.md`). **1·2번(상품 벌크 조회·장바구니 담기)이 없으면 인계 자체가 불가능하다.** 3번(회원 식별)은 셋 중 하나만 되면 오픈 가능. **외부 개발 대기라 리드타임이 가장 길다** — 이것부터 보내야 시계가 돈다 | **최우선** |
| ~~**S4/S5 고객 화면이 아직 자체 결제**~~ | **정정(2026-08-16, 기록자)** — 이미 틀려 있었다. `s4-cart.html` 은 2026-08-12부터 `/api/handoff` 를 부른다(확인법: `grep -n "api/handoff" mockups/mvp1/s4-cart.html`, 그 변경의 커밋 `d431710`). 남은 것은 `docs/design/screen-flow.md` §③이 여전히 미정으로 남긴 「가격 변동이 큰 건을 화면이 먼저 막을지」뿐 | 해소 |
| **가격 오차 — 엑셀 주기가 곧 오차** | 표본 대조에서 불일치가 나왔다. 특히 **우리가 더 싸게 안내하던 건**이 있었다 — 그대로 넘기면 장바구니에서 금액이 달라져 신뢰가 깨진다. 인계 시 재확인은 붙였고(`api/mall.py`), **원천 해소는 4순위 API(증분 동기화)** | 오픈 전 |
| **「저소음」 견적이 불성립** | 저소음 쿨러 중 후보 자격을 갖춘 것이 **시연용 가짜 상품 하나뿐**이었다(2026-08-11 발견). 실제 상품은 대부분 품절이고 나머지는 검수 대기다. 살릴 방법 둘 — SILENT LOOP 3 계열의 `cooler_tdp`·`cooler_height_mm` 검수 통과, 또는 품절 재입고. **운영 판단** | 판단 대기 |
| `cooler_tdp` 미충족 | 검수 대기의 큰 덩어리. **다나와에도 없어 크롤링으로 못 채운다.** 도구는 있다(사양별 큐 필터 + 형제 대조로 같은 모델 일괄 입력). 남은 건 사람이 앉아서 채우는 일 | 추천 품질 직결 |
| **신설 사양 26개에 규칙 0개** | `std` 표준의 절반이 아직 판정에 안 쓰인다. 다만 **지금 견적은 잘 나온다**(활성 규칙 9종이 후보를 100% 가깝게 커버). 규칙을 더 걸면 후보가 줄지 늘지 않으므로 **오픈 후** | 오픈 후 |
| 관리자 신원 dev 어댑터 | 비밀번호 인증이 앱 방벽 역할을 한다. 화면이 이 사실을 숨기지 않는다(배지 `미검증`). 실 OAuth 붙일 자리 = `api/admin_profile.PROVIDER_KO` + `_verify_google()` | 나중 |
| `tokens.css` 가 거의 안 붙어 있다 | 「디자인 계약」은 "토큰 단일원천"이라 적는데 실제로 읽는 화면은 일부다. 토큰을 써도 폴백으로 떨어진다 | 나중 |
| ~~**`api/talk.py`의 `_BUDGET_NUM` 옛 패턴 잔존**(2026-08-22 신규)~~ | **해소(2026-08-22 같은 날, 커밋 `c91b9d1`)** — 자릿수 상한을 없애고 콤마 표기도 받는다. 같은 커밋에서 예산 금액 표기를 천단위 콤마로 바꿨다(**A-94**, 사장님 지시) — 서버·화면 8곳을 함께 맞춰야 했다. 검증은 회귀가 아니라 실행 경로로 했다(회귀는 LLM을 안 불러 이 자리를 지나지 않는다). 확인법: `grep -n "_BUDGET_NUM" api/talk.py` | 해소 |
| 팬·확장카드·ODD (F 그룹) | 분류가 없어 `ETC` 에 묻혀 있다. 호환 관계는 `std.compat_pairs` 에 `in_scope=false` 로 자리를 잡아 뒀다 — **빠뜨린 게 아니라 미룬 것** | 차기 |

### 몰 카탈로그 재적재 — 코드 대응 커밋됨, 재적재 실행 여부는 다음 세션이 재확인 (2026-08-15~16)

사장님이 직접 내려받은 몰(윈윈) 상품 데이터를 다시 적재했다. 그 과정에서 **우리
카탈로그의 원천이 애초에 같은 몰에서 나온 것이었다**는 사실이 드러났다 — 그전까지
저장소 어디에도 적혀 있지 않던 사실이다.

적재가 손댄 것은 매입가·판매가만이 아니었다. `locked_fields` 가 지키는 컬럼이 그
둘뿐이라 같은 UPSERT 가 분류·검수 회부·상품명을 조건 없이 덮어썼다. 그 결과
완제품으로 올려놨던 분류 일부가 다시 `ETC` 로 내려갔고, 사람이 검수로 뺐던
상품이 후보로 되돌아갔고, 상품명 끝의 판매조건 꼬리 표기가 바뀌며 일부가 정제
로직을 빠져나갔다. **그 정제를 지키던 회귀 게이트 두 곳도 이번에 같이 드러났다**
— 문자열 존재·시작 일치만 보고 있어서 실제로는 아무것도 증명하지 못하고 있었다.

**정정(2026-08-16, 기록자)** — 이 절이 이전 판에서 "지금 보강 중"이라 적었던
항목 다섯(잠금 확장·중고 부품 분류 교정·완제품 분기·상품명 정규식·세션 시작·
배포 확인 도구 인코딩)이 **전부 코드로 끝나 커밋됐다**(회귀 전량 통과 확인 후).
확인법: `git show --stat 02743d1`(잠금 확장·중고 제외·완제품 분기·상품명)·
`c95f598`(CLI 인코딩) 두 커밋이 존재하는지 · `grep -n "locked_fields ?"
api/catalog_ingest.py`(이제 4개 컬럼, 당시엔 2개).
「이 결정들은 아직 decision-log.md 에 옮겨지지 않았다」던 것도 낡았다 — **A-48~51·
T-13** 으로 이미 옮겨졌다(확인법: `grep -n "^### A-4[89]\|^### A-5[01]\|^### T-13"
docs/decisions/decision-log.md`).
⚠ **다만 코드가 커밋된 것과 「이 코드로 실제 재적재가 다시 돌았는지」는 다른
사실이다** — 이 기록자 세션은 DB 에 닿지 않았으므로(문서·커밋만 다루는 역할)
후자를 확인하지 못했다. 다음 세션이 `csv_import_jobs` 최신 행과 위 「298건」
재확인 쿼리로 직접 재야 한다.
**미정 — 사장님 결정 대기(그대로)**: 몰 분류를 우리 스키마 어디에 담을지는
선택지만 올라간 상태다(U-11). 임의로 정하지 않는다.

**다음은** — ① 다음 세션이 실제 재적재 실행 여부·「298건」해소 여부를 DB 로
재확인한다 ② 그 결과로 회귀를 다시 돌려 영향을 본다 ③ 메뉴 연결(`api/admin_nav.py`)
은 **검증이 끝난 화면만 여는 것이 규약**(§2)이라 이 사고 대응 자체는 메뉴 항목이
아니므로 해당 없음 — 별도로 진행 중인 `templates/admin/spec_standard.html.j2`
팝업 결함 수정만 확인자 재검증 대기 상태로 남아 있다(아직 미커밋). ④ 몰 분류
저장 방식 결정(U-11).

**정정(2026-08-19, 하네스 실측)** — 위 「다음은 ①」이 풀렸다. `csv_import_jobs`
최신 행은 **job_id 27**(2026-08-16 09:41 · `상품다운_2026-08-15_병합_24721.csv` ·
24,721행 중 성공 20,566 · 오류 1) — **재적재가 실제로 다시 돌았다**(사고를 낸
판은 job_id 25, 2026-08-15 14:32였다). 「298건」 재확인 쿼리(완제품 분류인데
`part_type='ETC'`인 행 — decision-log 원문 그대로 다시 쟀다)도 **298건 → 0건.**
②(회귀 재실행)는 이 정정에 포함되지 않는다 — 하네스가 회귀를 돌렸는지는 여기
적지 않는다(이 기록자 세션은 DB·서버에 닿지 않는 역할이라 직접 못 쟀다). ③(메뉴
연결 해당 없음)·④(U-11 몰 분류 저장 방식, 여전히 미정)는 그대로다.
**확인법**: `SELECT * FROM csv_import_jobs ORDER BY job_id DESC LIMIT 3`(job_id 27이
최신인지) · decision-log.md의 A-31/298건 관련 쿼리를 그대로 다시 돌려 0인지 본다.

### 원장 결함 의심 — `sale_price` 를 바꿔도 이력이 안 남는 경로가 있다 (2026-08-15, 조사 중 — 결론 내지 말 것)

`PATCH /api/admin/products/{code}` 로 `sale_price` 를 바꿨는데 `product_price_history` 에
행이 안 남는 사례가 **두 번째로** 실증됐다(첫째 — 상품 46158, 판매가 관리 제작자 발견).

    2026-08-15  사장님이 「판매가 관리」에서 맞추기 클릭 (상품 119253)
                -> 853,200 -> 904,700 · 이력 행 남음(history_id 28995 · reason manual_match) 성공
                하네스가 PATCH 로 853,200 으로 되돌림
                -> 값은 바뀌었는데 이력 행이 «안» 남음 실패
                -> 이력 마지막 행은 904,700, 실제는 853,200 — 원장이 거짓말한다

⚠ **아직 판정 전이다**(의도된 설계일 수도 있다) — 제작자가 조사 중이니 여기서는
「조사 중」으로만 적는다. ⚠ **과거 데이터를 손으로 메우지 않는다.** 결론이 나면
`decision-log.md` 로 옮기고 여기서는 지운다.

**확인법**: `product_price_history WHERE product_code='119253' ORDER BY history_id DESC` —
마지막 행이 904,700(history_id 28995)인데 그 시점 이후 다른 값으로 바뀐 `products.sale_price`
와 어긋나는지 대조. 세 번째 사례가 더 나오면 "가끔 그런다"가 아니라 "어떤 경로가 그런다"로
좁혀진다 — 어느 호출 경로(운영자 화면 vs 스크립트 PATCH)가 이력을 남기는지 대조하면 된다.

**정정(2026-08-19, 하네스 실측)** — **판정은 안 바뀌었다. 바뀐 것은 DB의 현재 상태뿐이다.**
`product_price_history WHERE product_code='119253'`의 **지금** 마지막 행은 904,700
(**history_id 48186** · reason `csv` · 2026-08-15 14:32)이고 `products.sale_price`도
904,700이라 **지금은 어긋나 있지 않다.** ⚠ **이것을 무죄 판정으로 읽지 말 것** — 08-15
14:32는 위 표의 하네스 되돌림(853,200)보다 늦은 몰 카탈로그 재적재(§4 「몰 카탈로그
재적재」 job_id 25) 시각이다. **재적재가 그 위에 새 값을 덮어써 어긋남을 가린 것**이지,
「853,200 되돌림이 이력을 안 남긴 이유」가 밝혀진 게 아니다. **「PATCH 경로가 이력을 안
남긴다」는 의심은 여전히 미판정이다** — 위 「조사 중 — 결론 내지 말 것」 표시는 그대로
유지한다. **이 구분(지금 값이 맞는 것 ≠ 원인이 밝혀진 것)을 흐리지 말 것.**

### 정정 — 「상품 관리 화면에 병합 미리보기가 둘 있다」는 확인자의 오독이었다 (2026-08-15)

병합·편입 UI 는 상품 관리(ADM-PRD-010) 화면에 **없다**(서랍 탭은 기본 정보·사양·공급처·
분류 변경 4개뿐, `merge|편입|MergeBody|중복 정리` 전수 검색 0건). **하네스가 확인자의
원래(틀린) 보고를 검증 없이 제작자에게 그대로 전달한 전례**라 여기 남긴다.
`product_merge`/`product_merge_undo` API 자체는 실재한다(활동 로그 2026-07-28 자) —
**다만 이 화면 프런트에 붙어 있지 않을 뿐**이다. 근거·확인법 전문은 `decision-log.md`
T-04 다음 항목("정정 — ...확인자의 오독이었다"). **교훈: API 존재 ≠ 화면 연결.**

**미해결 결정(임의로 정하지 말 것)** — 원천 `decision-log §5`:
U-02 S2 커뮤니티 근거 소스 · U-04 게임 무게 대표 게임 · U-05 게이트웨이 라우팅 ·
U-06 비교견적 방향 · U-07 히어로 배경 톤 ·
견적 표시가와 몰 가격이 다를 때의 처리(제안 = 인계 시점 재확인 + 고객 고지) ·
**신규(2026-08-16): 몰 분류를 우리 스키마 어디에 담을지** — 위 「몰 카탈로그 재적재」 참조.

---

## 5. 다음 작업 후보

> **순서 원칙(2026-08-11 사용자 지시):** 큰 틀 -> LNB -> 각 메인화면 UI 확정 -> 상세 데이터.
> LNB 는 끝났다.
>
> **정정(2026-08-16, 기록자)** — 아래 목록의 절반 가까이(3·4·5·6)가 실측해 보니
> 이미 끝나 있었다. `docs/design/req/INDEX.md`·코드 확인 없이 이 목록만 읽으면
> 이미 된 일을 다시 시키게 된다. 지우지 않고 아래에서 각각 정정한다.

**남이 해야 하는 것 — 먼저 보내고 우리는 그동안 일한다**

1. **윈윈소프트 API 요청서 발송** — 리드타임이 가장 길다
2. ~~**특허 수정본·메일 발송** — 준비 완료(`E:\GDRIVE\000.임시\`).~~
   **정정(2026-08-19, 하네스 실측)** — **출원이 끝났다.**
   폴더: `E:\DEV\_private\patent-10-2026-0151699\` — 출원서류·우선심사신청서·
   관납료납부확인증(2026-08-13)이 있다. 「메일 발송 대기」는 낡은 상태다.
   명세서 핵심(청구항 1의 재현성 한정 수정 + 방법 독립항 추가 + 종속항 6개)은
   무엇이 출원됐는지의 기록으로 원문을 남긴다.

**이미 된 것 — 다시 시키지 않는다(2026-08-16 실측)**

3. ~~**S4/S5 인계 화면**~~ — 됨. §4 「S4/S5 고객 화면이 아직 자체 결제」 정정 참조
4. ~~**「인계·성과」 3화면**~~ — 됨. 쇼핑몰 동기화·인계 기록·유입 성과 셋 다
   `INDEX.md` 상 「지음」(라우트 실재 확인: `api/admin_ui_mall_sync.py` ·
   `api/admin_ui_handoff_log.py` · `api/admin_ui_funnel.py`)
5. ~~**「판매가」 화면 성격 재정의**~~ — 됨. `sale_price` 는 이미 `locked_fields`
   보호 대상이고(확인법: `grep -n "sale_price = CASE" api/catalog_ingest.py`),
   판매가 관련 화면군(관리·검토 대기·마진 정책·이력·재산정)이 `INDEX.md` 상
   전부 「지음」
6. ~~**에이전트 구조 마무리**~~ — 됨. 팀 구성·역할·검증 3축(확인자·검증자·계약자)이
   `CLAUDE.md` §팀 운용에 정착했다. 이 항목이 가리키던 「인계 규약」이 지금의
   §7 이고, 그 실행자가 기록자(archivist)다

**지금 진행 중 / 그다음** — §4 「몰 카탈로그 재적재」 참조. 그게 끝나면:

7. **이관 도구 · 운영 서버 신설**(I-01) — 카탈로그·규칙만 뽑아 새 DB 로.
   `data_origin='demo'` 제외 · `locked_fields` 보존
8. 검수 잔량 · 쿨러 소켓 모델군 웹 검증 · 팬/확장카드/ODD 분류

---

## 6. 새 세션 시작 절차

**정정(2026-08-16, 기록자)** — 아래는 2026-08-11 시점 절차라 지금과 안 맞는다
(`state.py` 로 먼저 재는 순서가 없고, 「작업은 한 번에 하나」는 지금의 다인원
제작팀 운용과 어긋난다). **정본은 `CLAUDE.md` §첫 세션 시작 시** 다 — 두 벌
두지 않는다. 절차가 바뀌면 `CLAUDE.md` 만 고친다(하네스 몫). 아래는 그 절이
서기 전 옛 기록으로 남긴다(CANON §5 — 지우지 않고 폐기라고 적는다).

1. ~~`CLAUDE.md` -> 이 파일 -> `decision-log.md` -> `.claude/CANON.md` **넷만** 읽는다.
   전체 폴더를 스캔하지 마라(속도 규약).~~
2. ~~현 상태를 5줄로 요약하고 다음 작업을 묻는다. 요청이 단계 원칙과 충돌하면
   코드를 짜지 말고 먼저 확인한다.~~
3. ~~작업은 한 번에 하나. 파일을 만들기 전 3~5줄로 접근을 요약하고 승인받는다.~~
4. ~~명령: 목업은 브라우저로 직접 연다(빌드 없음). API 는 `.venv` + `uvicorn api.main:app`.
   화면 확인은 `GET /api/admin/auth/dev-login` 을 한 번 열면 된다.~~
5. **비밀값은 리포·설정·메모리에 절대 저장하지 않는다.** `.env` 만(gitignore).
   (이것만은 지금도 그대로라 취소선을 안 그었다.)

---

## 7. 이 파일을 갱신하는 규약 (2026-08-11 신설)

**왜 있나** — 2026-08-10 판이 20커밋 뒤처진 채 남았고 **틀린 사실을 말하고 있었다.**
새 세션은 이 파일을 사실로 읽고 일을 시작한다. **틀린 인계는 인계가 없는 것보다 나쁘다.**

**언제 갱신하나**
- 작업 묶음이 끝났을 때 (커밋 몇 건이 한 덩어리를 이룰 때)
- **범위·원칙이 바뀌었을 때는 즉시** — 오늘처럼 「팔지 않는다」로 뒤집히면 그날 고친다
- 컨텍스트가 무거워져 새 세션으로 넘길 때

**무엇을 적나 — 셋을 갈라 둔다**

| 무엇 | 어디에 | 왜 |
|---|---|---|
| **결정**(확정·미정·폐기) | `decision-log.md` | 근거와 함께 남아야 나중에 되짚는다 |
| **사고와 교훈** | 커밋 메시지 · `CLAUDE.md` | 겪은 사람이 써야 껍데기가 안 된다 |
| **지금 상태**(어디까지·막힌 것·다음) | **이 파일** | 새 세션이 5줄로 파악할 것 |

**적지 않는 것**
- **세는 수.** 상품·후보·검수·회귀 건수는 머리말의 「어디서 보나」 표로 넘긴다
- 정체성·UX 결정·디자인 계약 — 다른 문서가 원천이다. 여기선 링크만
- 「고침」만 적힌 항목 — **확인법을 함께 적는다.** 그게 없으면 다음 사람이 코드를 다시 읽는다

**낡았는지 확인하는 법**

    git log -1 --format="%ad" --date=short -- HANDOFF.md   # 마지막 갱신
    git rev-list --count $(git log -1 --format=%H -- HANDOFF.md)..HEAD   # 그 뒤 커밋 수

커밋이 열 건 넘게 쌓였는데 이 파일이 그대로면 **이미 틀렸을 가능성이 높다.**
