# 팝콘PC AI — 인수인계 (현재 상태)

> **최종 갱신: 2026-08-08 · 회귀 795/795**
>
> ### 문서 역할 분담 — 같은 사실을 두 곳에 적지 않는다
>
> | 문서 | 담당 | 갱신 시점 |
> |------|------|-----------|
> | `CLAUDE.md` | **작업 규칙 + 축적된 교훈**(사고 기록). 매 턴 컨텍스트에 로드된다. | 규칙·교훈이 생길 때 |
> | `docs/decisions/decision-log.md` | **확정 / 미정 / 폐기된 결정** | 결정이 바뀔 때 |
> | **`HANDOFF.md`(이 파일)** | **지금 어디까지 왔나 · 무엇이 막혀 있나 · 다음은 무엇인가** | 슬라이스 묶음이 끝날 때 |
>
> 이 파일은 **상태만** 적는다. 정체성·UX 결정·디자인 계약은 위 두 문서가 원천이고 여기서는 링크만 한다.
> 2026-07-25판이 정체성·UX·디자인을 통째로 복사해 갖고 있다가 40슬라이스 뒤처졌고, §4 아이콘 정책이
> 현행과 **정반대**로 남아 새 세션을 오도할 뻔했다. 중복이 stale의 원인이었다 — 그래서 갈랐다.
>
> ### 숫자에는 잰 날짜를 붙인다 (2026-07-30 추가)
> 이번 갱신에서 **문서의 수가 실제와 어긋난 것을 셋 찾았다**: 관리자 화면 31(실제 33) ·
> 호환 규칙 8종(실제 9종 — 라디에이터 장착이 나중에 붙었다) · 그리고 sale_prices라는
> **존재하지 않는 테이블**을 근거로 적어 둔 backlog 항목. **세는 대상이 자라는 수는 적는 순간
> 낡기 시작한다.** 그래서 (a) 아래 수치는 전부 **2026-08-05 실측**이고, (b) 화면 수·규칙 수처럼
> 결정을 좌우하는 수는 **회귀가 문서와 실제를 대조한다**(`[31] 문서 정합`). 회귀가 지키지 않는
> 수는 참고값으로만 읽어라.

---

## 0. 30초 요약

- **무엇:** 자체 실재고 부품 기반 PC 견적·판매 시스템. 정체성 **"신뢰 판매자"** — 추천은 결정론적 룰/스코어 엔진이 하고, LLM은 입력 파싱과 근거 설명만 한다(견적을 생성하지 않는다). 근거는 `decision-log.md §1`.
- **단계:** 목업=스펙 원칙 아래 **화면 단위 수직 슬라이스**(DB→API→목업 바인딩 관통). 모델링 전용 단계는 2026-07-23에 끝났다.
- **어디까지:** 고객 축 S0~S5·MY 4화면 + **관리자 37화면**, API 모듈 56개, products **22,839건**. 내부 직원 베타가 GCP에서 돌고 있다.
  2026-08-05에 **카테고리 축**(판매 분류 트리 + 마진 정책)이 붙었고, 좌측 메뉴·디자인 토큰·폰트가 **단일 원천**으로 정리됐다.
- **범위(사용자 확정 2026-07-26, 2026-07-29 재확인):** **내부 직원 베타.** 고객 오픈은 나중이다. 그래서 PG·통신판매업·법정 표시·택배사 연동은 범위 밖.
- **검증 수단:** `.venv/Scripts/python tests/regression.py` — **795건 전량 불변식**. 엔진·규칙·게이트·원장·인증을 건드렸으면 돌리고 결과를 보고한다. 상세 원칙은 `CLAUDE.md §회귀 세트`.
  **세션 둘이 같이 쓸 때 (2026-08-08 정리)** — 예전엔 "한 번에 하나만" 이었다. 이제 두 가지가 열려 있다:
  · `REGRESSION_KEEP_SESSIONS=1` — `revoke-others` 왕복 6건을 건너뛴다. 그 검사가 **사람이 브라우저에
    로그인해 둔 세션을 끊는다**(재기동 탓이 아니었다 — 세션은 `admin_sessions` 테이블에 있다).
    건너뛴 사실은 실행 중에 크게 찍힌다.
  · `REGRESSION_BASE=http://127.0.0.1:8001` — 8000 을 다른 세션이 잡고 있으면 **남의 낡은 서버를
    검사하면서 내 코드를 판정한다.** 오늘 실제로 그래서 방금 넣은 필드가 "없다"고 나왔다.

**2026-08-05 실측 (아래 값은 회귀가 DB와 대조하거나 변화를 알린다)**

| 값 | 수 | 비고 |
|---|---|---|
| products | 22,839 | real 22,794 · demo 45 |
| 추천 후보(재고>0) | 3,308 | 4중 게이트를 전부 통과한 것 |
| 검수 대기 | 5,152 | 그중 `cooler_tdp` **972**(판매중·재고>0은 **307**) |
| 활성 호환 규칙 | **9종** | 8종 + 라디에이터 장착(수랭) |
| **재고 ≠ 원장 합** | **0건** | 슬라이스 98의 불변식이 유지되고 있다 |
| 관리자 계정 | 16 | 전원 dev 어댑터 또는 시드 — **검증된 신원 0명** |
| 판매 분류 노드 | 34 | 2단 트리. **엔진은 이 표를 읽지 않는다**(A-02) |
| 미매핑 상품 | 1 | 전량 매핑에 가깝다 — 실제 backlog 는 `미분류`(ETC) 쪽이다 |
| 카테고리 마진 정책 | 0 | 걸린 노드 없음 = 전 상품이 전역 마진 12% |
| 완성품(PC_COMPLETE·BAREBONE) | 422 | 완제품 318 · 베어본 104. **어느 후보 뷰에도 없다**(A-31) |
| `part_type='ETC'` | 4,726 | 그중 판매 분류가 '미분류'인 **실제 backlog 는 1,362** |
| 공급처 | 3 | 그중 1건은 2026-08-05 관통 점검이 만든 `점검-공급처-A` |

---

## 1. 어디까지 왔나 — 슬라이스 지도

전체 이력은 `git log`가 원천이다(슬라이스마다 원인·고친 것·검증을 커밋 메시지에 남긴다).
여기서는 **덩어리**만 적는다.

| 구간 | 무엇을 했나 |
|------|-------------|
| 1~18 | 목업 → DB 스키마(Cloud SQL, 테이블 46+뷰 2) → 첫 수직 슬라이스 |
| 19~36 | 관리자 화면 실배선 · 4중 게이트 확정 · 원장/되돌림 규약 · T1~T10 트랜잭션 |
| 37~38 | **인증 도입** — 관리자(승인 게이트·권한 3등급) / 고객(세션 경계) |
| 39~43 | **실카탈로그 적재**(22,838건) · 엔진 탐색 최적화 · 호환 규칙 8종 |
| 44~46 | 운영 전환 스위치 실배선 · S3가 조용히 깨져 있던 것 발견 |
| 47~49 | **화면 정직성** — S1이 후보 수를 지어내던 것 제거, 견적 가능 여부를 서버에서 받는다 |
| 50~52 | 업로드 적재(드라이런) · 검수 깔때기 · 다나와 제안 543건 승인 |
| 53~57 | 상품 관리 전면(단건 상세·수정·등록·중복 판정) · **사양 항목을 화면에서 늘린다** |
| 58~60 | **용도 하한** — 화면이 한 약속을 서버가 지킨다 · 첫 화면 정직성 |
| 61~71 | 운영자 작업 패널 · 검수 UX · 분류 변경 · 중복 편입 · **관리자 비밀번호 인증** |
| 73~78 | 비밀번호 발급 · 마진 정책 · 용도 하한 관리 화면 · 운영자 화면 전수 검증 |
| 81~86 | **빈 상태에서 시작하는 길 관통** · 수랭 규칙 · 스펙 관리 부품 축 · 페이징 사고 |
| 87~88 | 붙여넣기 사고 — 증상 1건 수정 후 **마크업 파싱으로 전수 감사**(회귀에 고정) |
| 89~90 | 문서 셋 역할 분리(중복이 stale의 원인) · **서버 역할 확정 I-01**(승격 아니라 신설) |
| 91~94 | HTTPS 설정 · **내 정보 화면**(죽은 링크 30개) · **공급처 관리**(빈 상태에서 막혀 있던 길) · **대시보드 운영 시작 체크리스트** |
| 95~98 | **상품 게이트 체크리스트**(막힌 이유를 전부 한 번에) · `사양 항목 정의` 이름 정정 · **검수 연속 입력** · **재고 = 원장 합 관통**(기초 재고) |
| 99 | nginx 설정을 서버 실제와 맞추고 **고객 경로만 404 차단** |
| 100 | **내 정보 화면을 사실에 맞춘다** — 시각이 9시간 어긋나 보이던 것(서울시각 렌더 모듈) · 세션 21개인데 끊을 수단이 없던 것 · 비밀번호가 화면에 그대로 보이던 것 · 빈 값에 이유를 적는 것 |
| 101 | **세 화면이 스타일 없이 이틀 돌았다** — 템플릿에만 있는 CSS를 참조해 404(브라우저는 조용히 넘어간다). 셸까지 입히고 **Phoenix 원본 지도**(`docs/design/phoenix-template-map.md`)를 만들어 재발을 막았다 |
| 102 | **승인 화면이 "본인 확인을 마친 신청"이라 적고 있었다** — 확인한 적이 없다(dev 어댑터). 승인 판단의 근거를 서버가 실제 상태에서 파생하게 바꾸고, 승인자 열의 `—` 하나가 덮고 있던 세 사실을 갈랐다 |
| 103 | 이 문서 갱신 — 그 과정에서 **문서의 수 셋이 실제와 어긋난 것**을 찾아 고치고, 다시 낡지 않게 **회귀가 문서와 실제를 대조**하게 했다(`[31] 문서 정합`) |
| 104~ | **카테고리 축 신설**(0027~0030) — 판매 분류 트리·상품 매핑·판매가 재산정·**마진 정책을 트리에**(A-29). 기존 임대 관리자(윈윈소프트)와 도매꾹 상품공급사센터를 조사해 물려받지 않을 것을 먼저 정했다(`docs/research/`) |
| ↑ | **Phoenix 템플릿 실사용** — 사용자 지적("라이선스 산 UI를 전혀 못 쓰고 있다")으로 treeview·bulk-select·data-list·echarts·choices를 실제로 붙이고 공용 헬퍼 2개(`ui-list.js`·`ui-choices.js`)로 모았다 |
| ↑ | **지어낸 것을 걷어냈다** — 더미 폴백 행(37화면) · 유령 List 예외(36화면) · RTL 404(36화면×2) · 외부 아이콘 CDN(37화면) |
| ↑ | **단일 원천 정리 4건** — 부품 어휘(쿨러 표시 축 A-28) · **좌측 메뉴**(36화면 복제 672KB → 8KB 정본, 끊겨 있던 길 2개 복구) · **디자인 토큰**(tokens.css를 아무도 안 읽고 있었다) · **폰트 자체 호스팅**(외부 CDN 0건, A-30) |
| ↑ | **완성품 축**(0031) — 완제품 318·베어본 104를 `part_type` 으로 옮겼다(A-31). 엔진 영향 0 |
| ↑ | **전 프로세스 관통 점검** — 상품·공급처·카테고리·마진·가격·재산정(7,239건 반영→되돌리기)·검수·주문·조회 20종을 실제 데이터로 등록→수정→삭제→되돌리기까지 97건 확인 |

---

## 2. 화면 현황 (실제 파일 기준)

**고객 — `mockups/mvp1/`**

| 화면 | 파일 | 상태 |
|------|------|------|
| 랜딩(세일즈) | `main-landing.html` | 실데이터(`GET /api/showcase`) |
| S0 입력 게이트 | `s0-landing.html` | 실데이터 |
| **S1 대화형 견적 세션** | `s1-session.html` | **품질 기준 파일** — 4모드 + UX-17 점진 공개 + 주변기기 인지 |
| S2 견적 제안서 | `s2-result.html` | 실데이터 · 근거·클릭 수집 |
| S3 부품 변경 | `s3-detail.html` | 실데이터 |
| S4 주문 확인 / S5 완료 | `s4-cart.html` · `s5-complete.html` | 실데이터 |
| MY 4화면 | `my-page` · `my-orders` · `my-payments` · `my-review-write` | 실데이터(세션 경계) |
| 내부자 게이트웨이 | `index.html` | 딥링크는 아직 "시늉"(decision-log U-05) |

**관리자 — `mockups/admin/` 37화면** (Phoenix 한글 셸 · `.html` 38개 중 `_demo-products.html`은
`data-screen-id`가 없는 템플릿 잔재라 화면이 아니고 감사 대상에서도 빠진다.
**이 수는 회귀가 파일 수와 대조한다** — 31로 적혀 있던 채 두 화면이 늘어 있었다)
A-10 커머스 전 구간(운영 전환·주문·환불·결제 정산·배송·후기·회원) + 카탈로그·검수·가격·재고·엔진 규칙.
화면 카탈로그와 ID 규약은 `docs/decisions/admin-identity.md`.

**화면 배지 어휘 3종(규약 · 2026-08-05 개정):**
`실데이터 · Cloud SQL` / `연결 안 됨`(원천은 있는데 못 불러옴) / `원천 준비 중`(저장 경로 자체가 없는 화면 — 0을 성과처럼 보여주지 않기 위한 구분).

~~`더미 데이터`~~ 는 **폐기했다.** 서버에 못 닿으면 이제 아무것도 지어내지 않고
"서버에 연결되면 표시됩니다"라고만 말한다. 배지가 '더미'라고 밝혀도 큼직한
"24,912개"는 사람이 사실로 읽는다 — 좌측 메뉴가 마크업 더미로 '상품 검수 9'라고 말했는데
실제가 5,162였던 것과 같은 문제다(그래서 `admin-panel.js`가 `clearDummyBadges()`로
배지를 지운다). 본문에도 같은 논리를 적용했다.

---

## 3. 인프라 · 배포

- GCP `popcorn-app`(asia-northeast3-b, 고정 IP `34.47.124.184`) · Cloud SQL `popcorn-db`.
- **접속 = `https://admin.popcornai.co.kr`**(2026-07-28 HTTPS 전환 완료 · Let's Encrypt).
- **접근 정책(2026-08-09 개정): 관리자·고객 모두 열려 있다.** Basic Auth는 폐기했다 —
  certbot이 설정을 고치며 `auth_basic`이 사라진 것을 발견하고 복구 대신 경로별 분리를 택했다.
  관리자는 비밀번호 인증(슬라이스 70) + `/api/admin/*` 미들웨어가 막는다.
  **고객 경로 10종은 2026-07-30~08-09 동안 nginx가 404로 막다가 사용자 결정으로 걷었다** —
  베타 테스터가 실제로 AI 상담 → 견적 → 구매까지 해보게 하기 위해서다.
  되돌리는 블록은 `deploy/nginx-popcorn.conf`에 주석으로 보존. 절차 `deploy/README.md §7`.
  **⚠ 열면서 dev 어댑터 구멍도 함께 열렸다 — 아래 §4 참조. 테스터에게 실제 개인정보를
  넣지 말라고 공지해야 한다.**
- 앱은 `127.0.0.1:8000`에만 바인드 · 방화벽 80·22만.
- **배포:** 로컬 커밋·푸시 → 서버에서 `git pull` + `systemctl restart popcorn-api`. 절차 `deploy/README.md`.
  **서버 코드 직접 수정 금지** — 배포 타깃이다(전역 규약). working tree drift가 나면 자동 배포가 깨진다.
- **백업·복구:** `deploy/RESTORE.md` (2026-07-27 리허설 통과). 자동 백업 매일 21:00 UTC·7건 + **PITR 7일**.
  복구는 원본을 덮어쓰지 않는다 — `instances clone --point-in-time`으로 복제본을 만들어 검증 후 전환한다.
- **도메인:** 5개 등록(`docs/99_domain.md`).
  **I-01로 배정이 갈렸다** — 정본 `popcornai.co.kr`은 **운영 서버 몫으로 남겨 둔다.**
  현재(개발) 서버는 **`admin.popcornai.co.kr`**을 쓴다(2026-07-28 적용됨).
  ~~`dev.` 서브도메인~~은 쓰지 않는다 — 2026-07-29 문서에 그렇게 적었지만 실제로는
  `admin.`이 이미 돌고 있었다(`dev.` 하나만 조회하고 "DNS 아직"이라 보고한 오류).
  정본을 개발에 붙였다가 운영으로 옮기면 인증서·OAuth 리다이렉트 URI·쿠키를 전부 다시 손봐야 한다.
- `COOKIE_SECURE=1` 서버에 적용됨(HTTPS 전환과 함께).
- **마이그레이션 head = `0031`** (0027 카테고리 · 0028 수수료 정밀도 · 0029 쿨러 노드 병합 ·
  0030 노드별 마진 · **0031 완성품 축**).
- **정적 서빙이 둘이다**(2026-08-05): `mockups/`(캐치올) + **`/design-system`**.
  토큰 파일은 "단일 원천"으로 만들어 두고 **아무 화면도 못 읽고 있었다** — 서버가 `mockups/`만
  마운트해서 닿을 길이 없었기 때문이다. 화면은 `../../design-system/tokens.css`로 참조한다
  (브라우저가 루트 위로 안 올라가므로 HTTP·`file://` 양쪽에서 같은 파일을 가리킨다).
- **외부 네트워크 의존 0건**(2026-08-05): 폰트(Pretendard 변수 + Hanken/Inter/JetBrains)와
  아이콘을 전부 `mockups/shared/`에서 내준다. 사내망·폐쇄망에서도 글꼴이 그대로다.
  전부 SIL OFL 1.1이고 라이선스 원문을 함께 둔다.

> **이 서버의 역할이 정해졌다(2026-07-29 · decision-log I-01):**
> **현재 서버 = 개발/스테이징으로 남는다. 운영은 새로 세운다**(승격하지 않는다).
> 베타 기간의 테스트 잔재가 운영 원장이 되는 것을 막기 위해서다 — 슬라이스 78의 owner 테스트 계정,
> 실주문 `ORD-84222`가 전례다. 운영 DB는 **원장을 비우고** 시작하되 **카탈로그·규칙은 이관**한다
> (사람이 검수로 채운 값은 원천 CSV로 재현되지 않는다). 대상 표는 I-01에 있다.
> 그래서 **이 서버에서는 부작용 있는 검사를 해도 된다** — 오염돼도 되는 곳이 됐다.
> 반대로 운영 서버가 서면 그곳에서는 절대 하지 않는다.

---

## 4. 지금 막혀 있는 것

| 항목 | 내용 | 급한가 |
|------|------|--------|
| ~~HTTPS 부재~~ | **해소(2026-07-28)** — `admin.popcornai.co.kr` HTTPS 전환 완료 | — |
| **고객 인증 dev 어댑터 — 2026-08-09부터 노출 중** | `api/customer_auth.py:130`의 로그인 검증이 `if "@" not in email` 하나다. 비밀번호·OAuth·인증메일 없음. **아무나 남의 이메일을 입력해 그 사람 세션을 받고 `/api/my/`로 주문·결제·환불·후기를 본다.** 2026-08-09에 nginx 차단을 걷으면서 이 구멍이 인터넷에 열렸다(사용자 결정 — 베타 테스트 목적). 개방 시점 실회원 6명은 전부 테스트 계정이었다. **테스터에게 실제 개인정보를 넣지 말라고 공지해야 하고, 진짜 회원이 생기기 전에 실 OAuth를 붙여야 한다.** 붙일 자리 `PROVIDER_KO` + `_verify_google()`. 급히 되돌리려면 `deploy/nginx-popcorn.conf`의 주석 블록을 풀면 된다. | **실 OAuth — 회원 생기기 전까지** |
| 관리자 신원 dev 어댑터 | 슬라이스 70의 비밀번호가 앱 방벽 역할을 하므로 급하지 않다. **슬라이스 102부터 화면이 이 사실을 숨기지 않는다** — 승인 대기 안내문이 "신원이 검증되지 않은 신청"이라 밝히고 배지가 `미검증`으로 뜬다(그전에는 "본인 확인을 마친 신청"이라 적혀 있었다). 계정 16개 중 **검증된 신원은 0명**이다. 실 OAuth를 붙일 자리는 `api/admin_profile.PROVIDER_KO` 하나로 모여 있고, 회귀가 "검증된 제공자가 생기면 안내문이 바뀐다"는 관계를 지킨다. | 나중 |
| ~~판매가가 정책과 따로 논다~~ | **해소(2026-08-05)** — `판매가 재산정`(ADM-PRC-050) 화면이 섰다. 미리보기 → 확인(건수 대조 409 가드) → 반영 → 되돌리기. **오름·내림을 합치지 않는다**(합치면 이상치 한 건이 나머지를 덮는다). 실측 관통: 7,239건 반영 후 되돌리기까지 원상 복귀. 마진은 이제 **판매 분류 노드별**로도 걸 수 있다(A-29) | — |
| `cooler_tdp` 미충족 | 검수 대기 972건 중 **판매중·재고 307건**(색상 변형을 묶으면 모델 232개). **다나와에도 없어 크롤링으로 못 채운다.** 슬라이스 85가 도구는 만들어 뒀다(사양별 큐 필터 + 형제 대조로 같은 모델 일괄 입력). **남은 건 사람이 앉아서 채우는 일이다.** | 추천 품질에 직결 |
| 고성능 티어 실용성 | 서버용 RAM이 카탈로그에 실재해 고가 구성이 나온다. 설계대로지만 실용성 판단이 필요하다(`HIGHEND_CAP_X=1.5`로 상한은 걸려 있다). | 판단 대기 |

| 첫 화면 이미지의 낡은 숫자 | `mockups/mvp1/assets/verify-2-compat.png` 그림 안에 **"호환성 5종 통과"**. 텍스트·alt 는 서버 값(8종)으로 고쳤지만 **그림은 못 바꾼다** — 회귀도 이미지 속 글자는 못 잡는다. **사용자가 다시 만들어 올리기로 함(2026-08-08)**: "호환성 8종 통과 · 소켓·전력·치수·발열까지 검증", 아이콘 8개(소켓 호환·메모리 규격·그래픽카드 길이·보드 규격·쿨러 소켓·쿨러 발열·쿨러 장착·전원 용량) | 대기 |
| 허용 종류 위반 422건 | 상품 매핑의 위반 탭 = 완제품 PC 318 + 베어본 104. `0031` 이 `part_type` 축에 넣었는데 **판매 분류 트리에 받아 줄 자리가 없다.** 화면은 정확히 동작 중이다 — 트리에 자리를 만들지, 기존 분류의 `allowed_part_types` 를 넓힐지는 **운영자 판단** | 판단 대기 |
| `tokens.css` 가 거의 안 붙어 있다 | 「디자인 계약」은 "토큰 단일원천"이라 적는데 **51화면 중 실제로 읽는 것은 12개**다(고객 8 + 관리자 3 + 데모 1). 2026-08-08 에 초록을 토큰화하며 고객 8화면을 연결했다. 나머지는 그대로 — 토큰을 써도 폴백으로 떨어진다 | 나중 |

**미해결 결정(임의로 정하지 말 것)** — 원천은 `decision-log.md §5`:
~~U-01 success(초록) 토큰~~ **확정(2026-08-08)** · U-02 S2 커뮤니티 근거 소스 · U-04 게임 무게 대표 게임 · U-05 게이트웨이 라우팅 · U-06 비교견적 방향 · U-07 히어로 배경 톤.

---

## 5. 다음 작업 후보

> **2026-08-06~08 묶음: UI 감사와 그 후속.** 계기는 사용자 지적 "화면 UI 들이 정말 맘에 안듭니다".
> 결과 문서 둘 — `docs/design/admin-screen-audit-2026-08-06.md`(관리자 36화면) ·
> `docs/design/customer-screen-audit-2026-08-06.md`(고객 13화면).
> **총평: 관리자 UI 는 생각보다 훨씬 나은 상태였고, 실제 결함은 고객 화면에 있었다.**
> 한 일 — 차트 공용 헬퍼(`mockups/shared/ui-chart.js`) + 3화면 · 아이콘 이름이 글자로
> 노출되던 것 15종 · `호환성 5종` → 서버 값 · 마크업에 박힌 콤마 숫자 20곳 · U-01 확정에 따른
> 초록 169곳 토큰화. 회귀는 735 → 795 로 늘었다.
>
> **두 번 틀린 방법을 적어 둔다**: 화면을 *측정*으로 줄 세웠다가 두 번 헛짚었다(정적 마크업
> 크기는 JS 로 그리는 화면에 무의미하고, 런타임 스캔은 숨은 요소를 놓친다).
> **화면이 무엇을 보여주는지는 열어 봐야 안다.** 정적 검사는 런타임보다 넓다(둘 다 필요하다).

1. ~~판매가 재산정 경로~~ — **완료(2026-08-05)**. 남은 것은 **실행 판단**이다:
   판매중·재고 상품 **7,239건**이 정책과 다르고 반영하면 오름 6,964건(**+2.50억**)이다.
   미리보기까지는 안전하고, 반영은 사업 판단이라 사용자 결정 사항으로 둔다.
2. ~~완제품 PC 318건의 축~~ — **완료(2026-08-05 · A-31)**. `PC_COMPLETE`·`BAREBONE` 으로
   옮겼고 엔진 영향 0(추천 후보 3,308 그대로). 남은 것은 **사양 항목을 무엇으로 걸지**와
   **커머스 발동 조건**(조립비·'조립중' 단계·`order_items.item_kind`)이다.
3. **도매꾹에서 추린 접목 항목**(`docs/research/domeggook-sc-2026-08-05.md`) —
   비동기 내보내기 큐 · 가격 N일 미갱신 임계 필터 · 주문 목록의 수수료·정산예정금액 ·
   **상품정보제공고시·KC 인증·원산지**(고객 오픈 전 법정 필수, 지금 스키마에 없다).
4. **이관 도구**(I-01 전제) — 카탈로그·규칙만 뽑아 새 DB에 넣는 도구. 드라이런 → 확인 → 적용.
   `data_origin='demo'` 제외, `locked_fields` 보존.
5. **운영 서버 신설** — 새 VM + 새 Cloud SQL → `alembic upgrade head`(슬라이스 81의 길) → 이관 도구 →
   **재고 실사 입고** → `popcornai.co.kr` 배정. 적재가 이제 재고 원장을 남기므로(슬라이스 98)
   처음부터 `재고 = 원장 합`이 성립한다(현재 어긋남 0건).
6. **검수 잔량 처리** — `cooler_tdp` 판매중·재고 307건(색상 변형을 묶으면 모델 232개).
   도구는 준비됐고 **누가 언제 하느냐**의 문제다.
7. **고객 오픈 준비** — 실 OAuth(카카오·네이버·구글) → nginx의 고객 차단 블록 해제. 운영 서버에서 한다.
   붙일 자리는 `PROVIDER_KO` + `_verify_google()`로 좁혀져 있다.

---

## 6. 새 세션 시작 절차

1. `CLAUDE.md` → 이 파일 → `decision-log.md` **셋만** 읽는다. **전체 폴더를 스캔하지 마라**(속도 규약).
2. 현 상태를 5줄로 요약하고 **다음 작업을 묻는다.** 요청이 단계 원칙과 충돌하면 코드를 짜지 말고 먼저 확인한다.
3. 작업은 **슬라이스 단위**로 계획을 3~5줄 승인받고 시작한다. 한 번에 하나.
4. 명령: 목업은 브라우저로 직접 연다(빌드 없음). API는 `.venv` + `uvicorn api.main:app`. `DATABASE_URL`은 루트 `.env`(gitignore).
5. **비밀값(DB 비밀번호·API 키)은 리포·설정·메모리에 절대 저장하지 않는다.**
