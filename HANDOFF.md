# 팝콘PC AI — 인수인계 (현재 상태)

> **최종 갱신: 2026-07-29 · 슬라이스 88 · 회귀 334/334**
>
> ### 문서 역할 분담 — 같은 사실을 두 곳에 적지 않는다
>
> | 문서 | 담당 | 갱신 시점 |
> |------|------|-----------|
> | `CLAUDE.md` | **작업 규칙 + 축적된 교훈**(사고 기록). 매 턴 컨텍스트에 로드된다. | 규칙·교훈이 생길 때 |
> | `docs/decisions/decision-log.md` | **확정 / 미정 / 폐기된 결정** | 결정이 바뀔 때 |
> | **`HANDOFF.md`(이 파일)** | **지금 어디까지 왔나 · 무엇이 막혀 있나 · 다음은 무엇인가** | 슬라이스 묶음이 끝날 때 |
>
> 이 파일은 **상태만** 적는다. 정체성·UX 결정·디자인 계약은 위 두 문서가 원천이고 여기서는 링크만 한다.
> 2026-07-25판이 정체성·UX·디자인을 통째로 복사해 갖고 있다가 40슬라이스 뒤처졌고, §4 아이콘 정책이
> 현행과 **정반대**로 남아 새 세션을 오도할 뻔했다. 중복이 stale의 원인이었다 — 그래서 갈랐다.

---

## 0. 30초 요약

- **무엇:** 자체 실재고 부품 기반 PC 견적·판매 시스템. 정체성 **"신뢰 판매자"** — 추천은 결정론적 룰/스코어 엔진이 하고, LLM은 입력 파싱과 근거 설명만 한다(견적을 생성하지 않는다). 근거는 `decision-log.md §1`.
- **단계:** 목업=스펙 원칙 아래 **화면 단위 수직 슬라이스**(DB→API→목업 바인딩 관통). 모델링 전용 단계는 2026-07-23에 끝났다.
- **어디까지:** **슬라이스 88 완료.** 고객 축 S0~S5·MY 4화면 + 관리자 31화면, API 모듈 47개, products 약 22,800건. 내부 직원 베타가 GCP에서 돌고 있다.
- **범위(사용자 확정 2026-07-26, 2026-07-29 재확인):** **내부 직원 베타.** 고객 오픈은 나중이다. 그래서 PG·통신판매업·법정 표시·택배사 연동은 범위 밖.
- **검증 수단:** `.venv/Scripts/python tests/regression.py` — **334건 전량 불변식**. 엔진·규칙·게이트·원장·인증을 건드렸으면 돌리고 결과를 보고한다. 상세 원칙은 `CLAUDE.md §회귀 세트`.

---

## 1. 어디까지 왔나 — 슬라이스 지도

전체 이력은 `git log`가 원천이다(슬라이스마다 원인·고친 것·검증을 커밋 메시지에 남긴다).
여기서는 **덩어리**만 적는다.

| 구간 | 무엇을 했나 |
|------|-------------|
| 1~18 | 목업 → DB 스키마(Cloud SQL, 테이블 46+뷰 2) → 첫 수직 슬라이스 |
| 19~36 | 관리자 화면 실배선 · 4중 게이트 확정 · 원장/되돌림 규약 · T1~T10 트랜잭션 |
| 37~38 | **인증 도입** — 관리자(승인 게이트·권한 3등급) / 고객(세션 경계) |
| 39~43 | **실카탈로그 적재**(22,838건) · 엔진 탐색 최적화 · 호환 규칙 8종 |
| 44~46 | 운영 전환 스위치 실배선 · S3가 조용히 깨져 있던 것 발견 |
| 47~49 | **화면 정직성** — S1이 후보 수를 지어내던 것 제거, 견적 가능 여부를 서버에서 받는다 |
| 50~52 | 업로드 적재(드라이런) · 검수 깔때기 · 다나와 제안 543건 승인 |
| 53~57 | 상품 관리 전면(단건 상세·수정·등록·중복 판정) · **사양 항목을 화면에서 늘린다** |
| 58~60 | **용도 하한** — 화면이 한 약속을 서버가 지킨다 · 첫 화면 정직성 |
| 61~71 | 운영자 작업 패널 · 검수 UX · 분류 변경 · 중복 편입 · **관리자 비밀번호 인증** |
| 73~78 | 비밀번호 발급 · 마진 정책 · 용도 하한 관리 화면 · 운영자 화면 전수 검증 |
| 81~86 | **빈 상태에서 시작하는 길 관통** · 수랭 규칙 · 스펙 관리 부품 축 · 페이징 사고 |
| 87~88 | 붙여넣기 사고 — 증상 1건 수정 후 **마크업 파싱으로 전수 감사**(회귀에 고정) |

---

## 2. 화면 현황 (실제 파일 기준)

**고객 — `mockups/mvp1/`**

| 화면 | 파일 | 상태 |
|------|------|------|
| 랜딩(세일즈) | `main-landing.html` | 실데이터(`GET /api/showcase`) |
| S0 입력 게이트 | `s0-landing.html` | 실데이터 |
| **S1 대화형 견적 세션** | `s1-session.html` | **품질 기준 파일** — 4모드 + UX-17 점진 공개 + 주변기기 인지 |
| S2 견적 제안서 | `s2-result.html` | 실데이터 · 근거·클릭 수집 |
| S3 부품 변경 | `s3-detail.html` | 실데이터 |
| S4 주문 확인 / S5 완료 | `s4-cart.html` · `s5-complete.html` | 실데이터 |
| MY 4화면 | `my-page` · `my-orders` · `my-payments` · `my-review-write` | 실데이터(세션 경계) |
| 내부자 게이트웨이 | `index.html` | 딥링크는 아직 "시늉"(decision-log U-05) |

**관리자 — `mockups/admin/` 31화면** (Phoenix 한글 셸 · `.html` 32개 중 `_demo-products.html`은
`data-screen-id`가 없는 템플릿 잔재라 화면이 아니고 감사 대상에서도 빠진다)
A-10 커머스 전 구간(운영 전환·주문·환불·결제 정산·배송·후기·회원) + 카탈로그·검수·가격·재고·엔진 규칙.
화면 카탈로그와 ID 규약은 `docs/decisions/admin-identity.md`.

**화면 배지 어휘 3종(규약):** `더미 데이터`(폴백) / `실데이터 · Cloud SQL` / `원천 준비 중`(저장 경로 자체가 없는 화면 — 0을 성과처럼 보여주지 않기 위한 구분).

---

## 3. 인프라 · 배포

- **접속:** `http://34.47.124.184/admin/login.html` · GCP `popcorn-app`(asia-northeast3-b, 고정 IP) · Cloud SQL `popcorn-db`.
- **nginx Basic Auth가 사이트 전체를 봉쇄한다.** 고객 인증이 아직 dev 어댑터라 이게 필요하다(§4 참조).
- 앱은 `127.0.0.1:8000`에만 바인드 · 방화벽 80·22만.
- **배포:** 로컬 커밋·푸시 → 서버에서 `git pull` + `systemctl restart popcorn-api`. 절차 `deploy/README.md`.
  **서버 코드 직접 수정 금지** — 배포 타깃이다(전역 규약). working tree drift가 나면 자동 배포가 깨진다.
- **백업·복구:** `deploy/RESTORE.md` (2026-07-27 리허설 통과). 자동 백업 매일 21:00 UTC·7건 + **PITR 7일**.
  복구는 원본을 덮어쓰지 않는다 — `instances clone --point-in-time`으로 복제본을 만들어 검증 후 전환한다.
- **도메인(2026-07-29):** 5개 등록 완료(`docs/99_domain.md`). **정본 = `popcornai.co.kr`.**
  다만 **아직 A 레코드가 없어 서버를 가리키지 않는다** — 가비아 DNS에서 `34.47.124.184`로 지정해야 한다.
  그 다음 certbot → `deploy/nginx-popcorn.conf`의 `server_name`·TLS 블록(현재 `_`와 예시인 채) 갱신 → `COOKIE_SECURE=1`.

> 🟡 **확인 필요(2026-07-29 미해결):** 사용자가 이 서버를 "실제 운영서버"라고 표현했다. 문서는 여태
> **개발 서버**로 적어 왔고 "운영 서버는 별도 시스템으로 추후 구현"이 전제였다. 어느 쪽인지에 따라
> 데이터 취급이 달라진다 — 테스트 주문이 진짜 원장에 남는지(슬라이스 78 전례), 백업·복구 기준을
> 실운영으로 올릴지. **정해지기 전까지 이 서버에서 부작용 있는 검사를 하지 않는다.**

---

## 4. 지금 막혀 있는 것

| 항목 | 내용 | 급한가 |
|------|------|--------|
| **HTTPS 부재** | 관리자 비밀번호(슬라이스 70)와 Basic Auth 비밀번호가 **평문으로 오간다.** 도메인은 있으니 A 레코드 + certbot이면 끝난다. | **지금** — 고객 오픈과 무관 |
| 고객 인증 dev 어댑터 | `api/customer_auth.py`의 로그인 검증이 `"@" in email` 하나다. 남의 이메일로 그 사람 주문·결제가 열린다. Basic Auth가 막고 있을 뿐이다. | 고객 오픈 전까지 |
| 관리자 신원 dev 어댑터 | 슬라이스 70의 비밀번호가 앱 방벽 역할을 하므로 급하지 않다. 실 OAuth는 `_verify_google()`만 채우면 되도록 격리돼 있다. | 나중 |
| `cooler_tdp` 미충족 | 검수 대기 972건 중 **판매중·재고 307건**(색상 변형을 묶으면 모델 232개). **다나와에도 없어 크롤링으로 못 채운다.** 슬라이스 85가 도구는 만들어 뒀다(사양별 큐 필터 + 형제 대조로 같은 모델 일괄 입력). **남은 건 사람이 앉아서 채우는 일이다.** | 추천 품질에 직결 |
| 고성능 티어 실용성 | 서버용 RAM이 카탈로그에 실재해 고가 구성이 나온다. 설계대로지만 실용성 판단이 필요하다(`HIGHEND_CAP_X=1.5`로 상한은 걸려 있다). | 판단 대기 |

**미해결 결정(임의로 정하지 말 것)** — 원천은 `decision-log.md §5`:
U-01 success(초록) 토큰 · U-02 S2 커뮤니티 근거 소스 · U-04 게임 무게 대표 게임 · U-05 게이트웨이 라우팅 · U-06 비교견적 방향 · U-07 히어로 배경 톤.

---

## 5. 다음 작업 후보

1. **HTTPS 관통** — 가비아 A 레코드(사용자) → `nginx-popcorn.conf` 갱신·푸시(로컬) → certbot·`COOKIE_SECURE=1`(서버).
2. **decision-log 정합** — §2 P-01·P-02가 "백엔드·DB·배포 전부 금지"인 채 멈춰 있다(2026-07-23에 뒤집힘). 확정 결정이 현행과 어긋나 있는 상태다.
3. **검수 잔량 처리** — `cooler_tdp` 모델 232개를 실제로 채운다. 도구는 준비돼 있으니 **누가 언제 하느냐**의 문제다(직원 배정 또는 제조사 자료 확보).
4. **고객 오픈 준비** — 실 OAuth(카카오·네이버·구글) → Basic Auth 해제. 오픈 시점이 정해지면 착수.

---

## 6. 새 세션 시작 절차

1. `CLAUDE.md` → 이 파일 → `decision-log.md` **셋만** 읽는다. **전체 폴더를 스캔하지 마라**(속도 규약).
2. 현 상태를 5줄로 요약하고 **다음 작업을 묻는다.** 요청이 단계 원칙과 충돌하면 코드를 짜지 말고 먼저 확인한다.
3. 작업은 **슬라이스 단위**로 계획을 3~5줄 승인받고 시작한다. 한 번에 하나.
4. 명령: 목업은 브라우저로 직접 연다(빌드 없음). API는 `.venv` + `uvicorn api.main:app`. `DATABASE_URL`은 루트 `.env`(gitignore).
5. **비밀값(DB 비밀번호·API 키)은 리포·설정·메모리에 절대 저장하지 않는다.**
