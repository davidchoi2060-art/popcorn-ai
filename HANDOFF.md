# HANDOFF — 2026-09-19 (재부팅 후 재개용)

> 이 파일은 «지금 무엇을 하던 중이었나»만 적는다. 숫자는 적지 않는다 — 재면 안다.
> 현황 측정: `.venv/Scripts/python scripts/state.py`

---

## 1. 지금까지 한 일 — 한 줄로

**격자·용도 체계를 「조립 가능한 조합」에서 「실제로 팔리는 물건」 기준으로 재설계했다.**
사장님 지적("사무용 저렴한 PC를 100만원 이상으로 살 사람은 없음")에서 시작해,
프로그램·게임 요구사양을 대규모 조사해 DB화하고, 용도 분류를 다시 짰다.

---

## 2. ⚠ 상태 — 전부 미커밋이다

```
DB alembic head    0104   (개발 DB에 적용 완료)
git                커밋 0건. 아래 파일 전부 미커밋
```

**마이그레이션 0095~0104 열 개가 미커밋 상태로 DB에만 적용돼 있다.**
재개하면 **커밋이 1순위**다 — 지금 리포를 clone 하면 DB와 코드가 어긋난다.

미커밋 목록:
```
수정  api/candidates.py · api/catalog_map.py · api/grid_public.py ·
      api/recommend.py · api/swap.py · api/usage_floors.py · tests/regression.py
신규  api/part_cond_rules.py
      db/migrations/versions/0095~0104 (10개)
      tools/ 아래 로더 8개
      docs/design/ 아래 설계문서 8개
```

---

## 3. 오늘 바뀐 것 — 무엇을 왜

### 데이터 (새 표)
| 표 | 내용 |
|---|---|
| `software` + `software_community_spec` | 프로그램 요구사양 — **공식값과 커뮤니티 실사용값을 나눠 담음** |
| `games` 확장 (컬럼 32개 추가) | 주사율·병목·VRAM·커뮤니티 근거 |
| `game_measured_fps` | 실측 fps — **임계값**("이 조건에서 60fps 넘는 최저 GPU")이지 평균이 아니다 |
| `game_popularity_snapshots` | PC방 점유율·스팀 동접 (시계열 — 스칼라로 두면 내일 거짓말이 된다) |
| `game_ratings` | 평점 + **표본 수 필수** |
| `game_customer_copy` | 고객용 상세설명 4필드 |
| `game_grade_suggestions` | 기계 제안 등급 — **확정(`game_grade_assignments`)과 분리** |
| `refresh_gpu_requirement` | 모니터 주사율 ↔ 필요 GPU (workload 축이 핵심) |
| `part_cond_rules` | 조건부 규칙 (VRAM 8GB → RAM 32GB) — **엔진 연결 아직 안 됨** |

### 용도 분류 — 7종 → 14종
```
환경축 (GPU 불필요)   단순 사무용 · 복합 사무용 · 개발 · 방송 송출 · 음악 작업 · 주식·트레이딩
사양축 (GPU 필요)     게임 · 캐주얼 게임 · 고사양 게임 · 영상편집 · 디자인 ·
                     캐드·설계 · 3D 렌더링 · AI 작업
```

**왜 두 축인가**: `software` 실측에서 개발 6종 전부 `rec_gpu`가 NULL(불요)이다.
RAM 32GB를 쓰면서도 GPU 사다리를 안 오른다 — 사양이 아니라 **환경**이 등급을 정한다.

### 고친 결함 (전부 견적에 직접 영향)
- **`cpu_gpu` 백필** — CPU 683행 중 682개가 NULL이었다. 사무용에 외장 GPU가 강제로 붙던 원인
- **엔진 iGPU 판정** — 컬럼을 만들어도 엔진이 안 읽었다. **iGPU 전용 탐색 패스를 먼저 돌리고 실패하면 폴백**
- **AMD Zen4/Zen5 오판** — "AMD는 G가 붙어야 iGPU"는 Zen3까지 규칙. 라이젠 7000/9000은 G 없이도 있다
- **`match()` 대소문자 구분** — 시드에 `'CAD'`만 있어 「캐드 돌리려고요」가 안 걸렸다(20개 중 19개 None)
- **`design` 낱말이 「디자인」 하나뿐** — 포샵·일러 고객이 아무 데도 안 걸렸다

---

## 4. 사장님 확정 사항 (되짚을 때 근거)

| 항목 | 확정 내용 |
|---|---|
| 사무용 상한 | **200만원**. 실판매 30벌 중 200만↑ 0벌 · 시장 iGPU 신품 262벌 중 3.4% |
| 사무 분할 기준 | **업무 환경**(개인·소규모·대량) — 사양이 아니다 |
| 단순/복합 | 「단순 사무용」·「복합 사무용」으로 가른다 |
| RAM | 단순 8GB · 복합 16GB. **8GB는 부족해서가 아니라 맞아서 그 값** |
| 가벼운 용도 | "사무·개발·방송 같이 프로그램이 가벼운 용도는 전부 환경 축으로" |
| 3D 분할 | **프로그램 계열로** — 캐드·설계 / 3D 렌더링 |
| 게임 최저구간 | **낮춘다** (PC방 상위 8개가 E등급인데 격자 최저가가 90만원대였다) |
| 조립공임 | **30,000원. 최종 장바구니에서 마지막에 추가** |
| 주사율 충돌 | 높은 쪽 — 단 "수긍이 가는" 값. 이상치는 버린다 |

---

## 5. ★ 다음에 할 일 — 순서대로

### ① 커밋 (1순위)
미커밋 10개 마이그레이션 + 코드 수정이 DB에만 반영돼 있다.
기록자에게 맡기되 **커밋 단위를 주제별로** 나눌 것(데이터 적재 / 엔진 수정 / 용도 재편).

### ② 격자 재생성 — 가장 큰 구멍
```
usage_floors 가 아는 용도   14종
grid_cells 가 아는 용도      7종   ← 오늘 만든 7개가 칸이 없다
```
고객이 "캐드 돌려요"라고 하면 **하한은 잡히는데 보여줄 카드가 없다.**
회귀 `[52]`도 지적한다: "배분 정보를 가진 현재 카드 0장".
→ `tools/grid_generate.py` 를 새 용도로 재실행해야 한다.

**그 전에 정할 것 둘:**
- **게임 3종을 합칠 것인가** — `game`/`game_casual`/`gaming_high`를 `game` 하나로 두고
  등급(E/L/A/B/C/S) × 해상도 × 주사율로 가르는 안. 기획안은 `docs/design/spec-axis-redesign-2026-09-18.md`
- **조립공임 반영** — 설계는 `docs/design/assembly-fee-2026-09-18.md`.
  사장님이 «장바구니 마지막»으로 확정해서 견적 엔진·회귀는 안 건드려도 된다.
  다만 **견적 화면에 "조립공임 30,000원 별도"를 반드시 표시**해야 한다

### ③ 미해결로 남긴 것
- **`part_cond_rules` 엔진 연결** — 표와 로더(`api/part_cond_rules.py`)는 있는데
  `api/recommend.py`가 아직 안 읽는다. 연결 지점 5단계는 `docs/design/usage-rules-rebuild-2026-09-18.md` §5
- **등급 확정** — 제안 65종이 `game_grade_suggestions`에 있다. 사장님 승인 흐름(관리자 화면)이 없다.
  9종 사양을 채운 뒤 4종이 「사람 확인 필요」를 벗어났고 5종이 남았다
- **디자인·AI VRAM 하한** — `design`에 GPU 조건이 없고, `ai`는 와트로만 거른다.
  스테이블 디퓨전은 VRAM 부족 시 **느려지는 게 아니라 실행 실패(OOM)**
- **영상편집 SSD 2TB 재검토** — 조사자가 "병목 5분류에 저장장치 축이 없다" 지적.
  프리미어 4K 렉의 실제 원인은 GPU가 아니라 H.264 디코딩이었다

---

## 6. 알려진 회귀 실패 1건 (무관)

```
사양 미등록 0건: 기대 0 / 실제 1
  -> 잘만 ZM700-TX 파워(105053). 품절 · product_specs 행 없음 ·
     추천 후보 뷰에 없음(견적 무영향). 2026-07-26 등록분으로 이번 작업과 무관
```

회귀 총계는 돌려서 확인한다(오늘 기준 1,061건).

---

## 7. 조사 산출물 위치 (DB에 안 들어간 원자료)

```
D:/Hermes-Workspace/
  pc_tier_ladder.json/.md          시장 가격 사다리 7단계
  office_pc_market.json/.md        사무용 PC 시장 조사 (중고 격리됨)
  software_catalog.json            프로그램 42종
  game_catalog_expanded.json       게임 86종
  game_catalog_community.json      커뮤니티 보강 33종
  game_refresh_rate.json           게임 목표 주사율 86종
  monitor_refresh_market.json      모니터 시장 + GPU 대응표
  game_customer_copy.json          고객용 설명 원고
  game_specs_missing9.json         PC방 상위 9종 사양
  usage_naming_3d.json             3D 이름 시장 조사
  _top20_copy.md                   PC방 상위 20종 설명 전문 (사장님용)
```

---

## 8. 이 작업의 판단 기준 (다음 세션이 흔들리지 않게)

- **실판매·시장 데이터가 먼저다.** 조사 수치보다 「사장님이 실제로 파는 것」이 이긴다
- **필요 없는 걸 권하지 않는 것도 「신뢰 판매자」의 일부다.**
  단순 사무용에 16GB를 권하면 30만원을 더 받고 얻는 게 없다
- **값을 지어내지 않는다.** 못 찾으면 비우고 «왜 못 찾았는지»를 적는다
- **공식 사양을 그대로 믿지 않는다.** 오공은 공식 RTX 2060인데 실측 4070급이고,
  킹덤컴2는 공식 4070인데 실측 3060이면 충분하다
- **화면이 말하는 수와 실제 결제액이 달라선 안 된다** (조립공임을 숨기지 않는 이유)
